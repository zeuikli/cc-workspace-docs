import{c as a,Q as t,j as n,m as s}from"./chunks/framework.BXc4yaFE.js";const u=JSON.parse('{"title":"Running Local LLMs: From First Run to Fine-Tuned — @Michaelzsguo","description":"","frontmatter":{"title":"Running Local LLMs: From First Run to Fine-Tuned — @Michaelzsguo","author":"Michael Guo（@Michaelzsguo）","date":"2026-05-09T00:00:00.000Z","source":"https://x.com/Michaelzsguo/status/2053217139369095252","type":"tweet"},"headers":[],"relativePath":"research/tweets/2026-05-09-@Michaelzsguo-095252.md","filePath":"research/tweets/2026-05-09-@Michaelzsguo-095252.md","lastUpdated":1779898012000}'),i={name:"research/tweets/2026-05-09-@Michaelzsguo-095252.md"};function o(l,e,r,p,c,h){return t(),n("div",null,[...e[0]||(e[0]=[s(`<h1 id="running-local-llms-from-first-run-to-fine-tuned-—-michaelzsguo" tabindex="-1">Running Local LLMs: From First Run to Fine-Tuned — @Michaelzsguo <a class="header-anchor" href="#running-local-llms-from-first-run-to-fine-tuned-—-michaelzsguo" aria-label="Permalink to &quot;Running Local LLMs: From First Run to Fine-Tuned — @Michaelzsguo&quot;">​</a></h1><p><strong>來源</strong>：<a href="https://x.com/Michaelzsguo/status/2053217139369095252" target="_blank" rel="noreferrer">https://x.com/Michaelzsguo/status/2053217139369095252</a><strong>作者</strong>：Michael Guo（@Michaelzsguo） <strong>發布日期</strong>：2026-05-09 <strong>收錄日期</strong>：2026-05-10 <strong>類型</strong>：Twitter Article <strong>統計</strong>：91 likes · 17 retweets · 253 bookmarks · 29729 views</p><hr><h2 id="全文" tabindex="-1">全文 <a class="header-anchor" href="#全文" aria-label="Permalink to &quot;全文&quot;">​</a></h2><h2 id="_0-tl-dr" tabindex="-1">0. TL;DR <a class="header-anchor" href="#_0-tl-dr" aria-label="Permalink to &quot;0. TL;DR&quot;">​</a></h2><p>Most people who struggle with local LLMs aren&#39;t stuck because the technology is hard. They&#39;re stuck because they don&#39;t know which layer they&#39;re stuck on. You think you&#39;re just running a model. You&#39;re actually making five decisions at once: hardware, memory, runtime, model selection, and quantization. Each layer has its own failure modes, each can be optimized independently, and they all constrain each other. Almost every problem traces back to one of these five. [圖 1] Two ways to read this: if you just want something running, §1 gets you there in ten minutes. If you&#39;re already tuning speed, context length, and quantization, §3 through §6 are the reference you want.</p><h2 id="_1-get-something-running-first" tabindex="-1">1. Get Something Running First <a class="header-anchor" href="#_1-get-something-running-first" aria-label="Permalink to &quot;1. Get Something Running First&quot;">​</a></h2><p>Don&#39;t start with 70B models, Q4 deep-dives, or GGUF format discussions. Get a model talking on your machine first. Apple Silicon Macs are the simplest starting point right now. Three commands, ten minutes:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bash</span></span>
<span class="line"><span>brew install ollama   # No Homebrew? Download the installer directly from ollama.com</span></span>
<span class="line"><span>ollama pull qwen3:8b  # Tag not found? Check ollama.com/library for current available tags</span></span>
<span class="line"><span>ollama run qwen3:8b</span></span></code></pre></div><p>Why qwen3:8b:</p><ul><li>small enough (~5GB at Q4 quantization),</li><li>smart enough for real use,</li><li>runs on 8GB of RAM,</li><li>handles both English and Chinese.</li><li>You don&#39;t need to optimize model choice on your first run. Once downloaded, you&#39;ll see a &gt;&gt;&gt; prompt. Ask it anything, watch it respond. Then run two more commands:</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bash</span></span>
<span class="line"><span>ollama list              # Installed models: name, quantization, file size</span></span>
<span class="line"><span>ollama show qwen3:8b     # Model details: quant level, chat template, layers, context length</span></span></code></pre></div><p>[圖 2] Reading the numbers:</p><ul><li>tokens/s (TPS): generation speed. On Apple M-series with an 8B model, expect 20–60 TPS — feels responsive. Below 5 TPS means a memory or quantization issue.</li><li>prompt eval: time to first token. Depends on model size and context length.</li><li>Memory usage: above 80% and you should start paying attention — system overhead will push latency up and may trigger swap. You&#39;re running. Now let&#39;s unpack why it works.</li></ul><h2 id="_2-hardware-bandwidth-is-the-real-metric" tabindex="-1">2. Hardware: Bandwidth Is the Real Metric <a class="header-anchor" href="#_2-hardware-bandwidth-is-the-real-metric" aria-label="Permalink to &quot;2. Hardware: Bandwidth Is the Real Metric&quot;">​</a></h2><p>Most people shopping for local LLM hardware focus on GPU compute (FLOPS). That instinct is wrong. Local inference is almost always memory-bandwidth-limited, not compute-limited. Every time the model generates a token, the model weights get read from memory to the compute units. Weights are fixed — they get read every single step. Bandwidth determines how fast that read happens, and therefore caps your TPS. This is why Apple Silicon consistently outperforms expectations for local LLM work. Apple Silicon path M-series chips use a Unified Memory architecture: CPU and GPU share the same memory pool, there&#39;s no discrete VRAM, and the path from memory to GPU cores is extremely short. Bandwidth efficiency is far higher than a discrete GPU connected over PCIe.</p><ul><li>MacBook Pro M4 Max: two tiers — 410 GB/s (14-core CPU) and 546 GB/s (16-core CPU / 40-core GPU). The 128GB config usually maps to the higher 546 GB/s tier.</li><li>Mac Studio M3 Ultra: 819 GB/s memory bandwidth, large memory options available — the most capable consumer Apple desktop path for local LLM work. NVIDIA path</li><li>RTX 4090 (24GB VRAM): ~1008 GB/s bandwidth, the consumer single-card ceiling</li><li>RTX 3090 (24GB VRAM): ~936 GB/s, lower cost, supports NVLink multi-GPU</li><li>Multi-GPU: 3090s can pool VRAM via NVLink. 4090 multi-GPU is typically model parallelism, not VRAM pooling — you can&#39;t simply think of two 4090s as 48GB usable VRAM</li><li>Datacenter cards (A100, H100): significantly higher bandwidth, significantly higher price — not a consumer option [圖 3]</li></ul><h2 id="_3-memory-math-the-real-constraint" tabindex="-1">3. Memory Math: The Real Constraint <a class="header-anchor" href="#_3-memory-math-the-real-constraint" aria-label="Permalink to &quot;3. Memory Math: The Real Constraint&quot;">​</a></h2><p>Hardware sets the ceiling. Memory math tells you what fits inside it. Local model memory usage breaks down into two parts: Total memory ≈ weights + KV cache + runtime overhead Weights are straightforward to calculate: Weights = parameters × bytes per parameter</p><p>FP16 / BF16 ≈ 2 bytes/param → 8B FP16 ≈ 16 GB Q8 ≈ 1 byte/param → 8B Q8 ≈ 8 GB Q4 ≈ 0.5 byte/param → 8B Q4 ≈ 4 GB (+20% overhead ≈ 5 GB) Q3 ≈ 0.375 byte/param → 8B Q3 ≈ 3 GB 8B FP16 ≈ 16GB. Q8 ≈ 8GB. Q4 ≈ 4–5GB. This is why Q4 is the sweet spot. KV Cache is the second variable — and the one most people ignore: KV Cache ≈ 2 × num_layers × num_kv_heads × head_dim × seq_len × batch_size × bytes/element Note: modern models widely use GQA (Grouped Query Attention) or MQA (Multi-Query Attention), where the number of KV heads is smaller than the number of attention heads. The formula uses num_kv_heads, not the full num_heads. For single-user chat, batch_size is typically 1. KV Cache scales linearly with context size. 128K vs 4K context is 32× the KV Cache. Total memory impact depends on how much of your budget is weights vs KV Cache. Context size is the hidden memory killer The most common beginner mistake: assuming memory usage is fixed once the model loads. Context window size is the actual memory variable.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bash</span></span>
<span class="line"><span># Set context size via API options</span></span>
<span class="line"><span># (Ollama parameter names change across versions — num_ctx is the stable approach)</span></span>
<span class="line"><span>curl http://localhost:11434/api/generate \\</span></span>
<span class="line"><span>  -d &#39;{</span></span>
<span class="line"><span>    &quot;model&quot;: &quot;qwen3:8b&quot;,</span></span>
<span class="line"><span>    &quot;prompt&quot;: &quot;Explain KV Cache&quot;,</span></span>
<span class="line"><span>    &quot;options&quot;: { &quot;num_ctx&quot;: 32768 },</span></span>
<span class="line"><span>    &quot;stream&quot;: false</span></span>
<span class="line"><span>  }&#39;</span></span></code></pre></div><p>qwen3:8b defaults to a 4096 context. Bumping to 32K, the memory difference is almost entirely KV Cache. This isn&#39;t a bug — longer context means more KV Cache to store previously computed tokens. [圖 4] Read this chart for ratios, not absolutes: the gap between 4K and 32K context on the same model is almost entirely the KV Cache segment. [圖 5] ok, so what do you actually run on your machine? [圖 6] 4. Runtime: Pick the Right Tool, Then Stop Thinking About It You think you&#39;re using qwen3:8b. You&#39;re actually using Ollama first — it decides how the model loads, which chat template gets applied, how many layers go to GPU. Runtime makes a lot of choices for you before you understand the model. [圖 7] Advanced users who want full control can skip Ollama and call llama.cpp directly. Everything Ollama handles for you, you now specify manually:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bash</span></span>
<span class="line"><span>./llama-cli \\</span></span>
<span class="line"><span>  --model ./models/Qwen3-30B-A3B-Q4_K_M.gguf \\</span></span>
<span class="line"><span>  --ctx-size 32768 \\      # context window size</span></span>
<span class="line"><span>  --n-gpu-layers 99 \\     # GPU layers to offload, 99 = all</span></span>
<span class="line"><span>  --flash-attn on \\       # Flash Attention</span></span>
<span class="line"><span>  --jinja                 # use the model&#39;s own chat template — fixes a lot of mysterious quality issues</span></span></code></pre></div><p>--jinja is worth calling out: it tells llama.cpp to use the model&#39;s bundled chat template instead of a default. Adding this flag makes a lot of inexplicable quality problems disappear. Local models aren&#39;t just chat UIs. Once running, they connect to your agents, scripts, and toolchains via API:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bash</span></span>
<span class="line"><span># Ollama native API (/api/generate)</span></span>
<span class="line"><span>curl http://localhost:11434/api/generate \\</span></span>
<span class="line"><span>  -d &#39;{</span></span>
<span class="line"><span>    &quot;model&quot;: &quot;qwen3:8b&quot;,</span></span>
<span class="line"><span>    &quot;prompt&quot;: &quot;Explain KV Cache in three sentences&quot;,</span></span>
<span class="line"><span>    &quot;stream&quot;: false</span></span>
<span class="line"><span>  }&#39;</span></span>
<span class="line"><span># For OpenAI-compatible clients, use /v1/chat/completions</span></span>
<span class="line"><span># Change one base_url in your code and you switch from cloud to local</span></span></code></pre></div><p>One boundary to state clearly: this article covers local single-machine inference. If you need multi-user production serving, that&#39;s vLLM and SGLang territory — a completely different set of tradeoffs.</p><h2 id="_5-model-selection-read-the-filename-first" tabindex="-1">5. Model Selection: Read the Filename First <a class="header-anchor" href="#_5-model-selection-read-the-filename-first" aria-label="Permalink to &quot;5. Model Selection: Read the Filename First&quot;">​</a></h2><p>A model filename on HuggingFace might look like this: Qwen3-30B-A3B-Q4_K_M.gguf Every segment means something. Read it and you know exactly what you&#39;re downloading. [圖 8] 5a. Base / Instruct / Chat Same model family, very different behavior. Base continues your prompt — it doesn&#39;t answer, it completes. Instruct has been fine-tuned to follow instructions. Chat adds dialog formatting on top. For local use, you almost always want Instruct or Chat. Base is for continued training. The prompt format trap Instruct models are sensitive to prompt format. Different model families use different chat templates. Qwen3 and Llama 3 format differently: Wrong (raw prompt sent directly): &quot;Explain KV Cache&quot;</p><p>Correct (Qwen3 chat template — let your runtime handle this automatically):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;|im_start|&gt;system</span></span>
<span class="line"><span>You are a helpful assistant.</span></span>
<span class="line"><span>&lt;|im_end|&gt;</span></span>
<span class="line"><span>&lt;|im_start|&gt;user</span></span>
<span class="line"><span>Explain KV Cache</span></span>
<span class="line"><span>&lt;|im_end|&gt;</span></span>
<span class="line"><span>&lt;|im_start|&gt;assistant</span></span></code></pre></div><p>Ollama applies the correct template automatically. llama.cpp with --jinja does too. Wrong format produces mysteriously poor output — a real-world failure mode that gets misdiagnosed as &quot;this model is bad.&quot; 5b. Quantization: trading memory for quality Quantization compresses weights from FP16 (16-bit float) to fewer bits. A 16GB 8B FP16 model becomes a ~5GB Q4 model. You lose a little precision, you gain the ability to actually load it. FP16/BF16 → Q8 → Q6 → Q4 → Q3 → Q2 Quality: high ←——————————————→ low Size: large ←——————————————→ small K-quants: the K in Q4_K_M stands for K-means clustering. _M/_S/_L is Medium/Small/Large — the quantization granularity. K-quants consistently outperform naive uniform quantization at the same bit width. Practical quantization rules:</p><ul><li>Memory to spare → Q8 (closest to FP16 quality)</li><li>Quality-sensitive with limited memory → Q5_K_M or Q6_K_M</li><li>Everyday use → Q4_K_M (the sweet spot)</li><li>Memory forces your hand → Q3 (carefully), Q2 almost never A counterintuitive result: a Q4 8B model outperforms an FP16 3B model on most everyday tasks. Quantization does introduce loss, but the gains from a larger model architecture usually outweigh it at Q4. Choosing a bigger model at Q4 is almost always better than choosing a smaller model at FP16. [圖 9] 5c. MoE: total parameters ≠ active parameters The A3B suffix is where most people get confused. It&#39;s telling you: the model has 30B total parameters, but only ~3B are activated per inference.</li><li>Memory requirement: based on total parameters — all weights load into memory</li><li>Compute: based on active parameters — only these participate in each inference pass So 30B-A3B: needs ~18GB of memory at Q4 (to hold all 30B weights), but each inference step computes like a ~3B model. Actual speed is still affected by how fast those 30B weights can be read from memory, runtime implementation, and hardware bandwidth — it&#39;s not literally 3B speed, but it&#39;s meaningfully faster than a dense 30B. MoE lets you fit a higher total parameter count into the same memory budget. That&#39;s the real value. But quality still depends on training data, routing quality, and quantization — MoE is not inherently smarter. Bigger isn&#39;t always better: for local agent use and tool calling, latency and instruction-following stability often matter more than benchmark rankings. A responsive 14B that reliably follows instructions may be more useful day-to-day than a slow 70B.</li></ul><h2 id="_6-optimization-from-running-to-running-well" tabindex="-1">6. Optimization: From Running to Running Well <a class="header-anchor" href="#_6-optimization-from-running-to-running-well" aria-label="Permalink to &quot;6. Optimization: From Running to Running Well&quot;">​</a></h2><p>If your model is already fast enough and accurate enough, skip this section. Come back when you have a specific bottleneck. 6a. Core parameters</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>bash</span></span>
<span class="line"><span># Ollama: set parameters via API options (more stable across versions)</span></span>
<span class="line"><span>curl http://localhost:11434/api/generate \\</span></span>
<span class="line"><span>  -d &#39;{</span></span>
<span class="line"><span>    &quot;model&quot;: &quot;qwen3:8b&quot;,</span></span>
<span class="line"><span>    &quot;prompt&quot;: &quot;Your question here&quot;,</span></span>
<span class="line"><span>    &quot;options&quot;: {</span></span>
<span class="line"><span>      &quot;num_ctx&quot;: 8192,    # context window size — directly affects KV Cache memory</span></span>
<span class="line"><span>      &quot;num_gpu&quot;: 99,      # GPU layers to offload, 99 = all</span></span>
<span class="line"><span>      &quot;num_batch&quot;: 512    # batch size for prompt processing</span></span>
<span class="line"><span>    },</span></span>
<span class="line"><span>    &quot;stream&quot;: false</span></span>
<span class="line"><span>  }&#39;</span></span></code></pre></div><p>temperature, top_p, top_k control generation randomness — not performance knobs. Leave them at defaults unless you have a specific reason. 6b. Attention optimization Flash Attention: standard attention materializes the full attention matrix in memory. Flash Attention computes it in tiles, dramatically reducing memory reads and writes. Same context size, faster and cheaper. Enabled with --flash-attn on in llama.cpp; check your runtime&#39;s docs to confirm whether it&#39;s on by default. KV Cache quantization: quantize the KV Cache itself (typically to Q8 or Q4). Meaningfully reduces memory at 32K+ context lengths. 6c. Smart quantization: imatrix Standard quantization applies the same precision reduction uniformly to all weights. imatrix (Importance Matrix) quantization first runs a calibration dataset, measures how much each weight group affects outputs, then applies aggressive compression to unimportant weights while preserving precision on important ones. Same Q4 target, but imatrix Q4 consistently produces better output quality than standard Q4. On HuggingFace, imat or imatrix in the filename is a good sign. Turbo Quant is a similar technique under a different name — importance-guided non-uniform quantization is the underlying idea. 6d. Emerging inference acceleration Speculative decoding: a small draft model generates candidate tokens quickly, then the main model verifies them in batch. Verified tokens are used directly; only rejected ones get recomputed. Can meaningfully improve TPS when draft and main models are well-matched. Same model family usually works best for the draft, but isn&#39;t strictly required. MTP (Multi-Token Prediction): instead of predicting one token per forward pass, the model predicts multiple. Some newer models have this built into training, so no separate draft model is needed. Both approaches point in the same direction: more output per forward pass. 6e. Don&#39;t just watch TPS TPS measures speed, not quality. A fast model that hallucinates is useless. Skip the benchmarks. Test on your actual tasks. Four things to check in under ten minutes:</p><ol><li>Instruction following: give it a task with a specific output format (e.g. JSON with exact fields). Does it comply consistently?</li><li>Long context: paste a 2,000-word document, then ask about a detail from the final paragraph. Does it still have it?</li><li>Code: ask it to write a simple function. Copy it out, run it. Does it pass?</li><li>Hallucination: ask a factual question you know the answer to. Does it make things up? Ten minutes of real-task testing tells you more than any public benchmark.</li></ol><h2 id="_7-glossary" tabindex="-1">7. Glossary <a class="header-anchor" href="#_7-glossary" aria-label="Permalink to &quot;7. Glossary&quot;">​</a></h2><p>[圖 10]</p><h2 id="_8-the-five-layers-one-more-time" tabindex="-1">8. The Five Layers, One More Time <a class="header-anchor" href="#_8-the-five-layers-one-more-time" aria-label="Permalink to &quot;8. The Five Layers, One More Time&quot;">​</a></h2><p>When a model won&#39;t run, don&#39;t blame the model first. Ask: is it a memory issue? Context too large? Wrong quantization? Runtime misconfigured? [圖 11] Each layer can be optimized independently, but they constrain each other:</p><ul><li>Better runtime doesn&#39;t help if bandwidth is the bottleneck</li><li>Lower quantization frees memory but costs quality</li><li>The best model in the world won&#39;t run if it doesn&#39;t fit Understanding the stack is understanding local LLMs. If you&#39;ve found a configuration that works well on your hardware, share it. References</li><li>Ollama API documentation</li><li>llama.cpp GitHub</li><li>MLX</li><li>Apple M4 Max Tech Specs</li><li>Apple Mac Studio Tech Specs</li><li>GGUF format specification</li></ul><h2 id="研究摘要" tabindex="-1">研究摘要 <a class="header-anchor" href="#研究摘要" aria-label="Permalink to &quot;研究摘要&quot;">​</a></h2><p><strong>TL;DR</strong>：本地 LLM 問題根源在於 5 層架構（硬體頻寬、記憶體數學、Runtime、模型選擇、量化優化）各有獨立失效點，理解分層比選對模型更重要。</p><h3 id="核心論點" tabindex="-1">核心論點 <a class="header-anchor" href="#核心論點" aria-label="Permalink to &quot;核心論點&quot;">​</a></h3><ol><li><strong>推論瓶頸是記憶體頻寬而非算力</strong>：本地推論是 memory-bandwidth-limited，Apple Silicon 的 Unified Memory 架構（M4 Max 546 GB/s）使其在同等 cost 下優於 PCIe 離散 GPU</li><li><strong>記憶體計算公式</strong>：<code>Total = weights + KV cache + overhead</code>；KV cache 隨 context 線性擴展，128K vs 4K = 32× KV cache，是最常被忽視的記憶體殺手</li><li><strong>Q4 是甜蜜點</strong>：Q4 8B 模型勝過 FP16 3B——更大架構的增益超過量化損失；Q4_K_M（K-means clustering）優於同 bit-width 的 naive uniform quantization</li><li><strong>MoE 的實際含義</strong>：<code>30B-A3B</code> = 記憶體需要裝 30B 全量 weights，但每次推論只激活 3B；速度快於 dense 30B 但不等於 3B 速度</li><li><strong>實際評測優先於 benchmark</strong>：4 項 10 分鐘測試（instruction format compliance / long context recall / code execution / hallucination check）比公開 leaderboard 更有預測力</li></ol><h3 id="關鍵數據-工具" tabindex="-1">關鍵數據 / 工具 <a class="header-anchor" href="#關鍵數據-工具" aria-label="Permalink to &quot;關鍵數據 / 工具&quot;">​</a></h3><ul><li>Apple M4 Max: 410/546 GB/s（取決於 CPU/GPU tier）</li><li>Mac Studio M3 Ultra: 819 GB/s</li><li>RTX 4090: ~1008 GB/s VRAM bandwidth</li><li>量化記憶體：FP16=2B/param, Q8=1B/param, Q4=0.5B/param, Q3=0.375B/param</li><li>工具：Ollama（易用）、llama.cpp + <code>--jinja</code>（完整控制）、vLLM/SGLang（多用戶生產）</li><li>模型：qwen3:8b（推薦入門，5GB Q4，8GB RAM 可跑）</li><li>技術：Flash Attention、imatrix 量化、Speculative Decoding、MTP（Multi-Token Prediction）</li></ul><h3 id="批判性觀察" tabindex="-1">批判性觀察 <a class="header-anchor" href="#批判性觀察" aria-label="Permalink to &quot;批判性觀察&quot;">​</a></h3><ol><li><strong>內容偏入門指南</strong>：8 節中有 3 節（§1、§2、§7）為概念導覽，對已熟悉本地部署的讀者附加資訊有限；§6 優化段落有深度，但篇幅相對薄弱</li><li><strong>缺乏實測對比數據</strong>：引用的 bandwidth 數字來自廠商規格（非 LLM 實測），imatrix vs standard Q4 的品質差異未提供 benchmark 佐證</li><li><strong>GCP/雲端場景對比缺失</strong>：文章明確界定「single-machine inference」，未討論 on-prem vs Cloud API 的 TCO 決策點，對雲端架構師的可行動性受限</li></ol><hr><h2 id="評分" tabindex="-1">評分 <a class="header-anchor" href="#評分" aria-label="Permalink to &quot;評分&quot;">​</a></h2><table tabindex="0"><thead><tr><th>維度</th><th>分數</th><th>說明</th></tr></thead><tbody><tr><td>A. Workspace 可行動性 (30%)</td><td>6/10</td><td>cc-workspace 以 GCP API 為主，本地 LLM 部署場景有限；記憶體計算公式和量化選擇準則對 on-prem TCO 評估有參考價值</td></tr><tr><td>B. 創新性 (20%)</td><td>5/10</td><td>整理清晰但概念非新穎；imatrix 量化和 MoE active params 區分是少數有深度的補充</td></tr><tr><td>C. 證據品質 (20%)</td><td>6/10</td><td>含具體規格數字（GB/s、bytes/param），但缺乏實測對比；內容偏指導性而非實驗性</td></tr><tr><td>D. 技術深度 (15%)</td><td>7/10</td><td>KV cache 計算公式（含 GQA/MQA 說明）、MoE active params 區分、imatrix 原理均有實質深度</td></tr><tr><td>E. 泛化性 (15%)</td><td>7/10</td><td>5 層診斷框架和 KV cache 線性擴展概念廣泛適用；Q4 vs 架構大小的 trade-off 規則通用</td></tr><tr><td><strong>加權總分</strong></td><td><strong>6.1/10</strong></td><td>6×0.3+5×0.2+6×0.2+7×0.15+7×0.15 = 1.8+1.0+1.2+1.05+1.05</td></tr></tbody></table><p><strong>整合決策</strong>：Skip <strong>理由</strong>：總分 6.1 勉強過線，但核心知識為業界常識整理，Workspace 主要使用 GCP 雲端推論，本地 LLM 部署場景無直接應用需求。</p>`,54)])])}const m=a(i,[["render",o]]);export{u as __pageData,m as default};
