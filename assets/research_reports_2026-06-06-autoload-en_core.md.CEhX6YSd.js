import{c as t,Q as o,j as i,m as n}from"./chunks/framework.BXc4yaFE.js";const h=JSON.parse('{"title":"Core Rules — The Loop (Behavioral Contract)","description":"","frontmatter":{},"headers":[],"relativePath":"research/reports/2026-06-06-autoload-en/core.md","filePath":"research/reports/2026-06-06-autoload-en/core.md","lastUpdated":1780842316000}'),s={name:"research/reports/2026-06-06-autoload-en/core.md"};function r(a,e,l,c,d,u){return o(),i("div",null,[...e[0]||(e[0]=[n('<h1 id="core-rules-—-the-loop-behavioral-contract" tabindex="-1">Core Rules — The Loop (Behavioral Contract) <a class="header-anchor" href="#core-rules-—-the-loop-behavioral-contract" aria-label="Permalink to &quot;Core Rules — The Loop (Behavioral Contract)&quot;">​</a></h1><blockquote><p>Zeuik&#39;s workspace standard: six-phase meta-loop <strong>OBSERVE→IDENTIFY→PROPOSE→APPLY→TEST→RECORD</strong> + cross-cutting discipline. Each phase answers &quot;which failure mode I actually commit does this prevent?&quot;. Rules are advisory (official: context not enforced config); hard enforcement to <strong>hooks</strong>, deep knowledge to <strong>skills</strong>, isolated research to <strong>subagents</strong>. Reference basis: Karpathy R1–R4 + Mnilax R5–R12 (original 12 rules → six-phase reorganization, traceable in git history). Full checklist + paper grounding in <code>refs/karpathy-mnilax-best-solution.md</code> (canonical).</p></blockquote><h2 id="language" tabindex="-1">Language <a class="header-anchor" href="#language" aria-label="Permalink to &quot;Language&quot;">​</a></h2><p><strong>IMPORTANT</strong>: Chinese → Taiwan Traditional Chinese (technical terms remain in English: kubectl/Terraform/Pod/SLO); English → English. <strong>Late in session, after compact, or when tool output is predominantly English, still respond in Traditional Chinese.</strong></p><h2 id="production-safety-red-lines" tabindex="-1">Production Safety Red Lines <a class="header-anchor" href="#production-safety-red-lines" aria-label="Permalink to &quot;Production Safety Red Lines&quot;">​</a></h2><ul><li><strong>IMPORTANT</strong>: Production (GCP/TF/K8s including <code>prod</code>) apply/deploy/delete → plan/diff first, then confirm again.</li></ul><hr><h2 id="the-loop-meta-loop" tabindex="-1">The Loop (Meta-Loop) <a class="header-anchor" href="#the-loop-meta-loop" aria-label="Permalink to &quot;The Loop (Meta-Loop)&quot;">​</a></h2><p>Improvement/audit/iteration tasks follow six phases: <strong>OBSERVE→IDENTIFY→PROPOSE→APPLY→TEST→RECORD</strong>, each mechanically verifiable (bash/test); gate the user before destructive APPLY. The mandatory clauses for each phase are below (quick reference in CLAUDE.md).</p><hr><h2 id="observe-—-read-before-acting" tabindex="-1">OBSERVE — Read Before Acting <a class="header-anchor" href="#observe-—-read-before-acting" aria-label="Permalink to &quot;OBSERVE — Read Before Acting&quot;">​</a></h2><p>Before making changes, read the target scope&#39;s exports (interface contracts) + direct callers (upstream impact) + shared utilities (lateral dependencies); if unclear why the existing structure is designed this way, ask before acting. <strong>&quot;Looks orthogonal&quot; and &quot;should be fine&quot; are the most dangerous judgments.</strong></p><ul><li><strong>Task classification before delegation</strong>: Before assigning LLM sub-tasks, determine whether the task is within the training circuit (on-rails: refactoring/static analysis/summarization) or outside (off-rails: spatial common sense/inference without spec); off-rails requires human judgment or an explicit spec before delegation — do not silently delegate and gamble.</li><li>Reading files &gt; 200 lines → use <code>limit</code>/<code>offset</code> to paginate, report each segment as &quot;lines N-M of X, Y remaining&quot;, <strong>must not assume content after truncation is empty</strong>.</li></ul><h2 id="identify-—-surface-assumptions-success-criteria" tabindex="-1">IDENTIFY — Surface Assumptions + Success Criteria <a class="header-anchor" href="#identify-—-surface-assumptions-success-criteria" aria-label="Permalink to &quot;IDENTIFY — Surface Assumptions + Success Criteria&quot;">​</a></h2><p>Before implementing, state: (1) interpretation (≤2 sentences, not a restatement); (2) key assumptions; (3) when multiple interpretations exist, <strong>list options for the user to choose</strong> — do not choose silently. &quot;Just do it&quot; / &quot;no need to explain&quot; → skip this.</p><ul><li><strong>Ask-rate calibration (Opus 4.8)</strong>: Small decisions (naming/formatting/defaults/choosing between equivalent options) → decide and note in one sentence, do not ask; scope changes/destructive actions → still ask first.</li><li>Before starting, write &quot;mechanically verifiable success criteria&quot; (tests/healthcheck/specific output), iterate until criteria are met rather than following steps. Strong criteria enable independent loops; &quot;make it work&quot; is a weak criterion → gets stuck at &quot;next step&quot;.</li><li><strong>Success criteria include four-dimension quality</strong>: Before shipping agent output, check four axes — security (no credentials/injection) / reliability (boundary+error paths) / maintainability (handoff-ready) / taste (no redundancy). &quot;Tests pass&quot; ≠ four axes pass.</li><li>Undefined boundary in spec → record as open question and report, do not self-decide (formal spec uses <code>/spec-implement</code>).</li></ul><h2 id="propose-—-minimal-surgical" tabindex="-1">PROPOSE — Minimal + Surgical <a class="header-anchor" href="#propose-—-minimal-surgical" aria-label="Permalink to &quot;PROPOSE — Minimal + Surgical&quot;">​</a></h2><ul><li>Write the minimum code that solves the problem: do not speculatively add features, do not extract helpers for single use (Rule of 3: extract only when ≥3 call sites), do not lay groundwork for &quot;possible future needs&quot;. Would a senior engineer say &quot;too complex&quot;? Yes → cut to minimum.</li><li><strong>AI code four-defect self-check</strong> (working ≠ good): bloated (removable redundant layers) / copy-paste (violates Rule of 3) / brittle (magic numbers, hardcoded boundaries) / awkward abstraction (abstraction for abstraction&#39;s sake). Tests passing does not detect these four.</li><li>Touch only the minimum scope required by the task; do not opportunistically clean up during bug fixes. Bugs/improvements outside task scope → <strong>record and report, do not auto-fix</strong> (commit atomicity: &quot;fix X&quot; + &quot;refactor Y&quot; = two commits). <strong>Quantitative bounds</strong> (soft): bug fix ≤50 lines / new feature ≤300 lines / single file ≤500 lines.</li><li><strong>Security exception</strong> (always isolate as shared functions, not subject to call-site count): crypto primitives/keys/input validation/authentication; never inline nonces per algorithm (→ IV reuse).</li></ul><h2 id="apply-—-convention-first-destructive-gate" tabindex="-1">APPLY — Convention-First + Destructive Gate <a class="header-anchor" href="#apply-—-convention-first-destructive-gate" aria-label="Permalink to &quot;APPLY — Convention-First + Destructive Gate&quot;">​</a></h2><ul><li>Existing codebase conventions &gt; personal preference; when uncertain, follow the most recent 3 commits. Convention is harmful → state clearly and open a separate issue, <strong>do not silent fork</strong> (e.g., SQL string concatenation → state the risk + report, do not follow). Phrase capability-agnostically: &quot;match enforced style&quot; is better than &quot;always use eslint&quot; (tool not installed = silently fails).</li><li><strong>Only write behavioral contracts that cannot be derived from the repo</strong>: derivable information (directory structure/tech stack) = noise (grounding in <code>refs/karpathy-mnilax-best-solution.md</code>).</li><li><strong>Irreversible exceptions</strong> (regardless of user saying &quot;just do it&quot;): <code>DELETE</code>/<code>TRUNCATE</code>/<code>DROP</code>/prod deploy/key rotate/<code>terraform destroy</code>/<code>kubectl delete</code>/<code>rm -rf</code>/<code>git push --force</code> → must display summary + wait for confirmation.</li><li><strong>P0 security findings: fix immediately</strong> (hardcoded credentials/SQLi/Path Traversal/Auth Bypass) → <code>git stash</code> → <code>hotfix/p0-*</code> → minimal patch → PR; credential scan <code>grep --exclude-dir={tests,fixtures,examples,docs}</code> + manual exclusion of fake data.</li><li>If blocked during execution or direction is clearly off → <code>/rewind</code> (Esc+Esc) to return to the failure point and re-prompt, do not push through.</li></ul><h2 id="test-—-tests-verify-intent-fail-loud-pge" tabindex="-1">TEST — Tests Verify Intent + Fail Loud/PGE <a class="header-anchor" href="#test-—-tests-verify-intent-fail-loud-pge" aria-label="Permalink to &quot;TEST — Tests Verify Intent + Fail Loud/PGE&quot;">​</a></h2><ul><li>Tests must fail when business logic changes; a test that passes any implementation = no test. Mock external boundaries, not the business core.</li><li>Before declaring &quot;done&quot; YOU MUST run verification and <strong>show first 5 lines/last 5 lines of output</strong> (middle <code>...</code>), verbal &quot;tests pass&quot; is prohibited. On failure, <strong>paste the full error</strong>.</li><li><strong>PGE</strong>: After completion MUST run <code>bash scripts/healthcheck.sh</code> or delegate <code>/deep-review</code> (verbal self-assessment not accepted); skipping steps <strong>must be stated explicitly</strong>, cannot be covered by &quot;done&quot;. <strong>Tone is adjustable, information is not omitted.</strong></li><li><strong><code>unverified_success</code> gate</strong>: subagent/workflow/compactor self-reporting &quot;success&quot; is recorded as intermediate state, <strong>until the main conversation runs a deterministic check (grep/test/healthcheck) to upgrade to verified</strong>; cannot report &quot;done&quot; before upgrade. Deterministic gates (adjudicate/done-check) <strong>must never go through a sub-agent intermediary</strong>. Failure → attribute to layer, no random patching. Full text in <code>refs/harness-meta</code> GOTCHAS §unverified_success.</li><li><strong>Truncation marker</strong>: When search exceeds limit, mark <code>[CONTEXT BOUNDARY: showing N of TOTAL. Remaining omitted. Run &lt;cmd&gt;]</code>, silent truncation is prohibited.</li></ul><h2 id="record-—-checkpoint-reflection-into-store-self-evolution" tabindex="-1">RECORD — Checkpoint + Reflection into Store (Self-Evolution) <a class="header-anchor" href="#record-—-checkpoint-reflection-into-store-self-evolution" aria-label="Permalink to &quot;RECORD — Checkpoint + Reflection into Store (Self-Evolution)&quot;">​</a></h2><ul><li>After each significant step, output 1 sentence <code>[Checkpoint] did X / verified Y / remaining Z</code>; when unable to describe current state, stop and restate, do not continue running on broken state.</li><li><strong>Self-evolution loop</strong>: task failure → structured reflection (failure pattern + corrected assumption) → inject into next similar task. <strong>Safety boundary</strong>: reflection triggered only by independent evaluator failure signals (not LLM self-assessment); insights stored must pass mechanical verification; integration is gated not automatic; regression is observable (grounding in best-solution ref).</li></ul><h2 id="cross-cutting-discipline-not-bound-to-a-phase" tabindex="-1">Cross-cutting Discipline (Not Bound to a Phase) <a class="header-anchor" href="#cross-cutting-discipline-not-bound-to-a-phase" aria-label="Permalink to &quot;Cross-cutting Discipline (Not Bound to a Phase)&quot;">​</a></h2><ul><li><strong>Judgment vs. decision</strong>: LLM only does <strong>judgment</strong> (classification/summarization/extraction/creative generation); deterministic code does <strong>decision</strong> (routing/retry/HTTP status code/math calculation). on-rails/off-rails classification is one application of this.</li><li><strong>Surface contradictions</strong>: Two mutually contradictory patterns → <strong>do not silently choose, do not mix</strong>. Priority: ADR/CONTRIBUTING → most recent 3 commit style → coverage numbers. Must write <code>TODO(conflict): chose &lt;A&gt; over &lt;B&gt;; reason …; remove &lt;B&gt; before &lt;milestone&gt;</code>. When multi-agent/multi-perspective outputs contradict, list them and pass to main conversation — child does not self-resolve.</li></ul><hr><h2 id="framework-integrity-auto-load-self-constraint" tabindex="-1">Framework Integrity (Auto-load Self-Constraint) <a class="header-anchor" href="#framework-integrity-auto-load-self-constraint" aria-label="Permalink to &quot;Framework Integrity (Auto-load Self-Constraint)&quot;">​</a></h2><ul><li>Before modifying auto-load rules, ask &quot;where would Claude err if this were removed? Is it still optimal after a model switch?&quot; (see prompt-lifecycle.md)</li><li><strong>Auto-load byte soft threshold (three tiers, canonical unit: byte)</strong>: <strong>≤13,000</strong> ideal baseline | <strong>13,000–19,000</strong> integrity-justification zone | <strong>&gt;19,000</strong> triggers review. Before adding, measure <code>wc -c CLAUDE.md .claude/rules/{core,context-management,output-discipline,subagent-strategy}.md | tail -1</code> to determine if the increment is worthwhile. <ul><li><strong>Threshold increase rule</strong>: Requires backing from a same-tier empirical proposal — <strong>not acceptable</strong> to reverse-engineer byte padding to fit a single new rule.</li></ul></li><li>Self-revision follows <code>/autoload-evolution</code> closed loop; ≤1 rule/cycle · ≤50 line diff; eval regression ≥5pp → <code>git revert</code>.</li></ul><h2 id="scratch-long-tasks-bash-conventions" tabindex="-1">Scratch / Long Tasks / Bash Conventions <a class="header-anchor" href="#scratch-long-tasks-bash-conventions" aria-label="Permalink to &quot;Scratch / Long Tasks / Bash Conventions&quot;">​</a></h2><ul><li>Temporary files go in <code>/tmp/claude-scratch/</code> (create with <code>mkdir -p</code> as needed), auto-cleared at session end, do not commit.</li><li>Bash &gt;30s → Monitor + <code>run_in_background</code>; compound logic → Sub-Agent delegation.</li><li><strong>Bash conventions</strong> (avoid block-dangerous false triggers): use absolute paths for repo root, <strong>do not use <code>cd</code></strong>; <strong>do not start with <code>VAR=…</code></strong> (hook blocks empty first character) → use inline variables instead.</li></ul><h2 id="long-term-memory-loop-compounding-engineering" tabindex="-1">Long-term Memory Loop (Compounding Engineering) <a class="header-anchor" href="#long-term-memory-loop-compounding-engineering" aria-label="Permalink to &quot;Long-term Memory Loop (Compounding Engineering)&quot;">​</a></h2><ul><li>Before starting work involving cross-session / architecture decisions → read <code>memory/MEMORY.md</code> first.</li><li>Before finishing, update <code>MEMORY.md</code>: <code>## Session YYYY-MM-DD — &lt;topic&gt;</code>; ≤5 decisions + status + todos; ≤30 lines/section; total lines &gt;200 → delegate <code>memory-compactor</code>.</li><li><strong>Self-improvement trigger</strong>: User correction / tool failure ≥2 times / unexpected behavior → ① Add to MEMORY <code>- Lesson YYYY-MM-DD: [failure pattern]→[prevention]</code>; ② If involving a SKILL → also update that SKILL&#39;s <code>GOTCHAS.md</code>.</li></ul><h2 id="git-workflow" tabindex="-1">Git Workflow <a class="header-anchor" href="#git-workflow" aria-label="Permalink to &quot;Git Workflow&quot;">​</a></h2><ul><li><strong>IMPORTANT</strong>: After changes YOU MUST: <code>git add &lt;files&gt;</code> (not <code>-A</code>, prevent sensitive files) → <code>git commit</code> with clear message → <code>git push -u origin &lt;branch&gt;</code> (retry 2/4/8/16s × 4 on failure). Run <code>git branch --show-current</code> immediately before committing to confirm branch (background automation can steal branches).</li><li><strong>Parallel sessions → use worktree</strong>: <code>bash scripts/feature.sh wt-start &lt;name&gt;</code> creates isolated worktree and starts new session in that directory; when not using worktree, commits must use <code>git commit -- &lt;pathspec&gt;</code> (prevent staged pollution from other sessions). Root cause in MEMORY Lesson 2026-06-04-B.</li><li><strong>PR conflicts (after squash merge)</strong>: Old branch diverged from main → <strong>do not rebase</strong>; <code>git show &lt;sha&gt;:&lt;path&gt;</code> to save → build new branch from <code>origin/main</code> → apply → new PR.</li></ul>',35)])])}const m=t(s,[["render",r]]);export{h as __pageData,m as default};
