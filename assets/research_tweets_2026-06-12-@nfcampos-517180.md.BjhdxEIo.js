import{c as t,Q as s,j as a,m as i}from"./chunks/framework.BXc4yaFE.js";const u=JSON.parse('{"title":"Loop Driven Development, in practice","description":"","frontmatter":{"url":"https://x.com/nfcampos/status/2065485993734517180","title":"Loop Driven Development, in practice","author":"Nuno Campos","archived":"2026-06-13T00:00:00.000Z","domains":["ai-coding","agentic-workflows","benchmarking","verification"],"external_link":"xlsx-corpus-bench (Apache 2.0)","followers":3298,"handle":"nfcampos","platform":"twitter","published":"2026-06-12T00:00:00.000Z","ratings":{"signal":5,"novelty":5,"actionability":5,"durability":4,"relevance":5,"total":24},"stats":{"likes":162,"retweets":19,"replies":2,"views":11488},"tags":["loop-engineering","ldd","oracle-testing","claude-code","evaluation-harness","xlsx-corpus-bench"],"type":"article"},"headers":[],"relativePath":"research/tweets/2026-06-12-@nfcampos-517180.md","filePath":"research/tweets/2026-06-12-@nfcampos-517180.md","lastUpdated":1785909234000}'),n={name:"research/tweets/2026-06-12-@nfcampos-517180.md"};function o(r,e,h,l,c,d){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h2 id="中文摘要" tabindex="-1">中文摘要 <a class="header-anchor" href="#中文摘要" aria-label="Permalink to &quot;中文摘要&quot;">​</a></h2><p>Nuno Campos（LangChain 核心、LangGraph 作者）將前一篇 LDD 理論落到<strong>一個真實專案</strong>：用 Claude Code 跑兩個 session（約 39 小時 wall-clock、~800 turns、多數無人值守）建出 <code>xlsx-corpus-bench</code>——對 5 個 xlsx 引擎跑 15,970 個真實工作簿（680 萬個公式 cell），逐 cell 比對「重算結果是否與真實 Excel 一致」。Agent 寫了全部程式碼；人類只做<strong>四個決策</strong>：</p><p><strong>Decision 1 — 選哪個 oracle（真值來源）</strong> 否決「比對檔案內快取值」（provenance 不明，可能是 LibreOffice 存的）→ 改用 <strong>Excel 本身</strong>：注入 <code>fullCalcOnLoad=&quot;1&quot;</code> 強制重算 → 凍結為 frozen truth（一次生成、永久離線比對）。</p><p><strong>Decision 2 — pin 哪些 property</strong> 只 pin 三項：能否開啟 / open→save→reopen 是否存活 / 重算是否符合 Excel。<strong>刻意不 pin</strong> 速度/記憶體/API（Excel 無對應答案，不主張）。判斷準則：「有疑慮時，做你能用一句話辯護的選擇」。拒絕「可能被視為 cherry-picking」的 heuristic 排除。</p><p><strong>Decision 3 — 比較的形狀</strong> Per-cell、binary、帶容差等值。整個比較政策 = 一個共用函式 <code>values_match</code>（數字 1e-9 相對容差、字串/錯誤碼精確比對）。雙指標並陳：「零錯 cell 的工作簿 %」（使用者問題）+「全 cell 符合 %」（工程訊號）。每檔有 JSONL receipt 可回溯。</p><p><strong>Decision 4 — agent 何時算完成</strong> 從不讓 agent 自己決定 done。Done-ness <strong>存在檔案裡</strong>：bench 把失敗寫進 checked-in report（<code>WITAN-FAILURES.md</code>），逐項列舉+排序 → 第二 session 直接餵這份 report 當工作佇列。明確圍欄 scope（&quot;stop when you get to the perf tracks&quot;）。Agent 宣稱行為細節時<strong>不採信口頭</strong>：「你確定跟 Excel 確認過 COUNTIFS 真的不同？」→ ask for the receipt。</p><p><strong>最關鍵的實戰教訓</strong>：recalc report 列出 281,941 個 mismatch，但最大宗根因<strong>不是引擎 bug</strong>——是 bench 與 truth 在不同 locale 下跑（~128,000 cell 取決於 <code>31/12/2023</code> 是否解析為日期）。pin locale + 重建 truth 後，真實引擎差距僅 <strong>10,604 cell（原始的 ~4%）</strong>，其餘 96% 是測量誤差。→ 印證「壞 oracle 比沒 oracle 更糟，因為把工作導向錯方向」。Debug 訊號本身是 loop 的一部分，且必須先做。</p><h2 id="the-recipe-可複用模板" tabindex="-1">The Recipe（可複用模板） <a class="header-anchor" href="#the-recipe-可複用模板" aria-label="Permalink to &quot;The Recipe（可複用模板）&quot;">​</a></h2><ol><li><strong>公開 corpus 當分母</strong>——行為取自 wild，非被測團隊自編。</li><li><strong>真實應用當 oracle，腳本化</strong>——frozen truth 一次生成、永久比對。</li><li><strong>一個 comparator、一個 universe、無可疑排除</strong>——每個方法學選擇偏向可辯護性。</li><li><strong>失敗 report 當 agent 工作佇列</strong>——done = 列舉的 gap 歸零，由找出它的同一 loop 重新驗證。</li><li><strong>全部 pin</strong>——corpus / truth snapshot / comparator / 甚至 frozen engine binary（commit pin），任何已發布數字可離線重現。</li></ol><h2 id="與-workspace-的關聯" tabindex="-1">與 workspace 的關聯 <a class="header-anchor" href="#與-workspace-的關聯" aria-label="Permalink to &quot;與 workspace 的關聯&quot;">​</a></h2><p>這是前一篇（@bibryam）LDD 理論的<strong>實作對照</strong>，與 cc-workspace The Loop 多處硬映射：</p><ul><li><strong>「不採信 agent 口頭、ask for the receipt」</strong> = core.md TEST 的 <strong>unverified_success 閘門</strong>（subagent 自報成功 = 中間態，主對話親跑確定性檢查才升 verified；確定性 gate 絕不經 sub-agent 中介）。Campos 的「the oracle is right there」就是 workspace「verdict 非證據必 grep 重驗」。</li><li><strong>「Done-ness 存在檔案裡，不讓 agent 自己決定」</strong> = workspace IDENTIFY「寫可機械驗證的成功條件」+ RECORD「claude-progress.json 跨 session 工作佇列」。</li><li><strong>四維品質中的 oracle/comparator 防 cherry-picking</strong> = APPLY「不 silent fork、慣例有害明說風險」+ 「偏向可辯護性」。</li><li><strong>128K cell 的 locale 假象</strong> = 「unverified_success」最佳實戰範例——若信了 report 的 281,941 表面數字，96% 工作會導向錯方向。對應 workspace「失敗歸因到層，禁隨機修補」。</li><li><strong>worktree 隔離 + atomic commits + 圍欄 scope</strong> = 直接對映 workspace Git 工作流程（worktree 隔離）+ PROPOSE「只動最小範圍、commit 原子性」。</li></ul><h2 id="評分-5-維-各-5-分-總-25" tabindex="-1">評分（5 維 / 各 5 分，總 25） <a class="header-anchor" href="#評分-5-維-各-5-分-總-25" aria-label="Permalink to &quot;評分（5 維 / 各 5 分，總 25）&quot;">​</a></h2><table tabindex="0"><thead><tr><th>維度</th><th>分數</th><th>理由</th></tr></thead><tbody><tr><td>Signal</td><td>5</td><td>verbatim transcript 引用 + 具體數字（39h/800turns/6.8M cells/281941→10604），訊號極密</td></tr><tr><td>Novelty</td><td>5</td><td>把抽象 LDD 落成可複現的四決策框架 + 揭露「96% 是測量誤差」這種反直覺實戰真相</td></tr><tr><td>Actionability</td><td>5</td><td>「The Recipe」5 步是可直接套用的模板；四決策每個都有 transcript 範例</td></tr><tr><td>Durability</td><td>4</td><td>xlsx 細節會過時，但四決策框架 + oracle-as-truth + done-in-files 的方法學持久</td></tr><tr><td>Relevance</td><td>5</td><td>與 workspace The Loop / unverified_success 閘門 / worktree 紀律一對一</td></tr><tr><td><strong>總分</strong></td><td><strong>24/25</strong></td><td>極高價值，LDD 理論的標竿實作案例，建議列 harness 必讀</td></tr></tbody></table><hr><h2 id="原文全文-英文" tabindex="-1">原文全文（英文） <a class="header-anchor" href="#原文全文-英文" aria-label="Permalink to &quot;原文全文（英文）&quot;">​</a></h2><p>In Loop Driven Development I made an argument: when coding agents do the implementation, the engineering that matters moves to the feedback loop, and the human&#39;s job comes down to four decisions — which oracle to trust, which properties to pin, what shape the comparison takes, and when the agent is done. This post walks through one real project, decision by decision, with verbatim excerpts from the two Claude Code sessions where it happened. The result is public: xlsx-corpus-bench (Apache 2.0). Here&#39;s the first message of the session that built it:</p><blockquote><p>how could we quantify what % of the ooxml spec or excel feature set our xlsx product covers, compared to alternatives, like openpyxl, libre office, aspose, epplus, closedxml etc. is this doable without fudging numbers, ie in a way that people wont question it Notice the question isn&#39;t really about tests. It&#39;s about credible numbers. Anyone can publish a benchmark where their own product wins; the hard part is building one where the methodology holds up for readers who have every incentive to find the flaw. &quot;In a way that people won&#39;t question it&quot; ended up being the constraint behind most of the decisions below. One session later I had a benchmark that runs 15,970 real-world workbooks (6.8 million formula cells) through five xlsx engines and compares every recalculated formula cell against what real Excel produces. The session ran for about 39 hours of wall clock (roughly 800 turns), most of it unattended. Two public corpora bracket the space: FUSE, 10,544 workbooks crawled from the open web — old, hostile, every producer imaginable — and SpreadsheetBench, 5,426 modern, formula-heavy workbooks from real Excel forum questions. Every number has an external denominator and a binary, automated pass/fail. The agent built all of it: the corpus plumbing, the Excel automation, the comparators, the reports, the charts. What I contributed is the four decisions. Let&#39;s go through them one at a time.</p></blockquote><h2 id="decision-1-which-oracle" tabindex="-1">Decision 1: which oracle <a class="header-anchor" href="#decision-1-which-oracle" aria-label="Permalink to &quot;Decision 1: which oracle&quot;">​</a></h2><p>The first version of the bench measured recalculation the obvious way: open each workbook, recalculate, and compare against the values already cached inside the file. Spreadsheet files store the last computed value of every formula, so this looks like free ground truth — millions of expected values, no extra work. I dropped it as soon as I noticed:</p><blockquote><p>definition for recalculation is compared to what was in the file, or to what excel would produce, only the 2nd is valuable i think? as we dont know where the file came from, for all i know it was last edited by libreoffice or something The problem with cached values is provenance. A workbook crawled from the open web might have last been saved by LibreOffice, by a Java library from 2009, or by a tool that never ran the calculation at all. Comparing against those caches doesn&#39;t measure &quot;does this engine calculate like Excel&quot;; it measures &quot;does this engine agree with whatever last touched the file.&quot; Only the first is worth publishing. So the harness asks Excel itself, fresh, for every file. harness/excel_truth.py stages a copy of each workbook with fullCalcOnLoad=&quot;1&quot; injected into its <code>&lt;calcPr&gt;</code> element (this forces Excel to recompute everything on open instead of trusting the stored values), opens it via AppleScript with links not updated, saves the result, and that recalculated copy becomes the frozen truth. This is slow, but it doesn&#39;t matter: truth generation runs once per corpus, and everything downstream compares against the frozen output, offline, forever.</p></blockquote><h2 id="decision-2-which-properties-to-pin" tabindex="-1">Decision 2: which properties to pin <a class="header-anchor" href="#decision-2-which-properties-to-pin" aria-label="Permalink to &quot;Decision 2: which properties to pin&quot;">​</a></h2><p>The bench pins three behaviors per library, and the README defines each in a sentence: does the file open, does it survive open→save→reopen (where the written output also has to pass a library-neutral structural validation — readable zip, well-formed XML, all relationship targets resolve), and does recalculation match Excel. Just as deliberate is what I didn&#39;t pin: speed, memory, API design. Excel has no answer for those, so the bench makes no claim about them. Inside those three metrics is where the human time actually went, on judgment calls that all followed the same rule: when in doubt, make the choice you&#39;d be comfortable defending. Two examples from the transcript. The agent proposed skipping formulas that reference external workbooks. Reasonable on the surface — the bench can&#39;t open files it doesn&#39;t have. But that&#39;s not how Excel handles them:</p><blockquote><p>im not sure this is right, as there should be cached values for the external refs, which should be used by good engines to compute those formulas Excel uses the cached values of external references it can&#39;t resolve, so an engine that does the same can be judged on those cells. Excluding them would have quietly excused real failures, and it&#39;s exactly the kind of hole a critical reader goes looking for. Later the agent went the other way and started implementing filters that would only ever be halfway houses — incomplete exclusions that, however well intentioned, could come across as cherry-picking: [Request interrupted by user] actually lets leave this as is, i dont want heuristics or exclusions that might be deemed dubious One exclusion did survive, and the reason it survived is the point: it can be defended in a sentence. Some corpus files were written by generator tools that cache an empty string for formulas they never evaluated, which is indistinguishable from a genuine empty-string result. Judging an engine against those cells would count the file&#39;s staleness as the engine&#39;s failure. The comparator&#39;s docstring in harness/cached_values.py carries the full argument, and cells the bench can&#39;t judge are reported, not judged. The same rule settled the denominator question: when we then compute stats for each of the runners we should fully exclude files excel cant open from both numerator and denominator, we must use exact same file set for all tools being tested This is now a load-bearing comment in harness/report.py: one universe for every tool — the files Excel itself could process — excluded from numerator and denominator of every metric, for every library. No engine gets a friendlier denominator than any other.</p></blockquote><h2 id="decision-3-what-shape-the-comparison-takes" tabindex="-1">Decision 3: what shape the comparison takes <a class="header-anchor" href="#decision-3-what-shape-the-comparison-takes" aria-label="Permalink to &quot;Decision 3: what shape the comparison takes&quot;">​</a></h2><p>Per-cell, binary, equality with a tolerance. The entire comparison policy fits in one function, shared by every engine:</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">def</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> values_match</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a, b):</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    &quot;&quot;&quot;Shared comparator: numbers/bools with 1e-9 relative tolerance,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    strings exact, errors exact by code.&quot;&quot;&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    (ka, va), (kb, vb) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> a, b</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    num_a, num_b </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ka </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;n&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;b&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), kb </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;n&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;b&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> num_a </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">and</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> num_b:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        tol </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> max</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1e-9</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">abs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(va) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1e-9</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> abs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(va </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> vb) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> tol</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ka </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> kb:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> False</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> va </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> vb</span></span></code></pre></div><p>Numbers get a 1e-9 relative tolerance because floating-point accumulation legitimately differs between engines, and pinning the 16th digit would punish implementation order rather than correctness. Strings and error codes get exact equality, because there&#39;s no legitimate reason for #VALUE! and #REF! to disagree. And the fact that this is one function matters as much as its contents: every engine is judged by the same dozen lines. Aggregation gets reported two ways, because they answer different questions. Percent of workbooks with zero mismatched cells is the user&#39;s question: is my file right, yes or no? Percent of all formula cells matching is the engineering signal: how much behavior is correct? A workbook with one wrong cell out of ten thousand fails the first metric and barely moves the second, so publishing both keeps either one from flattering anybody. Underneath the aggregates, every file gets a JSONL receipt, so any number in the README can be traced down to the exact cells that disagreed.</p><h2 id="decision-4-when-the-agent-is-done" tabindex="-1">Decision 4: when the agent is done <a class="header-anchor" href="#decision-4-when-the-agent-is-done" aria-label="Permalink to &quot;Decision 4: when the agent is done&quot;">​</a></h2><p>I never asked the agent to decide when it was finished. Done-ness lived in files: the bench writes its failures into checked-in reports — WITAN-FAILURES.md, WITAN-RECALC-GAPS.md — that are enumerated and prioritized. The second session, this time in the engine repo, opened by handing the agent one of them:</p><blockquote><p>in a new worktree, investigate these failures ~/dev/xlsx-corpus-bench/results-fuse/WITAN-FAILURES.md report back on your findings, ideally we want to handle all of these as gracefully as possible, if need be use excel oracle Scope was fenced explicitly, rather than trusting the agent to stop in the right place: proceed with the others in suggested priority, start a branch for this, atomic commits, add tests as well, stop when you get to the perf tracks And when the agent claimed a behavioral subtlety — that COUNTIFS and related functions ignore cached external-reference values where most functions use them — it didn&#39;t get taken at its word: you&#39;ve definitely confirmed with excel that countifs etc are different from most functions in that they ignore cache for external refs? This habit is worth copying. An agent in this loop is investigator and implementer at once, and the failure mode is subtle: it reasons its way to what Excel probably does instead of asking. The fix is cheap — ask for the receipt. The oracle is right there.</p></blockquote><h2 id="closing-the-loop" tabindex="-1">Closing the loop <a class="header-anchor" href="#closing-the-loop" aria-label="Permalink to &quot;Closing the loop&quot;">​</a></h2><p>Everything above is the first half of LDD: producing the signal. The second session — a long one in the engine repo, working through the failure reports — is what the signal is for. It didn&#39;t go the way I expected, and the way it actually went is more instructive. The load failures were the straightforward part. The agent worked through the checked-in report, fixed everything (it had already caught a false positive in the harness on its way in: &quot;the harness bug is being fixed by someone else, thanks for the catch&quot;), and the published tables now show 100.0% of both corpora opening and surviving round-trip. The recalculation report was the interesting one. It listed 281,941 mismatched cells, and the agent&#39;s investigation collapsed them into eight root causes — of which the single biggest wasn&#39;t an engine bug at all. The bench was running the engine under a different locale than the machine that generated the Excel ground truth, and the largest &quot;function bug&quot; groups in the report turned out to be cascades of that one mismatch: on the order of 128,000 cells that came down to whether 31/12/2023 parses as a date. The old truth data had problems of its own and was regenerated. With the locale pinned and the truth rebuilt, the real engine gap was 10,604 cells across 235 files — about 4% of the original number. The other 96% was measurement error. The LDD post says a bad oracle is worse than no oracle because it sends work in the wrong direction; here is that sentence as 271,000 cells. Debugging the signal is part of the loop, and it has to come first. The residual was genuine engine work, and the agent fixed it in priority order (&quot;fix all in proposed order, do same as excel does&quot;): lookup functions swallowing #REF! errors instead of propagating them, external-workbook references served from cache on some code paths but not others, legacy CSE array formulas, implicit intersection in pre-dynamic-array workbooks, TEXT formatting bugs down in the 1900 leap-bug region. One fix was a deliberate degradation: Excel returns #VALUE! for SUMIF/COUNTIFS over closed external workbooks even when cached values exist, so matching Excel meant making the engine strictly less useful on those cells. That&#39;s a call the oracle decision makes for you — parity is the contract, even when parity is worse — and it&#39;s also why the audit question quoted earlier got asked before the behavior was locked in. One more thing from that session worth mentioning: it ended with two off-thread review comments on the branch — a legacy-intersection flag silently dropped when formulas move between cells, and lowercase INDIRECT column references being rejected — both valid, both fed straight back to the agent. In terms of the LDD post&#39;s feedback ranking, that&#39;s the independent-reviewer rung running on top of the oracle rung, which is where it belongs. Where things stand, from the published tables: witan opens and round-trips 100.0% of both corpora, recalculates 95.3% of FUSE and 95.7% of SpreadsheetBench workbooks fully Excel-identically, and matches Excel on 99.7–99.8% of all formula cells. The remaining gap is enumerated in the same reports that drove the last session. That&#39;s the queue for the next one.</p><h2 id="the-recipe" tabindex="-1">The recipe <a class="header-anchor" href="#the-recipe" aria-label="Permalink to &quot;The recipe&quot;">​</a></h2><p>Strip away the spreadsheet specifics and what&#39;s left is a template I expect to reuse:</p><ol><li>A public corpus as the denominator. Behaviors sampled from the wild, not authored by the team being measured.</li><li>The real application as oracle, scripted. Frozen truth generated once, compared against forever.</li><li>One comparator, one universe, no dubious exclusions. Every methodological choice biased toward defensibility, because the numbers are only useful if people can&#39;t question them.</li><li>Failure reports as the agent&#39;s work queue. Done is enumerated gaps going to zero, re-verified by the same loop that found them.</li><li>Everything pinned. Corpus, truth snapshots, comparator — even a frozen engine binary, pinned by commit, so any published number reproduces offline. The total cost was two long agent sessions, most of them unattended. In the LDD post I argued that when oracle queries become approximately free, the equilibrium number of oracle-derived tests shifts dramatically. This is what that looks like in practice: 6.8 million assertions against real Excel, fix sessions whose work arrives pre-enumerated and pre-prioritized, and a loop that will judge every future engine change the same way it judged the last ones. One last detail: the result charts in the README are authored and rendered by witan itself. The engine under test draws its own report card — which is fine, because the loop is what established the report card is honest.</li></ol>`,36)])])}const g=t(n,[["render",o]]);export{u as __pageData,g as default};
