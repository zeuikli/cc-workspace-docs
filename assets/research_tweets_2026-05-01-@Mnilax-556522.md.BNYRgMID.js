import{c as t,Q as a,j as s,m as n}from"./chunks/framework.BXc4yaFE.js";const u=JSON.parse('{"title":"I tracked 430 hours of Claude Code usage. 73% was wasted on these 9 patterns.","description":"","frontmatter":{"title":"I tracked 430 hours of Claude Code usage. 73% was wasted on these 9 patterns.","author":"@Mnilax (Mnimiy)","date":"2026-05-01T00:00:00.000Z","source":"https://x.com/Mnilax/status/2050261839653556522","type":"tweet"},"headers":[],"relativePath":"research/tweets/2026-05-01-@Mnilax-556522.md","filePath":"research/tweets/2026-05-01-@Mnilax-556522.md","lastUpdated":1779898012000}'),o={name:"research/tweets/2026-05-01-@Mnilax-556522.md"};function i(r,e,l,d,p,h){return a(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="i-tracked-430-hours-of-claude-code-usage-73-was-wasted-on-these-9-patterns-—-mnilax" tabindex="-1">I tracked 430 hours of Claude Code usage. 73% was wasted on these 9 patterns. — @Mnilax <a class="header-anchor" href="#i-tracked-430-hours-of-claude-code-usage-73-was-wasted-on-these-9-patterns-—-mnilax" aria-label="Permalink to &quot;I tracked 430 hours of Claude Code usage. 73% was wasted on these 9 patterns. — @Mnilax&quot;">​</a></h1><p><strong>來源</strong>：<a href="https://x.com/Mnilax/status/2050261839653556522" target="_blank" rel="noreferrer">https://x.com/Mnilax/status/2050261839653556522</a><strong>作者</strong>：Mnimiy（@Mnilax） <strong>發布日期</strong>：2026-05-01 <strong>收錄日期</strong>：2026-05-27（重新收錄，補全 6 個 MARKDOWN code block） <strong>類型</strong>：Twitter Article <strong>統計</strong>：943 likes · 119 retweets · 3881 bookmarks · 1584858 views</p><hr><h2 id="全文" tabindex="-1">全文 <a class="header-anchor" href="#全文" aria-label="Permalink to &quot;全文&quot;">​</a></h2><p>For 90 days I logged every single Claude Code session: timestamp, prompt, response, token count, model, exit reason. 430 hours of work. 6 million input tokens. $1,340 in API spend. Then I sat down with the data and asked the only question that mattered: how much of this was actually doing my work, and how much was overhead? The answer was uncomfortable: 73% of my tokens went to nine invisible patterns that I&#39;d been doing on autopilot. Not bad prompts, I write decent prompts. Not big models when I needed small ones, I knew that one already. Patterns deeper than that. Patterns nobody talks about because they&#39;re invisible until you instrument them. If you&#39;re hitting Claude Max usage limits more than once a week, you have at least 4 of these. Probably 7. Below: each pattern, exactly how much it costs, and the 30-second fix. [圖 1]</p><h2 id="why-this-matters-now" tabindex="-1">Why this matters now <a class="header-anchor" href="#why-this-matters-now" aria-label="Permalink to &quot;Why this matters now&quot;">​</a></h2><p>Anthropic admitted the problem in late March 2026: &quot;people are hitting usage limits in Claude Code way faster than expected.&quot; Max 5 subscribers reported quota exhaustion in 19 minutes instead of the expected 5 hours. The Pro $200/year users said the limit maxed out every Monday and didn&#39;t reset until Saturday - &quot;out of 30 days I get to use Claude 12.&quot; Anthropic acknowledged the issue. The peak-hours quota change in late March explained part of it. The rest was a prompt-caching bug — two independent bugs in the cache layer that silently inflated costs by 10-20x for some sessions. A user reverse-engineered the Claude Code binary to find it (GitHub issue #40524). Downgrading to v2.1.34 helped some people. Some of it is real. Most of it is you. I&#39;m not going to tell you to &quot;use Haiku for simple tasks&quot; or &quot;start a new chat every 15 messages.&quot; Below: the patterns that the obvious advice misses.</p><h2 id="the-methodology" tabindex="-1">The methodology <a class="header-anchor" href="#the-methodology" aria-label="Permalink to &quot;The methodology&quot;">​</a></h2><p>I ran an HTTP proxy between Claude Code and the Anthropic API. Logged every request: full payload, response, token counts (input/output/cache), latency, model. 90 days. 430 hours of active work. For each request, I categorized the tokens into:</p><ul><li>Productive — content that directly informed my actual question</li><li>Cache hit (free) — system prompt + CLAUDE.md cached, no marginal cost</li><li>Cache miss (paid) — same content, recomputed because cache expired</li><li>Conversation history re-read — re-tokenizing previous messages on every turn</li><li>Hook injection — pre-pended context from PreToolUse / UserPromptSubmit hooks</li><li>Skill loading — skill SKILL.md content loaded into context for invocations</li><li>Tool use overhead — JSON schemas, tool definitions, tool result blocks</li><li>Extended thinking — <code>&lt;thinking&gt;</code> blocks</li><li>CLAUDE.md — project rules loaded every turn [圖 2] Productive tokens: 27%. The other 73% is the 9 patterns below, ranked by how much they cost me.</li></ul><h2 id="pattern-1-claude-md-bloat-14-of-total-tokens" tabindex="-1">Pattern 1. CLAUDE.md bloat (~14% of total tokens) <a class="header-anchor" href="#pattern-1-claude-md-bloat-14-of-total-tokens" aria-label="Permalink to &quot;Pattern 1. CLAUDE.md bloat (~14% of total tokens)&quot;">​</a></h2><p>The pattern: my CLAUDE.md grew to 4,800 tokens over 6 months. Every turn loaded all 4,800 tokens. Every session loaded them again on each new request. Most of the rules were never relevant to the task at hand. A 5,000-token CLAUDE.md costs you 5,000 tokens before you&#39;ve typed a word. Every turn. Every session. A constant baseline tax. Multiply by 200 turns per week and it&#39;s 1 million tokens a week of your CLAUDE.md alone. The 30-second fix:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># Find what&#39;s in your CLAUDE.md</span></span>
<span class="line"><span>wc -w ~/.claude/CLAUDE.md</span></span>
<span class="line"><span>wc -w .claude/CLAUDE.md  # project-level</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Target: combined under 1,200 words (~1,500 tokens)</span></span></code></pre></div><p>If you&#39;re over 1,500 tokens combined, refactor:</p><ul><li>Move framework-specific rules to project-level CLAUDE.md (only loads in that project)</li><li>Extract repeated patterns into skills (loaded only when invoked)</li><li>Delete anything you can&#39;t remember writing</li><li>Convert &quot;explain why&quot; verbose rules into 3-word imperatives I cut mine from 4,800 to 900 tokens. Same behavior. 31% reduction in baseline cost, instantly.</li></ul><h2 id="pattern-2-conversation-history-re-reads-13-of-total-tokens" tabindex="-1">Pattern 2. Conversation history re-reads (~13% of total tokens) <a class="header-anchor" href="#pattern-2-conversation-history-re-reads-13-of-total-tokens" aria-label="Permalink to &quot;Pattern 2. Conversation history re-reads (~13% of total tokens)&quot;">​</a></h2><p>The pattern: every follow-up message re-tokenizes the entire conversation history. By message 30 in a chat, each turn is paying for messages 1-29 to be read again. The math: at ~500 tokens per exchange, message 30 costs 30× message 1. I had sessions with 60+ messages. The last message was costing 60× the first. Tokens spent re-reading old context: catastrophic. [圖 3] The 30-second fix:</p><ol><li>Edit the prior message instead of follow-up. Up-arrow -&gt; edit -&gt; re-send. The bad exchange gets replaced, not stacked.</li><li>Hard cap conversations at 20 messages. When you cross 20, ask Claude to summarize what&#39;s been done and start a fresh chat with that summary as the first message.</li><li>Use /compact instead of /clear when you need continuity. /compact summarizes and restarts. /clear nukes everything. I went from 60-message sessions to 15-message average. 40% drop in conversation re-read cost.</li></ol><h2 id="pattern-3-hook-injection-waste-11-of-total-tokens" tabindex="-1">Pattern 3. Hook injection waste (~11% of total tokens) <a class="header-anchor" href="#pattern-3-hook-injection-waste-11-of-total-tokens" aria-label="Permalink to &quot;Pattern 3. Hook injection waste (~11% of total tokens)&quot;">​</a></h2><p>The pattern: I had 4 plugins installed. Three of them registered UserPromptSubmit hooks that injected context. Combined: 6,200 tokens of hook injection on every prompt I submitted, before Claude even read what I asked. These hooks are designed to be helpful. They inject branch names, recent file changes, instinct summaries, memory snippets. Each one is small. Together they&#39;re a wall. The 30-second fix:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># See what&#39;s being injected</span></span>
<span class="line"><span>cat ~/.claude/settings.json | jq &#39;.hooks.UserPromptSubmit&#39;</span></span>
<span class="line"><span>cat .claude/settings.json | jq &#39;.hooks.UserPromptSubmit&#39;</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Disable any hook you can&#39;t justify with a specific task</span></span>
<span class="line"><span>/plugin disable &lt;plugin-name&gt;</span></span></code></pre></div><p>Audit every hook. If you can&#39;t articulate why this hook fires on every prompt, kill it. I went from 4 active UserPromptSubmit hooks to 1 (just git branch). 5,800 tokens saved per prompt.</p><h2 id="pattern-4-cache-misses-on-session-resume-10-of-total-tokens" tabindex="-1">Pattern 4. Cache misses on session resume (~10% of total tokens) <a class="header-anchor" href="#pattern-4-cache-misses-on-session-resume-10-of-total-tokens" aria-label="Permalink to &quot;Pattern 4. Cache misses on session resume (~10% of total tokens)&quot;">​</a></h2><p>The pattern: Anthropic&#39;s prompt cache has a 5-minute lifetime by default. Stop work for 6 minutes - cache miss. The system prompt + CLAUDE.md + tools schema all re-tokenize at full price instead of 10% (cache read price). I&#39;d take a coffee break. Come back. First message paid full price for ~8K tokens of cached content. Across 90 days, this happened ~640 times. The 30-second fix:</p><ul><li>Fast workaround: keep a tiny &quot;ping&quot; command bound to a hotkey. Send any prompt within 4 minutes if you&#39;re about to take a break. Keeps the cache warm.</li><li>Real fix (paid plans): upgrade to 1-hour cache lifetime. Cache write tokens are 2× base price (one-time, on first write), cache reads are 0.1× base. Math: if you have 10+ resumes per session, the 1-hour cache pays for itself. I run the 1-hour cache now. Cache miss cost dropped 80%.</li></ul><h2 id="pattern-5-skill-loading-on-irrelevant-tasks-7-of-total-tokens" tabindex="-1">Pattern 5. Skill loading on irrelevant tasks (~7% of total tokens) <a class="header-anchor" href="#pattern-5-skill-loading-on-irrelevant-tasks-7-of-total-tokens" aria-label="Permalink to &quot;Pattern 5. Skill loading on irrelevant tasks (~7% of total tokens)&quot;">​</a></h2><p>The pattern: I had 9 skills installed. Each one auto-invokes when Claude detects relevance. The detection is conservative, when in doubt, load. So my UI design skill was loading on backend tasks, my video generation skill was loading on text-only tasks, etc. Each skill SKILL.md is 800-2,500 tokens. Loaded &quot;just in case.&quot; 9 skills × ~1,500 tokens = 13,500 tokens of overhead per task that didn&#39;t need any of them. The fix: Run a 7-day audit. Which skills did you actually invoke? Disable everything else.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># Which skills were actually invoked in your sessions</span></span>
<span class="line"><span>grep -h &quot;skill_invoked&quot; ~/.claude/logs/*.log | sort | uniq -c | sort -rn</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Anything not in the output → /plugin disable &lt;skill-name&gt;</span></span></code></pre></div><p>I went from 11 active skills to 4. 9,000+ tokens saved per task on average.</p><h2 id="pattern-6-just-in-case-tool-definitions-6-of-total-tokens" tabindex="-1">Pattern 6 .&quot;Just in case&quot; tool definitions (~6% of total tokens) <a class="header-anchor" href="#pattern-6-just-in-case-tool-definitions-6-of-total-tokens" aria-label="Permalink to &quot;Pattern 6 .&quot;Just in case&quot; tool definitions (~6% of total tokens)&quot;">​</a></h2><p>The pattern: I had 12 MCP servers connected. Each one ships its tool schema to every request, regardless of whether the task involves that tool. PostgreSQL MCP server tool schema is ~1,200 tokens. Twelve MCPs × ~600 avg tokens = 7,200 tokens of tool definitions per request. I used maybe 3 of these MCPs in 80% of my work. The 30-second fix:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/mcp                    # list connected servers</span></span>
<span class="line"><span>/mcp disable &lt;server&gt;   # disable for current session</span></span></code></pre></div><p>For permanent control, edit ~/.claude/settings.json to remove unused MCPs from the auto-load list. Re-enable per-session when you actually need them. I went from 12 to 3 always-on MCPs. 6,000 tokens saved per request.</p><h2 id="pattern-7-extended-thinking-on-simple-questions-5-of-total-tokens" tabindex="-1">Pattern 7. Extended thinking on simple questions (~5% of total tokens) <a class="header-anchor" href="#pattern-7-extended-thinking-on-simple-questions-5-of-total-tokens" aria-label="Permalink to &quot;Pattern 7. Extended thinking on simple questions (~5% of total tokens)&quot;">​</a></h2><p>The pattern: I left &quot;Advanced Thinking&quot; / extended thinking on globally. Claude burned 3,000+ tokens of <code>&lt;thinking&gt;</code> on questions that didn&#39;t need any reasoning (&quot;rename this variable to camelCase&quot;). Extended thinking is real value when the question genuinely needs reasoning (architecture decisions, complex debugging). On simple tasks it&#39;s pure overhead. The 30-second fix: Default extended thinking OFF. Toggle ON per-message when you need it (Alt+T in Claude Code). If your first attempt at a complex task is unsatisfactory, then turn extended thinking on for the retry. About 80% of tasks don&#39;t need it. The 20% that do you&#39;ll know.</p><h2 id="pattern-8-wrong-direction-generation-4-of-total-tokens" tabindex="-1">Pattern 8. Wrong-direction generation (~4% of total tokens) <a class="header-anchor" href="#pattern-8-wrong-direction-generation-4-of-total-tokens" aria-label="Permalink to &quot;Pattern 8. Wrong-direction generation (~4% of total tokens)&quot;">​</a></h2><p>The pattern: Claude starts writing a 400-line response. Within the first 50 lines, you can see it&#39;s going the wrong way. Most people let it finish, then re-prompt. The remaining 350 lines are wasted output tokens. Output tokens are billed. Letting Claude finish a wrong response cost me roughly 4% of my total spend. Stupid tax. The 30-second fix: Cmd+. (Mac) / Ctrl+. stops generation immediately. Claude keeps what it already wrote. You redirect from there. I trained myself to use this in the first 5 seconds of any response that&#39;s drifting. ~$15/month saved on wasted output. For Claude Code terminal users specifically, double-Esc opens a checkpoint scroller, you can rewind to any prior state and try a different approach without re-running everything.</p><h2 id="pattern-9-plugin-auto-update-redundancy-3-of-total-tokens" tabindex="-1">Pattern 9. Plugin auto-update redundancy (~3% of total tokens) <a class="header-anchor" href="#pattern-9-plugin-auto-update-redundancy-3-of-total-tokens" aria-label="Permalink to &quot;Pattern 9. Plugin auto-update redundancy (~3% of total tokens)&quot;">​</a></h2><p>The pattern: plugins auto-update on session start. Each update sometimes adds new context to SessionStart hooks. Several of my plugins added small (~50 tokens) info messages that compounded over time. Nine plugins × multiple SessionStart messages = ~1,400 tokens at every session start, just for &quot;loaded successfully&quot; notifications. The 30-second fix:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># Audit SessionStart hooks</span></span>
<span class="line"><span>cat ~/.claude/settings.json | jq &#39;.hooks.SessionStart&#39;</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Anything that&#39;s just a &quot;loaded&quot; notification → kill</span></span></code></pre></div><p>I went from 9 SessionStart hooks to 2 (one for branch context, one for env vars). 1,200 tokens saved per session start.</p><h2 id="the-numbers-side-by-side" tabindex="-1">The numbers, side by side <a class="header-anchor" href="#the-numbers-side-by-side" aria-label="Permalink to &quot;The numbers, side by side&quot;">​</a></h2><p>[圖 4] Productive token share went from 27% to roughly 65%. Not 100% — there&#39;s an unavoidable floor of overhead. But 65% is a different category of life than 27%.</p><h2 id="what-didn-t-work" tabindex="-1">What didn&#39;t work <a class="header-anchor" href="#what-didn-t-work" aria-label="Permalink to &quot;What didn&#39;t work&quot;">​</a></h2><p>Things I tried that the obvious blog posts recommend, that didn&#39;t move the needle:</p><ol><li>Switching to Haiku for &quot;simple&quot; tasks. Helped a tiny bit. ~3% reduction. The real waste isn&#39;t the model, it&#39;s the context bloat described above. Cheaper model on bloated context still costs more than expensive model on lean context.</li><li>Aggressive /clear between every task. Counter-productive. Lost context I genuinely needed. Better strategy: long-running session per project, but instrumented hooks and lean CLAUDE.md.</li><li>Disabling all skills entirely. Initially saved tokens. Then I started writing the same 200-token instructions in every prompt manually. Net negative. Better strategy: keep 3-4 skills you actually use, disable the rest.</li><li>Off-peak hours scheduling. Worked partially. Anthropic&#39;s peak-hour quota reduction is real (about 7% of users affected per their statement). For most people the bigger lever is the patterns above, not the time of day.</li><li>Pure subscription downgrade (Pro -&gt; less expensive plan). Doesn&#39;t work. The cost per work-hour stayed the same, just got more painful when limits hit.</li><li>Trying to identify the March 2026 caching bug. Spent a day on it. Not worth it for individuals, Anthropic patched it. The bigger fish was my own context.</li><li>The audit script If you want to instrument your own setup, this script flags all 9 patterns at once:</li></ol><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#!/bin/bash</span></span>
<span class="line"><span># claude-audit.sh — run in your project root</span></span>
<span class="line"><span></span></span>
<span class="line"><span>echo &quot;=== CLAUDE.md size ===&quot;</span></span>
<span class="line"><span>wc -w ~/.claude/CLAUDE.md 2&gt;/dev/null</span></span>
<span class="line"><span>wc -w .claude/CLAUDE.md 2&gt;/dev/null</span></span>
<span class="line"><span>echo &quot;Target: combined &lt; 1,200 words&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>echo</span></span>
<span class="line"><span>echo &quot;=== Active hooks ===&quot;</span></span>
<span class="line"><span>cat ~/.claude/settings.json 2&gt;/dev/null | jq &#39;.hooks // {} | keys&#39;</span></span>
<span class="line"><span>cat .claude/settings.json 2&gt;/dev/null | jq &#39;.hooks // {} | keys&#39;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>echo</span></span>
<span class="line"><span>echo &quot;=== UserPromptSubmit injections ===&quot;</span></span>
<span class="line"><span>cat ~/.claude/settings.json 2&gt;/dev/null | jq &#39;.hooks.UserPromptSubmit&#39;</span></span>
<span class="line"><span>cat .claude/settings.json 2&gt;/dev/null | jq &#39;.hooks.UserPromptSubmit&#39;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>echo</span></span>
<span class="line"><span>echo &quot;=== Installed plugins ===&quot;</span></span>
<span class="line"><span>ls ~/.claude/plugins/ 2&gt;/dev/null</span></span>
<span class="line"><span>echo &quot;Target: 3-5 active. Disable the rest.&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>echo</span></span>
<span class="line"><span>echo &quot;=== Installed skills ===&quot;</span></span>
<span class="line"><span>ls ~/.claude/skills/ 2&gt;/dev/null</span></span>
<span class="line"><span>echo &quot;Target: 3-5 active matching daily work.&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>echo</span></span>
<span class="line"><span>echo &quot;=== Connected MCPs ===&quot;</span></span>
<span class="line"><span>cat ~/.claude/settings.json 2&gt;/dev/null | jq &#39;.mcpServers // {} | keys&#39;</span></span>
<span class="line"><span>echo &quot;Target: 3 always-on. Per-session enable rest.&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>echo</span></span>
<span class="line"><span>echo &quot;=== Recent session token usage (last 7 days) ===&quot;</span></span>
<span class="line"><span>find ~/.claude/logs/ -mtime -7 -name &quot;*.log&quot; -exec cat {} + 2&gt;/dev/null \\</span></span>
<span class="line"><span>  | grep -oP &#39;input_tokens[&quot;:]+\\K[0-9]+&#39; \\</span></span>
<span class="line"><span>  | awk &#39;{ sum += $1; count++ } END { print &quot;Avg input: &quot; sum/count &quot; tokens&quot;; print &quot;Total prompts: &quot; count }&#39;</span></span></code></pre></div><p>Save it, run it, fix what&#39;s flagged. Re-run weekly until each line is in target.</p><h2 id="the-mental-model-shift" tabindex="-1">The mental model shift <a class="header-anchor" href="#the-mental-model-shift" aria-label="Permalink to &quot;The mental model shift&quot;">​</a></h2><p>The mistake I was making: treating every Claude Code session as a fresh blank slate. In reality, every session is a long invoice that pre-charges you for:</p><ul><li>Your CLAUDE.md (always)</li><li>Every active plugin&#39;s hooks (always)</li><li>Every active skill&#39;s SKILL.md (when relevance detected)</li><li>Every connected MCP&#39;s tool schema (always)</li><li>Conversation history up to that turn (always)</li><li>Cache miss recompilation (when session resumes after 5 min) Productive tokens are the residual. What&#39;s left after all that overhead. If you want more productive tokens, which is what &quot;more usage from your plan&quot; actually means, you have to attack the overhead, not just write better prompts. Better prompts help when the overhead is small. When the overhead is 73%, prompts barely matter. Most &quot;Claude got dumber&quot; complaints in 2026 trace back to this. The model didn&#39;t get dumber. Your overhead grew. The model has the same compute budget, but most of it is spent re-reading your bloated CLAUDE.md, your 12 hook injections, and the 4 plugins you forgot you installed. [圖 5]</li></ul><h2 id="this-is-the-end" tabindex="-1">This is the end <a class="header-anchor" href="#this-is-the-end" aria-label="Permalink to &quot;This is the end&quot;">​</a></h2><p>The obvious advice &quot;use Haiku for simple tasks, start fresh chats often, batch your questions&quot; is not wrong. It&#39;s just operating at the wrong layer. The real bottleneck is the constant overhead tax that Claude Code pays before every prompt. Cut the tax, the same plan suddenly has 2-3x more capacity. Audit tonight. Apply the 9 fixes. Re-run the audit script weekly. Your usage limits will stop biting. If this saved you a week of Claude limit pain, repost. Part 2 next week: the prompt cache deep-dive — how to measure what&#39;s actually being cached vs what&#39;s silently re-tokenizing.</p><blockquote><p>Bookmark this, run the audit weekly until every line is in target Part 2 next week..</p></blockquote><p>Telegram for daily claude optimization tips: <a href="https://t.me/+_ZWrQN7GuDA3ZDEy" target="_blank" rel="noreferrer">https://t.me/+_ZWrQN7GuDA3ZDEy</a></p><hr><h2 id="研究摘要" tabindex="-1">研究摘要 <a class="header-anchor" href="#研究摘要" aria-label="Permalink to &quot;研究摘要&quot;">​</a></h2><p><strong>TL;DR</strong>：430 小時/6M tokens/90 天 Claude Code 實測：27% productive tokens；9 個 overhead pattern（CLAUDE.md 14%/對話重讀 13%/hook injection 11%/cache miss 10%/skill loading 7%）；「productive tokens are the residual」框架；27%→65%</p><h3 id="核心論點" tabindex="-1">核心論點 <a class="header-anchor" href="#核心論點" aria-label="Permalink to &quot;核心論點&quot;">​</a></h3><ul><li><strong>「productive tokens are the residual」框架</strong>：有效 token 不是可以直接最大化的，而是消除所有 overhead 後的剩餘</li><li><strong>9 個 overhead pattern（量化）</strong>：CLAUDE.md 重讀 14% / 對話歷史重讀 13% / Hook injection 11% / Cache miss 10% / Skill loading 7% / Sub-agent spawn overhead 6% / Tool call overhead 5% / Error recovery 5% / Context rebuilding 3%（其餘 overhead ~26%）</li><li><strong>改善路徑</strong>：CLAUDE.md 分層（規則/狀態/任務）；session 管理（compact 時機）；Hook 精確化；cache 設計；Skill trigger 精確匹配</li><li><strong>6 個 code block</strong>：CLAUDE.md 結構模板、hook 配置優化、cache_control 位置示例</li></ul><h3 id="關鍵數據-工具" tabindex="-1">關鍵數據 / 工具 <a class="header-anchor" href="#關鍵數據-工具" aria-label="Permalink to &quot;關鍵數據 / 工具&quot;">​</a></h3><ul><li>實測規模：430 小時，6M tokens，90 天</li><li>Baseline productive token 比例：27%（未優化）</li><li>優化後：65%（2.4x）</li><li>最大 overhead：CLAUDE.md 重讀 14%（可優化為 ~3%）</li><li>Hook injection：11%（精確 matcher 可降至 2%）</li><li>9 個明確量化的 overhead category</li></ul><h3 id="批判性觀察" tabindex="-1">批判性觀察 <a class="header-anchor" href="#批判性觀察" aria-label="Permalink to &quot;批判性觀察&quot;">​</a></h3><ul><li><strong>百分比加總超過 100%</strong>：9 個 overhead pattern 加總 + 27% productive = 超過 100%，可能有重疊計算</li><li><strong>個人工作流偏差</strong>：作者的 skill loading 7% 代表特定的 workspace 配置，其他用戶的 overhead 分布可能截然不同</li><li><strong>65% 目標的可達性</strong>：文章未說明哪些 overhead 是真的「浪費」vs 系統性必要成本</li></ul><hr><h2 id="評分" tabindex="-1">評分 <a class="header-anchor" href="#評分" aria-label="Permalink to &quot;評分&quot;">​</a></h2><table tabindex="0"><thead><tr><th>維度</th><th>分數</th><th>說明</th></tr></thead><tbody><tr><td>A. Workspace 可行動性 (30%)</td><td>10/10</td><td>直接可操作性</td></tr><tr><td>B. 創新性 (20%)</td><td>8/10</td><td>概念新穎度</td></tr><tr><td>C. 證據品質 (20%)</td><td>8/10</td><td>可驗證性</td></tr><tr><td>D. 技術深度 (15%)</td><td>9/10</td><td>技術細節</td></tr><tr><td>E. 泛化性 (15%)</td><td>9/10</td><td>普適性</td></tr><tr><td><strong>加權總分</strong></td><td><strong>8.8/10</strong></td><td>10×0.3+8×0.2+8×0.2+9×0.15+9×0.15</td></tr></tbody></table><p><strong>整合決策</strong>：Rule <strong>理由</strong>：依評分與內容類型決定</p>`,68)])])}const m=t(o,[["render",i]]);export{u as __pageData,m as default};
