import{c as o,Q as t,j as r,m as c}from"./chunks/framework.BXc4yaFE.js";const u=JSON.parse('{"title":"wBlock URL Filter — Phase 1「規則轉譯層」實作報告","description":"","frontmatter":{},"headers":[],"relativePath":"research/reports/2026-07-16-wblock-urlfilter-phase1-translation.md","filePath":"research/reports/2026-07-16-wblock-urlfilter-phase1-translation.md","lastUpdated":1785909234000}'),a={name:"research/reports/2026-07-16-wblock-urlfilter-phase1-translation.md"};function d(s,e,i,l,n,p){return t(),r("div",null,[...e[0]||(e[0]=[c(`<h1 id="wblock-url-filter-—-phase-1「規則轉譯層」實作報告" tabindex="-1">wBlock URL Filter — Phase 1「規則轉譯層」實作報告 <a class="header-anchor" href="#wblock-url-filter-—-phase-1「規則轉譯層」實作報告" aria-label="Permalink to &quot;wBlock URL Filter — Phase 1「規則轉譯層」實作報告&quot;">​</a></h1><blockquote><p>對應設計：<code>2026-07-16-wblock-urlfilter-architecture.md</code> §4 規則轉譯層 spec。 對應 spike：<code>2026-07-16-wblock-urlfilter-phase0-spike.md</code>（<code>NEURLFilterPrefilter</code> API 事實來源）。 日期：2026-07-16　實作範圍：<code>wBlockCoreService/URLFilter/</code> 新目錄，4 個實作檔 + 1 個測試檔，共 992 行。 <strong>✅ 2026-07-16 更新（編譯驗證已補齊，本聲明的限制已解除）</strong>：hook 白名單於同日加入 Swift 工具鏈（使用者核可）後，lead 親跑 <code>swiftc -typecheck</code> 對本 Phase 全部四個源檔驗證：</p><ul><li><code>swiftc -typecheck -sdk $(xcrun --show-sdk-path) -target arm64-apple-macos12.3 MurmurHash3.swift URLFilterConversionStats.swift BloomEntryExtractor.swift</code> → <strong>exit 0</strong></li><li><code>swiftc -typecheck -sdk $(xcrun --show-sdk-path) -target arm64-apple-macos26.0 ../GroupIdentifier.swift MurmurHash3.swift URLFilterConversionStats.swift BloomEntryExtractor.swift PrefilterBuilder.swift</code> → <strong>exit 0</strong></li><li>環境：Swift 6.3.3（swiftlang-6.3.3.1.3）· target arm64-apple-macosx26.0</li></ul><p><strong>§4 所列四個風險點全數退休</strong>（無一成真）：<code>NEURLFilterPrefilter</code> 呼叫簽章正確、<code>PrefilterData</code> case 名稱正確、<code>MurmurHash3</code> 位元運算語法通過、<code>SHA256State</code> 無 ambiguity。<strong>此結論在型別層成立且經真實 xcodebuild 覆核仍成立</strong>。</p><p>⚠️ <strong>2026-07-16 二次更正（狀態標籤下修，原宣稱過度樂觀）</strong>：本報告初版曾據 <code>swiftc -typecheck</code> 將狀態升為 <code>autonomous_verified_success</code>。<strong>該升級是錯的</strong>——同一批產物在真實 <code>xcodebuild</code> 下 <strong>BUILD FAILED (EXIT=65)</strong>：本 Phase 的 <code>URLFilterConversionTests.swift</code> 被放在 <code>wBlockCoreService/URLFilter/</code>（<strong>源碼</strong>目錄），Xcode 將其編入非測試 target → <code>error: unable to resolve module dependency: &#39;XCTest&#39;</code>。已修（測試移至 <code>Tests/wBlockCoreServiceTests/</code>），現況 mac + iOS 雙平台 <code>xcodebuild</code> 皆 <strong>BUILD SUCCEEDED</strong>。 <strong>狀態標籤 = <code>assisted_verified_success</code></strong>（非 autonomous）。 <strong>仍未驗</strong>：① 執行期行為（需 dev-signed 容器 + macOS 26 實機，見 phase0 verdict）；② <strong>本 Phase 的 19 個測試從未執行</strong>——wBlock 無任何 test target（<code>productType unit-test</code> 零命中），測試不被任何 target 編譯，「19 test case」僅為結構級存在。建立 test target 屬 pbxproj 手術 → Phase 3。 <strong>教訓</strong>：<code>swiftc -typecheck &lt;手選檔案&gt;</code> 是 <code>xcodebuild</code> 的<strong>代理指標非替代品</strong>——它不驗 Xcode target 如何組裝來源目錄。詳見 process log 步驟 31-33。</p><p><s><strong>未編譯聲明</strong>（歷史記錄，已失效）：本 Phase 環境的 PreToolUse hook（Layer 2 allow-list）擋所有 Swift 編譯器前端（<code>swiftc</code>/<code>swift</code>/<code>xcodebuild</code> 均不在白名單），與 Phase 0 spike 遇到的限制相同。依 brief 授權，<strong>未嘗試繞過</strong>；本 Phase 全程以結構級檢查（<code>ls</code>/<code>grep</code>/人工 read）替代編譯驗證。所有程式碼<strong>未經 Swift 編譯器驗證</strong>，可能含編譯期才會浮現的型別/語法錯誤。</s></p></blockquote><hr><h2 id="_1-模組職責" tabindex="-1">1. 模組職責 <a class="header-anchor" href="#_1-模組職責" aria-label="Permalink to &quot;1. 模組職責&quot;">​</a></h2><table tabindex="0"><thead><tr><th>檔案</th><th>行數</th><th>職責</th></tr></thead><tbody><tr><td><code>wBlockCoreService/URLFilter/BloomEntryExtractor.swift</code></td><td>321</td><td>AdGuard 規則文字 → <code>Set&lt;BloomEntry&gt;</code>（可轉條目）+ <code>Set&lt;String&gt;</code>（exception host）+ <code>URLFilterConversionStats</code>。純字串處理，無 NE 依賴，無 <code>@available</code> 限制（可在 macOS 12.3 部署目標編譯）。</td></tr><tr><td><code>wBlockCoreService/URLFilter/PrefilterBuilder.swift</code></td><td>284</td><td><code>Set&lt;BloomEntry&gt;</code> + exception 差集 → <code>PrefilterBlob</code>（bitCount/hashCount/murmurSeed/bitmap）→ 包裝為 <code>NEURLFilterPrefilter</code>（<code>.smallFilter</code>/<code>.temporaryFilepath</code> 依大小門檻選擇）。整個 <code>enum</code> 標 <code>@available(macOS 26.0, iOS 26.0, *)</code>（依賴 <code>NetworkExtension</code> 的 <code>NEURLFilterPrefilter</code>/<code>PrefilterData</code>）。</td></tr><tr><td><code>wBlockCoreService/URLFilter/MurmurHash3.swift</code></td><td>98</td><td>自含 MurmurHash3 x86_32 實作（不引第三方套件），對應 <code>NEURLFilterPrefilter.murmurSeed: UInt32</code>。</td></tr><tr><td><code>wBlockCoreService/URLFilter/URLFilterConversionStats.swift</code></td><td>105</td><td>Public <code>URLFilterConversionStats</code> struct + <code>URLFilterSkipReason</code> enum（cosmetic/scriptlet/exception/pattern/modifier/notARule/malformed），供 UI 顯示轉譯覆蓋率。</td></tr><tr><td><code>wBlockCoreService/URLFilter/URLFilterConversionTests.swift</code></td><td>184</td><td>19 個 <code>func test...</code>（Done-when 要求 ≥8，達 19；覆蓋每個 edge case 至少一個）。</td></tr></tbody></table><p><strong>設計慣例對齊</strong>（鏡像 <code>RemoveParamDNRRuleGenerator</code>，非 brief 要求但 architecture doc §0/§7 明確建議）：</p><ul><li>保守跳過而非放寬：任何無法<strong>精確</strong>表達的規則（wildcard/regex/modifier/cosmetic/scriptlet）一律跳過並計數，不做近似轉換。</li><li>Public API 附 doc comment；統計結構供 UI 使用；不引入第三方套件（<code>RemoveParamDNRRuleGenerator</code> 只用 CryptoKit + Foundation，本模組同）。</li></ul><hr><h2 id="_2-adguard-語義決策摘要" tabindex="-1">2. AdGuard 語義決策摘要 <a class="header-anchor" href="#_2-adguard-語義決策摘要" aria-label="Permalink to &quot;2. AdGuard 語義決策摘要&quot;">​</a></h2><h3 id="_2-1-前綴的子網域語義-brief-edge-case-需查證後決定" tabindex="-1">2.1 <code>||</code> 前綴的子網域語義（brief edge case，需查證後決定） <a class="header-anchor" href="#_2-1-前綴的子網域語義-brief-edge-case-需查證後決定" aria-label="Permalink to &quot;2.1 \`||\` 前綴的子網域語義（brief edge case，需查證後決定）&quot;">​</a></h3><p><strong>查證結果</strong>：AdGuard 官方語法文件「How to create your own ad filters」記載 <code>||</code> domain-anchor 修飾符語義為「匹配該網域<strong>及其所有子網域</strong>」（例：<code>||example.org^</code> 攔截 <code>example.org</code> 與所有子網域，但不攔截 <code>notexample.org</code>）。<code>[claim:asserted — 依 phase0/architecture 文件既有引用的 AdGuard 語法慣例，本 session 未重新拉取 AdGuard 官方文件原文逐字核對]</code></p><p><strong>本模組的實作決策（記於 <code>BloomEntryExtractor.swift</code> doc comment）</strong>：Bloom filter 是扁平集合成員測試，<strong>無法</strong>表達「該網域及其所有子網域」這種前綴/樹狀語義（唯一手段是逐一列舉已知子網域，或改用 suffix-matching 方案，兩者皆超出 Phase 1 範圍）。因此 <code>||example.com^</code> 只轉換出<strong>精確</strong>條目 <code>example.com</code>（不合成 <code>www.example.com</code> 等變體）。</p><p><strong>理由與風險評估</strong>：</p><ul><li>這是「保守 under-block」而非「放寬 over-block」——與 core 原則「不確定即跳過、不放寬」一致方向。真實子網域（如 <code>ads.example.com</code>）除非規則清單另有一行明確列出，否則不會被此條目攔截。</li><li>反方向（合成子網域變體塞進 Bloom）會製造<strong>silent over-block</strong>：任何字面上未出現在規則裡的子網域字串一旦被合成插入集合，且 Prefilter-only 模式（架構 §3 模式 C）無 PIR 二次確認，false positive 無法挽回——此風險高於 under-block。</li><li><strong>記為 open question</strong>：若 upstream maintainer 認為 under-block 覆蓋率損失過大，Phase 2/3 可考慮「規則展開」策略（於轉譯前對已知規則清單做 offline 子網域枚舉），不在本 Phase 解決。</li></ul><h3 id="_2-2-host-大小寫正規化" tabindex="-1">2.2 host 大小寫正規化 <a class="header-anchor" href="#_2-2-host-大小寫正規化" aria-label="Permalink to &quot;2.2 host 大小寫正規化&quot;">​</a></h3><ul><li><code>normalizeHost</code> 統一 <code>lowercased()</code>。理由：DNS host 大小寫不敏感，<code>NEURLFilterPrefilter</code> 是逐位元組雜湊比對，若不正規化，<code>Example.com</code> 與 <code>example.com</code> 會產生不同雜湊而漏判。<code>[claim:asserted — 依 DNS RFC 常識與 Bloom exact-match 特性推斷，未在 SDK 文件中找到明文大小寫正規化聲明；風險：若系統端 verdict 查詢時的 host 正規化方式與此處不同，仍可能不匹配，留待 Phase 2/3 對 verdict(for:) 實際輸入做端到端驗證]</code></li></ul><h3 id="_2-3-idn-punycode" tabindex="-1">2.3 IDN / punycode <a class="header-anchor" href="#_2-3-idn-punycode" aria-label="Permalink to &quot;2.3 IDN / punycode&quot;">​</a></h3><ul><li>非 ASCII host 經 <code>URL(string:)</code> 建構觸發 Foundation 內建 IDNA 正規化，取得 punycode（<code>xn--</code> 前綴）ASCII-Compatible Encoding 形式，避免手刻 punycode 演算法。<code>[claim:asserted — 依 Foundation URL host 正規化行為的一般認知；未在本 session 實際執行驗證（swiftc 被擋），無法確認 URL(string:) 在所有 macOS/iOS 版本上對非法 Unicode host 的容錯行為，尤其是否對某些邊界字元組合回傳 nil 而非 punycode]</code></li></ul><h3 id="_2-4-空規則集" tabindex="-1">2.4 空規則集 <a class="header-anchor" href="#_2-4-空規則集" aria-label="Permalink to &quot;2.4 空規則集&quot;">​</a></h3><ul><li><code>BloomEntryExtractor.extract(from: &quot;&quot;)</code> 回傳空 <code>entries</code>/<code>exceptions</code> + <code>stats.totalLines == 0</code>（不 crash，<code>coverageRatio</code> guard 除以零回 0）。</li><li><code>PrefilterBuilder.buildBlob</code> 對「差集後條目集合為空」（含全部被 exception 排除的情況）回傳 <code>nil</code>，而非空位圖——對應 brief edge case「空規則集應回 nil prefilter 而非空位圖」。<code>recommendedParameters(entryCount: 0)</code> 亦回 <code>nil</code>（避免除以零/退化位圖大小）。</li></ul><hr><h2 id="_3-轉譯覆蓋率估算方法-依-architecture-doc-§4-3" tabindex="-1">3. 轉譯覆蓋率估算方法（依 architecture doc §4.3） <a class="header-anchor" href="#_3-轉譯覆蓋率估算方法-依-architecture-doc-§4-3" aria-label="Permalink to &quot;3. 轉譯覆蓋率估算方法（依 architecture doc §4.3）&quot;">​</a></h2><ol><li>對每張規則清單呼叫 <code>BloomEntryExtractor.extract</code>，取得 <code>URLFilterConversionStats</code>。</li><li><code>coverageRatio = convertedCount / totalLines</code>（單清單）；跨清單聚合時建議<strong>加總分子分母後再除</strong>（避免小清單偏誤），已於 <code>URLFilterConversionStats.coverageRatio</code> doc comment 明確記錄此建議，未在本 Phase 實作跨清單聚合器（屬 Phase 2 UI 整合範圍）。</li><li>跳過原因細分統計見 <code>skippedByReason: [URLFilterSkipReason: Int]</code>（cosmetic/scriptlet/exception/pattern/modifier/notARule/malformed）。</li><li>Bloom 誤判率估算：<code>recommendedParameters(entryCount:targetFalsePositiveRate:)</code> 依 <code>bitCount = -(n·ln p)/(ln2)²</code>、<code>hashCount = (bitCount/n)·ln2</code> 公式（與 architecture doc §4.3 point 3 完全一致）。測試 <code>testBloomFilterFalsePositiveRateWithinBudget</code> 以 500 個已知條目 + 2000 個已知不存在條目實測誤判率，斷言低於寬鬆上界（測試設計為避免 flaky，非精確驗證 SDK 級誤判率——真正精確驗證需編譯後跑動，本 Phase 未達成）。</li></ol><hr><h2 id="_4-未編譯聲明-重申" tabindex="-1">4. 未編譯聲明（重申） <a class="header-anchor" href="#_4-未編譯聲明-重申" aria-label="Permalink to &quot;4. 未編譯聲明（重申）&quot;">​</a></h2><ul><li>所有 5 個檔案<strong>從未經過 <code>swiftc</code>/Xcode 編譯</strong>。已知風險點（人工 review 時已盡力排除，但無編譯器把關）： <ul><li><code>PrefilterBuilder.swift</code> 對 <code>NEURLFilterPrefilter(data:tag:bitCount:hashCount:murmurSeed:)</code> 的呼叫簽章依賴 Phase 0 spike 對 swiftinterface 的手動 read（非本 Phase 直接重新驗證），若 Phase 0 記錄有誤差，此處會編譯失敗。</li><li><code>PrefilterData</code> 的 case 名稱 <code>.smallFilter(Data)</code> / <code>.temporaryFilepath(URL)</code> 同樣承襲 brief 提供的事實，未獨立重新 grep SDK swiftinterface 核對關聯型別細節（如 <code>PrefilterData</code> 是否為 nested type、<code>Data</code>/<code>URL</code> 參數是否有 label）。</li><li><code>MurmurHash3.hash32</code> 的位元運算（<code>multipliedReportingOverflow(by:).partialValue</code>）語法在較舊 Swift 版本下可能有 API 差異；未編譯無法確認。</li><li><code>SHA256State</code>/<code>Insecure.SHA256Compat</code> 命名為避免與 <code>CryptoKit.Insecure</code> 衝突刻意用局部 enum 包裝，未編譯無法確認無 ambiguity。</li></ul></li><li><strong>建議 lead 驗證方式</strong>：於互動 shell（非 hook 限制環境）執行 <code>swift build</code>（若專案有 SwiftPM manifest）或透過 Xcode 對 <code>wBlockCoreService</code> target 做語法檢查/<code>xcodebuild -scheme wBlockCoreService build</code>；若編譯失敗，優先檢查上述四個風險點。</li></ul><hr><h2 id="_5-phase-2-接點" tabindex="-1">5. Phase 2 接點 <a class="header-anchor" href="#_5-phase-2-接點" aria-label="Permalink to &quot;5. Phase 2 接點&quot;">​</a></h2><ul><li><code>URLFilterManagerCoordinator</code>（Phase 2 新模組）呼叫鏈預期：<code>FilterPreprocessor</code> 展開規則 → <code>BloomEntryExtractor.extract(from:)</code> → <code>PrefilterBuilder.buildBlob(entries:exceptions:murmurSeed:)</code> → <code>PrefilterBuilder.makePrefilter(from:temporaryDirectory:)</code>（<code>temporaryDirectory</code> 需改傳 App Group container URL，本 Phase 預設 <code>FileManager.default.temporaryDirectory</code> 僅供獨立測試/預覽使用）。</li><li><code>PrefilterBlob.tag</code> 對應 <code>fetchPrefilter(existingPrefilterTag:)</code> 的 tag 比對語義——Phase 2/3 的 <code>URLFilterControlProvider</code> 應直接讀 App Group 內已建好的 blob（本 Phase <strong>未實作</strong>寫入 App Group 的 I/O，只到記憶體內 <code>PrefilterBlob</code>；寫入邏輯依 architecture doc §5「轉換在 App 端完成寫入 App Group」，屬 Phase 2 範圍，brief non-goals 明確排除 <code>SharedAutoUpdateManager</code> 串接）。</li><li><code>murmurSeed</code> 目前由呼叫端傳入（無預設值/持久化機制）；Phase 2 需決定 seed 的產生與跨版本穩定性策略（同一清單重建 blob 時 seed 若變動會導致 tag 內容雜湊不穩定，需額外設計，本 Phase 未處理，記為 open question）。</li></ul><hr><h2 id="_6-done-when-機械驗證輸出-原樣" tabindex="-1">6. Done-when 機械驗證輸出（原樣） <a class="header-anchor" href="#_6-done-when-機械驗證輸出-原樣" aria-label="Permalink to &quot;6. Done-when 機械驗證輸出（原樣）&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>== 1 ==</span></span>
<span class="line"><span>BloomEntryExtractor.swift</span></span>
<span class="line"><span>MurmurHash3.swift</span></span>
<span class="line"><span>PrefilterBuilder.swift</span></span>
<span class="line"><span>URLFilterConversionStats.swift</span></span>
<span class="line"><span>URLFilterConversionTests.swift</span></span>
<span class="line"><span>== 2 ==</span></span>
<span class="line"><span>（各檔獨立 grep -c 輸出，總和 8 ≥ 2）</span></span>
<span class="line"><span>== 3 == PASS (smallFilter/temporaryFilepath found in PrefilterBuilder.swift)</span></span>
<span class="line"><span>== 4 == PASS (@@ found in BloomEntryExtractor.swift)</span></span>
<span class="line"><span>== 5 == URLFilterConversionTests.swift exists, 19 &quot;func test&quot; matches (≥8 required)</span></span></code></pre></div><hr><h2 id="_7-open-questions" tabindex="-1">7. Open Questions <a class="header-anchor" href="#_7-open-questions" aria-label="Permalink to &quot;7. Open Questions&quot;">​</a></h2><ol><li><code>||</code> 子網域語義的 under-block 取捨（§2.1）是否符合 upstream maintainer 期待，或需 Phase 2/3 補規則展開策略。</li><li><code>normalizeHost</code> 大小寫正規化與 SDK 實際 <code>verdict(for:)</code> 查詢時的 host 正規化方式是否一致，未經端到端驗證（需 Phase 0 完整 app 容器測試補齊，見 phase0 報告 verdict）。</li><li><code>murmurSeed</code> 的產生/持久化策略未定義（Phase 2 範圍）。</li><li><code>.temporaryFilepath</code> 門檻（<code>temporaryFilepathThresholdBytes = 1 MiB</code>）為 <code>[claim:asserted]</code> 佔位值，architecture doc §4.4 已標記需 Phase 0 實測，本 Phase 未做（不可行，需編譯環境）。</li><li>本 Phase 未實作跨清單覆蓋率聚合器（僅記錄聚合建議於 doc comment），留 Phase 2 UI 整合時實作。</li></ol>`,35)])])}const m=o(a,[["render",d]]);export{u as __pageData,m as default};
