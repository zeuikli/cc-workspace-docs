import{c as t,Q as a,j as n,m as s}from"./chunks/framework.BXc4yaFE.js";const u=JSON.parse('{"title":"Harness engineering: leveraging Codex in an agent-first world","description":"","frontmatter":{"title":"Harness engineering: leveraging Codex in an agent-first world","published":"2026-02-11T00:00:00.000Z","source_tier":"P"},"headers":[],"relativePath":"research/papers/2026-02-11-openai-harness-engineering-codex.md","filePath":"research/papers/2026-02-11-openai-harness-engineering-codex.md","lastUpdated":1779796674000}'),o={name:"research/papers/2026-02-11-openai-harness-engineering-codex.md"};function i(r,e,l,d,c,p){return a(),n("div",null,[...e[0]||(e[0]=[s(`<h1 id="harness-engineering-leveraging-codex-in-an-agent-first-world" tabindex="-1">Harness engineering: leveraging Codex in an agent-first world <a class="header-anchor" href="#harness-engineering-leveraging-codex-in-an-agent-first-world" aria-label="Permalink to &quot;Harness engineering: leveraging Codex in an agent-first world&quot;">​</a></h1><p><strong>Author:</strong> Ryan Lopopolo, Member of the Technical Staff<br><strong>Published:</strong> February 11, 2026<br><strong>Source:</strong> <a href="https://openai.com/index/harness-engineering/" target="_blank" rel="noreferrer">https://openai.com/index/harness-engineering/</a><br><strong>Archived:</strong> 2026-05-04</p><hr><p>Over the past five months, our team has been running an experiment: building and shipping an internal beta of a software product with 0 lines of manually-written code.</p><p>The product has internal daily users and external alpha testers. It ships, deploys, breaks, and gets fixed. What&#39;s different is that every line of code—application logic, tests, CI configuration, documentation, observability, and internal tooling—has been written by Codex. We estimate that we built this in about 1/10th the time it would have taken to write the code by hand.</p><p><strong>Humans steer. Agents execute.</strong></p><p>We intentionally chose this constraint so we would build what was necessary to increase engineering velocity by orders of magnitude. We had weeks to ship what ended up being a million lines of code. To do that, we needed to understand what changes when a software engineering team&#39;s primary job is no longer to write code, but to design environments, specify intent, and build feedback loops that allow Codex agents to do reliable work.</p><p>This post is about what we learned by building a brand new product with a team of agents—what broke, what compounded, and how to maximize our one truly scarce resource: human time and attention.</p><h2 id="we-started-with-an-empty-git-repository" tabindex="-1">We started with an empty git repository <a class="header-anchor" href="#we-started-with-an-empty-git-repository" aria-label="Permalink to &quot;We started with an empty git repository&quot;">​</a></h2><p>The first commit to an empty repository landed in late August 2025.</p><p>The initial scaffold—repository structure, CI configuration, formatting rules, package manager setup, and application framework—was generated by Codex CLI using GPT-5, guided by a small set of existing templates. Even the initial AGENTS.md file that directs agents how to work in the repository was itself written by Codex.</p><p>There was no pre-existing human-written code to anchor the system. From the beginning, the repository was shaped by the agent.</p><p>Five months later, the repository contains on the order of a million lines of code across application logic, infrastructure, tooling, documentation, and internal developer utilities. Over that period, roughly 1,500 pull requests have been opened and merged with a small team of just three engineers driving Codex. This translates to an average throughput of 3.5 PRs per engineer per day, and surprisingly the throughput has increased as the team has grown to now seven engineers. Importantly, this wasn&#39;t output for output&#39;s sake: the product has been used by hundreds of users internally, including daily internal power users.</p><p>Throughout the development process, humans never directly contributed any code. This became a core philosophy for the team: no manually-written code.</p><h2 id="redefining-the-role-of-the-engineer" tabindex="-1">Redefining the role of the engineer <a class="header-anchor" href="#redefining-the-role-of-the-engineer" aria-label="Permalink to &quot;Redefining the role of the engineer&quot;">​</a></h2><p>The lack of hands-on human coding introduced a different kind of engineering work, focused on systems, scaffolding, and leverage.</p><p>Early progress was slower than we expected, not because Codex was incapable, but because the environment was underspecified. The agent lacked the tools, abstractions, and internal structure required to make progress toward high-level goals. The primary job of our engineering team became enabling the agents to do useful work.</p><p>In practice, this meant working depth-first: breaking down larger goals into smaller building blocks (design, code, review, test, etc), prompting the agent to construct those blocks, and using them to unlock more complex tasks. When something failed, the fix was almost never &quot;try harder.&quot; Because the only way to make progress was to get Codex to do the work, human engineers always stepped into the task and asked: &quot;what capability is missing, and how do we make it both legible and enforceable for the agent?&quot;</p><p>Humans interact with the system almost entirely through prompts: an engineer describes a task, runs the agent, and allows it to open a pull request. To drive a PR to completion, we instruct Codex to review its own changes locally, request additional specific agent reviews both locally and in the cloud, respond to any human or agent given feedback, and iterate in a loop until all agent reviewers are satisfied (effectively this is a Ralph Wiggum Loop). Codex uses our standard development tools directly (gh, local scripts, and repository-embedded skills) to gather context without humans copying and pasting into the CLI.</p><p>Humans may review pull requests, but aren&#39;t required to. Over time, we&#39;ve pushed almost all review effort towards being handled agent-to-agent.</p><h2 id="increasing-application-legibility" tabindex="-1">Increasing application legibility <a class="header-anchor" href="#increasing-application-legibility" aria-label="Permalink to &quot;Increasing application legibility&quot;">​</a></h2><p>As code throughput increased, our bottleneck became human QA capacity. Because the fixed constraint has been human time and attention, we&#39;ve worked to add more capabilities to the agent by making things like the application UI, logs, and app metrics themselves directly legible to Codex.</p><p>For example, we made the app bootable per git worktree, so Codex could launch and drive one instance per change. We also wired the Chrome DevTools Protocol into the agent runtime and created skills for working with DOM snapshots, screenshots, and navigation. This enabled Codex to reproduce bugs, validate fixes, and reason about UI behavior directly.</p><blockquote><p><strong>Diagram:</strong> &quot;Codex drives the app with Chrome DevTools MCP to validate its work.&quot; Codex selects a target, snapshots the state before and after triggering a UI path, observes runtime events via Chrome DevTools, applies fixes, restarts, and loops re-running validation until the app is clean.</p></blockquote><p>We did the same for observability tooling. Logs, metrics, and traces are exposed to Codex via a local observability stack that&#39;s ephemeral for any given worktree. Codex works on a fully isolated version of that app—including its logs and metrics, which get torn down once that task is complete. Agents can query logs with LogQL and metrics with PromQL. With this context available, prompts like &quot;ensure service startup completes in under 800ms&quot; or &quot;no span in these four critical user journeys exceeds two seconds&quot; become tractable.</p><blockquote><p><strong>Diagram:</strong> &quot;Giving Codex a full observability stack in local dev.&quot; An app sends logs, metrics, and traces to Vector, which fans out data to an observability stack containing Victoria Logs, Metrics, and Traces, each queried via LogQL, PromQL, or TraceQL APIs. Codex uses these signals to query, correlate, and reason, then implements fixes in the codebase, restarts the app, re-runs workloads, tests UI journeys, and repeats in a feedback loop.</p></blockquote><p>We regularly see single Codex runs work on a single task for upwards of six hours (often while the humans are sleeping).</p><h2 id="we-made-repository-knowledge-the-system-of-record" tabindex="-1">We made repository knowledge the system of record <a class="header-anchor" href="#we-made-repository-knowledge-the-system-of-record" aria-label="Permalink to &quot;We made repository knowledge the system of record&quot;">​</a></h2><p>Context management is one of the biggest challenges in making agents effective at large and complex tasks. One of the earliest lessons we learned was simple: give Codex a map, not a 1,000-page instruction manual.</p><p>We tried the &quot;one big AGENTS.md&quot; approach. It failed in predictable ways:</p><ul><li><strong>Context is a scarce resource.</strong> A giant instruction file crowds out the task, the code, and the relevant docs—so the agent either misses key constraints or starts optimizing for the wrong ones.</li><li><strong>Too much guidance becomes non-guidance.</strong> When everything is &quot;important,&quot; nothing is. Agents end up pattern-matching locally instead of navigating intentionally.</li><li><strong>It rots instantly.</strong> A monolithic manual turns into a graveyard of stale rules. Agents can&#39;t tell what&#39;s still true, humans stop maintaining it, and the file quietly becomes an attractive nuisance.</li><li><strong>It&#39;s hard to verify.</strong> A single blob doesn&#39;t lend itself to mechanical checks (coverage, freshness, ownership, cross-links), so drift is inevitable.</li></ul><p>So instead of treating AGENTS.md as the encyclopedia, we treat it as the table of contents.</p><p>The repository&#39;s knowledge base lives in a structured <code>docs/</code> directory treated as the system of record. A short AGENTS.md (roughly 100 lines) is injected into context and serves primarily as a map, with pointers to deeper sources of truth elsewhere.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>AGENTS.md</span></span>
<span class="line"><span>ARCHITECTURE.md</span></span>
<span class="line"><span>docs/</span></span>
<span class="line"><span>├── design-docs/</span></span>
<span class="line"><span>│   ├── index.md</span></span>
<span class="line"><span>│   ├── core-beliefs.md</span></span>
<span class="line"><span>│   └── ...</span></span>
<span class="line"><span>├── exec-plans/</span></span>
<span class="line"><span>│   ├── active/</span></span>
<span class="line"><span>│   ├── completed/</span></span>
<span class="line"><span>│   └── tech-debt-tracker.md</span></span>
<span class="line"><span>├── generated/</span></span>
<span class="line"><span>│   └── db-schema.md</span></span>
<span class="line"><span>├── product-specs/</span></span>
<span class="line"><span>│   ├── index.md</span></span>
<span class="line"><span>│   ├── new-user-onboarding.md</span></span>
<span class="line"><span>│   └── ...</span></span>
<span class="line"><span>├── references/</span></span>
<span class="line"><span>│   ├── design-system-reference-llms.txt</span></span>
<span class="line"><span>│   ├── nixpacks-llms.txt</span></span>
<span class="line"><span>│   ├── uv-llms.txt</span></span>
<span class="line"><span>│   └── ...</span></span>
<span class="line"><span>├── DESIGN.md</span></span>
<span class="line"><span>├── FRONTEND.md</span></span>
<span class="line"><span>├── PLANS.md</span></span>
<span class="line"><span>├── PRODUCT_SENSE.md</span></span>
<span class="line"><span>├── QUALITY_SCORE.md</span></span>
<span class="line"><span>├── RELIABILITY.md</span></span>
<span class="line"><span>└── SECURITY.md</span></span></code></pre></div><p><em>In-repository knowledge store layout.</em></p><p>Design documentation is catalogued and indexed, including verification status and a set of core beliefs that define agent-first operating principles. Architecture documentation provides a top-level map of domains and package layering. A quality document grades each product domain and architectural layer, tracking gaps over time.</p><p>Plans are treated as first-class artifacts. Ephemeral lightweight plans are used for small changes, while complex work is captured in execution plans with progress and decision logs that are checked into the repository. Active plans, completed plans, and known technical debt are all versioned and co-located, allowing agents to operate without relying on external context.</p><p>This enables progressive disclosure: agents start with a small, stable entry point and are taught where to look next, rather than being overwhelmed up front.</p><p>We enforce this mechanically. Dedicated linters and CI jobs validate that the knowledge base is up to date, cross-linked, and structured correctly. A recurring &quot;doc-gardening&quot; agent scans for stale or obsolete documentation that does not reflect the real code behavior and opens fix-up pull requests.</p><h2 id="agent-legibility-is-the-goal" tabindex="-1">Agent legibility is the goal <a class="header-anchor" href="#agent-legibility-is-the-goal" aria-label="Permalink to &quot;Agent legibility is the goal&quot;">​</a></h2><p>As the codebase evolved, Codex&#39;s framework for design decisions needed to evolve, too.</p><p>Because the repository is entirely agent-generated, it&#39;s optimized first for Codex&#39;s legibility. In the same way teams aim to improve navigability of their code for new engineering hires, our human engineers&#39; goal was making it possible for an agent to reason about the full business domain directly from the repository itself.</p><p>From the agent&#39;s point of view, anything it can&#39;t access in-context while running effectively doesn&#39;t exist. Knowledge that lives in Google Docs, chat threads, or people&#39;s heads are not accessible to the system. Repository-local, versioned artifacts (e.g., code, markdown, schemas, executable plans) are all it can see.</p><blockquote><p><strong>Diagram:</strong> &quot;The limits of agent knowledge: What Codex can&#39;t see doesn&#39;t exist.&quot; Codex&#39;s knowledge is shown as a bounded bubble. Below it are examples of unseen knowledge—Google Docs, Slack messages, and tacit human knowledge. Arrows indicate that to make this information visible to Codex, it must be encoded into the codebase as markdown.</p></blockquote><p>We learned that we needed to push more and more context into the repo over time. That Slack discussion that aligned the team on an architectural pattern? If it isn&#39;t discoverable to the agent, it&#39;s illegible in the same way it would be unknown to a new hire joining three months later.</p><p>Giving Codex more context means organizing and exposing the right information so the agent can reason over it, rather than overwhelming it with ad-hoc instructions. In the same way you would onboard a new teammate on product principles, engineering norms, and team culture (emoji preferences included), giving the agent this information leads to better-aligned output.</p><p>This framing clarified many tradeoffs. We favored dependencies and abstractions that could be fully internalized and reasoned about in-repo. Technologies often described as &quot;boring&quot; tend to be easier for agents to model due to composability, API stability, and representation in the training set. In some cases, it was cheaper to have the agent reimplement subsets of functionality than to work around opaque upstream behavior from public libraries. For example, rather than pulling in a generic p-limit-style package, we implemented our own map-with-concurrency helper: it&#39;s tightly integrated with our OpenTelemetry instrumentation, has 100% test coverage, and behaves exactly the way our runtime expects.</p><p>Pulling more of the system into a form the agent can inspect, validate, and modify directly increases leverage—not just for Codex, but for other agents (e.g. Aardvark) that are working on the codebase as well.</p><h2 id="enforcing-architecture-and-taste" tabindex="-1">Enforcing architecture and taste <a class="header-anchor" href="#enforcing-architecture-and-taste" aria-label="Permalink to &quot;Enforcing architecture and taste&quot;">​</a></h2><p>Documentation alone doesn&#39;t keep a fully agent-generated codebase coherent. By enforcing invariants, not micromanaging implementations, we let agents ship fast without undermining the foundation. For example, we require Codex to parse data shapes at the boundary, but are not prescriptive on how that happens (the model seems to like Zod, but we didn&#39;t specify that specific library).</p><p>Agents are most effective in environments with strict boundaries and predictable structure, so we built the application around a rigid architectural model. Each business domain is divided into a fixed set of layers, with strictly validated dependency directions and a limited set of permissible edges. These constraints are enforced mechanically via custom linters (Codex-generated, of course!) and structural tests.</p><p>The diagram below shows the rule: within each business domain (e.g. App Settings), code can only depend &quot;forward&quot; through a fixed set of layers (Types -&gt; Config -&gt; Repo -&gt; Service -&gt; Runtime -&gt; UI). Cross-cutting concerns (auth, connectors, telemetry, feature flags) enter through a single explicit interface: Providers. Anything else is disallowed and enforced mechanically.</p><blockquote><p><strong>Diagram:</strong> &quot;Layered domain architecture with explicit cross-cutting boundaries.&quot; Inside the business logic domain are modules: Types -&gt; Config -&gt; Repo, and Providers -&gt; Service -&gt; Runtime -&gt; UI, with App Wiring + UI at the bottom. A Utils module sits outside the boundary and feeds into Providers.</p></blockquote><p>This is the kind of architecture you usually postpone until you have hundreds of engineers. With coding agents, it&#39;s an early prerequisite: the constraints are what allows speed without decay or architectural drift.</p><p>In practice, we enforce these rules with custom linters and structural tests, plus a small set of &quot;taste invariants.&quot; For example, we statically enforce structured logging, naming conventions for schemas and types, file size limits, and platform-specific reliability requirements with custom lints. Because the lints are custom, we write the error messages to inject remediation instructions into agent context.</p><p>In a human-first workflow, these rules might feel pedantic or constraining. With agents, they become multipliers: once encoded, they apply everywhere at once.</p><p>At the same time, we&#39;re explicit about where constraints matter and where they do not. This resembles leading a large engineering platform organization: enforce boundaries centrally, allow autonomy locally. You care deeply about boundaries, correctness, and reproducibility. Within those boundaries, you allow teams—or agents—significant freedom in how solutions are expressed.</p><p>The resulting code does not always match human stylistic preferences, and that&#39;s okay. As long as the output is correct, maintainable, and legible to future agent runs, it meets the bar.</p><p>Human taste is fed back into the system continuously. Review comments, refactoring pull requests, and user-facing bugs are captured as documentation updates or encoded directly into tooling. When documentation falls short, we promote the rule into code.</p><h2 id="throughput-changes-the-merge-philosophy" tabindex="-1">Throughput changes the merge philosophy <a class="header-anchor" href="#throughput-changes-the-merge-philosophy" aria-label="Permalink to &quot;Throughput changes the merge philosophy&quot;">​</a></h2><p>As Codex&#39;s throughput increased, many conventional engineering norms became counterproductive.</p><p>The repository operates with minimal blocking merge gates. Pull requests are short-lived. Test flakes are often addressed with follow-up runs rather than blocking progress indefinitely. In a system where agent throughput far exceeds human attention, corrections are cheap, and waiting is expensive.</p><p>This would be irresponsible in a low-throughput environment. Here, it&#39;s often the right tradeoff.</p><h2 id="what-agent-generated-actually-means" tabindex="-1">What &quot;agent-generated&quot; actually means <a class="header-anchor" href="#what-agent-generated-actually-means" aria-label="Permalink to &quot;What &quot;agent-generated&quot; actually means&quot;">​</a></h2><p>When we say the codebase is generated by Codex agents, we mean everything in the codebase.</p><p>Agents produce:</p><ul><li>Product code and tests</li><li>CI configuration and release tooling</li><li>Internal developer tools</li><li>Documentation and design history</li><li>Evaluation harnesses</li><li>Review comments and responses</li><li>Scripts that manage the repository itself</li><li>Production dashboard definition files</li></ul><p>Humans always remain in the loop, but work at a different layer of abstraction than we used to. We prioritize work, translate user feedback into acceptance criteria, and validate outcomes. When the agent struggles, we treat it as a signal: identify what is missing—tools, guardrails, documentation—and feed it back into the repository, always by having Codex itself write the fix.</p><p>Agents use our standard development tools directly. They pull review feedback, respond inline, push updates, and often squash and merge their own pull requests.</p><h2 id="increasing-levels-of-autonomy" tabindex="-1">Increasing levels of autonomy <a class="header-anchor" href="#increasing-levels-of-autonomy" aria-label="Permalink to &quot;Increasing levels of autonomy&quot;">​</a></h2><p>As more of the development loop was encoded directly into the system—testing, validation, review, feedback handling, and recovery—the repository recently crossed a meaningful threshold where Codex can end-to-end drive a new feature.</p><p>Given a single prompt, the agent can now:</p><ol><li>Validate the current state of the codebase</li><li>Reproduce a reported bug</li><li>Record a video demonstrating the failure</li><li>Implement a fix</li><li>Validate the fix by driving the application</li><li>Record a second video demonstrating the resolution</li><li>Open a pull request</li><li>Respond to agent and human feedback</li><li>Detect and remediate build failures</li><li>Escalate to a human only when judgment is required</li><li>Merge the change</li></ol><p>This behavior depends heavily on the specific structure and tooling of this repository and should not be assumed to generalize without similar investment—at least, not yet.</p><h2 id="entropy-and-garbage-collection" tabindex="-1">Entropy and garbage collection <a class="header-anchor" href="#entropy-and-garbage-collection" aria-label="Permalink to &quot;Entropy and garbage collection&quot;">​</a></h2><p>Full agent autonomy also introduces novel problems. Codex replicates patterns that already exist in the repository—even uneven or suboptimal ones. Over time, this inevitably leads to drift.</p><p>Initially, humans addressed this manually. Our team used to spend every Friday (20% of the week) cleaning up &quot;AI slop.&quot; Unsurprisingly, that didn&#39;t scale.</p><p>Instead, we started encoding what we call &quot;golden principles&quot; directly into the repository and built a recurring cleanup process. These principles are opinionated, mechanical rules that keep the codebase legible and consistent for future agent runs. For example: (1) we prefer shared utility packages over hand-rolled helpers to keep invariants centralized, and (2) we don&#39;t probe data &quot;YOLO-style&quot;—we validate boundaries or rely on typed SDKs so the agent can&#39;t accidentally build on guessed shapes. On a regular cadence, we have a set of background Codex tasks that scan for deviations, update quality grades, and open targeted refactoring pull requests. Most of these can be reviewed in under a minute and automerged.</p><p>This functions like garbage collection. Technical debt is like a high-interest loan: it&#39;s almost always better to pay it down continuously in small increments than to let it compound and tackle it in painful bursts. Human taste is captured once, then enforced continuously on every line of code. This also lets us catch and resolve bad patterns on a daily basis, rather than letting them spread in the code base for days or weeks.</p><h2 id="what-we-re-still-learning" tabindex="-1">What we&#39;re still learning <a class="header-anchor" href="#what-we-re-still-learning" aria-label="Permalink to &quot;What we&#39;re still learning&quot;">​</a></h2><p>This strategy has so far worked well up through internal launch and adoption at OpenAI. Building a real product for real users helped anchor our investments in reality and guide us towards long-term maintainability.</p><p>What we don&#39;t yet know is how architectural coherence evolves over years in a fully agent-generated system. We&#39;re still learning where human judgment adds the most leverage and how to encode that judgment so it compounds. We also don&#39;t know how this system will evolve as models continue to become more capable over time.</p><p>What&#39;s become clear: building software still demands discipline, but the discipline shows up more in the scaffolding rather than the code. The tooling, abstractions, and feedback loops that keep the codebase coherent are increasingly important.</p><p>Our most difficult challenges now center on designing environments, feedback loops, and control systems that help agents accomplish our goal: build and maintain complex, reliable software at scale.</p><p>As agents like Codex take on larger portions of the software lifecycle, these questions will matter even more. We hope that sharing some early lessons helps you reason about where to invest your effort so you can just build things.</p>`,85)])])}const g=t(o,[["render",i]]);export{u as __pageData,g as default};
