import{c as t,Q as a,j as s,m as r}from"./chunks/framework.BXc4yaFE.js";const u=JSON.parse('{"title":"How to build your own agent harness???","description":"","frontmatter":{"url":"https://x.com/mfpiccolo/status/2060069083878408689","title":"How to build your own agent harness???","author":"Mike Piccolo","archived":"2026-05-31T00:00:00.000Z","handle":"@mfpiccolo","published":"2026-05-28T00:00:00.000Z","stats":"1927 likes · 241 retweets · 4886 bookmarks · 333501 views","type":"twitter-article"},"headers":[],"relativePath":"research/tweets/2026-05-28-@mfpiccolo-408689.md","filePath":"research/tweets/2026-05-28-@mfpiccolo-408689.md","lastUpdated":1780990164000}'),o={name:"research/tweets/2026-05-28-@mfpiccolo-408689.md"};function i(n,e,h,l,c,d){return a(),s("div",null,[...e[0]||(e[0]=[r(`<h1 id="how-to-build-your-own-agent-harness-—-mfpiccolo" tabindex="-1">How to build your own agent harness??? — @mfpiccolo <a class="header-anchor" href="#how-to-build-your-own-agent-harness-—-mfpiccolo" aria-label="Permalink to &quot;How to build your own agent harness??? — @mfpiccolo&quot;">​</a></h1><p><strong>來源</strong>：<a href="https://x.com/mfpiccolo/status/2060069083878408689" target="_blank" rel="noreferrer">https://x.com/mfpiccolo/status/2060069083878408689</a><strong>作者</strong>：Mike Piccolo（@mfpiccolo） <strong>發布日期</strong>：2026-05-28 <strong>收錄日期</strong>：2026-05-31 <strong>類型</strong>：Twitter Article <strong>統計</strong>：1927 likes · 241 retweets · 4886 bookmarks · 333501 views</p><hr><h2 id="全文" tabindex="-1">全文 <a class="header-anchor" href="#全文" aria-label="Permalink to &quot;全文&quot;">​</a></h2><p>Most agent teams don&#39;t build a harness. They adopt one. LangChain, LangGraph, OpenAI Agents SDK, Anthropic SDK, CrewAI, AutoGen, the loop, the tools, the memory, and the orchestration are picked off the shelf as a single decision. The harness is a framework you import. If something inside it doesn&#39;t fit, you fork it, fight it, or work around it.</p><p>[圖 1]</p><p>I think that shape is wrong, and it&#39;s the reason every long-running agent team eventually ends up rewriting its harness from scratch. The harness isn&#39;t one thing. It&#39;s ten or twelve different things bundled together because the surrounding ecosystem doesn&#39;t give you a way to compose them. Pi agent packages are on the right track, but they are still in the paradigm of &quot;Add another service and integrate it with all others.&quot; The iii engine treats all workers the same and removes the integration logic completely. The provider router, the credential vault, the policy engine, the approval gate, the model catalog, the session storage, the budget tracker, the after-call hook fanout, and the durable turn loop are independent concerns. These are all interoperable with your queue, http/api server, streaming, even browser workers. A framework that ships them as one block is selling you a tradeoff you didn&#39;t have to make.</p><p>The bet underneath iii is that they shouldn&#39;t be one block. There should be a set of workers on a shared engine, each replaceable, each versioned independently, each connected by a single primitive: a trigger (iii.trigger()) that every other worker also uses. The harness becomes a stack of installable workers, and &quot;build your own&quot; stops meaning &quot;fork a framework.&quot; It means &quot;swap a few workers.&quot;</p><p>This post walks through what that actually looks like. The complete stack that drives an iii agent turn today, why each layer is its own worker, and how you replace any of them.</p><h2 id="the-15-jobs-an-agent-harness-has-to-do" tabindex="-1">The 15 jobs an agent harness has to do <a class="header-anchor" href="#the-15-jobs-an-agent-harness-has-to-do" aria-label="Permalink to &quot;The 15 jobs an agent harness has to do&quot;">​</a></h2><p>If you strip a production agent harness back to its responsibilities, you get a list that looks roughly like this:</p><ol><li>Accept a turn request from a client and persist it</li><li>Resolve credentials for whichever model provider gets called</li><li>Look up what the chosen model can actually do (vision, tools, streaming, context window)</li><li>Drive the per-turn state machine, provision, stream assistant, run tools, steer, tear down</li><li>Load and serve skill bodies that describe each function&#39;s request shape, error codes, and usage notes</li><li>Assemble the system prompt, mode paragraph, identity preamble, working directory, and default skills appendix</li><li>Stream tokens back to the client as the model produces them</li><li>Check every tool call (that&#39;s just a function) against a policy before it runs</li><li>Pause tool calls that need a human decision and route the answer back to the right turn</li><li>Track LLM spend against per-workspace or per-agent budgets</li><li>Run hooks before and after tool calls (logging, redaction, custom side effects)</li><li>Persist the session as a branching tree so forks and resumes work</li><li>Compact session history when the context window fills up</li><li>Emit an event stream that the UI subscribes to</li><li>Missing piece from every agent&#39;s company building, I see. Carry one OpenTelemetry trace across every step so you can debug it</li></ol><p>Every serious agent harnesses most of these. The expensive ones do all of them. The cheap ones cut corners and rebuild the corners later when they hit production. The frameworks bundle them into a monolith and ship one version of each. That last part is the part that costs you, because a year in, you find out that the policy engine you want is not the policy engine the framework ships, and replacing it means replacing the harness.</p><p>The iii harness ships every one of those thirteen jobs as a separate worker on the workers.iii.dev registry. Each speaks the same WebSocket protocol. Each registers functions and triggers on the same engine bus. Each is iii worker add-able, swappable, and writable in any language with an SDK.</p><h2 id="the-stack-by-worker" tabindex="-1">The stack, by worker <a class="header-anchor" href="#the-stack-by-worker" aria-label="Permalink to &quot;The stack, by worker&quot;">​</a></h2><p>Here is the actual production stack from the iii-hq/workers monorepo, with each worker&#39;s job in one line. The whole bundle ships at github.com/iii-hq/workers/harness:</p><p>[圖 2]</p><p>Eleven workers. One engine. Each is on a published version. Each is independently runnable as a standalone process (pnpm dev:<code>&lt;worker&gt;</code> in dev, iii worker add <code>&lt;specific-worker&gt;</code> as a release binary) or as part of the composite entry point that spins them up together.</p><p>The reason this matters: every box in that table is a place where someone can hand you a different worker, and you keep the rest. Don&#39;t like the static model catalogue? Plug in a worker that registers models::list and reads from a live API. Don&#39;t like file-backed credentials? Plug in a worker that registers auth::get_token and reads from a secrets manager. Want a different turn FSM for a workflow that branches differently? Replace turn-orchestrator, every dependent calls run::start and reads turn_state through the same bus, so the rest of the stack doesn&#39;t change.</p><h2 id="how-the-loop-actually-runs" tabindex="-1">How the loop actually runs <a class="header-anchor" href="#how-the-loop-actually-runs" aria-label="Permalink to &quot;How the loop actually runs&quot;">​</a></h2><p>The shape of one turn looks like this, walking through the workers in the order they fire.</p><p>A browser/cli/chat POSTs a turn through harness::trigger with {session_id, message_id, payload}. The harness meta-worker forwards payload to run::start. That hop exists so the OpenTelemetry span wrapper can seed the session and message IDs as baggage, which propagates to every nested iii.trigger call across every worker in the stack. The trace tree on the other side is one connected graph.</p><p>run::start lands on the turn-orchestrator. It persists the run request, seeds the initial TurnStateRecord in iii state at session/<code>&lt;sid&gt;</code>/turn_state, and returns immediately. The actual work happens inside the durable per-state machine, woken by publishes to the turn-step FIFO.</p><p>The two terminal states are stopped (clean exit via finishSession()) and failed (an unexpected handler throw routes here, acks the queue so it stops retrying, and surfaces message_complete<code>{stop_reason:&#39;error&#39;}</code> plus agent_end so the UI shows the reason). Teardown is an inline finishSession() port called from any turn-end path, not a separate enqueued step.</p><p>provisioning does three things. It boots a iii-sandbox microVM if the run needs isolated execution. It calls directory::skills::download for every namespace in system_default_skills (default [&quot;iii://iii-directory/index&quot;]) so iii-directory pre-caches the skill bodies the run starts with. And it assembles the system prompt in three layers: a mode paragraph picked from run_request.mode (plan, ask, or agent), the iii identity preamble that teaches the model the agent_trigger convention and the directory::skills::get on-demand discovery pattern, and an appended index of the default skills the agent boots with. The caller can override the whole prompt by passing system_prompt on run::start; otherwise the orchestrator builds it. Function schemas come from the live engine catalog.</p><p>assistant_streaming calls provider::<code>&lt;name&gt;</code>::stream on whichever provider worker matches the run&#39;s provider field. The provider worker pulls credentials via auth::get_token (auth-credentials), streams the model&#39;s SSE response into an iii channel, and the orchestrator drains that channel emitting message_update events on agent::events for the UI fanout. Channel creation and the read loop live behind a pull-based MessagePump in provider-stream.ts, so the streaming state stays focused on transitions.</p><p>When the assistant returns tool calls, the FSM enters function_execute. Every tool call passes through dispatchWithHook, the single chokepoint in the orchestrator. consultBefore calls policy::check_permissions directly with a 5-second timeout. The policy worker (the harness meta-worker, in the default stack) reads iii-permissions.yaml, matches the call&#39;s function_id against the rule set, and returns one of three outcomes:</p><ul><li>allow: dispatch proceeds; the orchestrator triggers the target function and writes the result</li><li>deny: dispatch short-circuits with a DenialEnvelope, the result becomes a denial record</li><li>needs_approval: the individual call parks into the turn&#39;s awaiting_approval list. The rest of the batch keeps dispatching. The turn transitions to function_awaiting_approval only when one or more entries are pending.</li></ul><p>The approval wake is reactive and shared. The orchestrator registers exactly one turn::on_approval state trigger on scope approvals. When the console calls approval::resolve, the approval-gate worker writes approvals/<code>&lt;sid&gt;</code>/<code>&lt;cid&gt;</code> = {decision, reason} to iii state. That write fires turn::on_approval, which advances the affected session. function_awaiting_approval reads only the decisions that just landed, dispatches each one as it arrives (allow becomes a pre-approved dispatch, deny or aborted becomes a synthetic denial), and advances when awaiting_approval[] is empty. No per-call resume functions to register. No startup re-scan to recover pending approvals. One trigger covers every session.</p><p>Fail-closed by construction: if the policy worker is unreachable or the 5-second timeout fires, consultBefore denies the call with a gate_unavailable envelope. If iii::durable::publish itself errored, the hook fanout returns publish_failed: true and the orchestrator treats it as a deny.</p><p>A few latency wins fall out of this shape. The after-function-call hook short-circuits publish_collect via a subscriber-presence cache when no durable subscriber is registered for the topic, removing roughly 500ms per executed function call. tearing_down is inlined into finishSession(), removing one durable queue hop per turn. context-compaction subscribes to a dedicated agent::turn_end stream the orchestrator emits at turn boundaries, so compactor wakeups are per-turn instead of per-event. The session-create fanout state trigger gates by scope alone and matches in-process, so the previous per-write harness::session::is_create_event RPC is gone.</p><p>After the batch completes, steering_check decides whether to continue, stop, or hit max_turns. If continue, loop back to assistant_streaming. If stop or max, finishSession() runs inline: emit agent_end, free the sandbox, transition to stopped.</p><p>Throughout the whole run, every worker that participates emits OTel spans tagged with iii.session.id, iii.message.id, and iii.function.id. Those tags are what the engine&#39;s engine::traces::group_by reads to populate &quot;Group by Session&quot; / &quot;Group by Message&quot; / &quot;Group by Function&quot; in the traces UI. The instrumentation is automatic: src/runtime/worker.ts wraps every registerFunction in a Proxy so no per-worker code has to remember to add spans.</p><h2 id="build-your-own" tabindex="-1">Build your own <a class="header-anchor" href="#build-your-own" aria-label="Permalink to &quot;Build your own&quot;">​</a></h2><p>The interesting part is that none of the workers above are special. Each one is a process that opens a WebSocket to the engine, registers some functions and triggers, and runs. The contract is the same as the contract every application worker uses. The harness is built on the same primitive your business logic is built on.</p><p>Which means &quot;build your own harness&quot; decomposes into the same operation as &quot;write any worker.&quot; You pick the layer you want to replace, you write a worker that registers the same functions on the bus, you iii worker add it, and the rest of the stack starts using your worker.</p><p>Two layers don&#39;t show up in the worker table above but matter for how the harness behaves. Skills are how each worker advertises what its functions do. Every worker can publish a skill at iii://<code>&lt;worker&gt;</code>/<code>&lt;function&gt;</code> that the agent fetches via directory::skills::get before calling that function for the first time. The system prompt is assembled per turn from a mode paragraph, the iii identity preamble, and the default skill bodies the run was configured with. Both are bus-driven: skills are served by the iii-directory worker, the system prompt is assembled by the turn-orchestrator. Both are replaceable.</p><p>Five concrete examples.</p><p>Replace the model catalogue with a live API. Write a worker that registers models::list, models::get, models::supports. Have it fetch from your provider&#39;s catalog endpoint every N minutes and cache. Publish it. iii worker add your-org/dynamic-models-catalog. Stop the static models-catalog worker. The turn-orchestrator never knows the difference. It calls iii.trigger(&#39;models::list&#39;) and the engine routes to whichever worker registered that function id most recently.</p><p>Add a new provider. The shape is provider-kimi and provider-lmstudio already prove out. Each is one worker that registers provider::<code>&lt;name&gt;</code>::stream and provider::<code>&lt;name&gt;</code>::complete, drains an SSE stream from the upstream API into an iii channel, and writes its model usage to llm-budget via budget::record. Adding a fifth provider is writing one folder with one iii.worker.yaml and one register.ts. Publish to the registry, or keep it local. The turn-orchestrator picks the provider by the run&#39;s provider field; new providers become available the instant the worker connects.</p><p>Serve skills from a private artifact store. Write a worker that registers directory::skills::get and directory::skills::list, backed by your internal docs system or a private S3 bucket. Disconnect or rename the default iii-directory worker. The orchestrator&#39;s bootstrap calls directory::skills::download per namespace; your worker answers. The agent&#39;s &quot;fetch the per-function skill before calling a new function&quot; pattern keeps working unchanged because the wire shape is the same.</p><p>Override the system prompt entirely. run::start accepts an optional system_prompt field. Pass it and the orchestrator uses your string verbatim, skipping the mode paragraph + identity preamble + skills appendix assembly. Useful when you have an existing prompt asset you want the harness to honour without modification. Skill download still runs in bootstrap, so the agent keeps directory::skills::get on-demand discovery even with a custom prompt.</p><p>Replace the approval gate UI surface. The default approval-gate worker registers approval::resolve. The wire schema is one function call:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">iii.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">trigger</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;approval::resolve&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  session_id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;...&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  function_call_id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;...&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  decision: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;allow&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;deny&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;aborted&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  reason: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;optional human text&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p>The handler persists approvals/<code>&lt;sid&gt;</code>/<code>&lt;cid&gt;</code> = {decision, reason} to iii state. The orchestrator&#39;s single turn::on_approval state trigger picks that write up and wakes the right session. If you want to drive approvals from Slack instead of the console, write a Slack worker that listens for /approve <code>&lt;id&gt;</code> and /deny <code>&lt;id&gt;</code> slash commands, then calls approval::resolve with the right payload. The orchestrator never knows the difference. The whole approval-gate worker stays untouched. You added a new worker; you didn&#39;t replace the existing one.</p><p>If you want a different policy engine (OPA, Cedar, your own DSL), write a worker that registers policy::check_permissions and returns { decision, rule_id?, matched_constraint? }. Disconnect the default policy worker (which is wrapped inside the harness meta-worker, so you&#39;d disable that handler or run a stripped-down meta-worker). The turn-orchestrator&#39;s consultBefore doesn&#39;t know the difference. Same 5-second timeout, same fail-closed semantics, same wire shape.</p><p>The point of these examples isn&#39;t the specific replacements. It&#39;s the shape of the operation. Every harness layer in the iii stack is reachable through one or two function ids on the bus. Replacing a layer is writing a worker that registers those ids. The rest of the system stays.</p><h2 id="the-harness-is-a-slider-not-a-fork-in-the-road" tabindex="-1">The harness is a slider, not a fork in the road <a class="header-anchor" href="#the-harness-is-a-slider-not-a-fork-in-the-road" aria-label="Permalink to &quot;The harness is a slider, not a fork in the road&quot;">​</a></h2><p>The classic harness debate frames itself as thin vs thick. Anthropic&#39;s thin loop versus LangGraph&#39;s explicit DAG. The framing assumes you pick one side and live with it.</p><p>When the harness is composed of workers on the same bus, thin vs thick is just a count of how many workers you install. A thin harness is turn-orchestrator plus provider-anthropic plus auth-credentials plus a minimal harness meta-worker. That&#39;s it. No approvals, no budgets, no policy engine, no hook fanout. Run anything. Trust the model. Useful for autonomous research agents, experimental loops, anything internal.</p><p>A thick harness is all thirteen workers plus context-compaction plus a custom policy worker plus a custom approval-gate plus a Slack-integrated approval surface plus the budget worker enforcing per-workspace caps. Useful for an agent running customer workflows where every tool call needs to be auditable and every model spend has to roll up to a finance dashboard.</p><p>The architectural distance between thin and thick isn&#39;t a rewrite. It&#39;s a config change. Same wire protocol, same trace shape, same observability story. The slider moves by adding and removing workers from your config.yaml. Everything else holds.</p><p>It applies inside a single worker too. The turn-orchestrator just shipped a refactor that collapsed its FSM from eleven states to seven, deleted the per-call turn::approval_resume::<code>&lt;sid&gt;</code>/<code>&lt;cid&gt;</code> mechanism in favour of one reactive turn::on_approval state trigger on scope approvals, and inlined tearing_down into a finishSession() port. Every other worker in the stack (approval-gate, session, llm-budget, providers, models-catalog, auth-credentials, hook-fanout, context-compaction) stayed unchanged. The approval::resolve wire shape didn&#39;t move. The contracts held. That&#39;s the property the composition gives you: a major internal rewrite of one worker is a self-contained change because every neighbour talks to it through bus-level function ids.</p><p>This is the part the framework model can&#39;t give you. A framework picks a position on the slider for you and locks you in. The worker model leaves the slider in your hand.</p><h2 id="what-this-means-in-practice" tabindex="-1">What this means in practice <a class="header-anchor" href="#what-this-means-in-practice" aria-label="Permalink to &quot;What this means in practice&quot;">​</a></h2><p>If you&#39;ve been running an agent on top of a framework and feeling the same boundary problems most teams hit at scale, the answer is probably not &quot;rewrite the harness in our own framework.&quot; The policy engine doesn&#39;t extend the way you need. The approval UI is wired into the framework&#39;s chat surface. The credential store can&#39;t talk to your secrets manager. The budget tracker is in a sidecar database the trace can&#39;t see. The answer is to switch to a substrate where the harness is decomposed in the first place.</p><p>The fastest way to feel the argument is to clone github.com/iii-hq/workers, pnpm install, pnpm build, and run the composite entry point. You&#39;ll get the full fourteen-worker harness pointed at an iii engine. You can disable any worker by removing its entry from the boot list. You can swap any worker by writing a replacement that registers the same function ids. You can extend any worker by adding a subscriber to its hook topics. hook-fanout::publish_collect is the generic every iii hook builds on.</p><p>The docs live at iii.dev/docs. The engine is at github.com/iii-hq/iii. The worker registry is at workers.iii.dev. The harness bundle is at github.com/iii-hq/workers/harness.</p><h2 id="the-bet" tabindex="-1">The bet <a class="header-anchor" href="#the-bet" aria-label="Permalink to &quot;The bet&quot;">​</a></h2><p>A harness is not a thing you install. A harness is a set of jobs your system has to do for an agent to run durably, safely and observably. The framework era bundled those jobs together because nothing underneath gave you a way to compose them.</p><p>iii&#39;s bet is that one primitive: a worker that connects to the engine over WebSocket and registers functions and triggers is small enough to absorb every one of those jobs separately, and that the resulting stack is more useful than any framework because every layer is independently replaceable.</p><p>You don&#39;t adopt the iii harness. You install the workers you want, write the ones you need, and end up with a harness shaped exactly like your system. Same protocol on every layer. Same trace across every call. Same iii worker add for the parts you take from the registry as for the parts you publish yourself.</p><p>That&#39;s what &quot;build your own agent harness&quot; looks like when the substrate is the right shape. Pick the workers. Write the missing ones. Compose. The harness is the composition.</p><p>Join us in building the perfect agent harness that the modern world needs: discord.gg/iiidev</p><p>iii is open source. Get started at iii.dev/docs. The harness workers are at github.com/iii-hq/workers and the engine is at github.com/iii-hq/iii.</p><p>— Mike Piccolo, Founder &amp; CEO @iiidevs</p><hr><h2 id="研究摘要" tabindex="-1">研究摘要 <a class="header-anchor" href="#研究摘要" aria-label="Permalink to &quot;研究摘要&quot;">​</a></h2><p><strong>TL;DR</strong>：別採用 LangChain 等框架——把 agent harness 的 15 個職責拆成獨立 worker，透過 WebSocket bus 組合，讓每層可獨立替換而無需 fork 整個框架。</p><h3 id="核心論點" tabindex="-1">核心論點 <a class="header-anchor" href="#核心論點" aria-label="Permalink to &quot;核心論點&quot;">​</a></h3><ol><li>現有框架（LangChain/LangGraph/CrewAI/AutoGen）把 harness 的 10-15 個職責打包成一個黑箱，導致任何一個不合適的部分都需要 fork 整個框架</li><li>iii 的解法：每個職責（policy engine、credential vault、approval gate、budget tracker、context-compaction 等）是一個獨立 worker，透過 <code>iii.trigger()</code> 相互呼叫</li><li>thin vs thick harness 不是二選一，而是 worker 安裝數量的 slider（config.yaml 增減 worker 即可）</li><li>替換任何 harness layer 只需寫一個新 worker 並在 bus 上 register 相同 function id，其他 worker 不需改變</li><li>FSM 從 11 states 縮減到 7、approval 機制從 per-call resume 改為 one reactive trigger，全部只動一個 worker，合約未變</li></ol><h3 id="關鍵數據-工具" tabindex="-1">關鍵數據 / 工具 <a class="header-anchor" href="#關鍵數據-工具" aria-label="Permalink to &quot;關鍵數據 / 工具&quot;">​</a></h3><ul><li><strong>15 個 harness 職責</strong>：詳列於 &quot;The 15 jobs&quot; 節</li><li><strong>11 workers in production stack</strong>：turn-orchestrator、provider-anthropic、auth-credentials、approval-gate、llm-budget、hook-fanout、context-compaction 等</li><li><strong>WebSocket + iii.trigger()</strong> 作為核心通訊 primitive</li><li><strong>OpenTelemetry spans 自動注入</strong>：<code>src/runtime/worker.ts</code> Proxy 包裝，無需 per-worker 手動加</li><li><strong>~500ms 節省</strong> per function call（subscriber-presence cache 避免 publish_collect RPC）</li><li><strong>FSM 精簡</strong>：11 states → 7 states，per-call resume → 1 reactive trigger on scope approvals</li><li><strong>GitHub repos</strong>：<code>github.com/iii-hq/workers</code>、<code>github.com/iii-hq/iii</code></li><li><strong>Policy engine</strong>：fail-closed，5-second timeout → deny with gate_unavailable</li></ul><h3 id="批判性觀察" tabindex="-1">批判性觀察 <a class="header-anchor" href="#批判性觀察" aria-label="Permalink to &quot;批判性觀察&quot;">​</a></h3><ol><li><strong>廣告成分明顯</strong>：作者是 iii (iiidevs) 的 Founder &amp; CEO，文章本質是產品行銷，技術論點清晰但缺乏第三方驗證或採用案例</li><li><strong>Worker decomposition = distributed microservices 的 agent 版</strong>：面臨相同挑戰（網路延遲、版本相容性、partial failure、分散式 trace）；文章描述的好處（獨立替換）在任何 well-designed microservices 架構下都能實現</li><li><strong>學習曲線未討論</strong>：文章迴避了「11 workers 的 WebSocket mesh 比單一 LangGraph 更難 debug」的問題；OTel 整合確實有幫助，但初期配置複雜度也更高</li></ol><hr><h2 id="評分" tabindex="-1">評分 <a class="header-anchor" href="#評分" aria-label="Permalink to &quot;評分&quot;">​</a></h2><table tabindex="0"><thead><tr><th>維度</th><th>分數</th><th>說明</th></tr></thead><tbody><tr><td>A. Workspace 可行動性 (30%)</td><td>8/10</td><td>有完整 GitHub repo + docs + CLI 命令，可直接 clone 試用；harness 分解概念可直接套用到自建系統設計</td></tr><tr><td>B. 創新性 (20%)</td><td>7/10</td><td>Worker decomposition 不是新概念，但 agent-specific 實作 + open source 生態 + WebSocket bus primitive 有實質創新</td></tr><tr><td>C. 證據品質 (20%)</td><td>7/10</td><td>有具體程式碼、效能數字、GitHub repo；但為廠商自述，缺乏獨立第三方驗證</td></tr><tr><td>D. 技術深度 (15%)</td><td>8/10</td><td>FSM 設計、approval reactive trigger、OTel Proxy instrumentation、subscriber-presence cache 等細節非常深入</td></tr><tr><td>E. 泛化性 (15%)</td><td>8/10</td><td>核心原則（harness = 可替換 worker 組合）可泛化到任何需要 agent 框架選型的場景</td></tr><tr><td><strong>加權總分</strong></td><td><strong>7.6/10</strong></td><td>8×0.3+7×0.2+7×0.2+8×0.15+8×0.15</td></tr></tbody></table><p><strong>整合決策</strong>：Agent <strong>理由</strong>：提供完整的多步驟自主 agent 架構藍圖，涵蓋 turn FSM、policy engine、approval gate、OTel 整合，分數 7.6 且直接可 clone 試用。</p>`,79)])])}const g=t(o,[["render",i]]);export{u as __pageData,g as default};
