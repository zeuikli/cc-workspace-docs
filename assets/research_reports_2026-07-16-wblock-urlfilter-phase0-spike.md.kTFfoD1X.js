import{c as t,Q as n,j as s,m as o}from"./chunks/framework.BXc4yaFE.js";const u=JSON.parse('{"title":"Phase 0 可行性 spike — NEURLFilterManager stub/local PIR 配置可行性","description":"","frontmatter":{},"headers":[],"relativePath":"research/reports/2026-07-16-wblock-urlfilter-phase0-spike.md","filePath":"research/reports/2026-07-16-wblock-urlfilter-phase0-spike.md","lastUpdated":1785909234000}'),a={name:"research/reports/2026-07-16-wblock-urlfilter-phase0-spike.md"};function i(r,e,l,d,c,p){return n(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="phase-0-可行性-spike-—-neurlfiltermanager-stub-local-pir-配置可行性" tabindex="-1">Phase 0 可行性 spike — NEURLFilterManager stub/local PIR 配置可行性 <a class="header-anchor" href="#phase-0-可行性-spike-—-neurlfiltermanager-stub-local-pir-配置可行性" aria-label="Permalink to &quot;Phase 0 可行性 spike — NEURLFilterManager stub/local PIR 配置可行性&quot;">​</a></h1><blockquote><p>對應設計：<code>2026-07-16-wblock-urlfilter-architecture.md</code> §3 PIR 決策矩陣「模式 C（stub-PIR / prefilter-only）」成立前提。 日期：2026-07-16　環境：macOS 26（Darwin 25.5）· Xcode macOS 26 SDK（<code>MacOSX26.sdk</code>）· arm64 權威來源分級：SDK swiftinterface（實測 read）&gt; 系統 daemon strings（實測）&gt; 推斷。</p></blockquote><hr><h2 id="環境限制-影響本-spike-的執行方式" tabindex="-1">環境限制（影響本 spike 的執行方式） <a class="header-anchor" href="#環境限制-影響本-spike-的執行方式" aria-label="Permalink to &quot;環境限制（影響本 spike 的執行方式）&quot;">​</a></h2><p><code>.claude/hooks/block-dangerous.sh</code> 為 <strong>Layer 2 allow-list</strong>（白名單模式，只放行清單內 command）。<code>swiftc</code> / <code>swift</code> / <code>xcodebuild</code> <strong>均不在白名單</strong> → agent 無法自動觸發任何 Swift 編譯器前端。brief 授權的替代路徑 <code>/usr/bin/swift</code> 同被擋。依 brief「若其他指令被 hook 擋，記錄後改走替代路徑，不硬闖」與 core.md「不改治理層」原則，<strong>未修改 hook、未硬闖</strong>。</p><p>後果：</p><ul><li><strong>實驗一（編譯）與實驗二（執行）的實際跑動未由 agent 完成</strong> → .swift 原始檔 + 編譯命令已備妥，供 lead/使用者在互動 shell 手動執行（<code>build_and_run.sh</code>）。</li><li><strong>實驗一的核心目的（API 形狀確認）改由直接 read SDK swiftinterface 達成</strong>（比編譯更權威——編譯只是間接驗證 swiftinterface）。</li><li><strong>實驗三（entitlement）不需編譯器，已完整以 <code>strings</code> 取證。</strong></li></ul><p>產物路徑（spike 目錄 <code>/private/tmp/claude-501/-Users-zeuik-cc-workspace/c0b4d20e-195c-472c-972e-8c7a5b6f5f75/scratchpad/urlfilter-spike/</code>）：</p><ul><li><code>main.swift</code> — 實驗一/二 CLI 原始檔</li><li><code>build_and_run.sh</code> — 編譯 + 執行命令（手動跑）</li></ul><hr><h2 id="實驗一-編譯層驗證-—-api-形狀確認" tabindex="-1">實驗一：編譯層驗證 — API 形狀確認 <a class="header-anchor" href="#實驗一-編譯層驗證-—-api-形狀確認" aria-label="Permalink to &quot;實驗一：編譯層驗證 — API 形狀確認&quot;">​</a></h2><p><strong>方法</strong>：直接 read macOS 26 SDK 的 <code>NetworkExtension.swiftinterface</code>（權威簽章來源）；並備妥 CLI 供手動編譯。</p><p><strong>命令</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SWIFTIF=.../MacOSX26.sdk/System/Library/Frameworks/NetworkExtension.framework/Versions/A/Modules/NetworkExtension.swiftmodule/arm64e-apple-macos.swiftinterface</span></span>
<span class="line"><span>grep -n setConfiguration &quot;$SWIFTIF&quot;</span></span></code></pre></div><p><strong>輸出（權威簽章，原樣）</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>245:  public func setConfiguration(pirServerURL: Foundation.URL, pirPrivacyPassIssuerURL: Foundation.URL?, pirAuthenticationToken: Swift.String, controlProviderBundleIdentifier: Swift.String) throws</span></span></code></pre></div><p>（同簽章亦出現於 tbd mangled symbol <code>NEURLFilterManagerC16setConfiguration...pirServerURL...pirPrivacyPassIssuerURL...pirAuthenticationToken...controlProviderBundleIdentifier</code> 及 x86_64/macabi swiftinterface — 四架構一致。）</p><p><strong>class 定義（swiftinterface line 168-171）</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>@available(watchOS, unavailable)</span></span>
<span class="line"><span>@available(visionOS, unavailable)</span></span>
<span class="line"><span>@available(tvOS, unavailable)</span></span>
<span class="line"><span>public class NEURLFilterManager : @unchecked Swift.Sendable {</span></span></code></pre></div><p>（class 級 <code>@available(iOS 26.0, macOS 26.0, macCatalyst 26.0, *)</code>。）</p><p><strong>結論</strong>：brief 指定的呼叫形狀 <code>setConfiguration(pirServerURL: URL(string:&quot;http://127.0.0.1:8080&quot;)!, pirPrivacyPassIssuerURL: nil, pirAuthenticationToken: &quot;stub&quot;, controlProviderBundleIdentifier: &quot;test.stub&quot;)</code><strong>逐參數與 SDK swiftinterface 完全相符</strong>（型別、optional、順序、throws 皆一致）。</p><ul><li><code>pirServerURL: URL</code>（非 optional）→ <strong>PIR server URL 是必填</strong>，無 PIR-free 過載，證實設計 §3 「純 prefilter-only 在 SDK 層無法配置」的硬事實。</li><li>型別上 <code>URL(string:&quot;http://127.0.0.1:8080&quot;)</code> 是合法 <code>URL</code> → <strong>編譯層不拒絕 local/stub 端點</strong>（是否 runtime 接受見實驗二/三）。</li></ul><p><strong>exit code</strong>：實際 <code>swiftc</code> 編譯未跑（hook 擋）。預期 exit 0（簽章已逐項比對相符，無不匹配風險點）。手動驗證命令見 <code>build_and_run.sh</code> 第一段。</p><hr><h2 id="實驗二-執行層驗證-best-effort" tabindex="-1">實驗二：執行層驗證（best effort） <a class="header-anchor" href="#實驗二-執行層驗證-best-effort" aria-label="Permalink to &quot;實驗二：執行層驗證（best effort）&quot;">​</a></h2><p><strong>方法</strong>：<code>main.swift</code> 呼叫 <code>setConfiguration</code> → <code>saveToPreferences()</code>，捕捉 <code>NEURLFilterManager.Error</code> rawValue。</p><p><strong>Error enum rawValue 對照表</strong>（自 swiftinterface line 187-210，<code>Int</code> enum 由 0 起）：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>0=configurationUnchanged  1=configurationInvalid  2=configurationDisabled</span></span>
<span class="line"><span>3=configurationStale      4=configurationCannotBeRemoved</span></span>
<span class="line"><span>5=configurationPermissionDenied   6=configurationInternalError</span></span>
<span class="line"><span>7=configurationNotLoaded  8=serverSetupIncomplete  9=internalError</span></span>
<span class="line"><span>10=extensionCancelled 11=extensionNotFound 12=extensionFailedToLoad 13=unknown</span></span></code></pre></div><p><strong>exit code / 輸出</strong>：<strong>未跑</strong>（hook 擋編譯器，無 binary 可執行）。</p><p><strong>預期（依 edge case + 實驗三證據推斷，標 <code>[claim:asserted]</code>）</strong>：unsigned/無 entitlement 的 swift CLI 呼叫 <code>saveToPreferences()</code> → 預期 throw <code>configurationPermissionDenied</code>(rawValue=5) 或落在 <code>serverSetupIncomplete</code>(8)/<code>configurationInvalid</code>(1)。實驗三已獨立證實系統會對缺 entitlement 的 provider 拒絕，故執行層失敗屬<strong>預期關鍵發現</strong>，非 spike 失敗。手動跑動命令見 <code>build_and_run.sh</code> 第二段。</p><hr><h2 id="實驗三-entitlement-需求確認-完整取證" tabindex="-1">實驗三：entitlement 需求確認（完整取證） <a class="header-anchor" href="#實驗三-entitlement-需求確認-完整取證" aria-label="Permalink to &quot;實驗三：entitlement 需求確認（完整取證）&quot;">​</a></h2><p><strong>方法</strong>：<code>strings</code> 掃系統 NE daemon（<code>/usr/libexec/{neagent,nehelper,nesessionmanager}</code>）+ SDK apinotes。</p><p><strong>命令</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>strings -a /usr/libexec/neagent          | grep -iE &quot;url.filter|url-filter&quot;</span></span>
<span class="line"><span>strings -a /usr/libexec/nesessionmanager | grep -iE &quot;networkextension|url-filter&quot;</span></span></code></pre></div><p><strong>輸出（原樣，前段）</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># neagent:</span></span>
<span class="line"><span>%@: %s - URL Filter Provider has %s entitlement</span></span>
<span class="line"><span>%@: %s - URL Filter Provider is missing the required NetworkExtension entitlement</span></span>
<span class="line"><span>url-filter-provider</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># nesessionmanager:</span></span>
<span class="line"><span>com.apple.developer.networking.networkextension</span></span>
<span class="line"><span>com.apple.private.restrict-post.nesessionmanager.url-filter-fail-closed</span></span>
<span class="line"><span>url-filter</span></span>
<span class="line"><span>url-filter-provider</span></span></code></pre></div><p><strong>證據解讀</strong>：</p><ol><li><strong><code>url-filter-provider</code></strong> = NE provider-type value，屬 <code>com.apple.developer.networking.networkextension</code> entitlement 陣列（與既有 <code>content-filter-provider</code> / <code>packet-tunnel-provider</code> 同 pattern）。→ 印證設計 §1.2 所列 <code>com.apple.developer.networking.networkextension.url-filter-provider</code>（形式上是該 NE entitlement 的 provider-type value）。</li><li><strong>&quot;URL Filter Provider is missing the required NetworkExtension entitlement&quot;</strong> = 系統對缺 entitlement 的 provider 有明確拒絕路徑 → <strong>確認 entitlement 為載入前提</strong>。</li><li><strong><code>com.apple.private.restrict-post.nesessionmanager.url-filter-fail-closed</code></strong> = <strong>fail-closed 語義由 private restricted entitlement 守門</strong>（<code>com.apple.private.*</code> 通常僅 Apple/系統可持有）。→ <strong>重大發現</strong>：設計 §3 的 <code>shouldFailClosed=true</code> 進階選項可能<strong>受限於 private entitlement，第三方 dev-signed app 無法自行開啟 fail-closed</strong>。<code>shouldFailClosed=false</code>（fail-open，設計預設）不受此限。</li><li>SDK apinotes 僅含 API 映射（<code>NEURLFilter.verdictForURL:</code>），無 entitlement key（正常 — entitlement 由 provisioning/系統守門，非 SDK header）。</li></ol><p><code>nehelper</code> 未含 url-filter entitlement key（守門在 <code>neagent</code>/<code>nesessionmanager</code>）。</p><hr><h2 id="最終-verdict" tabindex="-1">最終 Verdict <a class="header-anchor" href="#最終-verdict" aria-label="Permalink to &quot;最終 Verdict&quot;">​</a></h2><p><strong>模式 C = 「需完整 app 容器再驗（conditionally viable, requires dev-signed container）」</strong></p><p>證據鏈：</p><ul><li><strong>編譯層 [verified]</strong>：<code>setConfiguration</code> 接受 <code>URL(string:&quot;http://127.0.0.1:8080&quot;)</code> 型別合法 → local/stub 端點<strong>在 API/編譯層不被拒絕</strong>。stub-PIR 配置<strong>在型別層可表達</strong>。</li><li><strong>必填 PIR [verified from SDK]</strong>：<code>pirServerURL</code> 非 optional、無 PIR-free 過載 → 模式 C 必須指向<strong>某個</strong> URL（可為 local stub），無法「不配 PIR」。</li><li><strong>entitlement 守門 [verified from daemon strings]</strong>：URL Filter provider 載入<strong>強制需要</strong> <code>com.apple.developer.networking.networkextension</code>(含 url-filter-provider value) entitlement；unsigned swift CLI 無法通過 → <strong>runtime 可行性無法用裸 CLI 驗證</strong>，必須用 dev-signed app + extension 容器（正如 edge case 預測）。</li><li><strong>fail-closed 受限 [verified from daemon strings]</strong>：<code>url-filter-fail-closed</code> 是 <code>com.apple.private.restrict-post.*</code> → 第三方預設只能 fail-open；設計把 fail-open 設為預設<strong>正確且可能是唯一合法選項</strong>。</li></ul><p><strong>對設計的行動建議</strong>：</p><ul><li>模式 C 的「stub-PIR 是否被 runtime 接受、是否 fail-closed 全擋」<strong>必須進 Phase 0 的完整 dev-signed app 容器驗證</strong>（本 spike 的裸 CLI 路徑不足以判定 runtime）。設計 §6 open question 2 維持未決，但<strong>已排除「編譯層/型別層不支援」這一否決可能</strong>。</li><li>fail-closed 進階選項可能<strong>不可實作</strong>（private entitlement）→ 建議設計移除或降級為「僅說明系統預設，不提供使用者開關」，並更新 §3 退化行為表。</li></ul><hr><h2 id="done-when-對帳" tabindex="-1">Done-when 對帳 <a class="header-anchor" href="#done-when-對帳" aria-label="Permalink to &quot;Done-when 對帳&quot;">​</a></h2><table tabindex="0"><thead><tr><th>#</th><th>條件</th><th>狀態</th></tr></thead><tbody><tr><td>1</td><td>實驗一 .swift + 編譯命令 exit 0（或完整錯誤）</td><td>部分 — 原始檔+命令備妥；編譯未跑（hook 擋 swiftc）；API 形狀改由 swiftinterface 直讀確認相符</td></tr><tr><td>2</td><td>實驗二執行輸出完整記錄</td><td>部分 — 未跑（無 binary）；Error 對照表 + 預期 rawValue 記錄；手動命令備妥</td></tr><tr><td>3</td><td>報告寫入指定路徑 + verdict 三選一 + 證據</td><td>達成 — verdict = 需完整 app 容器再驗</td></tr></tbody></table><p><strong>未達標項的阻擋原因</strong>：hook 白名單擋所有 Swift 編譯器前端，agent 不硬闖/不改治理層。手動解除：在互動 shell 跑 <code>build_and_run.sh</code>（或將 <code>swiftc</code> 加入 hook Layer 2 白名單後重委派）。</p>`,52)])])}const h=t(a,[["render",i]]);export{u as __pageData,h as default};
