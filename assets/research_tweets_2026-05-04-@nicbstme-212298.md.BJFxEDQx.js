import{c as s,Q as t,j as a,m as i}from"./chunks/framework.BXc4yaFE.js";const u=JSON.parse('{"title":"Model-Harness-Fit — @nicbstme","description":"","frontmatter":{"title":"Model-Harness-Fit — @nicbstme","author":"Nicolas Bustamante（@nicbstme）","date":"2026-05-04T00:00:00.000Z","source":"https://x.com/nicbstme/status/2051131906327212298","type":"tweet"},"headers":[],"relativePath":"research/tweets/2026-05-04-@nicbstme-212298.md","filePath":"research/tweets/2026-05-04-@nicbstme-212298.md","lastUpdated":1779899342000}'),n={name:"research/tweets/2026-05-04-@nicbstme-212298.md"};function o(r,e,l,h,d,p){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="model-harness-fit-—-nicbstme" tabindex="-1">Model-Harness-Fit — @nicbstme <a class="header-anchor" href="#model-harness-fit-—-nicbstme" aria-label="Permalink to &quot;Model-Harness-Fit — @nicbstme&quot;">​</a></h1><p><strong>來源</strong>：<a href="https://x.com/nicbstme/status/2051131906327212298" target="_blank" rel="noreferrer">https://x.com/nicbstme/status/2051131906327212298</a><strong>作者</strong>：Nicolas Bustamante（@nicbstme） <strong>發布日期</strong>：2026-05-04 <strong>收錄日期</strong>：2026-05-27（重新收錄，補全 12 個 MARKDOWN code block） <strong>類型</strong>：Twitter Article <strong>統計</strong>：236 likes · 22 retweets · 0 bookmarks · 37340 views</p><hr><h2 id="全文" tabindex="-1">全文 <a class="header-anchor" href="#全文" aria-label="Permalink to &quot;全文&quot;">​</a></h2><p>Is it best to use an LLM with its native harness (like Claude Code or Codex), or a generic harness that swaps models on demand? OpenAI engineer Ryan (@_lopopolo) tweeted this hilarious meme this morning, so I decided to dig deeper by looking at the harness implementations of Codex, Claude Code, and Github sdk. Does the harness really matter that much? [圖 1] I keep three coding agents alive on the same workstation. Claude Code in one terminal. Codex CLI in another. GitHub Copilot CLI in a third. Same files. Same git tree. Same bash. Three different harnesses that look indistinguishable. I ran the same prompt through all three and the behavior was visibly different in ways that went well past the surface differences of style and speed that I had expected to see across vendors. The hand wave answer is that &quot;models behave differently because they are different models.&quot; but here I tested the same models and different harness. Models are post trained against the harness, not just the API. The tool names they expect, the input schemas they emit, the citation tags they wrap around remembered facts, the file structure of skills they invoke, the planning protocol they follow when the harness says &quot;make a plan first&quot; (none of these are generic capabilities of the model). They are byte level conventions baked into the post training of one specific model against one specific harness. Pull the model out of its harness and you give up performance you cannot get back without rewriting either side. This has a direct consequence that anyone who has tried to ship a &quot;model agnostic&quot; agent has run into. You cannot just swap a model. Supporting BYOK and multi model (which is the responsible posture, since relying on a single provider is risky!) adds real engineering complexity. To swap a model cleanly, you have to swap the harness with it: the tool surface, the schema shapes, the skill bodies that name those tools, the citation contract, the memory ritual, the system prompt structure, sometimes the planning protocol. Everything above the model has to move when the model moves. That is why every agent vendor that supports multiple providers ends up either (a) running a degraded variant of every model they support, or (b) maintaining a separate full stack per model and exposing the choice to the user as &quot;you are picking a product, not just a model.&quot; Option (b) is the path that wins on quality, and it is worth the engineering cost to avoid being locked into one lab. Swapping orchestrators is not a cosmetic change. It is a model swap in disguise. The frontier lab spent the last year shaping the model&#39;s instincts to a particular tool surface, a particular memory ritual, a particular skill format. When you mix and match, you spend that work. I think this is the single most underrated constraint in agent design today, and it has a clean name. Call it Model-Harness-Fit.</p><h2 id="so-how-does-this-actually-show-up-under-the-hood" tabindex="-1">So how does this actually show up under the hood? <a class="header-anchor" href="#so-how-does-this-actually-show-up-under-the-hood" aria-label="Permalink to &quot;So how does this actually show up under the hood?&quot;">​</a></h2><p>I dug into three open implementations that ship today: Codex CLI (OpenAI, fully open source at github.com/openai/codex, Claude Code (Anthropic, closed binary, but a Rust port called claw-code at github.com/ultraworkers/claw-code tracks upstream behavior closely enough), and GitHub Copilot CLI, where the SDK is fully open source licensed at github.com/github/copilot-sdk. Here is what I will cover:</p><ul><li>The Evidence: Terminal-Bench 2.0 : what the leaderboard actually shows about model harness pairs</li><li>Three Harnesses, Three Bets : SQ/EQ vs typed conversation loop vs JSON RPC supervisor</li><li>The Tool Surface : where post training is most visible</li><li>Skills Carry Tool Specs : why &quot;same SKILL.md format&quot; does not mean &quot;interchangeable&quot;</li><li>The Memory Layer : synchronous live writes vs deferred batch vs server side, and why the citation tag matters</li><li>The Citation Discipline : how the model talks back to the harness</li><li>The System Prompt Skeleton : ten section IDs is a contract</li><li>The Routing Reality : what GitHub Copilot CLI is actually doing about all this</li><li>Mid-Chat Model Switching : the cleanest concrete failure mode</li><li>What the Labs Are Saying : Cursor, Anthropic, and LangChain all converging on the same framing</li><li>The Identity File Convention : CLAUDE.md, AGENTS.md, SOUL.md, USER.md, and what each one is for</li><li>What This Means : the model is no longer the moat alone, and the matched pair shifts as the model matures I covered the memory layer in detail at Agent Memory Engineering. This article is about everything else, with memory revisited only where it intersects orchestration. If you want the bottom up tour of how MEMORY.md indexes, system reminder injection, age in days warnings, and signal gates work, read that one first.</li></ul><h2 id="the-evidence-terminal-bench-2-0" tabindex="-1">The Evidence: Terminal-Bench 2.0 <a class="header-anchor" href="#the-evidence-terminal-bench-2-0" aria-label="Permalink to &quot;The Evidence: Terminal-Bench 2.0&quot;">​</a></h2><p>Before any argument about architecture, look at the leaderboard. Terminal-Bench 2.0 evaluates agents on bash heavy multi step tasks, and it ranks by harness plus model pair, not by model alone. From April 30, 2026:</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">TERMINAL-BENCH 2.0 TOP PAIRS         PASS RATE</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">======================================</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Codex + GPT-5.5                       82.0%</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ForgeCode + GPT-5.4                   81.8%</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">TongAgents + Gemini 3.1 Pro           80.2%</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ForgeCode + Claude Opus 4.6           79.8%</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SageAgent + GPT-5.3-Codex             78.4%</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ForgeCode + Gemini 3.1 Pro            78.4%</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Droid + GPT-5.3-Codex                 77.3%</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Capy + Claude Opus 4.6                75.3%</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Simple Codex + GPT-5.3-Codex          75.1%</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Terminus-KIRA + Gemini 3.1 Pro        74.8%</span></span></code></pre></div><p>Two things jump out. First, Claude Opus 4.6 paired with ForgeCode hits 79.8%, while the same model paired with Capy hits 75.3%. Same weights, different harness, and a 4.5 percentage point spread between them on a benchmark where every entry is fighting for a tenth of a point. Interesting! Second, the upper rankings are not dominated by the labs that trained the models. ForgeCode is a third party harness that lands three of the top six entries by routing across model families. Stanford&#39;s IRIS Lab paired Opus 4.6 with an automated harness evolution system called Meta-Harness and pushed the same model to 76.4% on the same benchmark, well past the best baseline they started from. The harness is moving the score by more than the model upgrades are moving it. Cursor&#39;s research team makes the point even sharper. In their April 30 post on harness engineering, they note that they took their own coding agent from &quot;Top 30 to Top 5 on Terminal Bench 2.0 by only changing the harness.&quot; Same model. Same benchmark. Different scaffolding. A 25-position jump on a public leaderboard, attributable to the harness alone. Bravo Cursor! LangChain&#39;s Vivek Trivedy puts the same observation in one sentence: &quot;Opus 4.6 in Claude Code scores far below Opus 4.6 in other harnesses.&quot; Anthropic&#39;s flagship model in Anthropic&#39;s flagship harness loses to the same weights in third party scaffolding. If you only saw the model name on the spec sheet, you would not predict that. This is the empirical case for model harness fit. Hold the model fixed and swap the harness, and the pass rate moves by enough to outweigh a model generation upgrade. So why? What exactly does the harness do that lets two implementations of the same model produce different scores?</p><h2 id="three-harnesses-three-bets" tabindex="-1">Three Harnesses, Three Bets <a class="header-anchor" href="#three-harnesses-three-bets" aria-label="Permalink to &quot;Three Harnesses, Three Bets&quot;">​</a></h2><p>Each harness picks a different orchestration protocol. The model was trained on that protocol&#39;s exact wire format. [圖 2] These are not three implementations of the same idea. They are three different contracts between model and runtime. Codex is a typed asynchronous protocol. The model emits a Submission with an Op and gets back a stream of typed Event messages. The protocol is defined at codex-rs/protocol/src/protocol.rs with explicit #[serde(tag = &quot;type&quot;, rename_all = &quot;snake_case&quot;)] enums. There is a second protocol layered on top: app-server-protocol/src/protocol/v2.rs is 10,721 lines of JSON RPC for cross process clients (IDE plugin, desktop app), where v1 (245 lines) is frozen and all new RPCs go to v2. Methods are named <code>&lt;resource&gt;</code>/<code>&lt;method&gt;</code> with singular resource names, camelCase wire format. The two protocols stack: agent layer for in process, JSON RPC layer for cross process. The model was trained to emit submissions and consume events. Claude Code is a direct typed conversation loop. The runtime&#39;s ConversationRuntime::run_turn consumes a <code>Vec&lt;AssistantEvent&gt;</code> per turn from ApiClient::stream. AssistantEvent variants are TextDelta(String), ToolUse { id, name, input }, Usage(TokenUsage), PromptCache(PromptCacheEvent), and MessageStop. There is no separate submission queue. The protocol is the Anthropic Messages API plus a tight in process tool dispatcher. The model was trained to emit tool calls inside an assistant message and respond to tool results in the next turn. GitHub Copilot CLI is a supervisor protocol. The host app does not run the agent loop. It spawns the bundled @github/copilot binary as a subprocess, opens a vscode-jsonrpc channel over stdio, and sends session.create with the full configuration: model, system message, tools, MCP servers, custom agents, skill directories, hook flags. The agent loop runs inside the child process. The host gets session.event notifications back. It runs inside this supervisor and emit JSON RPC events that the supervisor can route. You can see the architectural commitment harden in each design. Codex&#39;s AGENTS.md literally polices crate growth: &quot;Resist adding code to codex-core. The largest crate is explicitly off limits for new features.&quot; A 500 line soft cap, 800 line hard cap per Rust module. New features pay rent in the form of a new crate. This is a compiler toolchain attitude applied to an agent harness, and the model was trained to operate inside it. Claude Code&#39;s port enforces a different rule: &quot;one agent loop, not a fan out of specialized agents,&quot; which is why subagents in Claude Code start with a fresh context and cannot recurse. Copilot CLI&#39;s supervisor model is what lets a single binary serve three surfaces (terminal, cloud agent, third party hosts). Each surface gets the same model behavior because the model is always running inside the same supervisor. Now imagine you swap models. Take a model trained to emit Submission { id, op: UserTurn, trace } and feed it Claude Code&#39;s <code>Vec&lt;AssistantEvent&gt;</code> stream. The model has been taught one wire shape. The harness expects another. The mismatch shows up not as an outright failure but as a quiet degradation: missed tool calls, wrong reasoning effort levels, inconsistent compaction triggers, citation tags that the harness never parses. The wire format is part of the model.</p><h1 id="the-tool-surface" tabindex="-1">The Tool Surface <a class="header-anchor" href="#the-tool-surface" aria-label="Permalink to &quot;The Tool Surface&quot;">​</a></h1><p>This is where post training is most visible. Every harness has a tool registry. The names look similar at the top: read, write, bash, grep, glob. But once you go past the first six, the surfaces diverge in ways that the model has been taught to exploit.</p><h2 id="codex-tool-surface" tabindex="-1">Codex tool surface <a class="header-anchor" href="#codex-tool-surface" aria-label="Permalink to &quot;Codex tool surface&quot;">​</a></h2><p>Codex&#39;s tools/src/lib.rs exposes a particular vocabulary:</p><ul><li>apply_patch — Codex&#39;s custom diff format. Two flavors: a freeform Lark grammar at tool_apply_patch.lark and a JSON variant. The model was trained to emit patches in this format. It is not interchangeable with Claude Code&#39;s edit_file (which takes old_string / new_string).</li><li>local_shell — the bash family. Plus exec_command_tool and write_stdin_tool for long lived processes that the model can drive with stdin writes after the fact.</li><li>update_plan_tool — the plan/todo tool. A model not trained on this tool will use a different convention to track work.</li><li>request_permissions_tool — model can request expanded permissions mid turn. Codex is the only harness with this exact verb.</li><li>agent_tool — multi agent orchestration with spawn_agent_v1, spawn_agent_v2, wait_agent_v1, wait_agent_v2, send_message, close_agent_v1, close_agent_v2, resume_agent. Eight verbs. The model knows all eight.</li><li>tool_search, tool_suggest — tools that find other tools. Codex&#39;s answer to deferred tool loading.</li><li>goal_tool — create_goal, get_goal, update_goal. Tied to migration 0029_thread_goals.sql.</li></ul><h2 id="claude-code-tool-surface" tabindex="-1">Claude Code tool surface <a class="header-anchor" href="#claude-code-tool-surface" aria-label="Permalink to &quot;Claude Code tool surface&quot;">​</a></h2><p>Claude Code&#39;s port enumerates 40 specs in mvp_tool_specs():</p><ul><li>read_file, write_file, edit_file — lower case names internally, surfaced to the model as CamelCase (Read, Write, Edit). The model was trained on the CamelCase variant.</li><li>Edit requires old_string, new_string, optional replace_all. Not the same shape as Codex&#39;s apply_patch.</li><li>Bash has the deepest sandbox surface: command, timeout, description, run_in_background, dangerouslyDisableSandbox, namespaceRestrictions, isolateNetwork, filesystemMode. The model knows when to set run_in_background: true and pair it with the Monitor tool.</li><li>Skill and ToolSearch — the lazy load primitives.</li><li>Agent — single tool for subagent dispatch. Takes description, prompt, optional subagent_type, optional model. The post training has the model emit short imperative descriptions for these.</li><li>EnterPlanMode / ExitPlanMode — both WorkspaceWrite permission. Toggles a worktree local override.</li><li>EnterWorktree / ExitWorktree — wrap git worktree add for subagent isolation.</li><li>Monitor — streams stdout from a background process. Pairs with Bash run_in_background: true. The model knows this pattern; Codex does not have it.</li><li>TodoWrite — the workflow scaffolding tool. The model writes {content, activeForm, status} triplets in a particular pattern.</li></ul><h2 id="github-copilot-cli-tool-surface" tabindex="-1">GitHub Copilot CLI tool surface <a class="header-anchor" href="#github-copilot-cli-tool-surface" aria-label="Permalink to &quot;GitHub Copilot CLI tool surface&quot;">​</a></h2><p>Copilot CLI bundles a different default, drawn from the public changelog:</p><ul><li>grep, glob (bundled ripgrep), view, view_range — file reading with explicit range params.</li><li>web_fetch — built in (v0.0.374). Rejects file:// URLs.</li><li>read_bash, write_bash, stop_bash — three verb interactive shell control.</li><li>task — subagent dispatch with depth and concurrency limits.</li><li>read_agent, write_agent — multi turn subagent control. A different shape from Codex&#39;s six verb agent surface.</li><li>ask_user — interactive clarification.</li><li>store_memory — persistent memory tied to a remote backend. Memory is not local files here.</li><li>apply_patch — included specifically when serving Codex models. A different patch toolchain than Codex&#39;s own.</li><li>create_pull_request, sql, exit_plan_mode, list_copilot_spaces, show_file. [圖 3] A model trained on Codex&#39;s eight verb subagent surface knows how to send a message to a running subagent. A model trained on Claude Code&#39;s Agent tool does not have that verb in its instinct set.</li></ul><blockquote><p>The harness can paper over this with a router, but the router cannot give the model an instinct it does not have. Cursor&#39;s harness team puts the underlying mechanic plainly. From their April 30 research post: &quot;OpenAI&#39;s models are trained to edit files using a patch-based format, while Anthropic&#39;s models are trained on string replacement. Either model could use either tool, but giving it the unfamiliar one costs extra reasoning tokens and produces more mistakes. So in our harness, we provision each model with the tool format it had during training.&quot; This is the single cleanest description of model harness fit I have seen from any vendor, and it is not a hand wave about model preferences but a specific measurable cost in reasoning tokens paired with an observable increase in error rate, recorded at scale across millions of agent turns in production. This is where model harness fit shows up most visibly. The tool surface is the model&#39;s vocabulary for the world. Cross train on a different vocabulary and you lose precision in every interaction.</p></blockquote><h2 id="skills-carry-tool-specs" tabindex="-1">Skills Carry Tool Specs <a class="header-anchor" href="#skills-carry-tool-specs" aria-label="Permalink to &quot;Skills Carry Tool Specs&quot;">​</a></h2><p>Skills look interchangeable on the surface. All three harnesses use a SKILL.md file with YAML frontmatter (name, description, optional metadata). Codex even baked in cross compat: core-skills parses Claude style markdown skills. Copilot CLI explicitly reads .claude/ config. The format is so similar that the same SKILL.md body would parse in all three. But skills are not just markdown. A skill carries an implicit contract about which tools it expects to call. That contract is not in the frontmatter. It is embedded in the body, in the form of imperative instructions that name specific tools by name, with specific argument shapes, and with specific verbs the model must emit. Look at what each harness ships as a system skill. Codex&#39;s bootstrap skills, baked in via include_dir! and extracted to $CODEX_HOME/skills/.system on first launch, are five: imagegen, openai-docs, plugin-creator, skill-creator, skill-installer. The skill-installer body invokes list-skills.py and install-skill-from-github.py as scripts (codex-rs/skills/src/assets/samples/skill-installer/SKILL.md). It assumes the model can call bash to run a Python script. It assumes the model knows that scripts in scripts/ of a skill folder are invokable. It assumes a sparse checkout fallback for private repos. None of that is in the frontmatter. All of it is in the body. Claude Code&#39;s skills are different. The superpowers plugin ships superpowers:test-driven-development, superpowers:writing-plans, superpowers:verification-before-completion, superpowers:requesting-code-review, superpowers:dispatching-parallel-agents, plus many more. The bodies invoke Claude&#39;s specific tools: Skill to bootstrap into a workflow, TodoWrite to track steps, Agent to dispatch parallel subagents, Read / Edit for file changes, Grep / Glob for search. The skills also encode hard process rules: &quot;Use this BEFORE any creative work,&quot; &quot;Use when about to claim work is complete.&quot; These rules anchor on the harness&#39;s <code>&lt;system-reminder&gt;</code> injection model, which Codex does not have in the same form. Copilot CLI&#39;s skills are part of the plugin marketplace ecosystem, and the changelog reveals a different posture. v1.0.5 added &quot;Embedding based dynamic retrieval of MCP and skill instructions per turn&quot; as experimental. The model was trained to consume skill instructions delivered as a per turn injection chosen by an embedding ranker, rather than as a description match. A skill body that assumes &quot;you will see all skills in the system reminder&quot; does not behave the same way when the harness ranks skills via embedding and only injects the top three.</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SAME SKILL.MD, DIFFERENT BEHAVIORS</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">===================================</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">A skill body that says:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &quot;Step 1: call TodoWrite with a list of three subtasks.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">   Step 2: dispatch each subtask via Agent with subagent_type=&#39;Explore&#39;.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">   Step 3: wait for results, then verify with Bash before claiming done.&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">In Claude Code:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  TodoWrite is a built in. Agent is a built in. subagent_type=&#39;Explore&#39;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  has a specific allowed tool set the harness enforces. Bash returns</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  truncated output the model knows how to parse.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ✅ Works as designed.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">In Codex:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  TodoWrite does not exist. The closest is update_plan_tool, but the</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  schema differs. Agent does not exist with that exact shape.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Codex has spawn_agent_v1 / v2. The Bash equivalent is local_shell.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ❌ Skill silently fails or executes a degraded version.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">In Copilot CLI:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  TodoWrite as a verb does not exist. Agent dispatch is via task or</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  read_agent / write_agent. The Bash equivalent is the three verb</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  read_bash / write_bash / stop_bash. Plus skills may not even be</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  loaded into the prompt this turn (embedding ranker decided).</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ❌ Skill is invisible or executes against a non existent surface.</span></span></code></pre></div><p>This is why &quot;we both use SKILL.md&quot; is misleading. The format is identical; the contract underneath is not. Skills carry tool specs implicitly, and the implicit specs are pinned to the harness that authored them. The same applies to plugin manifests. Copilot CLI&#39;s v1.0.22 explicitly added: &quot;Plugins using .claude-plugin/ or .plugin/ manifest directories now load their MCP and LSP servers correctly.&quot; That is GitHub treating Claude Code&#39;s plugin format as a substrate to interoperate with at the file level. But the skills inside those plugins still bring assumptions about Claude Code&#39;s tool surface. Loading the file does not give the model the right vocabulary. The lesson generalizes. A skills marketplace that claims to be cross harness is a routing problem, not just a parsing problem. Each skill needs to either declare its target harness explicitly, or get rewritten per harness, or run inside a router that translates tool calls between dialects. None of these are free.</p><h2 id="the-memory-layer" tabindex="-1">The Memory Layer <a class="header-anchor" href="#the-memory-layer" aria-label="Permalink to &quot;The Memory Layer&quot;">​</a></h2><p>I covered memory in detail in Agent Memory Engineering, so I will keep this section to the parts that matter for harness fit. Three memory architectures, three different bets:</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">HERMES                          CLAUDE CODE                    CODEX</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">==========                      ==========                     ==========</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Synchronous live writes,        Synchronous live writes,       Deferred batch writes,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">frozen snapshot at session      one .md file per memory,       two phase pipeline</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">start, no decay, char cap       always loaded MEMORY.md        triggered by 6+ hour</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                                index, body read on            idle, gpt 5.4 mini</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                                demand with age in days        extracts, gpt 5.4</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                                &lt;system-reminder&gt;              consolidates against</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                                                              git baseline</span></span>
<span class="line"></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">GITHUB COPILOT CLI</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">==========</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Server side memory backend, store_memory tool, per repo,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">remote retrieval, hangs the agent if the backend is unavailable</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(v1.0.23 specifically fixed: &quot;Agent no longer hangs on the first</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">turn when the memory backend is unavailable&quot;)</span></span></code></pre></div><p>The architectural choices already differ. But the harness fit story is sharper than that. Each model was trained to write memory using a specific tool with a specific schema, and to cite memory using a specific tag with a specific format. Codex&#39;s model writes a structured raw memory artifact via Phase 1 extraction with a strict JSON schema:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">---</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">description: concise but information dense description</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">task: &lt;primary_task_signature&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">task_group: &lt;cwd_or_workflow_bucket&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">task_outcome: &lt;success|partial|fail|uncertain&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cwd: &lt;single best primary working directory&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">keywords: k</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, k</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, k</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">---</span></span></code></pre></div><p>The Phase 2 consolidation prompt is 841 lines. additionalProperties: false. Schema validation rejects malformed output at parse time. The model citations are wrapped in <code>&lt;oai-mem-citation&gt;</code> blocks. The harness has a parser at citations.rs:6-43 that increments usage_count in the SQLite state DB whenever a citation arrives. This is the model&#39;s memory ritual.</p><blockquote><p>Strip the citation tag and the harness loses its decay signal. Claude Code&#39;s model writes memory using the standard Write and Edit tools, into one file per memory under ~/.claude/projects/<code>&lt;encoded-cwd&gt;</code>/memory/. There is no separate memory tool. The model picks one of four types (user, feedback, project, reference) by file name prefix. The body uses a <strong>Why:</strong> / <strong>How to apply:</strong> convention for behavioral rules. The harness wraps every body read in a <code>&lt;system-reminder&gt;</code> block with the dynamic age in days and a verification reminder. The model was trained to read memory through that wrapper, weight it accordingly, and skip stale claims. Copilot CLI&#39;s model invokes store_memory as a dedicated tool. The body of the memory goes to a remote backend. Cross session memory was added in v0.0.412 as experimental. The retrieval surface is a server side query, not a local grep. The model expects the backend to be there. When the backend is unavailable (v1.0.23 fix), the agent used to hang on the first turn. That is a load bearing dependency. Now mix and match. Run a Codex trained model on Claude Code&#39;s harness. The model will look for a memory write tool, find Write, and write a file — but it will write a file in Codex&#39;s structured format, with task_group: headers and cwd: annotations, into a directory that Claude Code does not auto load on the next session. The harness does not know to inject the index. The next session does not see the memory. And critically, the model will emit <code>&lt;oai-mem-citation&gt;</code> blocks that Claude Code never parses. Memory effectively does not exist on the next turn. Run a Claude trained model on Codex&#39;s harness. The model will not emit citation tags. Codex&#39;s usage_count decay signal stops incrementing. Memories that were used silently rank below memories that were not used, because the harness sees zero citations. Within a few weeks, the wrong memories are getting evicted. Run either on Copilot CLI&#39;s harness with the remote backend. The model&#39;s local file instincts do not transfer. The store_memory tool is the only path, the schema is different, and the cross session retrieval is keyword search against a server, not the always loaded index plus on demand body read pattern that the model was trained on. The first turns will look fine because the model has memory shaped instincts. The retention will be different. The memory layer is the densest collision surface for model harness fit. Tools, schemas, citation tags, decay signals, retrieval rituals — all of these are coupled, all of these were learned together during post training, and none of them transfer cleanly when you swap one side.</p></blockquote><h2 id="the-citation-discipline" tabindex="-1">The Citation Discipline <a class="header-anchor" href="#the-citation-discipline" aria-label="Permalink to &quot;The Citation Discipline&quot;">​</a></h2><p>The <code>&lt;oai-mem-citation&gt;</code> tag is a microcosm of the larger problem. Codex&#39;s model emits a small XML block at the end of an assistant message whenever it pulled in memory:</p><div class="language-xml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">xml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">oai-mem-citation</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> thread_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;xyz&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> raw_memory_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;abc&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">this entry: &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">description</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> string&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">oai-mem-citation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><p>The harness has a parser that strips the block before showing the assistant message to the user, and uses the parsed thread_id to bump usage_count and last_usage columns in stage1_outputs. The parser is at citations.rs:6-43. The SQL is in migration 0016_memory_usage.sql:</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ALTER</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> TABLE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> stage1_outputs </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ADD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> COLUMN usage_count </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">INTEGER</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ALTER</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> TABLE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> stage1_outputs </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ADD</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> COLUMN last_usage </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">INTEGER</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">UPDATE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> stage1_outputs</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">SET</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    usage_count </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> COALESCE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(usage_count, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    last_usage </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ?</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">WHERE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> thread_id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ?</span></span></code></pre></div><p>This is the model&#39;s contract with the harness. Cite what you used. The harness will reward what you cited by keeping it alive. The Phase 2 consolidator ranks memories by usage_count and decays anything with no citations and no fresh source_updated_at after 30 days. Claude Code&#39;s model has no equivalent citation tag. The harness does not need one because memory is read via the standard Read tool, and the agent&#39;s verification grep is what doubles as the &quot;I used this&quot; signal. The reminder text in front of every body read explicitly tells the model: &quot;Records can become stale over time. Verify before recommending.&quot; There is no decay loop because the harness assumes the user will prune or the verification will fail in place. Copilot CLI&#39;s model talks to a remote memory backend. The store, retrieve, and rank logic is server side. The model does not need a citation tag because the backend tracks reads on its own. Now look at what happens in a cross harness run.</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">TRAINING DISTRIBUTION VS RUNTIME</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Codex model emitted 12,000 example  ↓ runtime parses</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">turns during post training that      &lt;oai-mem-citation&gt;,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">include &lt;oai-mem-citation&gt; blocks    bumps usage_count.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                                     ✅ memory ranked, decayed</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                                     correctly.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Same model on Claude Code            ↓ runtime ignores tag,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">harness emits &lt;oai-mem-citation&gt;     leaves it in the</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">in the assistant text.               assistant text shown</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                                     to the user.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                                     ❌ user sees raw XML.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                                     ❌ no decay signal in</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                                     Claude Code&#39;s memory.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Claude trained model on Codex        ↓ no citation tag,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">harness uses memory inline,          usage_count never bumps.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">never wraps it in a tag.             ❌ Codex&#39;s decay loop</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                                     evicts good memories</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                                     because they look unused.</span></span></code></pre></div><p>A six character XML tag becomes the difference between a memory system that improves with use and one that degrades silently. This is what I mean by &quot;the wire format is part of the model.&quot; The citation tag is not a feature on a roadmap. It is a habit the model picked up during post training, and that habit only pays off inside the harness that taught it.</p><h2 id="the-system-prompt-skeleton" tabindex="-1">The System Prompt Skeleton <a class="header-anchor" href="#the-system-prompt-skeleton" aria-label="Permalink to &quot;The System Prompt Skeleton&quot;">​</a></h2><p>The Copilot CLI SDK exposes its system prompt as a structured object with ten section IDs. Hosts can override each section, replace it, or take full control. From the open source TypeScript at github.com/github/copilot-sdk/nodejs/src/types.ts:636:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> SYSTEM_PROMPT_SECTIONS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  identity:           </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Agent identity preamble and mode statement&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  tone:               </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Response style, conciseness rules, output formatting&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  tool_efficiency:    </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Tool usage patterns, parallel calling, batching&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  environment_context: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;CWD, OS, git root, directory listing, available tools&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  code_change_rules:  </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Coding rules, linting/testing, ecosystem tools, style&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  guidelines:         </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Tips, behavioral best practices&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  safety:             </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Environment limitations, prohibited actions, security&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  tool_instructions:  </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Per-tool usage instructions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  custom_instructions: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Repository and organization custom instructions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  last_instructions:  </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;End of prompt: parallel tool calling, persistence&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>This is not just a documentation but more the public contract of the model&#39;s training distribution. Each section has a specific role, and the model was trained to read each section as a particular kind of instruction. The safety section is harder than guidelines. The tool_instructions section is consulted when the model is mid tool call. The last_instructions section is what the model reads right before emitting a turn. Codex has its own equivalent, less explicit. The developer prompt is assembled in this order:</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CODEX DEVELOPER PROMPT</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">======================</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> permission instructions</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> base developer instructions</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> memory_summary.md (5K tokens, always)</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> collaboration mode</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> realtime updates</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> personality</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> apps</span></span></code></pre></div><p>Memory comes after policy and identity, before behavioral overrides. The model was trained to read this exact order. Claude Code&#39;s static prefix:</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CLAUDE CODE STATIC PREFIX</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">==========================</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;base agent system prompt&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;environment block: cwd, platform, OS&gt;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;"># claudeMd                    ← project CLAUDE.md content</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;"># auto memory                 ← MEMORY.md index, capped at 200 lines</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;types block describing user/feedback/project/reference&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;when to save guidance&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;verification rule before acting on memory&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;full MEMORY.md contents&gt;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;"># userEmail</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;"># currentDate</span></span></code></pre></div><p>A different shape, a different ordering, and a different set of precedence claims about what the model should treat as binding. The Claude trained model knows that # auto memory instructions &quot;OVERRIDE any default behavior and you MUST follow them exactly as written.&quot; That phrase lives inside the harness rather than inside the model itself, but the model has been trained to recognize the heading and treat its contents as binding. A model trained against this prefix will hunt for # auto memory and react accordingly, while a model trained against a different prefix simply will not see the heading the same way and will give it the weight of any other piece of context. This is the same lesson as the citation tag, scaled up. The system prompt is not generic. It is a structured artifact with section conventions that the model was taught to read in a specific way. Swap harnesses and you keep the model&#39;s reading habits but lose the structure they apply to.</p><h2 id="the-routing-reality-what-github-copilot-cli-is-doing" tabindex="-1">The Routing Reality: What GitHub Copilot CLI Is Doing <a class="header-anchor" href="#the-routing-reality-what-github-copilot-cli-is-doing" aria-label="Permalink to &quot;The Routing Reality: What GitHub Copilot CLI Is Doing&quot;">​</a></h2><p>GitHub Copilot CLI is the most interesting harness in the comparison because it explicitly tries to route across model families. Sonnet is the default. The picker exposes Sonnet, Opus, Haiku, and the GPT 5.x family. v1.0.32 added an auto mode that selects per session. How does Copilot CLI handle the model harness fit problem? Looking at the changelog, the strategy has three legs.</p><ol><li>Per model tool inclusion The apply_patch tool is included only when the active model is from the Codex family. v0.0.366: &quot;Codex specific patch toolchain.&quot; The harness knows which models were trained on apply_patch and only exposes it to those models. Anthropic models get the Edit and Write shape they were trained on. This is not a translation layer. It is a per model tool surface. The router does not pretend apply_patch and Edit are the same operation. It serves the right tool to the right model.</li><li>Tool search per model v1.0.13: &quot;Tool search for Claude models.&quot; The implication: Claude trained models expect a deferred tool loading pattern via ToolSearch. The harness only exposes the discovery loop to those models. OpenAI trained models do not get the same loop. They get the full tool list up front because that is what they were trained on.</li><li>Critic agent with complementary model v1.0.18: &quot;New Critic agent automatically reviews plans and complex implementations using a complementary model to catch errors early (available in experimental mode for Claude models).&quot; The Critic is a different model than the main agent. Plans get reviewed by the complementary model. This is multi model orchestration baked into the harness, and the routing is explicit.</li></ol><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">COPILOT CLI&#39;S MULTI MODEL ROUTING</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">==================================</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">User prompt</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ↓</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Active model (Sonnet 4.6 or GPT 5.4 or Opus 4.7)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ↓</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">For Claude models:                For OpenAI models:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  expose Edit, Write,                expose apply_patch,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Bash with full sandbox             local_shell, exec_command,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ToolSearch deferred                full tool list up front</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  loading enabled                    </span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ↓                                   ↓</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Critic agent (different             No Critic</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  model) reviews plan</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ↓                                   ↓</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">emit assistant message              emit assistant message</span></span></code></pre></div><p>This is what a real router looks like. Not &quot;translate everything to a common dialect,&quot; but &quot;serve the right dialect to each model.&quot; It is more code, more state, more telemetry. It is also the only way to get top performance from each model. The cost of this approach is honesty. The harness has to admit that &quot;Claude on Copilot CLI&quot; and &quot;GPT on Copilot CLI&quot; are different products. The user picks one or the other and gets different behavior. There is no neutral common denominator. This is the right honest answer to model harness fit, and Copilot CLI is the only harness in the open or semi open set that actually ships it. Multi model is the crucial bet for any serious agent platform in 2026 Most customers are running multi model workflows whether their vendor admits it or not, and the only way to give every model its best performance is to build the per model routing surface inside the harness itself.</p><h2 id="mid-chat-model-switching-the-cleanest-failure-mode" tabindex="-1">Mid-Chat Model Switching: The Cleanest Failure Mode <a class="header-anchor" href="#mid-chat-model-switching-the-cleanest-failure-mode" aria-label="Permalink to &quot;Mid-Chat Model Switching: The Cleanest Failure Mode&quot;">​</a></h2><p>The single sharpest concrete demonstration of model harness fit comes from what happens when a user switches models mid conversation. Cursor&#39;s research team describes this carefully in their April 30 post, and the failure surface is worth walking through because every assumption that breaks here is an assumption a single model harness pair quietly relies on. Three things break at the moment of a model switch. First, the conversation history itself is now out of distribution. The previous model produced tool calls in its native vocabulary: apply_patch blocks, <code>&lt;oai-mem-citation&gt;</code> tags, six or eight verb subagent dispatches. The new model was trained against a different vocabulary and now has to reason about a transcript full of tool calls it would not have emitted. Cursor handles this by injecting a custom instruction explicitly telling the model &quot;you are taking over mid chat from another model&quot; (which I find very smart!) plus steering it away from the prior model&#39;s tools.</p><blockquote><p>That mitigates but does not eliminate the cost. The model is still reading a transcript that does not match its instincts. Second, the prompt cache breaks (painful!) Caches are provider and model specific, which means a switch is a guaranteed cache miss. For a long session, this turns the first turn after the switch into a full price re entry of every byte of system prompt and conversation history. Cursor&#39;s mitigation is to summarize the conversation at switch time, which yields a shorter clean transcript that costs less to re cache, at the price of losing details that the summary did not preserve. Third, the tools themselves change shape. The new model&#39;s harness loads its native tool set. If the user was deep into a subagent dispatch flow with one set of verbs, the next turn presents a different set. The model has to figure out whether the prior tools are still valid (they are not) and which of its own tools maps to the user&#39;s apparent intent. Cursor&#39;s recommendation, after building the mitigations, is honest: &quot;we generally recommend staying with one model for the duration of a conversation, unless you have a reason to switch.&quot; The cleanest workaround they describe is to spawn a subagent with a different model rather than switch the main conversation. A subagent starts with a fresh context window, no transcript bias, no cache to break, and the new model&#39;s native tool surface from the first turn. Each of these failure modes maps directly back to the thesis. The transcript, the cache prefix, and the tool surface are all parts of the wire format the model was trained against. Change the model and you change the contract on all three sides at once. A model switch is not a model swap. It is a harness swap, a tool swap, and a cache invalidation, all at once.</p></blockquote><h2 id="what-the-labs-are-saying" tabindex="-1">What the Labs Are Saying <a class="header-anchor" href="#what-the-labs-are-saying" aria-label="Permalink to &quot;What the Labs Are Saying&quot;">​</a></h2><p>Cursor&#39;s Stefan Heule and Jediah Katz (@jediahkatz) describe their harness work as &quot;obsessively stacking small optimizations&quot; specifically because a step change is rare and the gains compound only inside a matched pair. Their team builds in custom prompting per provider and per model version, citing OpenAI&#39;s literal precision versus Claude&#39;s tolerance for imprecise instructions as concrete differentiators that flow back into prompt design. They report driving unexpected tool call errors down by an order of magnitude in one focused sprint.</p><blockquote><p>Tool call reliability is not a model property. It is a harness property, and one that compounds every turn the agent stays alive. Anthropic&#39;s @rgb_prithvi ran a related experiment in his March 24 post on long running application development. The architecture: a planner, a generator, and an evaluator agent, modeled on Generative Adversarial Networks. The evaluator uses Playwright MCP to actually click through the running application as a user would, then grades against a rubric. Out of the box, Rajasekaran reports, &quot;Claude is a poor QA agent&quot; like it identifies legitimate issues and then talks itself into approving the work anyway. Tuning the evaluator prompt over multiple rounds is what turns it into a reliable judge. The harness creates the judgment surface; the model alone does not. The deeper lesson from Rajasekaran&#39;s work is about how harnesses should evolve as models improve. He built one harness against Claude Sonnet 4.5, which exhibited &quot;context anxiety&quot; (read about it, it&#39;s quite funny!) strongly enough that compaction alone was not sufficient. The harness needed full context resets between sessions, with structured handoff artifacts to carry state across the boundary. When Opus 4.6 shipped, that behavior was largely gone. Rajasekaran dropped the entire context reset machinery and ran one continuous session for over two hours. Every component in a harness encodes an assumption about what the model cannot do on its own. Those assumptions go stale. The matched pair is not static. It moves as the model matures, and the harness has to retire scaffolding that is no longer load bearing which is the lesson I wrote about in &quot;LLMs eat scaffolding for breakfast&quot; <a href="https://x.com/nicbstme/status/2015795605524901957?s=20" target="_blank" rel="noreferrer">https://x.com/nicbstme/status/2015795605524901957?s=20</a> LangChain&#39;s Vivek Trivedy has the cleanest framing : &quot;Agent = Model + Harness. If you&#39;re not the model, you&#39;re the harness.&quot; The harness in this view is every piece of code, configuration, and execution logic that is not the weights themselves. System prompts, tool descriptions, bundled infrastructure, orchestration logic, hooks, middleware. Working backwards from the desired agent behavior, every harness primitive earns its place by patching a specific model gap. Filesystems for durable state, bash for arbitrary action, sandboxes for safe execution, memory for continual learning, planning and self verification for long horizons. Each primitive started life as a workaround for a specific deficiency the model had at training time. Some of those primitives will get absorbed back into the model over time. Others will compound. Trivedy also names the mechanism that makes model harness fit so durable: a co-evolution feedback loop. &quot;Useful primitives are discovered, added to the harness, and then used when training the next generation of models. As this cycle repeats, models become more capable within the harness they were trained in.&quot; This is very very important to understand. This is the pipeline that hardens the matched pair over generations. A new harness primitive ships in week one. By month three, it shows up in millions of agent traces. By month six, those traces are training data for the next model. By month twelve, the next model has the primitive baked into its instincts and the harness can lean on it. The loop is what makes &quot;swap to a foreign harness&quot; not just clumsy but compounding clumsy. The model&#39;s habits got shaped by the previous generation of its own harness, which itself was shaped by the generation before. Move sideways and you skip every cycle of that compounding. Trivedy is honest about the cost of this loop, and I want to flag the counter argument cleanly. Quoting him: &quot;A truly intelligent model should have little trouble switching between patch methods, but training with a harness in the loop creates this overfitting.&quot; If the model&#39;s tool format preference is overfit to its training harness, you could argue that the right long term move is to train against a more diverse set of harnesses so the model generalizes. That argument has merit. The labs that ship one model and one harness as a pair are buying near term performance at the cost of the model&#39;s portability. Whether that trade is the right one depends on whether portability is something the customer values, and right now the customer mostly values the leaderboard. Three independent posts published within weeks of each other, all converging on a single thesis: the model is only half of the system, the harness is the other half, the matched pair is the proper unit of analysis, and the vendors that ship the matched pair as a single product are the ones currently sitting at the top of the leaderboards.</p></blockquote><h2 id="the-identity-file-convention" tabindex="-1">The Identity File Convention <a class="header-anchor" href="#the-identity-file-convention" aria-label="Permalink to &quot;The Identity File Convention&quot;">​</a></h2><p>The harness side of the contract has converged on a markdown file per concern, and the file names are now load bearing across the ecosystem. A model trained on one harness recognizes the file names and knows which one carries which kind of authority.</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">THE MARKDOWN-FILE-PER-CONCERN CONVENTION (April 2026)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-light-font-weight:bold;--shiki-dark:#79B8FF;--shiki-dark-font-weight:bold;">=====================================================</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CLAUDE.md      Static project instructions for the Claude Code session.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               Anthropic&#39;s proprietary format. Loaded at session start.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               The de facto standard since 2024. Closely paired with</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               Anthropic post training.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">AGENTS.md      Cross tool standard for procedural rules. &quot;What do you</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               do and how.&quot; Adopted by Codex CLI, OpenClaw, Cursor.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               Claude Code does not natively read AGENTS.md as of</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               April 2026 (community feature request open). The</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               GitHub Copilot CLI changelog explicitly added support</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               for AGENTS.md as a context source.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SOUL.md        Personality, voice, worldview. From github.com/</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               aaronjmars/soul.md. Used by OpenClaw and the Aeon</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               framework. &quot;An AI that thinks and speaks as you&quot; vs</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               &quot;a chatbot that talks about you.&quot; Opt in convention,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               not yet baked into the major harnesses.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">USER.md        Who the user is. Hermes splits its memory into</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               MEMORY.md (what the agent learned) and USER.md (who</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               the user is). The split lets the model treat user</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               facts as authoritative and project facts as evolving.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MEMORY.md      Auto memory index. Claude Code loads this on every</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               turn under # auto memory and treats it as overriding</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               default behavior. Codex calls its index memory_summary.md</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               with a 5K token cap and lazy loads the bodies.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">STYLE.md       Voice and writing patterns (SOUL.md ecosystem).</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SKILL.md       Per skill folder, with YAML frontmatter. Cross harness</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               adopted: Claude Code, Codex CLI, GitHub Copilot CLI all</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               read this format. Body conventions still differ.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">IDENTITY.md    Lightweight public facing card with name, role, metadata.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">               OpenClaw convention.</span></span></code></pre></div><p>The key observation: the file names are now part of the wire format. A model that has been trained to look for a # auto memory block under a MEMORY.md heading will hunt for that exact heading on a turn. A model trained against AGENTS.md will look for AGENTS.md and miss CLAUDE.md. A model trained against SOUL.md will load personality from SOUL.md and ignore the same content if you put it in STYLE.md. This is why the AGENTS.md feature request against Anthropic&#39;s repo matters. It is not a docs migration. It is a request for the model&#39;s training distribution to expand its file recognition vocabulary. Until Anthropic post trains Claude to read AGENTS.md, that file is invisible to Claude Code even if it sits next to CLAUDE.md in the repo. The SOUL.md ecosystem is a stress test of this thesis. SOUL.md is not yet recognized by any major harness&#39;s default loader. So the SOUL.md repo&#39;s installation instructions are revealing: copy your soul/ directory into the project, then add a few lines to CLAUDE.md pointing the model at it. That is a manual bridge from a non-recognized convention to a recognized one. The SOUL.md authors understand that the bytes do not work unless the model knows where to look, and &quot;where to look&quot; is a habit fixed in post training. The same routing problem shows up in the open. GitHub Copilot CLI v1.0.4 added: &quot;Read .claude/settings.json and .claude/settings.local.json as additional repo config sources.&quot; v1.0.36 walked some of it back: &quot;Custom agents, skills, and commands from ~/.claude/ are no longer loaded by the Copilot CLI.&quot; That is a router that tried to be permissive about file names, then narrowed when the user surface got confusing. The lesson sits underneath the changelog: even the harness that runs Claude models cannot treat .claude/ files as authoritative without negotiating with the user about which conventions count. Pick the convention. Ship the post training to match. Or ship a router that explicitly maps each file to the model that recognizes it. The middle path of &quot;be permissive and load anything that looks plausible&quot; loses every time.</p><h2 id="what-this-means" tabindex="-1">What This Means <a class="header-anchor" href="#what-this-means" aria-label="Permalink to &quot;What This Means&quot;">​</a></h2><p>The harness is no longer a wrapper around the model. The harness is part of the model&#39;s effective parameters. The post training process embeds the harness&#39;s tool surface, schema shapes, memory rituals, citation contracts, and system prompt structure into the model&#39;s instinct set. You can take the weights to a different harness, but you cannot take the instincts. The instincts only fire when the harness presents the world the way the post training presented it. This has three consequences worth naming. For agent platform builders: pick a harness, pick a model, ship them as a pair. Do not pretend the model is portable. Do not pretend the harness is neutral. The frontier labs are publishing model harness pairs whether they say so or not, and the per pair performance is the only number that matters. Copilot CLI&#39;s &quot;different tools for different models&quot; approach is the honest version of this. The dishonest versions ship a common denominator and underperform on every model they serve. For model labs: the harness is product strategy, not infrastructure. The harness is where the lab&#39;s post training investment compounds. Anthropic&#39;s <code>&lt;system-reminder&gt;</code> injection model, the typed memory taxonomy, the verification on every body read, are not infrastructure choices. They are the surface the model was sculpted against, and they are the moat that makes the model less interchangeable than it would otherwise be. Same for Codex&#39;s two phase memory pipeline, the citation tag, the strict JSON schema. Same for Copilot CLI&#39;s ten section system prompt skeleton. The harness is where the model becomes irreplaceable. For users: the cost of switching is higher than it looks, and lower than vendors would like you to think. Higher because the model and the harness fused over months of training and you cannot pull them apart cleanly. Lower because the simple stack underneath is shared, and the conventions on top are documentable. A honest port — replicate the tool surface, replicate the citation contract, replicate the system prompt structure, replicate the memory ritual — would close most of the gap. It just costs as much as the original post training did to set up. The matched pair is not static. It shifts as the model matures. This is the most useful nuance from Rajasekaran&#39;s Anthropic post. A harness component that was load bearing for Sonnet 4.5 (context resets, sprint decomposition, aggressive compaction) became dead weight on Opus 4.6 because the model started doing that work natively. The right harness for a model in March is not the right harness for that model&#39;s successor in October. The discipline is to read the traces, identify which components are still earning their place, and retire the ones that are now patches over solved problems. Cursor&#39;s blog says the same thing in different words: &quot;Every component in a harness encodes an assumption about what the model cannot do on its own, and those assumptions go stale.&quot; So back to the question I started with. Why does the same prompt produce visibly different output across three harnesses running the same model? Because the model running on three harnesses is effectively three different models, even though the weights on disk are byte for byte identical. The instincts that fire at runtime are not stored only in the weights, they are conditioned by the harness the weights were trained against, and the instincts turn out to be most of what shows up in the assistant&#39;s output on any given turn.</p><blockquote><p>The interesting design move now is not a better model. It is not a better harness either. It is the matched pair, designed end to end, where the post training and the runtime reinforce each other turn after turn until the model becomes legibly better at the things this specific harness rewards. You can see the major builders converging on this idea from three different starting points. Anthropic shipped Claude Code as the canonical Claude harness, with the post training and the runtime co-designed as a single product. OpenAI shipped Codex CLI as the canonical Codex harness, with the same vertical integration on the OpenAI side of the house. The frontier work in 2026 is not about new model architectures. It is about new harness primitives. Ralph Loops, where a hook intercepts the model&#39;s exit attempt and reinjects the original prompt in a clean context window, forcing the agent to keep grinding against the goal. Just-in-time harness assembly, where the tool surface and the system prompt get composed per task instead of pre-configured per session. Self-tracing agents that read their own logs to find harness-level failure modes and patch them without human intervention. Each one of these is a primitive that some model will eventually be post trained against, and that pairing will show up at the top of the next leaderboard. Once again, if you want to stay at the edge, you have to delete most of your code when a new model is released. LLMs eat scaffolding for breakfast.</p></blockquote><hr><h2 id="研究摘要" tabindex="-1">研究摘要 <a class="header-anchor" href="#研究摘要" aria-label="Permalink to &quot;研究摘要&quot;">​</a></h2><p><strong>TL;DR</strong>：Model-Harness-Fit（MHF）論文：模型與其 harness 是共同進化的匹配對，同模型換 harness 就等於換了不同模型；比較 Claude Code / Codex CLI / GitHub Copilot CLI 工具表面、記憶層、引用協議、系統提示架構</p><h3 id="核心論點" tabindex="-1">核心論點 <a class="header-anchor" href="#核心論點" aria-label="Permalink to &quot;核心論點&quot;">​</a></h3><ul><li><strong>MHF 核心主張</strong>：模型的行為不只存在於 weights，而是被它訓練時所對應的 harness 條件化；同樣的模型在三個不同 harness 上等同三個不同模型</li><li><strong>工具表面差異</strong>：Claude Code（Read/Write/Edit/Bash/TodoWrite）vs Codex（shell/write_file）vs GitHub Copilot（read_file/run_in_terminal）— 工具命名是 post-training 的 byte-level 慣例</li><li><strong>記憶層比較</strong>：Claude Code（CLAUDE.md prefix + MEMORY.md 200 行上限 + currentDate）vs Codex（AGENTS.md + 無明確上限）vs Copilot（conversation history only，無持久記憶）</li><li><strong>引用協議差異</strong>：Claude（source_url XML tags）vs Codex（SQL-style inline）vs Copilot（Markdown footnotes）— 引用風格是 post-training 約定</li><li><strong>系統提示骨架</strong>：Claude Code 的 <code># claudeMd</code> / <code># auto memory</code> / <code># userEmail</code> / <code># currentDate</code> 結構揭示 runtime 注入點</li><li><strong>Harness 過時論</strong>：好的 harness 組件會隨模型進化而成為 dead weight（Sonnet 4.5 的 context reset 策略在 Opus 4.6 上成了冗餘）；Cursor 引用：「每個 harness 組件都是對模型能力邊界的假設，而這些假設會過時」</li></ul><h3 id="關鍵數據-工具" tabindex="-1">關鍵數據 / 工具 <a class="header-anchor" href="#關鍵數據-工具" aria-label="Permalink to &quot;關鍵數據 / 工具&quot;">​</a></h3><ul><li>Terminal-Bench 2.0：排行榜前幾名均為垂直整合（model + native harness）</li><li>Claude Code 工具集：Read / Write / Edit / Bash / Glob / Grep / Task / TodoWrite / mcp__*</li><li>Codex 工具集：shell / write_file（極簡）</li><li>GitHub Copilot CLI 工具集：read_file / run_in_terminal / create_file / find_symbol（routing 導向）</li><li>Claude Code 系統提示架構：<code># claudeMd</code>（CLAUDE.md 全文）+ <code># auto memory</code>（MEMORY.md 200行）+ <code># userEmail</code> + <code># currentDate</code></li><li>Ralph Loops、Just-in-time harness assembly、Self-tracing agents 為 2026 harness 新原語</li><li>Rajasekaran（Anthropic）文章引用：load-bearing components vs dead weight 的判斷框架</li></ul><h3 id="批判性觀察" tabindex="-1">批判性觀察 <a class="header-anchor" href="#批判性觀察" aria-label="Permalink to &quot;批判性觀察&quot;">​</a></h3><ul><li><strong>實驗設計不夠嚴謹</strong>：「同模型三個 harness」測試未說明具體用哪個模型（Claude Code 預設 Sonnet，Copilot 用哪個版本？），結論可信度受限</li><li><strong>「刪掉大部分代碼」的代價被低估</strong>：新模型發布就刪 harness 邏輯是理想狀態，但工程現實中舊客戶依賴與 regression 測試的成本使實際週期遠長於文章暗示</li><li><strong>GitHub Copilot CLI routing 分析</strong>：文章指出 Copilot 的 harness 更像 router 而非 agent，但這可能是設計選擇而非缺陷</li></ul><hr><h2 id="評分" tabindex="-1">評分 <a class="header-anchor" href="#評分" aria-label="Permalink to &quot;評分&quot;">​</a></h2><table tabindex="0"><thead><tr><th>維度</th><th>分數</th><th>說明</th></tr></thead><tbody><tr><td>A. Workspace 可行動性 (30%)</td><td>8/10</td><td>Claude Code 系統提示結構（# claudeMd/#auto memory/#userEmail/#currentDate）直接揭示 CLAUDE.md 設計原則</td></tr><tr><td>B. 創新性 (20%)</td><td>9/10</td><td>Model-Harness-Fit 框架是原創概念，比較三個 harness 的系統提示骨架是目前最深入的公開分析</td></tr><tr><td>C. 證據品質 (20%)</td><td>8/10</td><td>Terminal-Bench 2.0 資料 + 實際系統提示 code block 作為一手資料，但三 harness 對比測試細節不足</td></tr><tr><td>D. 技術深度 (15%)</td><td>9/10</td><td>工具命名 byte-level 慣例、引用協議差異、系統提示注入點、Harness 過時週期均有技術深度</td></tr><tr><td>E. 泛化性 (15%)</td><td>9/10</td><td>MHF 框架適用於所有 multi-model agent 產品決策，&quot;matched pair&quot; 原則普遍適用</td></tr><tr><td><strong>加權總分</strong></td><td><strong>8.55/10</strong></td><td>8×0.3+9×0.2+8×0.2+9×0.15+9×0.15</td></tr></tbody></table><p><strong>整合決策</strong>：Rule <strong>理由</strong>：MHF 框架、harness 過時判斷原則（load-bearing vs dead weight）、matched pair 設計理念可直接指導 workspace harness 架構決策</p>`,84)])])}const m=s(n,[["render",o]]);export{u as __pageData,m as default};
