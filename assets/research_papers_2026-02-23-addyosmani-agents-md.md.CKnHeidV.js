import{c as t,Q as a,j as o,m as n}from"./chunks/framework.BXc4yaFE.js";const p=JSON.parse('{"title":"Stop Using /init for AGENTS.md","description":"","frontmatter":{"title":"Stop Using /init for AGENTS.md","published":"2026-02-23T00:00:00.000Z","source_tier":"P"},"headers":[],"relativePath":"research/papers/2026-02-23-addyosmani-agents-md.md","filePath":"research/papers/2026-02-23-addyosmani-agents-md.md","lastUpdated":1779894392000}'),i={name:"research/papers/2026-02-23-addyosmani-agents-md.md"};function s(r,e,h,l,d,c){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="stop-using-init-for-agents-md" tabindex="-1">Stop Using /init for AGENTS.md <a class="header-anchor" href="#stop-using-init-for-agents-md" aria-label="Permalink to &quot;Stop Using /init for AGENTS.md&quot;">​</a></h1><p><strong>Author:</strong> Addy Osmani<br><strong>Date:</strong> February 23, 2026<br><strong>Source:</strong> <a href="https://addyosmani.com/blog/agents-md/" target="_blank" rel="noreferrer">https://addyosmani.com/blog/agents-md/</a><br><strong>Archived:</strong> 2026-05-04</p><hr><p>TL;DR: A good mental model is to treat AGENTS.md as a living list of codebase smells you haven&#39;t fixed yet, not a permanent configuration. Auto-generated AGENTS.md files hurt agent performance and inflate costs by 20%+ because they duplicate what agents can already discover. Human-written files help only when they contain non-discoverable information - tooling gotchas, non-obvious conventions, landmines. Every other line is noise.</p><p>There&#39;s a ritual that&#39;s become almost universal among developers adopting AI coding agents. You set up a new repo, run <code>/init</code>, watch the agent scan your codebase, and get back a shiny AGENTS.md. It describes your directory structure, your tech stack, your testing conventions. You skim it, it looks reasonable, you commit it. You feel like you&#39;ve done the responsible thing. Your agent is configured.</p><p>Two papers published in early 2026 suggest you might have just made your agent slower, more expensive, and no more accurate. And the implications go further than whether to include a codebase overview.</p><p>Beyond what to put in it, there&#39;s a structural problem worth naming early: a single AGENTS.md at the root of your repo isn&#39;t sufficient for any codebase of real complexity. What you actually need is a hierarchy of AGENTS.md files - placed at the relevant directory or module level - automatically maintained so that each agent gets context scoped precisely to the code it&#39;s working in, rather than a monolithic file that conflates concerns across the entire project.</p><h2 id="what-the-research-actually-says" tabindex="-1">What the research actually says <a class="header-anchor" href="#what-the-research-actually-says" aria-label="Permalink to &quot;What the research actually says&quot;">​</a></h2><p>Lulla et al. (ICSE JAWs 2026) ran a clean paired experiment: 124 real GitHub pull requests, executed with and without an AGENTS.md file, everything else held constant. Same task, same repo snapshot, same agent. They found that presence of an AGENTS.md file reduced <strong>median wall-clock runtime by 28.64%</strong> and <strong>output token consumption by 16.58%</strong>. Statistically significant. The agent got cheaper and faster.</p><p>That sounds like a clear win. Hold that thought.</p><p>A separate study out of ETH Zurich tested four agents across SWE-bench and a custom benchmark of repos that already had developer-authored context files. Their finding cuts the other way: LLM-generated context files reduced task success by 2-3% while increasing cost by over 20%. Developer-written files improved success by about 4% - but also increased cost by up to 19%.</p><p>So which is it? Does AGENTS.md help or hurt?</p><p>Both, depending on what you put in it. And that&#39;s actually the more interesting result.</p><p>The ETH Zurich paper discovered something that reframes the whole debate: when they stripped all documentation from the repos - READMEs, docs folders, markdown files - and <em>then</em> tested with LLM-generated context files, those files suddenly improved performance by 2.7%. The auto-generated content isn&#39;t useless. It&#39;s redundant. The agent could find all of it anyway by reading the repo. Hand it the same information twice and you&#39;ve just added noise.</p><p>The Lulla paper, by contrast, used human-authored AGENTS.md files from repos where developers had actually been maintaining them. Real project-specific knowledge. Non-obvious tooling requirements. Actual gotchas. That&#39;s the context that saves the agent time because it doesn&#39;t have to infer what it can&#39;t discover.</p><p>The question isn&#39;t whether to have an AGENTS.md. The question is what earns a line in it.</p><h2 id="the-pink-elephant-problem" tabindex="-1">The pink elephant problem <a class="header-anchor" href="#the-pink-elephant-problem" aria-label="Permalink to &quot;The pink elephant problem&quot;">​</a></h2><p>There&#39;s a subtler cost to AGENTS.md files that doesn&#39;t show up in efficiency metrics: the anchoring effect.</p><p>If your AGENTS.md mentions that you use tRPC on the backend - even just as a passing note in an architecture overview - the model now has tRPC in context for every single prompt. If tRPC is only used in a handful of legacy endpoints and everything new runs on something else, you&#39;ve just biased your agent toward the wrong pattern for the current codebase. You said it, it&#39;s there, and LLMs don&#39;t distinguish between &quot;this is what we used to do&quot; and &quot;this is what you should do.&quot;</p><p>This is the same reason prompt engineering wisdom has always said to be careful about what you put in your system prompt. Everything you add competes with the actual task. Research on LLM context more broadly shows a consistent pattern - more context often degrades performance, not just through cost but through diluted attention. Liu et al.&#39;s &quot;Lost in the Middle&quot; result (2024) showed LLMs struggle with information placed in the middle of long contexts. Levy et al. showed that longer context degrades task performance even when the content is perfectly relevant. Every line in AGENTS.md is a line competing with the thing you actually asked the agent to do.</p><h2 id="the-case-against-init-specifically" tabindex="-1">The case against /init specifically <a class="header-anchor" href="#the-case-against-init-specifically" aria-label="Permalink to &quot;The case against /init specifically&quot;">​</a></h2><p>Here&#39;s the core problem with auto-generated context files. What does <code>/init</code> produce? Codebase overviews. Directory structure. Tech stack description. Module explanations. The ETH Zurich paper found that 100% of Sonnet 4.5&#39;s auto-generated context files contained codebase overviews. 99% of GPT-5.2&#39;s did the same.</p><p>These are precisely the things an agent can discover on its own by listing directories and reading your existing READMEs - which it does anyway, regardless of whether AGENTS.md exists. So you&#39;ve added a file that the agent reads, then goes and confirms by reading your actual code, and now has to reconcile two sources of truth. More reasoning tokens. More steps. Same outcome, except slower and more expensive.</p><h2 id="what-actually-earns-a-line" tabindex="-1">What actually earns a line <a class="header-anchor" href="#what-actually-earns-a-line" aria-label="Permalink to &quot;What actually earns a line&quot;">​</a></h2><p>The ETH Zurich paper showed something concrete about what works. When a developer-written context file mentioned <code>uv</code>, agents used it 1.6 times per task on average. When it wasn&#39;t mentioned, agents used it fewer than 0.01 times. The same pattern held for other repo-specific tools: 2.5 uses per task when mentioned, versus fewer than 0.05 when not.</p><p><code>uv</code> versus <code>pip</code> is a perfect example of what belongs in AGENTS.md. It&#39;s:</p><ul><li>Not discoverable from the codebase without inference</li><li>Operationally significant (it changes the commands the agent runs)</li><li>Impossible for the agent to guess correctly by convention</li></ul><p>Compare that to &quot;this project uses a monorepo structure with packages in /packages&quot; - something the agent finds in the first directory listing it runs. One of these earns a line. One of these is noise.</p><p>The practical filter is simple: can the agent discover this on its own by reading your code? If yes, delete it. Every line should represent information that isn&#39;t already in the repo.</p><p>That means your AGENTS.md should look more like:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>- Use `uv` for package management</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>- Always run tests with `–no-cache` or you&#39;ll get false positives from the fixture setup</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>- The auth module uses a custom middleware pattern; do not refactor to standard Express middleware</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>- The `legacy/` directory is deprecated but imported by three production modules - don&#39;t delete anything in it</span></span></code></pre></div><p>And almost nothing else.</p><h2 id="the-static-file-problem" tabindex="-1">The static file problem <a class="header-anchor" href="#the-static-file-problem" aria-label="Permalink to &quot;The static file problem&quot;">​</a></h2><p>Even if you write a tight, non-redundant AGENTS.md, it has a structural weakness: it&#39;s static, but tasks are dynamic.</p><p>Your file says &quot;always run the test suite before committing.&quot; The agent is doing a documentation change. It faithfully runs the full test suite. Tokens burned, minutes wasted, on an instruction that made sense for code changes but not for this one.</p><p>This isn&#39;t a hypothetical failure mode. It&#39;s baked into the architecture. A flat instruction set can&#39;t condition on what kind of task is being run. An agent working on a CSS refactor doesn&#39;t need your database migration warnings. One implementing a security fix probably should skip the performance optimization hints. A single monolithic file gets loaded the same way every time.</p><p>The ACE framework (Agentic Context Engineering, ICLR 2026) tackles this directly by treating context as an evolving playbook that adapts through a generator/reflector/curator pipeline, rather than a static file. On agent benchmarks, it outperformed static approaches by 12.3%. The architecture matters.</p><h2 id="the-better-architecture-people-aren-t-building-yet" tabindex="-1">The better architecture people aren&#39;t building yet <a class="header-anchor" href="#the-better-architecture-people-aren-t-building-yet" aria-label="Permalink to &quot;The better architecture people aren&#39;t building yet&quot;">​</a></h2><p>Several people in the AGENTS.md discourse have converged on roughly the same idea independently: the right structure isn&#39;t a monolithic file, it&#39;s a routing layer with focused context loaded on demand.</p><p>The structure would look something like:</p><p><strong>Layer 1: Protocol file</strong> (what AGENTS.md should actually be) Not a codebase overview. Not a style guide. A routing document. Available personas and when to invoke them. Available skills and what task classes they cover. Available MCP connections and what they&#39;re for. The minimum essential repo facts that the agent genuinely cannot discover - and nothing else.</p><p><strong>Layer 2: Focused persona/skill files</strong> Each loaded selectively based on task type. A UX-focused agent working on component architecture loads different context than a backend agent debugging a data pipeline. Neither loads the other&#39;s context. The total context for any given task stays bounded.</p><p><strong>Layer 3: A maintenance subagent</strong> Whose only job is keeping the protocol file accurate as the codebase evolves. Because here&#39;s the thing nobody talks about: documentation rots. The ETH Zurich study used freshly-generated context files and still found degraded performance. Real-world AGENTS.md files that haven&#39;t been touched in six months - describing a dependency you&#39;ve since replaced, a directory structure that&#39;s changed - are going to be worse. Much worse.</p><p>None of the major coding agents expose the lifecycle hooks to make this architecture easy to build. You can approximate it with sub-agents and scoped context, but there&#39;s no clean solution yet. That&#39;s a tooling gap waiting to be filled.</p><h2 id="the-automated-optimization-angle" tabindex="-1">The automated optimization angle <a class="header-anchor" href="#the-automated-optimization-angle" aria-label="Permalink to &quot;The automated optimization angle&quot;">​</a></h2><p>The most provocative finding in this space comes from Arize AI&#39;s prompt learning work. Instead of manually writing CLAUDE.md instructions, they used an automated optimization loop - run the agent on training tasks, evaluate output, generate LLM feedback on why solutions failed, use meta-prompting to refine the instructions. Repeat.</p><p>Results: +5.19% accuracy improvement on a cross-repo test split. +10.87% on an in-repo split (training on past Django issues, testing on future ones).</p><p>What the optimizer discovered is the uncomfortable implication: what helps a human understand a codebase and what helps an LLM navigate it are often different things. An instruction that seems obviously useful to you - &quot;this service uses the repository pattern&quot; - might be noise to the model. And something the model actually needs - some particular import path disambiguation, some non-obvious file naming convention - might never occur to you to write down because you&#39;ve internalized it.</p><p>The manual approach to AGENTS.md assumes that you know what the agent needs. The empirical evidence suggests you probably don&#39;t. The optimizer figures out the delta between what you think matters and what actually moves the needle.</p><p>This doesn&#39;t mean you should give up on writing context files - the automated optimization research is still early, and 5% is meaningful but not transformative. It means you should hold your intuitions about what to include loosely, and probably test them.</p><h2 id="the-right-mindset-for-agents-md" tabindex="-1">The right mindset for AGENTS.md <a class="header-anchor" href="#the-right-mindset-for-agents-md" aria-label="Permalink to &quot;The right mindset for AGENTS.md&quot;">​</a></h2><p>Think of AGENTS.md as a living document of friction you haven&#39;t fixed yet.</p><p>Every line you add is a signal about something in your codebase that&#39;s confusing enough to trip an AI agent - which means it&#39;s probably confusing enough to trip a new human contributor too. The right response to that signal isn&#39;t to grow the context file. It&#39;s to fix the actual problem.</p><p>Agent keeps putting new utilities in the wrong directory? Maybe the directory structure is confusing and should be reorganized. Agent keeps reaching for a deprecated dependency? Maybe the import structure makes the wrong one too easy to grab. Agent forgetting to run type checks? Maybe the build pipeline should catch that automatically instead of relying on a prose instruction.</p><p>The AGENTS.md becomes a diagnostic tool rather than a permanent configuration. You add a line, you investigate why the agent keeps making this mistake, you fix the underlying thing, and then you can probably delete the line.</p><p>One technique worth trying: start your AGENTS.md nearly empty, and add a single instruction: &quot;If you encounter something surprising or confusing in this project, flag it as a comment.&quot; Most of the agent&#39;s proposed additions aren&#39;t things you want to keep permanently - they&#39;re indicators of where the codebase is unclear. Fix those. Keep the file minimal.</p><h2 id="what-the-research-doesn-t-resolve" tabindex="-1">What the research doesn&#39;t resolve <a class="header-anchor" href="#what-the-research-doesn-t-resolve" aria-label="Permalink to &quot;What the research doesn&#39;t resolve&quot;">​</a></h2><p>Both papers have real limitations worth naming.</p><p>The Lulla paper doesn&#39;t measure correctness at all - only efficiency. You can&#39;t conclude from it that AGENTS.md improves output quality. The authors are clear about this: they performed a sanity check to confirm agents produced non-trivial output, but a full correctness evaluation is future work. Faster and cheaper is meaningful, but not if the code is wrong.</p><p>The ETH Zurich paper used fresh context files, which is ideal for experimental control but arguably tests a scenario most developers don&#39;t actually live in. The real-world case is context files that have been in the repo for months and are partially out of date. That should perform worse than what the paper measured.</p><p>Neither paper tests the hierarchical, dynamically-loaded approach that multiple practitioners argue is the correct architecture. The &quot;flat monolithic context file&quot; is the only thing being evaluated. Whether a properly layered system with selective context loading performs differently is still an open empirical question.</p><p>And there&#39;s the model trajectory problem: every few months, developers report needing less in their context files because the underlying models have gotten better at codebase navigation. Instructions that were essential six months ago become redundant as models improve. The AGENTS.md you write today might be pure overhead by next quarter.</p><h2 id="practical-takeaways" tabindex="-1">Practical takeaways <a class="header-anchor" href="#practical-takeaways" aria-label="Permalink to &quot;Practical takeaways&quot;">​</a></h2><ul><li>Stop running <code>/init</code>. The auto-generated output is redundant with your existing documentation and adds overhead without benefit, unless your repo has genuinely zero documentation - in which case it&#39;s marginally better than nothing.</li><li>Before adding any line to AGENTS.md, ask: can the agent find this by reading the code? If yes, don&#39;t write it.</li><li>When an agent struggles with something repeatedly, treat it as a codebase problem before treating it as a context problem. Restructure the code. Add a linter rule. Improve the test coverage. Reach for AGENTS.md only after you&#39;ve exhausted those options.</li><li>If you&#39;re running agents at scale in CI/CD or automated review pipelines, the 15-20% cost overhead from context files compounds across thousands of runs. Calculate it before assuming the tradeoff is worth it.</li><li>Consider building a maintenance agent whose job is keeping the context file accurate rather than letting it rot.</li><li>Hold your intuitions about what the agent needs loosely. What seems obviously useful to you might be noise to the model. The empirical evidence suggests the gap between what we think agents need and what actually helps them is substantial - and probably not in the direction we&#39;d expect.</li></ul><p>The instinct to onboard your coding agent like a new hire - give it the office tour, explain the org chart, walk it through the architecture - comes from a reasonable place. But coding agents aren&#39;t new hires. They can grep the entire codebase before you finish typing your prompt. What they need isn&#39;t a map. They need to know where the landmines are.</p><p>And maybe, increasingly, they don&#39;t even need that.</p><h2 id="community-reactions" tabindex="-1">Community reactions <a class="header-anchor" href="#community-reactions" aria-label="Permalink to &quot;Community reactions&quot;">​</a></h2><blockquote><p>&quot;You should delete your CLAUDE.md/AGENTS.md file. I have a study to prove it.&quot;<br> — Theo - t3.gg (@theo), February 23, 2026</p></blockquote><blockquote><p>&quot;Never run claude /init. It&#39;ll burn tokens, go out of date in days, and bloat your system prompt.&quot;<br> — Matt Pocock (@mattpocockuk), February 24, 2026</p></blockquote>',69)])])}const m=t(i,[["render",s]]);export{p as __pageData,m as default};
