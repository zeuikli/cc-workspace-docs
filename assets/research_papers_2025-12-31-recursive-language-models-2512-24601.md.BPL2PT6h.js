import{c as t,Q as a,j as n,m as s}from"./chunks/framework.BXc4yaFE.js";const g=JSON.parse('{"title":"Recursive Language Models","description":"","frontmatter":{"title":"Recursive Language Models","authors":"Alex L. Zhang, Tim Kraska, Omar Khattab","published":"2025-12-31T00:00:00.000Z","source":"https://arxiv.org/abs/2512.24601"},"headers":[],"relativePath":"research/papers/2025-12-31-recursive-language-models-2512-24601.md","filePath":"research/papers/2025-12-31-recursive-language-models-2512-24601.md","lastUpdated":1779901665000}'),o={name:"research/papers/2025-12-31-recursive-language-models-2512-24601.md"};function i(r,e,l,d,h,c){return a(),n("div",null,[...e[0]||(e[0]=[s(`<h1 id="recursive-language-models" tabindex="-1">Recursive Language Models <a class="header-anchor" href="#recursive-language-models" aria-label="Permalink to &quot;Recursive Language Models&quot;">​</a></h1><p><strong>Authors</strong>: Alex L. Zhang, Tim Kraska, Omar Khattab<br><strong>Affiliations</strong>: MIT CSAIL<br><strong>Correspondence</strong>: <a href="mailto:altzhang@mit.edu" target="_blank" rel="noreferrer">altzhang@mit.edu</a>, <a href="mailto:okhattab@mit.edu" target="_blank" rel="noreferrer">okhattab@mit.edu</a><br><strong>Published</strong>: December 31, 2025 (v1); revised May 11, 2026 (v3)<br><strong>Source</strong>: <a href="https://arxiv.org/abs/2512.24601" target="_blank" rel="noreferrer">https://arxiv.org/abs/2512.24601</a><br><strong>arXiv ID</strong>: 2512.24601<br><strong>DOI</strong>: 10.48550/arXiv.2512.24601<br><strong>License</strong>: CC BY 4.0<br><strong>Code</strong>: <a href="https://github.com/alexzhang13/rlm" target="_blank" rel="noreferrer">https://github.com/alexzhang13/rlm</a><br><strong>Categories</strong>: cs.AI, cs.CL, cs.LG</p><hr><h2 id="abstract" tabindex="-1">Abstract <a class="header-anchor" href="#abstract" aria-label="Permalink to &quot;Abstract&quot;">​</a></h2><p>We study allowing large language models (LLMs) to process arbitrarily long prompts through the lens of inference-time scaling. We propose Recursive Language Models (RLMs), a general inference paradigm that treats long prompts as part of an external environment and allows the LLM to programmatically examine, decompose, and recursively call itself over snippets of the prompt.</p><p>We find that RLMs can successfully process inputs more than an order of magnitude beyond model context window limits and, even for shorter prompts, dramatically outperform the quality of vanilla frontier LLMs and common long-context and coding scaffolds (e.g., on GPT-5 by a median across the evaluated benchmarks of 26% against compaction, 130% against CodeAct with sub-calls, and 13% against Claude Code) across four diverse long-context tasks while having comparable cost.</p><p>At a small scale, we post-train the first model around the RLM. Our model, RLM-Qwen3-8B, outperforms the underlying Qwen3-8B model by a median of 28% and even approaches the quality of vanilla GPT-5 on three long-context tasks. Code is available at <a href="https://github.com/alexzhang13/rlm" target="_blank" rel="noreferrer">https://github.com/alexzhang13/rlm</a>.</p><hr><h2 id="_1-introduction" tabindex="-1">1 Introduction <a class="header-anchor" href="#_1-introduction" aria-label="Permalink to &quot;1 Introduction&quot;">​</a></h2><p>Frontier reasoning models have limited context windows and, even within their limits, tend to exhibit context rot (Hong et al., 2025), a phenomenon where quality degrades steeply as prompts get longer. Though we expect context lengths to steadily rise through improvements to training, architecture, and infrastructure, we are interested in whether it is possible to scale the context size of general-purpose LLMs by orders of magnitude. This is increasingly urgent as LLMs begin to be widely adopted for long-horizon tasks, in which they must routinely process tens if not hundreds of millions of tokens.</p><p>We study this question through the lens of scaling inference-time compute. We are inspired by the way that reasoning models, another inference strategy, have become the fundamental interface to LLMs, resulting not only in empirical gains but also additional theoretical expressive power (Merrill and Sabharwal, 2024) compared to vanilla Transformers. Though most inference-time methods for dealing with long context are task-specific (Wu et al., 2021; Chang et al., 2024), the most popular general approach is context condensation or compaction (Khattab et al., 2021; Smith, 2025; OpenAI, 2025a; Wu et al., 2025), where context from user requests or agent trajectories is repeatedly summarized once it exceeds a length threshold. Unfortunately, compaction is rarely expressive enough for tasks that require dense access throughout the prompt. It presumes that some details that appear early in the prompt can safely be forgotten to make room for new content.</p><p>We introduce Recursive Language Models (RLMs), a general-purpose inference paradigm for dramatically scaling the effective input and output lengths of LLMs. The key insight is that arbitrarily long user prompts should not be fed into the neural network (e.g., Transformer) directly but should instead be treated as part of the environment that the LLM is tasked to symbolically and recursively interact with. This system serves as an abstracted &quot;language model&quot; without context limitations.</p><p>An RLM exposes the same external interface as an LLM or a reasoning model: it accepts a string prompt of arbitrary structure and produces a string response. Given a prompt P, the RLM initializes a Read-Eval-Print Loop (REPL) programming environment in which P is set as the value of a variable. It then offers the LLM general context about the REPL environment (e.g., the length of the string P), and permits it to write code that peeks into and decomposes P, and to iteratively observe any side effects from execution. Crucially, RLMs encourage the LLM to understand, transform, and execute the input prompt by writing symbolic programs that invoke the LLM itself on as many slices of the input as necessary.</p><p>By treating the prompt itself as an external object and enabling symbolic recursion, RLMs tackle limitations of expressive power in recent work on coding agents, retrieval agents, and sub-agent delegation. In particular, prior coding agents and retrieval agents treat some designated external data source (e.g., a filesystem or a corpus of search documents) as an environment for fetching snippets. However, they can only fill up the underlying LLM&#39;s context window with snippets before facing compaction. Similarly, prior self-delegation approaches (Anthropic, 2025; Sentient AI, 2025; Schroeder et al., 2025; Sun et al., 2025) allow LLMs to invoke themselves as sub-agents. However, they are handicapped by the underlying LLM&#39;s limited output lengths because they are designed to verbalize sub-calls autoregressively rather than producing them programmatically.</p><p>We evaluate RLMs using a frontier closed model (GPT-5; Singh et al. 2025) and a frontier open model (Qwen3-Coder-480B-A35B; Qwen Team 2025b) across four tasks with varying levels of complexity: deep research (Chen et al., 2025), information aggregation (Bertsch et al., 2025), code repository understanding (Bai et al., 2025), and a synthetic pairwise reasoning task where even frontier models fail catastrophically. We compare RLMs against direct LLM calls as well as context compaction, retrieval tool-use agents, and code-generation agents with and without sub-calls.</p><p>We find that RLMs demonstrate extremely strong performance even at the 10M+ token scale, and substantially outperform other approaches at long-context processing, in many cases by double-digit percentage gains while maintaining comparable cost. RLMs exhibit far less severe degradation for longer contexts and more sophisticated tasks.</p><p>Finally, at a small scale, we post-train the first natively recursive language model, demonstrating that RLMs can be improved quickly with little additional training. While a small open model (Qwen3-8B; Yang et al. 2025) struggles to solve long context tasks even in an RLM scaffold, our simple general-purpose training recipe uses only 1,000 samples from unrelated domains to improve its performance by a median of 28.3% across the four evaluation tasks.</p><hr><h2 id="_2-recursive-language-models" tabindex="-1">2 Recursive Language Models <a class="header-anchor" href="#_2-recursive-language-models" aria-label="Permalink to &quot;2 Recursive Language Models&quot;">​</a></h2><p>Given a base neural language model M with maximum context size K, a Recursive Language Model (RLM) is an inference-time scaffold around M that treats the user prompt as part of the environment without giving up the ability to densely process its content through different calls to M.</p><p>Given an arbitrary-length prompt P ∈ Σ*, an RLM interacts with a persistent external environment E and returns a response string Y ∈ Σ*. We would like effectively unbounded input tokens (|P| &gt;&gt; K), unbounded output tokens, and an unbounded semantic horizon, e.g. the ability to do Ω(|P|) or Ω(|P|²) semantic work.</p><p><strong>Algorithm 1</strong> describes how an RLM achieves this. Given a prompt P, the RLM initializes a persistent REPL programming environment with a variable containing the user prompt as a string and a function for invoking a sub-RLM with a new prompt. Then, it starts the RLM loop. In the first iteration, the algorithm invokes the root neural model M with only (constant-size) metadata about the user prompt, like its length, a short prefix, and how to access parts of it.</p><p>The root is instructed via prompting and/or fine-tuning to operate like an RLM: that is, to generate code that helps it understand and transform parts of its prompt P, and to build up intermediate values and the final response into new variables, potentially by invoking the sub-RLM within loops.</p><p>Each iteration of the RLM loop executes code in the REPL, updates REPL state (intermediate variables), and collects in stdout any printed text. Only (constant-size) metadata about stdout, like a short prefix and length, is appended to M&#39;s history for the next iteration. This is key: it forces M to rely on variables and sub-calls to manage long strings instead of polluting its window. In principle, if we trim each turn to c tokens, we will have at most K/c root iterations, each of which can launch arbitrarily many sub-calls. Once the RLM sets the variable <code>Final</code> inside the REPL, iteration stops and the value in <code>Final</code> is returned as the response.</p><p><strong>Algorithm 1</strong> (A recursive language model, around LLM M, which itself acts as a &quot;language model&quot;):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Input: prompt P</span></span>
<span class="line"><span>Output: response Y</span></span>
<span class="line"><span></span></span>
<span class="line"><span>state ← InitREPL(prompt=P)</span></span>
<span class="line"><span>state ← AddFunction(state, sub_RLM_M)</span></span>
<span class="line"><span>hist ← [Metadata(state)]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>while True do:</span></span>
<span class="line"><span>    code ← LLM_M(hist)</span></span>
<span class="line"><span>    (state, stdout) ← REPL(state, code)</span></span>
<span class="line"><span>    hist ← hist || code || Metadata(stdout)</span></span>
<span class="line"><span>    if state[Final] is set then:</span></span>
<span class="line"><span>        return state[Final]</span></span></code></pre></div><p>RLMs make three simple design choices that are missing from many existing scaffolds. To highlight these, we include <strong>Algorithm 2</strong> to illustrate a deceptively &quot;similar&quot; algorithm that is far less expressive. Both algorithms support some notion of sub-calls, external objects, and code execution, but they differ in terms of where the prompt and intermediate values live and where recursion occurs.</p><p><strong>Algorithm 2</strong> (Alternate scaffold with standard poor design choices):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Input: prompt P</span></span>
<span class="line"><span>Output: response Y</span></span>
<span class="line"><span></span></span>
<span class="line"><span>actions ← {Finish, Exec, Search, sub_LLM_M}</span></span>
<span class="line"><span>hist ← [Metadata(actions), P]    ← Flaw #1</span></span>
<span class="line"><span></span></span>
<span class="line"><span>while True do:</span></span>
<span class="line"><span>    (action, val) ← LLM_M(hist)</span></span>
<span class="line"><span>    if action is Finish then:</span></span>
<span class="line"><span>        return val    ← Flaw #2</span></span>
<span class="line"><span>    out ← RUN(action, val)    ← Flaw #3</span></span>
<span class="line"><span>    hist ← hist || (action, val, out)</span></span>
<span class="line"><span>    if Tok(hist) &gt; K then:</span></span>
<span class="line"><span>        hist ← Compact(hist)</span></span></code></pre></div><p><strong>Flaw #1 — No symbolic handle</strong>: An RLM must give the underlying LLM M a symbolic handle to the user prompt P, so the model can manipulate it without copying text into the root context window. Instead, ineffective Algorithm 2 starts by putting the user prompt P into the LLM context window (hist), inheriting the window limitations of M and falling back to heuristics like context compaction. Even though the scaffold can access external data with, say, a Search action, it is bounded with respect to user input.</p><p><strong>Flaw #2 — Bounded outputs</strong>: Ineffective Algorithm 2 asks M to generate the output directly, via a Finish action. This may seem innocuous, but it means outputs cannot be longer than the context window of M.</p><p><strong>Flaw #3 — No symbolic recursion</strong>: An RLM requires symbolic recursion. That is, code running inside E must be able to invoke M on programmatically constructed transformations of P (e.g., inside arbitrarily large loops), storing intermediate results symbolically. Though Algorithm 2 includes both a code execution action and a &quot;sub-LLM&quot; action separately, it is not able to invoke the sub-LLM programmatically and hence can only delegate a few explicitly verbalized tasks rather than writing short programs that can, say, loop over slices of the prompt and launch Ω(|P|) or even Ω(|P|²) processes to understand or transform all parts of P.</p><p>We implement our RLM definition in Algorithm 1 as follows: we equip an LLM with a Python REPL, where all tools, including sub-LM or sub-RLM calls, are available as modules. The initial prompt is stored as a variable in the REPL. The LLM interacts in a loop until it provides a final answer, which can be from either a variable in the REPL, or from the LLM itself. The LLM can also print from the REPL, but it is truncated to prevent overflowing the context too quickly.</p><hr><h2 id="_3-scaling-long-context-tasks" tabindex="-1">3 Scaling Long Context Tasks <a class="header-anchor" href="#_3-scaling-long-context-tasks" aria-label="Permalink to &quot;3 Scaling Long Context Tasks&quot;">​</a></h2><p>We hypothesize that the effective context window of an LLM cannot be understood independently of the specific task. That is, more &quot;complex&quot; problems will exhibit degradation at even shorter lengths than simpler ones. Because of this, we must characterize tasks in terms of how their complexity scales with prompt length.</p><p>For example, needle-in-a-haystack (NIAH) problems generally keep &quot;needles&quot; constant as prompt length is scaled. As a result, frontier models can now reliably solve these tasks in RULER (Hsieh et al., 2024) in the 1M+ token settings but struggle at far shorter lengths on OOLONG (Bertsch et al., 2025), a task where the answer depends explicitly on almost every line in the prompt.</p><h3 id="_3-1-tasks" tabindex="-1">3.1 Tasks <a class="header-anchor" href="#_3-1-tasks" aria-label="Permalink to &quot;3.1 Tasks&quot;">​</a></h3><p>We design our evaluation around tasks where we can vary the lengths of the prompts, so we can consider problems whose difficulties scale differently with context length.</p><p><strong>S-NIAH.</strong> Following the single needle-in-the-haystack task in RULER (Hsieh et al., 2024), we consider a set of 50 single tasks that require finding a specific phrase or number in a large set of unrelated text. Here, the information being sought scales as O(1) with respect to input length.</p><p><strong>BrowseComp-Plus (1K documents)</strong> (Chen et al., 2025). A multi-hop question-answering benchmark for DeepResearch (OpenAI, 2025b) questions that requires reasoning over multiple different documents in an offline corpus. Following Sun et al. (2025), we use 150 randomly sampled instances as our evaluation set; we provide 1000 randomly chosen documents as input, in which the gold and evidence documents are guaranteed to exist. We report the percentage of correct answers. The answer to each task requires piecing together information from several documents, making this harder than S-NIAH despite also requiring a constant number of documents.</p><p><strong>OOLONG</strong> (Bertsch et al., 2025). A long reasoning benchmark that requires semantically labeling and aggregating these labels to form a final answer. We focus specifically on the <code>trec_coarse</code> split, a set of 50 tasks over a dataset of questions with semantic labels. Each task requires using nearly all dataset questions, and therefore scales linearly in processing complexity relative to the input length.</p><p><strong>OOLONG-Pairs.</strong> A modified variant of the <code>trec_coarse</code> split of OOLONG with 20 queries that specifically require aggregating pairs of chunks to construct the final answer. We report F1 scores over the answer, which is a list of entries. Each task requires using nearly all pairs of entries of the dataset, and therefore requires processing quadratically-many items relative to the input length.</p><p><strong>LongBench-v2 CodeQA</strong> (Bai et al., 2025). A multi-choice code repository understanding split from LongBench-v2 that is challenging for modern frontier models. Each instance requires reasoning over a fixed number of files in a codebase to find the right answer.</p><h3 id="_3-2-methods-and-baselines" tabindex="-1">3.2 Methods and Baselines <a class="header-anchor" href="#_3-2-methods-and-baselines" aria-label="Permalink to &quot;3.2 Methods and Baselines&quot;">​</a></h3><p>We compare RLMs against commonly used task-agnostic inference methods, using two modern LMs, GPT-5 with medium reasoning (Singh et al., 2025) and default sampling parameters, and Qwen3-Coder-480B-A35B (Yang et al., 2025) using the sampling parameters described in Qwen Team (2025b). For Qwen3-Coder-480B-A35B, we compute costs based on the compute provider Fireworks (Fireworks AI, 2025). In addition to evaluating the base model on all tasks, we also evaluate the following methods and baselines:</p><p><strong>CodeAct.</strong> We compare directly to a CodeAct (Wang et al., 2024) agent that can execute code inside of a ReAct (Yao et al., 2023) loop. Unlike an RLM, CodeAct does not offload the user prompt to the code environment, and instead provides it directly to the LM. We consider two variants: (1) a version following Jimenez et al. (2024); Chen et al. (2025) where we equip this agent with a BM25 (Robertson and Zaragoza, 2009) retriever; (2) a version with a sub-call tool inside of the REPL. Compared to RLMs, this method loads the context directly into the model.</p><p><strong>Compaction agent.</strong> Following Sun et al. (2025); Wu et al. (2025); Yu et al. (2025), we consider an iterative agent that compacts the context as it is filled. For example, given a corpus of documents, it will iteratively accumulate the documents and summarize when full. In cases where a single document exceeds the model window, the agent will chunk the document and iteratively compact it. For the GPT-5 experiments, due to the extremely high cost of applying this strategy to millions of tokens, we use GPT-5-nano for compaction and GPT-5 to provide the final answer.</p><p><strong>Coding agents.</strong> We compare against commonly used coding agents like OpenCode (Anomaly, 2026) and Claude Code (Anthropic, 2025). We consider two variants, one where the context is offloaded to a file, and another where it is directly used as the initial prompt. Closed source agents like Claude Code are designed around a corresponding model, so we use Claude Opus 4.1 with Claude Code v2.0.0 (released around the same time as the GPT-5 model we use in our main results) for this baseline.</p><p><strong>RLM.</strong> We implement an RLM with a Python REPL environment, which loads a module for querying a sub-LM and uses a system prompt presented in Appendix C. For the GPT-5 experiments, we use GPT-5-mini for the recursive LMs and GPT-5 for the root LM, as we found this choice to strike a good balance between the capabilities of RLMs and the cost of the recursive calls. We also evaluate several different max recursion depths allowable to the RLM, from 0–3. Max recursion depth 0 is an RLM without sub-calling capabilities. Max recursion depth 1 allows sub-calling LLMs, while max depth &gt;1 allows sub-calling RLMs. We notate a RLM with max recursion depth N using a model as RLM(model, depth=N), e.g. RLM(GPT-5, depth=2), and assume depth=1 if not stated otherwise.</p><p><strong>Fine-tuning.</strong> To create RLM-Qwen3-8B, we fine-tune Qwen3-8B on 1,000 filtered trajectories of Qwen3-Coder-480B-A35B as an RLM with Qwen3-8B sub-calls on LongBenchPro (Chen et al., 2026) tasks. We use sampling parameters described in Qwen Team (2025a), and evaluate the fine-tuned RLM-Qwen3-8B as an RLM. The key insight for training is that being an effective sub-call model is roughly similar to being a general purpose reasoning model, so we can make the training much more tractable at small scale by focusing on improving the root model&#39;s ability to manipulate the REPL and to launch recursive calls.</p><hr><h2 id="_4-results-and-discussion" tabindex="-1">4 Results and Discussion <a class="header-anchor" href="#_4-results-and-discussion" aria-label="Permalink to &quot;4 Results and Discussion&quot;">​</a></h2><h3 id="main-results-table-1" tabindex="-1">Main Results (Table 1) <a class="header-anchor" href="#main-results-table-1" aria-label="Permalink to &quot;Main Results (Table 1)&quot;">​</a></h3><p>Performance comparison across long-context benchmarks. Task lengths: CodeQA 23K–4.2M tokens, BrowseComp+ 6M–11M tokens, OOLONG 131K tokens, OOLONG-Pairs 32K tokens. (∗ = method sometimes hit input context limits.)</p><p><strong>GPT-5 (with RLM sub-calls to GPT-5-mini):</strong></p><table tabindex="0"><thead><tr><th>Model</th><th>CodeQA</th><th>BrowseComp+ (1K)</th><th>OOLONG</th><th>OOLONG-Pairs</th></tr></thead><tbody><tr><td>Base Model</td><td>24.0∗ ($0.13)</td><td>0.0∗</td><td>44.0 ($0.14)</td><td>0.1 ($0.16)</td></tr><tr><td>CodeAct (+ BM25)</td><td>22.0∗ ($0.06)</td><td>51.0 ($0.71)</td><td>38.0 ($0.61)</td><td>24.7 ($0.75)</td></tr><tr><td>CodeAct (+ sub-calls)</td><td>24.0∗ ($0.06)</td><td>0.0∗</td><td>40.0 ($0.85)</td><td>28.4 ($1.11)</td></tr><tr><td>Compaction agent</td><td>58.0 ($1.31)</td><td>70.5 ($0.57)</td><td>46.0 ($0.13)</td><td>0.1 ($0.13)</td></tr><tr><td>OpenCode</td><td>18.0∗</td><td>0.0∗</td><td>32.0</td><td>3.1</td></tr><tr><td>OpenCode (+ context offloading)</td><td>64.0</td><td>94.0</td><td>52.0</td><td>4.8</td></tr><tr><td>RLM (depth=0)</td><td>58.0 ($0.18)</td><td>88.0 ($0.44)</td><td>36.0 ($0.37)</td><td>43.9 ($0.69)</td></tr><tr><td>RLM (depth=1)</td><td>62.0 ($0.11)</td><td>91.3 ($0.99)</td><td>56.0 ($0.43)</td><td>58.0 ($0.33)</td></tr><tr><td>RLM (depth=2)</td><td>66.0 ($0.15)</td><td>92.0 ($0.55)</td><td>56.5 ($1.10)</td><td>65.5 ($0.33)</td></tr><tr><td>RLM (depth=3)</td><td>58.0 ($0.15)</td><td>92.0 ($0.51)</td><td>58.0 ($0.51)</td><td>76.0 ($0.39)</td></tr></tbody></table><p><strong>Qwen3-Coder-480B-A35B:</strong></p><table tabindex="0"><thead><tr><th>Model</th><th>CodeQA</th><th>BrowseComp+ (1K)</th><th>OOLONG</th><th>OOLONG-Pairs</th></tr></thead><tbody><tr><td>Base Model</td><td>20.0∗ ($0.13)</td><td>0.0∗</td><td>36.0 ($0.06)</td><td>0.1 ($0.05)</td></tr><tr><td>CodeAct (+ BM25)</td><td>24.0∗ ($0.17)</td><td>12.7 ($0.39)</td><td>38.0 ($1.51)</td><td>0.3 ($1.54)</td></tr><tr><td>CodeAct (+ sub-calls)</td><td>26.0∗ ($0.28)</td><td>0.0∗</td><td>32.0 ($1.83)</td><td>0.1 ($1.49)</td></tr><tr><td>Compaction agent</td><td>50.0 ($1.26)</td><td>38.0 ($8.98)</td><td>44.1 ($0.15)</td><td>0.31 ($0.05)</td></tr><tr><td>OpenCode</td><td>12.0∗</td><td>0.0∗</td><td>36.0</td><td>0.0</td></tr><tr><td>OpenCode (+ context offloading)</td><td>40.0</td><td>58.0</td><td>24.0</td><td>2.1</td></tr><tr><td>RLM (depth=0)</td><td>66.0 ($0.18)</td><td>46.0 ($0.82)</td><td>43.5 ($0.32)</td><td>17.3 ($1.77)</td></tr><tr><td>RLM (depth=1)</td><td>56.0 ($0.92)</td><td>44.7 ($0.84)</td><td>48.0 ($0.61)</td><td>23.1 ($1.02)</td></tr><tr><td>RLM (depth=2)</td><td>54.0 ($1.88)</td><td>68.0 ($1.05)</td><td>26.0 ($1.03)</td><td>19.0 ($1.61)</td></tr><tr><td>RLM (depth=3)</td><td>44.0 ($1.65)</td><td>68.7 ($1.10)</td><td>32.0 ($0.80)</td><td>21.1 ($1.67)</td></tr></tbody></table><p><strong>Claude Opus 4.1:</strong></p><table tabindex="0"><thead><tr><th>Model</th><th>CodeQA</th><th>BrowseComp+ (1K)</th><th>OOLONG</th><th>OOLONG-Pairs</th></tr></thead><tbody><tr><td>Claude Code</td><td>12.0∗ ($2.03)</td><td>0.0∗</td><td>40.2 ($3.43)</td><td>0.1 ($6.75)</td></tr><tr><td>Claude Code (+ context offloading)</td><td>62.0 ($1.25)</td><td>84.0 ($2.03)</td><td>48.0 ($0.98)</td><td>6.5 ($2.99)</td></tr></tbody></table><h3 id="key-observations" tabindex="-1">Key Observations <a class="header-anchor" href="#key-observations" aria-label="Permalink to &quot;Key Observations&quot;">​</a></h3><p><strong>Observation 1: RLMs can scale to the 10M+ token regime and can outperform base LMs and existing task-agnostic agent scaffolds on long context tasks.</strong> Across all tasks, RLMs demonstrate strong performance on prompts well beyond the effective context window of a frontier LM, outperforming base models and common long-context scaffolds by up to 2× the performance while maintaining comparable or cheaper average token costs. Notably, RLMs scale well beyond the base models&#39; context window. For instance, on BrowseComp-Plus (1K), a linearly extrapolated cost for GPT-5-mini ingesting 6–11M input tokens is $1.50–$2.75, while RLM(GPT-5, depth=1) has an average cost of $0.99 and outperforms both the compaction and retrieval baselines by over 29%.</p><p>Furthermore, on tasks where processing costs scale with the input context, RLMs make significant improvements over the base model, even on tasks within the model&#39;s context window. On OOLONG, the RLM(depth=1) with GPT-5 and Qwen3-Coder outperform the base model by 28.4% and 33.3% respectively. On OOLONG-Pairs, both GPT-5 and Qwen3-Coder make little progress with F1 scores of ≤0.1%, while the RLM(depth=1) using these models achieve F1 scores of 58.0% and 23.1% respectively, highlighting the capability of RLMs to handle extremely information-dense tasks.</p><p><strong>Observation 2: The REPL is necessary for handling long inputs, while the recursive sub-calling of RLMs provides strong benefits on information-dense inputs.</strong> A key characteristic of RLMs is offloading the context as a variable in an environment E that the model can interact with. In particular, RLM(depth=0) and coding agents like Claude Code and OpenCode are able to scale beyond the context limit of the model and outperform other task-agnostic baselines on most long context settings. On CodeQA in particular with Qwen3-Coder-480B-A35B, the no-sub-calling RLM(depth=0) is able to outperform all sub-calling variants of the RLM.</p><p>On information-dense tasks like OOLONG or OOLONG-Pairs, we observed several cases where programmatic recursive LM sub-calling is necessary. RLM(Qwen3-Coder) performs the necessary semantic transformation line-by-line through recursive sub-calls, while the ablation without sub-calls is forced to use keyword heuristics to solve these tasks. On OOLONG-Pairs in particular, the higher recursive depth variants of the RLM for GPT-5 outperform all other methods including Claude Code and OpenCode by a large margin.</p><p><strong>Observation 3: LM performance degrades as a function of input length and problem complexity, while RLM performance scales better.</strong> The benchmarks S-NIAH, OOLONG, and OOLONG-Pairs contain a fixed number of tasks over contexts with lengths ranging from 2^13 to 2^20. Each benchmark can be categorized by different processing complexity of the input context with respect to length (roughly constant, linear, and quadratic respectively). GPT-5 performance degrades significantly faster for more complex tasks, while RLM performance degrades at a slower rate. For context lengths beyond 2^14, the RLM consistently outperforms GPT-5.</p><p><strong>Observation 4: The inference cost of RLMs remains comparable to other methods, and in some cases base LM calls.</strong> On average, the inference cost of RLMs is cheaper or comparable to most other baselines, including standard coding agents. The median RLM run is cheaper than the median base model run, but more expensive on average due to outlier trajectories where the RLM struggles to find an answer.</p><p><strong>Observation 5: Beyond long-context, RLMs enable longer reasoning capabilities.</strong> (Table 2 — LongCoT-mini results)</p><table tabindex="0"><thead><tr><th>Model</th><th>Overall</th><th>MATH</th><th>CHEM</th><th>CS</th><th>LOGIC</th><th>CHESS</th></tr></thead><tbody><tr><td>GPT-5.2 (base)</td><td>38.7</td><td>26.0</td><td>37.0</td><td>40.4</td><td>53.6</td><td>36.6</td></tr><tr><td>RLM (GPT-5.2, depth=1)</td><td>50.6</td><td>5.6</td><td>50.0</td><td>11.0</td><td>86.7</td><td>93.0</td></tr><tr><td>RLM (GPT-5.2, depth=1) + decomposition hints</td><td>65.6</td><td>32.0</td><td>52.0</td><td>46.0</td><td>99.0</td><td>99.0</td></tr></tbody></table><p>RLM(GPT-5.2, depth=1) uses the REPL to outperform the base model. When providing explicit hints on how to decompose tasks, we find the RLM is able to reliably generate a graph of the problem, solving each node using sub-calls as it programmatically traverses the reasoning graph. It outperforms the base model on all domains and by a 69.5% performance increase overall.</p><p><strong>Observation 6: Training RLMs on one domain can improve general downstream RLM performance, as well as efficiency. Training also exhibits length generalization.</strong> Certain behaviors in RLM trajectories are common among different domains, such as probing the input and recursively sub-calling on shorter contexts. RLM-Qwen3-8B, a Qwen3-8B model fine-tuned on RLM(Qwen3-Coder-480B-A35B) trajectories on a small, unrelated set of tasks (LongBenchPro; Chen et al. 2026) considerably outperforms the base Qwen3-8B as a RLM across all tasks. Furthermore, its inference costs are much lower and more than 3× faster due to better decision making and fewer mistakes as a RLM. Training RLMs also exhibits length generalization; by purely training through reinforcement learning with verifiable rewards (RLVR) on a smaller split, RLM(Qwen3-4B-Instruct-0527) is able to generalize to longer, more difficult splits.</p><hr><h2 id="_5-analyses-of-rlm-trajectories" tabindex="-1">5 Analyses of RLM Trajectories <a class="header-anchor" href="#_5-analyses-of-rlm-trajectories" aria-label="Permalink to &quot;5 Analyses of RLM Trajectories&quot;">​</a></h2><p>RLMs exhibit interesting context and problem decomposition behavior. We discuss observable behavior in small and large LLMs as RLMs to understand how we can steer and improve their performance and efficiency through training and prompt tuning.</p><p><strong>Observed RLM decomposition patterns.</strong> Current models as RLMs attempt to probe, then decompose a task into sub-tasks for recursive sub-calls to solve. In many cases such as on BrowseComp-Plus, the LM uses model priors to programmatically narrow the search space of sub-calls. RLMs are also able to output beyond their context window by stitching together sub-LM calls inside the REPL, which is required to solve tasks like OOLONG-Pairs.</p><p><strong>First decomposition and errors in RLM trajectories.</strong> RLMs defer essentially unbounded-length reasoning chains to sub-LM calls. The choice of decomposition can greatly affect task performance, especially for information-dense problems. In-context RLM trajectories greatly improve both overall performance and the initial decomposition attempt made by the RLM, even if the example is unrelated to the actual task. Furthermore, while RLMs frequently recover from an initially incorrect decomposition pattern, the first decomposition attempt is important for overall performance.</p><p>RLM(Qwen3-Coder) trajectories contain significantly more syntax errors, even for correct trajectories, compared to RLM(GPT-5). These errors explain why higher recursion depths for RLM(Qwen3-Coder) perform worse on average: Qwen3-Coder-480B-A35B often makes syntax errors that result in failed outputs, and having sub-RLM calls propagates this issue to sub-calls.</p><hr><h2 id="_6-related-works" tabindex="-1">6 Related Works <a class="header-anchor" href="#_6-related-works" aria-label="Permalink to &quot;6 Related Works&quot;">​</a></h2><p><strong>Long-Context LM Systems.</strong> There have primarily been two orthogonal directions for long-context management in language model systems: (1) directly changing the architecture of and retraining the base LM to handle longer contexts (Press et al., 2022; Gu et al., 2022; Munkhdalai et al., 2024), and (2) building a scaffold around the LM that implicitly handles the context — RLMs focus on the latter. One popular class of such strategies is lossy context management (Chen et al., 2023), which uses compaction or truncation to compress the input context at the cost of potentially losing fine-grained information. For example, ReSum (Wu et al., 2025) adds a summarization tool to periodically compress the context of a multi-turn agent. Another class of strategies implement an explicit memory hierarchy in the agent scaffold (Packer et al., 2024; Chhikara et al., 2025; Zhang et al., 2025). RLMs differ from these works in that all context window management is implicitly handled by the LM itself.</p><p><strong>Task Decomposition through sub-LM calls.</strong> Many LM-based agents (Guo et al., 2024; Anthropic, 2025) use multiple, well-placed LM calls to solve a problem; however, many of these calls are placed based on human-engineered workflows. Several methods like ViperGPT (Surís et al., 2023), THREAD (Schroeder et al., 2025), ReDel (Zhu et al., 2024), Context Folding (Sun et al., 2025), and AgentFold (Ye et al., 2025) have explored deferring the choice of sub-LM calls to the LM. These techniques emphasize task decomposition through recursive LM calls, but are unable to handle long context inputs beyond the length of the base LM. DisCIPL (Grand et al., 2025) generates programs with sub-LM calls, but these programs are generated in a single-step and cannot recover from generation mistakes. RLMs, on the other hand, are enabled by an extremely simple intuition (i.e., placing the prompt in the external environment) to symbolically manipulate arbitrarily long strings and to iteratively refine their recursion via execution feedback from the persistent REPL.</p><hr><h2 id="_7-limitations-and-future-work" tabindex="-1">7 Limitations and Future Work <a class="header-anchor" href="#_7-limitations-and-future-work" aria-label="Permalink to &quot;7 Limitations and Future Work&quot;">​</a></h2><p>While RLMs show strong performance on tasks beyond the context window limitations of existing LMs at reasonable inference costs, evaluations for more difficult and natural long-context processing tasks and the best mechanisms for implementing guardrails for RLMs both remain highly under-explored. Broadly, RLMs add a layer of complexity on top of existing LMs that may lead to unintentional side-effects like exploding sub-call costs, which we leave for future work to solve. We also note that future strategies involving asynchronous sub-calls and sandboxed REPLs can potentially significantly reduce the runtime and inference cost of RLMs, but further contribute to this complexity.</p><p>We focused our experiments on evaluating RLMs using existing frontier models, but show initial evidence on a Qwen3-8B model that explicit training as a RLM provides very rapid performance improvements, even outside the training domain. We hypothesize that RLM trajectories can be viewed as a form of reasoning (OpenAI et al., 2024; DeepSeek-AI et al., 2025), which can be trained by bootstrapping existing models (Zelikman et al., 2022, 2024). We hope that training native RLMs can be treated as a new axis of scale to improve LM performance on general and long-horizon tasks.</p><hr><h2 id="_8-conclusion" tabindex="-1">8 Conclusion <a class="header-anchor" href="#_8-conclusion" aria-label="Permalink to &quot;8 Conclusion&quot;">​</a></h2><p>We introduced Recursive Language Models (RLMs), a general inference framework for language models that offloads the input context and enables language models to recursively sub-query language models before providing an output. We explored an instantiation of this framework that offloads the context into a Python REPL environment as a variable in memory, enabling the LM to reason over its context in code and recursive LM calls, rather than purely in token space. Our results across multiple settings and models demonstrated that RLMs are an effective task-agnostic paradigm for both long-context problems and general reasoning. Building on our small fine-tuning experiments, we are excited to see future work that explicitly trains models to reason as RLMs, which could result in another axis of scale for the next generation of language model systems.</p><hr><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li>Anomaly (2026). OpenCode: the open source AI coding agent.</li><li>Anthropic (2025). Claude Code: subagents — modular AI workflows with isolated agent contexts.</li><li>Bai, Y., Tu, S., Zhang, J., et al. (2025). LongBench v2: towards deeper understanding and reasoning on realistic long-context multitasks. arXiv:2412.15204.</li><li>Bertsch, A., Pratapa, A., Mitamura, T., Neubig, G., and Gormley, M.R. (2025). OOLONG: evaluating long context reasoning and aggregation capabilities. arXiv:2511.02817.</li><li>Chang, Y., Lo, K., Goyal, T., and Iyyer, M. (2024). BooookScore: a systematic exploration of book-length summarization in the era of LLMs. ICLR 2024.</li><li>Chen, H., Pasunuru, R., Weston, J., and Celikyilmaz, A. (2023). Walking down the memory maze: beyond context limit through interactive reading. arXiv:2310.05029.</li><li>Chen, Z., Ma, X., Zhuang, S., et al. (2025). BrowseComp-plus: a more fair and transparent evaluation benchmark of deep-research agent. arXiv:2508.06600.</li><li>Chen, Z., Wu, X., Jia, J., et al. (2026). LongBench Pro: a more realistic and comprehensive bilingual long-context evaluation benchmark. arXiv:2601.02872.</li><li>Chhikara, P., Khant, D., Aryan, S., Singh, T., and Yadav, D. (2025). Mem0: building production-ready AI agents with scalable long-term memory. arXiv:2504.19413.</li><li>DeepSeek-AI, Guo, D., Yang, D., et al. (2025). DeepSeek-R1: incentivizing reasoning capability in LLMs via reinforcement learning. arXiv:2501.12948.</li><li>Fireworks AI (2025). Qwen3 Coder 480B A35B Instruct. <a href="https://fireworks.ai/models/fireworks/qwen3-coder-480b-a35b-instruct" target="_blank" rel="noreferrer">https://fireworks.ai/models/fireworks/qwen3-coder-480b-a35b-instruct</a></li><li>Goldman, O., Jacovi, A., Slobodkin, A., et al. (2025). Is it really long context if all you need is retrieval? Towards genuinely difficult long context NLP. arXiv:2407.00402.</li><li>Grand, G., Tenenbaum, J.B., Mansinghka, V.K., Lew, A.K., and Andreas, J. (2025). Self-steering language models. arXiv:2504.07081.</li><li>Gu, A., Goel, K., and Ré, C. (2022). Efficiently modeling long sequences with structured state spaces. arXiv:2111.00396.</li><li>Guo, T., Chen, X., Wang, Y., et al. (2024). Large language model based multi-agents: a survey of progress and challenges. arXiv:2402.01680.</li><li>Hong, K., Troynikov, A., and Huber, J. (2025). Context rot: how context degradation affects LLM performance.</li><li>Hsieh, C., Sun, S., Kriman, S., et al. (2024). RULER: what&#39;s the real context size of your long-context language models? arXiv:2404.06654.</li><li>Jimenez, C.E., Yang, J., Wettig, A., et al. (2024). SWE-bench: can language models resolve real-world GitHub issues? arXiv:2310.06770.</li><li>Khattab, O., Potts, C., and Zaharia, M. (2021). Baleen: robust multi-hop reasoning at scale via condensed retrieval. NeurIPS 34, pp. 27670–27682.</li><li>Merrill, W. and Sabharwal, A. (2024). The expressive power of transformers with chain of thought. ICLR 2024.</li><li>Motwani, S.R., Nichols, D., London, C., et al. (2026). LongCoT: benchmarking long-horizon chain-of-thought reasoning. arXiv:2604.14140.</li><li>Munkhdalai, T., Faruqui, M., and Gopal, S. (2024). Leave no context behind: efficient infinite context transformers with Infini-attention. arXiv:2404.07143.</li><li>OpenAI (2024). OpenAI o1 system card. arXiv:2412.16720.</li><li>OpenAI (2025a). Codex CLI: a lightweight coding agent for your terminal.</li><li>OpenAI (2025b). Deep research. AI-powered research assistant tool.</li><li>Packer, C., Wooders, S., Lin, K., et al. (2024). MemGPT: towards LLMs as operating systems. arXiv:2310.08560.</li><li>Press, O., Smith, N.A., and Lewis, M. (2022). Train short, test long: attention with linear biases enables input length extrapolation. arXiv:2108.12409.</li><li>Qwen Team (2025a). Qwen3-8B. <a href="https://huggingface.co/Qwen/Qwen3-8B" target="_blank" rel="noreferrer">https://huggingface.co/Qwen/Qwen3-8B</a></li><li>Qwen Team (2025b). Qwen3-Coder-480B-A35B-Instruct. <a href="https://huggingface.co/Qwen/Qwen3-Coder-480B-A35B-Instruct" target="_blank" rel="noreferrer">https://huggingface.co/Qwen/Qwen3-Coder-480B-A35B-Instruct</a></li><li>Robertson, S. and Zaragoza, H. (2009). The probabilistic relevance framework: BM25 and beyond. Found. Trends Inf. Retr. 3(4), pp. 333–389.</li><li>Schroeder, P., Morgan, N., Luo, H., and Glass, J. (2025). THREAD: thinking deeper with recursive spawning. arXiv:2405.17402.</li><li>Sentient AI (2025). ROMA: the backbone for open-source meta-agents.</li><li>Singh, A., et al. (2025). GPT-5 technical report.</li><li>Surís, D., Menon, S., and Vondrick, C. (2023). ViperGPT: visual inference via python execution for reasoning. ICCV 2023.</li><li>Vodrahalli, K., et al. (2024). MRCRv2.</li><li>Wang, X., et al. (2024). CodeAct: executable code actions elicit better LLM agents.</li><li>Wu, J., et al. (2021). SCROLLS: standardized comparison over long language sequences.</li><li>Wu, Z., et al. (2025). ReSum: context compaction via summarization for multi-turn agents.</li><li>Yang, A., et al. (2025). Qwen3 technical report.</li><li>Yao, S., et al. (2023). ReAct: synergizing reasoning and acting in language models. ICLR 2023.</li><li>Ye, J., et al. (2025). AgentFold: folding long-context tasks via multi-agent delegation.</li><li>Yu, Z., et al. (2025). Context compaction for long-horizon agents.</li><li>Zelikman, E., et al. (2022). STaR: bootstrapping reasoning with reasoning. NeurIPS 2022.</li><li>Zelikman, E., et al. (2024). Quiet-STaR: language models can teach themselves to think before speaking.</li><li>Zhang, A.L., et al. (2025). MemoryOS: operating system inspired memory management for language model agents.</li><li>Zhu, J., et al. (2024). ReDel: a toolkit for LLM-powered recursive multi-agent systems.</li></ul><hr><h2 id="citation" tabindex="-1">Citation <a class="header-anchor" href="#citation" aria-label="Permalink to &quot;Citation&quot;">​</a></h2><div class="language-bibtex vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bibtex</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@article</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">zhang2025recursive</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  title</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">{</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Recursive Language Models</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  author</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">{</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Zhang, Alex L. and Kraska, Tim and Khattab, Omar</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  journal</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">{</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">arXiv preprint arXiv:2512.24601</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  year</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">{</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">2025</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><hr><p><strong>Keywords</strong>: recursive language models, long-context processing, inference-time scaling, REPL, symbolic recursion, context window, programmatic sub-calls, RLM-Qwen3-8B, LongBench, OOLONG</p>`,97)])])}const u=t(o,[["render",i]]);export{g as __pageData,u as default};
