import{c as t,Q as o,j as a,m as s}from"./chunks/framework.BXc4yaFE.js";const u=JSON.parse('{"title":"Prompt Caching Is Everything：Claude Code 官方快取架構設計五原則 — @trq212","description":"","frontmatter":{"title":"Prompt Caching Is Everything：Claude Code 官方快取架構設計五原則 — @trq212","author":"Thariq（@trq212）","date":"2026-02-19T00:00:00.000Z","source":"https://x.com/trq212/status/2024574133011673516","type":"tweet"},"headers":[],"relativePath":"research/tweets/2026-02-19-@trq212-673516.md","filePath":"research/tweets/2026-02-19-@trq212-673516.md","lastUpdated":1779894392000}'),n={name:"research/tweets/2026-02-19-@trq212-673516.md"};function r(i,e,h,d,l,c){return o(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="prompt-caching-is-everything-claude-code-官方快取架構設計五原則-—-trq212" tabindex="-1">Prompt Caching Is Everything：Claude Code 官方快取架構設計五原則 — @trq212 <a class="header-anchor" href="#prompt-caching-is-everything-claude-code-官方快取架構設計五原則-—-trq212" aria-label="Permalink to &quot;Prompt Caching Is Everything：Claude Code 官方快取架構設計五原則 — @trq212&quot;">​</a></h1><p><strong>來源</strong>：<a href="https://x.com/trq212/status/2024574133011673516" target="_blank" rel="noreferrer">https://x.com/trq212/status/2024574133011673516</a><strong>作者</strong>：Thariq（@trq212） <strong>發布日期</strong>：2026-02-19 <strong>收錄日期</strong>：2026-05-10 <strong>類型</strong>：Twitter Article <strong>統計</strong>：5568 likes · 535 retweets · 14432 bookmarks · 2323198 views</p><hr><h2 id="全文" tabindex="-1">全文 <a class="header-anchor" href="#全文" aria-label="Permalink to &quot;全文&quot;">​</a></h2><p>It is often said in engineering that &quot;Cache Rules Everything Around Me&quot;, and the same rule holds for agents.</p><p>Long running agentic products like Claude Code are made feasible by prompt caching which allows us to reuse computation from previous roundtrips and significantly decrease latency and cost.</p><p>What is prompt caching, how does it work and how do you implement it technically? Read more in @RLanceMartin&#39;s piece on prompt caching and our new auto-caching launch.</p><p>At Claude Code, we build our entire harness around prompt caching. A high prompt cache hit rate decreases costs and helps us create more generous rate limits for our subscription plans, so we run alerts on our prompt cache hit rate and declare SEVs if they&#39;re too low.</p><p>These are the (often unintuitive) lessons we&#39;ve learned from optimizing prompt caching at scale.</p><h2 id="lay-out-your-prompt-for-caching" tabindex="-1">Lay Out Your Prompt for Caching <a class="header-anchor" href="#lay-out-your-prompt-for-caching" aria-label="Permalink to &quot;Lay Out Your Prompt for Caching&quot;">​</a></h2><p>Prompt caching works by prefix matching — the API caches everything from the start of the request up to each cache_control breakpoint. This means the order you put things in matters enormously, you want as many of your requests to share a prefix as possible.</p><p>The best way to do this is static content first, dynamic content last. For Claude Code this looks like:</p><ol><li>Static system prompt &amp; Tools (globally cached)</li><li>Claude.MD (cached within a project)</li><li>Session context (cached within a session)</li><li>Conversation messages</li></ol><p>This way we maximize how many sessions share cache hits.</p><p>But this can be surprisingly fragile! Examples of reasons we&#39;ve broken this ordering before include: putting an in-depth timestamp in the static system prompt, shuffling tool order definitions non-deterministically, updating parameters of tools (e.g. what agents the AgentTool can call), etc.</p><h2 id="use-messages-for-updates" tabindex="-1">Use Messages for Updates <a class="header-anchor" href="#use-messages-for-updates" aria-label="Permalink to &quot;Use Messages for Updates&quot;">​</a></h2><p>There may be times when the information you put in your prompt becomes out of date, for example if you have the time or if the user changes a file. It may be tempting to update the prompt, but that would result in a cache miss and could end up being quite expensive for the user.</p><p>Consider if you can pass in this information via messages in the next turn instead. In Claude Code, we add a <code>&lt;system-reminder&gt;</code> tag in the next user message or tool result with the updated information for the model (e.g. it is now Wednesday), which helps preserve the cache.</p><h2 id="don-t-change-models-mid-session" tabindex="-1">Don&#39;t change Models Mid-Session <a class="header-anchor" href="#don-t-change-models-mid-session" aria-label="Permalink to &quot;Don&#39;t change Models Mid-Session&quot;">​</a></h2><p>Prompt caches are unique to models and this can make the math of prompt caching quite unintuitive.</p><p>If you&#39;re 100k tokens into a conversation with Opus and want to ask a question that is fairly easy to answer, it would actually be more expensive to switch to Haiku than to have Opus answer, because we would need to rebuild the prompt cache for Haiku.</p><p>If you need to switch models, the best way to do it is with subagents, where Opus would prepare a &quot;handoff&quot; message to another model on the task that it needs done. We do this often with the Explore agents in Claude Code which use Haiku.</p><h2 id="never-add-or-remove-tools-mid-session" tabindex="-1">Never Add or Remove Tools Mid-Session <a class="header-anchor" href="#never-add-or-remove-tools-mid-session" aria-label="Permalink to &quot;Never Add or Remove Tools Mid-Session&quot;">​</a></h2><p>Changing the tool set in the middle of a conversation is one of the most common ways people break prompt caching. It seems intuitive — you should only give the model tools you think it needs right now. But because tools are part of the cached prefix, adding or removing a tool invalidates the cache for the entire conversation.</p><p><strong>Plan Mode — Design Around the Cache</strong></p><p>Plan mode is a great example of designing features around caching constraints. The intuitive approach would be: when the user enters plan mode, swap out the tool set to only include read-only tools. But that would break the cache.</p><p>Instead, we keep all tools in the request at all times and use EnterPlanMode and ExitPlanMode as tools themselves. When the user toggles plan mode on, the agent gets a system message explaining that it&#39;s in plan mode and what the instructions are — explore the codebase, don&#39;t edit files, call ExitPlanMode when the plan is complete. The tool definitions never change.</p><p>This has a bonus benefit: because EnterPlanMode is a tool the model can call itself, it can autonomously enter plan mode when it detects a hard problem, without any cache break.</p><p><strong>Tool Search — Defer Instead of Remove</strong></p><p>The same principle applies to our tool search feature. Claude Code can have dozens of MCP tools loaded, and including all of them in every request would be expensive. But removing them mid-conversation would break the cache.</p><p>Our solution: <code>defer_loading</code>. Instead of removing tools, we send lightweight stubs — just the tool name, with <code>defer_loading: true</code> — that the model can &quot;discover&quot; via a ToolSearch tool when needed. The full tool schemas are only loaded when the model selects them. This keeps the cached prefix stable: the same stubs are always present in the same order.</p><p>Luckily you can use the tool search tool through our API to simplify this.</p><h2 id="forking-context-—-compaction" tabindex="-1">Forking Context — Compaction <a class="header-anchor" href="#forking-context-—-compaction" aria-label="Permalink to &quot;Forking Context — Compaction&quot;">​</a></h2><p>Compaction is what happens when you run out of the context window. We summarize the conversation so far and continue a new session with that summary.</p><p>Surprisingly, compaction has many edge cases with prompt caching that can be unintuitive.</p><p>In particular, when we compact we need to send the entire conversation to the model to generate a summary. If this is a separate API call with a different system prompt and no tools (which is the simple implementation), the cached prefix from the main conversation doesn&#39;t match at all. You pay full price for all those input tokens, drastically increasing the cost for the user.</p><p><strong>The Solution — Cache-Safe Forking</strong></p><p>When we run compaction, we use the <em>exact same</em> system prompt, user context, system context, and tool definitions as the parent conversation. We prepend the parent&#39;s conversation messages, then append the compaction prompt as a new user message at the end.</p><p>From the API&#39;s perspective, this request looks nearly identical to the parent&#39;s last request — same prefix, same tools, same history — so the cached prefix is reused. The only new tokens are the compaction prompt itself.</p><p>This does mean however that we need to save a &quot;compaction buffer&quot; so that we have enough room in the context window to include the compact message and the summary output tokens.</p><p>Compaction is tricky but luckily, you don&#39;t need to learn these lessons yourself — based on our learnings from Claude Code we built compaction directly into the API, so you can apply these patterns in your own applications.</p><h2 id="lessons-learned" tabindex="-1">Lessons Learned <a class="header-anchor" href="#lessons-learned" aria-label="Permalink to &quot;Lessons Learned&quot;">​</a></h2><ol><li><strong>Prompt caching is a prefix match.</strong> Any change anywhere in the prefix invalidates everything after it. Design your entire system around this constraint. Get the ordering right and most of the caching works for free.</li><li><strong>Use messages instead of system prompt changes.</strong> You may be tempted to edit the system prompt to do things like entering plan mode, changing the date, etc. but it would actually be better to insert these into messages during the conversation.</li><li><strong>Don&#39;t change tools or models mid-conversation.</strong> Use tools to model state transitions (like plan mode) rather than changing the tool set. Defer tool loading instead of removing tools.</li><li><strong>Monitor your cache hit rate like you monitor uptime.</strong> We alert on cache breaks and treat them as incidents. A few percentage points of cache miss rate can dramatically affect cost and latency.</li><li><strong>Fork operations need to share the parent&#39;s prefix.</strong> If you need to run a side computation (compaction, summarization, skill execution), use identical cache-safe parameters so you get cache hits on the parent&#39;s prefix.</li></ol><p>Claude Code is built around prompt caching from day one, you should do the same if you&#39;re building an agent.</p><hr><h2 id="研究摘要" tabindex="-1">研究摘要 <a class="header-anchor" href="#研究摘要" aria-label="Permalink to &quot;研究摘要&quot;">​</a></h2><p><strong>TL;DR</strong>：Claude Code 官方揭露 prompt caching 五大原則：static-first 排列、動態資訊用 messages 傳遞、model/tool 切換限制、cache-safe compaction forking。</p><h3 id="核心論點" tabindex="-1">核心論點 <a class="header-anchor" href="#核心論點" aria-label="Permalink to &quot;核心論點&quot;">​</a></h3><ol><li><strong>Static-first prefix 排列</strong>：System prompt → CLAUDE.MD → Session context → Messages。任何前綴改動都 invalidate 後續所有 cache。時間戳放 system prompt、非確定性 tool 排序是最常見 cache break 原因。</li><li><strong><code>&lt;system-reminder&gt;</code> 傳動態資訊</strong>：時間、檔案更新等動態資訊放 user message 而非 system prompt，保護 cached prefix 不被打破。</li><li><strong>Model 切換透過 subagent handoff</strong>：100k token 對話中途切 Haiku 比留在 Opus 更貴（需重建 Haiku cache）；切換需 subagent 準備 handoff message。</li><li><strong>Tool 永遠不增刪</strong>：Plan Mode 用 <code>EnterPlanMode</code>/<code>ExitPlanMode</code> 工具代替切換 tool set；MCP 大量工具用 <code>defer_loading: true</code> stubs，按需展開 full schema。</li><li><strong>Compaction 需 cache-safe forking</strong>：compact 時沿用完全相同 system prompt + tools，prepend 原始 messages，只在末尾新增 compact prompt——僅計費新 token，prefix 全部命中 cache。</li></ol><h3 id="關鍵技術細節" tabindex="-1">關鍵技術細節 <a class="header-anchor" href="#關鍵技術細節" aria-label="Permalink to &quot;關鍵技術細節&quot;">​</a></h3><ul><li><code>cache_control</code> breakpoint 定義 cache 邊界（Anthropic API 欄位）</li><li><code>defer_loading: true</code>：Tool Search 機制的 lightweight stub 實作</li><li>Compaction buffer：預留 context window 空間給 compact prompt + 輸出 token</li><li>SEV 制度：cache hit rate 下降觸發事故回應，量化為 SLO 指標</li></ul><h3 id="批判性觀察" tabindex="-1">批判性觀察 <a class="header-anchor" href="#批判性觀察" aria-label="Permalink to &quot;批判性觀察&quot;">​</a></h3><ol><li><strong>第一手資料，缺量化數字</strong>：Thariq 為 Claude Code PM，資料可信度高；但未提供具體 cache hit rate 目標值或成本節省百分比。</li><li><strong><code>defer_loading</code> 已對應本 workspace 現況</strong>：本 workspace ToolSearch + deferred tools 設計與文章描述完全吻合，驗證方向正確，可作為此設計的官方佐證。</li><li><strong>Cache-safe compaction 原則影響 <code>/compact</code> 使用策略</strong>：本 workspace 若自定義 compact 流程需遵守相同 prefix 規則，否則 compact 本身會觸發昂貴的 full-price token 計費。</li></ol><hr><h2 id="_5-維度評分" tabindex="-1">5 維度評分 <a class="header-anchor" href="#_5-維度評分" aria-label="Permalink to &quot;5 維度評分&quot;">​</a></h2><table tabindex="0"><thead><tr><th>維度</th><th>權重</th><th>分數</th><th>說明</th></tr></thead><tbody><tr><td>A. Workspace 可行動性</td><td>30%</td><td>9.0</td><td>直接對應 CLAUDE.md、prompt-caching-rules.md、harness-design.md 現有規則</td></tr><tr><td>B. 創新性</td><td>20%</td><td>8.0</td><td>Cache-safe forking + defer_loading 為首次官方完整揭露</td></tr><tr><td>C. 證據品質</td><td>20%</td><td>8.0</td><td>官方 PM 第一手，SEV 制度具體佐證</td></tr><tr><td>D. 技術深度</td><td>15%</td><td>9.0</td><td>涵蓋 API 機制、Plan Mode 設計、compaction buffer 細節</td></tr><tr><td>E. 泛化性</td><td>15%</td><td>9.0</td><td>原則適用所有 agentic LLM 產品</td></tr></tbody></table><p><strong>加權總分</strong>：9.0×0.3 + 8.0×0.2 + 8.0×0.2 + 9.0×0.15 + 9.0×0.15 = <strong>8.60/10</strong></p><p><strong>整合決策</strong>：Rule（系統性設計規則，適合納入 <code>.claude/refs/prompt-caching-rules.md</code>）</p><hr><h2 id="後續行動建議" tabindex="-1">後續行動建議 <a class="header-anchor" href="#後續行動建議" aria-label="Permalink to &quot;後續行動建議&quot;">​</a></h2><ul><li>更新 <code>.claude/refs/prompt-caching-rules.md</code>：補充 cache-safe forking 原則（compaction 使用相同 system prompt + tools）</li><li>確認 <code>.claude/refs/harness-design.md</code>：defer_loading stub 設計已對齊，可加入官方來源引用</li><li>考慮新增規則：「mid-session model 切換紅線」明文記錄成本反直覺案例</li></ul>',61)])])}const m=t(n,[["render",r]]);export{u as __pageData,m as default};
