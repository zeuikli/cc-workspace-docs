import{c as e,Q as a,j as n,m as t}from"./chunks/framework.BXc4yaFE.js";const u=JSON.parse('{"title":"Loop and Harness engineering: 7 files, 5 steps. Every config inside","description":"","frontmatter":{"url":"https://x.com/ArchiveExplorer/status/2071192832455430283","title":"Loop and Harness engineering: 7 files, 5 steps. Every config inside","author":"ArchiveExplorer","archived":"2026-06-28T00:00:00.000Z","handle":"@ArchiveExplorer","published":"2026-06-28T00:00:00.000Z","stats":"46 likes · 4 retweets · 58 bookmarks · 1437 views","tags":["harness","loop-engineering","claude-code","skills","hooks","agents","context-rot","verifier"],"type":"twitter-article"},"headers":[],"relativePath":"research/tweets/2026-06-28-@ArchiveExplorer-430283.md","filePath":"research/tweets/2026-06-28-@ArchiveExplorer-430283.md","lastUpdated":1785909234000}'),o={name:"research/tweets/2026-06-28-@ArchiveExplorer-430283.md"};function i(p,s,r,l,c,h){return a(),n("div",null,[...s[0]||(s[0]=[t(`<h1 id="loop-and-harness-engineering-7-files-5-steps-every-config-inside-—-archiveexplorer" tabindex="-1">Loop and Harness engineering: 7 files, 5 steps. Every config inside — @ArchiveExplorer <a class="header-anchor" href="#loop-and-harness-engineering-7-files-5-steps-every-config-inside-—-archiveexplorer" aria-label="Permalink to &quot;Loop and Harness engineering: 7 files, 5 steps. Every config inside — @ArchiveExplorer&quot;">​</a></h1><p><strong>來源</strong>：<a href="https://x.com/ArchiveExplorer/status/2071192832455430283" target="_blank" rel="noreferrer">https://x.com/ArchiveExplorer/status/2071192832455430283</a><strong>作者</strong>：ArchiveExplorer（@ArchiveExplorer） <strong>發布日期</strong>：2026-06-28 <strong>收錄日期</strong>：2026-06-28 <strong>類型</strong>：Twitter Article <strong>統計</strong>：46 likes · 4 retweets · 58 bookmarks · 1437 views</p><hr><h2 id="全文" tabindex="-1">全文 <a class="header-anchor" href="#全文" aria-label="Permalink to &quot;全文&quot;">​</a></h2><p>Most builders fight the loop. The loop is fine. The folder underneath isn&#39;t set up. Open .claude/ in any working Claude Code project and you find roughly seven things doing the actual work: CLAUDE.md, settings.json, hooks/, agents/, skills/, .mcp.json, and a state file like MEMORY.md. Most builders have opened one of those files. Maybe two. That is why their loops stall on the third iteration. By the end of this article you will know what each file does, the five loop steps that ride on top, the three failure modes that kill most first attempts, and the single next file to add tonight. No framework. No subscription. One walkthrough with exact paths and exact contents. The harness is the floor. Pour it first.</p><h1 id="two-layers-one-setup" tabindex="-1">Two layers, one setup <a class="header-anchor" href="#two-layers-one-setup" aria-label="Permalink to &quot;Two layers, one setup&quot;">​</a></h1><p>The harness is the .claude/ folder. It does not change between runs. The loop is what runs inside it: a goal, an action, a verification step, a memory write, and a decision to keep going or stop. The harness is the kitchen. The loop is the recipe. Both fail without the other. A kitchen with no recipe is unused space. A recipe with no kitchen is wishful thinking. Most builders treat the whole thing as one blob (&quot;my agent setup&quot;) and miss that failures live in different layers. Token blowups, prompt fatigue, dropped permissions: harness problems. Loops that never converge, verifications that pass garbage, scheduled runs that drift: loop problems. Naming the layer fixes the diagnosis. You stop rewriting prompts when the real bug is a missing permission. I thought building the loop first would teach me which harness files I needed. It was the other way around. The harness sets what each iteration is allowed to do. Permissions decide whether the loop can write to disk. Subagents decide whether verification runs in a clean context. Skills decide whether the loop can specialize. Hooks decide whether the loop even gets to fire on the trigger you wanted. Without those decisions locked in, the loop guesses. When the loop guesses, it fabricates: invented files, invented commands, passing tests that pass nothing. The harness stops the guessing. So the order is harness first, loop second, always.</p><h1 id="the-harness-file-by-file" tabindex="-1">The harness, file by file <a class="header-anchor" href="#the-harness-file-by-file" aria-label="Permalink to &quot;The harness, file by file&quot;">​</a></h1><h2 id="claude-md" tabindex="-1">CLAUDE.md <a class="header-anchor" href="#claude-md" aria-label="Permalink to &quot;CLAUDE.md&quot;">​</a></h2><p>The first file Claude Code reads on every launch. Its contents become standing context for the entire session. Put the project shape there: directory layout, language and framework, commands that actually work, conventions the agent must respect, and an explicit list of things it must not do. Lives at repo root, not buried in docs. Minimal working shape:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># Project: my-app</span></span>
<span class="line"><span>Stack: Next.js 14, TypeScript, Postgres, Tailwind.</span></span>
<span class="line"><span>Layout: \`app/\` (routes), \`lib/\` (helpers), \`db/migrations/\`.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>## Commands</span></span>
<span class="line"><span>- \`pnpm dev\` - local</span></span>
<span class="line"><span>- \`pnpm test\` - vitest</span></span>
<span class="line"><span>- \`pnpm db:migrate\` - apply migrations</span></span>
<span class="line"><span></span></span>
<span class="line"><span>## Never</span></span>
<span class="line"><span>- Edit \`db/migrations/*\` after merge.</span></span>
<span class="line"><span>- Add deps without justification in the PR body.</span></span>
<span class="line"><span>- Bypass \`lib/auth/\` to access user data.</span></span></code></pre></div><p>The trap is bloat. The paper Less Context, Better Agents (arXiv 2606.10209) measured task completion dropping from 91.6% to 71% purely from oversized standing context. Keep it under 300 lines. Prune it weekly. Every added paragraph is a tax on every future turn. The canonical reference is centminmod/my-claude-code-setup, which ships three working CLAUDE.md shapes side by side. [圖 1]</p><h2 id="settings-json" tabindex="-1">settings.json <a class="header-anchor" href="#settings-json" aria-label="Permalink to &quot;settings.json&quot;">​</a></h2><p>Where the tool allowlist, environment variables, and hook registrations live. Two locations matter for daily work: .claude/settings.json at repo root for repo-scoped rules, and ~/.claude/settings.json for your personal defaults. Scope hierarchy resolves managed &gt; project &gt; local &gt; user, so project always overrides personal. The first move that pays off in one afternoon is an allow array for read-only Bash and MCP calls:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{</span></span>
<span class="line"><span>  &quot;permissions&quot;: {</span></span>
<span class="line"><span>    &quot;allow&quot;: [</span></span>
<span class="line"><span>      &quot;Bash(ls:*)&quot;,</span></span>
<span class="line"><span>      &quot;Bash(git status:*)&quot;,</span></span>
<span class="line"><span>      &quot;Bash(git diff:*)&quot;,</span></span>
<span class="line"><span>      &quot;Bash(cat:*)&quot;,</span></span>
<span class="line"><span>      &quot;Read(*)&quot;</span></span>
<span class="line"><span>    ],</span></span>
<span class="line"><span>    &quot;deny&quot;: [</span></span>
<span class="line"><span>      &quot;Bash(rm -rf:*)&quot;,</span></span>
<span class="line"><span>      &quot;Bash(git push --force:*)&quot;</span></span>
<span class="line"><span>    ]</span></span>
<span class="line"><span>  }</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>The agent stops blocking on permission prompts for every ls, git status, cat. Destructive ops still gate. Full key reference: Claude Code docs - Settings. Keep secrets in .claude/settings.local.json and gitignore it.</p><h2 id="hooks" tabindex="-1">hooks <a class="header-anchor" href="#hooks" aria-label="Permalink to &quot;hooks&quot;">​</a></h2><p>Deterministic scripts that fire on tool events: PreToolUse before a tool runs, PostToolUse after, Stop when the agent finishes a turn. Registered inside settings.json with a matcher pattern and a shell command. Canonical first hook: a PostToolUse matching Edit|Write that pipes the file through prettier.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{</span></span>
<span class="line"><span>  &quot;hooks&quot;: {</span></span>
<span class="line"><span>    &quot;PostToolUse&quot;: [</span></span>
<span class="line"><span>      {</span></span>
<span class="line"><span>        &quot;matcher&quot;: &quot;Edit|Write&quot;,</span></span>
<span class="line"><span>        &quot;hooks&quot;: [</span></span>
<span class="line"><span>          {&quot;type&quot;: &quot;command&quot;, &quot;command&quot;: &quot;npx prettier --write \\&quot;$CLAUDE_FILE_PATH\\&quot;&quot;}</span></span>
<span class="line"><span>        ]</span></span>
<span class="line"><span>      }</span></span>
<span class="line"><span>    ]</span></span>
<span class="line"><span>  }</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>Every edit now exits in a known state. This is your policy floor. Without hooks, every run is a vibe. Keep hooks silent on success, loud only on failure. Reference: Claude Code docs - Hooks.</p><h2 id="subagents" tabindex="-1">subagents <a class="header-anchor" href="#subagents" aria-label="Permalink to &quot;subagents&quot;">​</a></h2><p>Live under .claude/agents/ as markdown files with YAML frontmatter. Main agent invokes them through the Task tool. They run in a fresh context window. Minimal verifier subagent:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>---</span></span>
<span class="line"><span>name: verifier</span></span>
<span class="line"><span>description: Reviews a diff against the goal spec. Invoke after every code change.</span></span>
<span class="line"><span>model: haiku</span></span>
<span class="line"><span>tools: [Read, Grep, Bash]</span></span>
<span class="line"><span>---</span></span>
<span class="line"><span></span></span>
<span class="line"><span>You are a verifier. Read the goal spec in \`PROMPT.md\`. Read the diff.</span></span>
<span class="line"><span>Return a JSON verdict: {passes: bool, failures: [{line, reason}]}.</span></span>
<span class="line"><span>Do not propose fixes. Do not run code. Do not be polite.</span></span></code></pre></div><p>The reviewer that lives inside the maker&#39;s context always agrees with itself. Pulling review into a fresh context closes the loudest failure mode. Reference: wshobson/agents (37K stars) for 194 ready-made shapes. For an adversarial verifier with 11 named shortcut-checks (relaxed tests, swallowed errors, fake renames), pull moonrunnerkc/swarm-orchestrator. [圖 2]</p><h2 id="skills" tabindex="-1">skills <a class="header-anchor" href="#skills" aria-label="Permalink to &quot;skills&quot;">​</a></h2><p>Live under .claude/skills/ as folders containing SKILL.md with YAML frontmatter. Load progressively: at session start, only name and description enter context. Full body loads only when the agent decides the trigger matches.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>---</span></span>
<span class="line"><span>name: db-migration-writer</span></span>
<span class="line"><span>description: Writes Postgres migration files for this repo. Use when the user</span></span>
<span class="line"><span>  asks to add/alter a table, column, index, or constraint.</span></span>
<span class="line"><span>when_to_use: schema change requested, new feature requires a new column,</span></span>
<span class="line"><span>  index missing on a hot query path</span></span>
<span class="line"><span>---</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Steps</span></span>
<span class="line"><span>1. Read \`db/schema.sql\` to confirm current state.</span></span>
<span class="line"><span>2. Write the migration to \`db/migrations/NNN_&lt;verb&gt;_&lt;noun&gt;.sql\`.</span></span>
<span class="line"><span>3. Include both up and down. Test with \`pnpm db:migrate --dry\`.</span></span>
<span class="line"><span>4. Never touch existing migration files.</span></span></code></pre></div><p>This discipline keeps a fifty-skill library from costing fifty skills&#39; worth of tokens on every prompt. Canonical pattern: anthropics/skills (155K stars). Maximal pre-built kit: affaan-m/ECC (222K stars). [圖 3] Three skills built when you hit the same task a third time beat fifty skills built speculatively from a tutorial.</p><h2 id="mcp" tabindex="-1">MCP <a class="header-anchor" href="#mcp" aria-label="Permalink to &quot;MCP&quot;">​</a></h2><p>Servers declared in .mcp.json at repo root. Model Context Protocol is the spec that lets the loop call out to live external tools. Three rules: only servers your current work uses, prefer official ones for credentialed tools, never install five &quot;just in case&quot;.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{</span></span>
<span class="line"><span>  &quot;mcpServers&quot;: {</span></span>
<span class="line"><span>    &quot;github&quot;: {</span></span>
<span class="line"><span>      &quot;command&quot;: &quot;npx&quot;,</span></span>
<span class="line"><span>      &quot;args&quot;: [&quot;-y&quot;, &quot;@modelcontextprotocol/server-github&quot;],</span></span>
<span class="line"><span>      &quot;env&quot;: {&quot;GITHUB_TOKEN&quot;: &quot;\${GITHUB_TOKEN}&quot;}</span></span>
<span class="line"><span>    },</span></span>
<span class="line"><span>    &quot;context7&quot;: {</span></span>
<span class="line"><span>      &quot;command&quot;: &quot;npx&quot;,</span></span>
<span class="line"><span>      &quot;args&quot;: [&quot;-y&quot;, &quot;@upstash/context7-mcp&quot;]</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span>  }</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>Anthropic-maintained set: modelcontextprotocol/servers (87K stars). Code-host integration: github/github-mcp-server (31K stars). Live library docs (kills stale-API problems): upstash/context7 (58K stars). Discovery index: punkpeye/awesome-mcp-servers (89K stars). The first mistake is enabling a server with write scope before you have a hook that logs every call.</p><h2 id="state-and-memory" tabindex="-1">state and memory <a class="header-anchor" href="#state-and-memory" aria-label="Permalink to &quot;state and memory&quot;">​</a></h2><p>The seventh piece, the one most people skip until the third project goes sideways. Shape: a MEMORY.md index file at a known path, plus a vault directory for project canon.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>~/.claude/memory/</span></span>
<span class="line"><span>  MEMORY.md            # index, links to topic files below</span></span>
<span class="line"><span>  user-prefs.md        # preferences, terse-vs-verbose, voice</span></span>
<span class="line"><span>  project-decisions.md # &quot;we picked Postgres over Mongo on 2026-03-12, here is why&quot;</span></span>
<span class="line"><span>  feedback-recent.md   # corrections you keep applying</span></span>
<span class="line"><span></span></span>
<span class="line"><span>~/vault/               # project canon (does not change session to session)</span></span>
<span class="line"><span>  architecture.md</span></span>
<span class="line"><span>  api-spec.md</span></span>
<span class="line"><span>  post-mortems/</span></span></code></pre></div><p>Memory holds what changes across sessions. Vault holds what does not. For production-grade session compression (200K-token transcript -&gt; 4K-token recap without losing load-bearing facts): thedotmack/claude-mem (84K stars). Theory behind why this matters: Anthropic engineering on context engineering names the failure mode: context rot. The first mistake is treating memory as append-only. Prune it every session, or it becomes the rot.</p><h1 id="the-loop-on-top-of-the-harness" tabindex="-1">The loop, on top of the harness <a class="header-anchor" href="#the-loop-on-top-of-the-harness" aria-label="Permalink to &quot;The loop, on top of the harness&quot;">​</a></h1><h2 id="_1-goal-spec" tabindex="-1">1. Goal spec <a class="header-anchor" href="#_1-goal-spec" aria-label="Permalink to &quot;1. Goal spec&quot;">​</a></h2><p>The external contract that says what &quot;done&quot; looks like. Lives on disk, not in the agent&#39;s head. The loop re-reads it every iteration. Name: PROMPT.md, AGENTS.md, or AGENT_SPEC.md. The re-read is what matters.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># Goal</span></span>
<span class="line"><span>Migrate \`users.password\` from bcrypt to argon2id across the codebase.</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Done when</span></span>
<span class="line"><span>- All new password writes use argon2id (\`lib/auth/hash.ts\`).</span></span>
<span class="line"><span>- Existing bcrypt hashes are rehashed on next successful login.</span></span>
<span class="line"><span>- Test suite green: \`pnpm test auth\`.</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Never touch</span></span>
<span class="line"><span>- \`db/migrations/*\` already merged.</span></span>
<span class="line"><span>- Anything under \`legacy/\`.</span></span>
<span class="line"><span>- The session cookie format.</span></span>
<span class="line"><span></span></span>
<span class="line"><span># Stop if</span></span>
<span class="line"><span>- More than 3 files outside \`lib/auth/\` need edits.</span></span>
<span class="line"><span>- A test that already passes starts failing.</span></span></code></pre></div><p>Without this file the agent drifts after about three iterations. Smallest possible reference: ghuntley/how-to-ralph-wiggum (1.7K stars) - PROMPT.md plus an IMPLEMENTATION_PLAN.md state file the loop updates in place. When the spec is missing, failure looks like progress. Code is written, tests pass, the goal it solved is not yours.</p><h2 id="_2-plan-to-act-to-verify" tabindex="-1">2. Plan to Act to Verify <a class="header-anchor" href="#_2-plan-to-act-to-verify" aria-label="Permalink to &quot;2. Plan to Act to Verify&quot;">​</a></h2><p>The minimum viable loop is three steps. The agent plans against the goal spec, executes, then a separate verification pass checks the result before the next iteration is allowed to start. Fresh context each iteration is the Ralph pattern. State lives on disk in the spec file plus a running log.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>#!/usr/bin/env bash</span></span>
<span class="line"><span># minimal loop runner: fresh context each turn, state on disk</span></span>
<span class="line"><span>set -euo pipefail</span></span>
<span class="line"><span></span></span>
<span class="line"><span>while true; do</span></span>
<span class="line"><span>  # plan + act in fresh context</span></span>
<span class="line"><span>  claude -p &quot;Read PROMPT.md, IMPLEMENTATION_PLAN.md. Do the next step. Commit on green.&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  # verify in fresh context (different subagent)</span></span>
<span class="line"><span>  if claude -p &quot;/verify&quot;; then</span></span>
<span class="line"><span>    echo &quot;iter ok&quot;</span></span>
<span class="line"><span>  else</span></span>
<span class="line"><span>    echo &quot;verify failed, will retry&quot;</span></span>
<span class="line"><span>  fi</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  # exit when spec says done</span></span>
<span class="line"><span>  grep -q &quot;^STATUS: done$&quot; IMPLEMENTATION_PLAN.md &amp;&amp; break</span></span>
<span class="line"><span>  sleep 5</span></span>
<span class="line"><span>done</span></span></code></pre></div><p>Canonical patterns and CLI starters: cobusgreyling/loop-engineering (3K stars). Production TypeScript reference with verifyCompletion: vercel-labs/ralph-loop-agent (805 stars). Full installable Plan-to-Work-to-Review-to-Release cycle: Chachamaru127/claude-code-harness (2.9K stars). Drop the verify step and confident garbage compounds. Every wrong output becomes the next iteration&#39;s input.</p><h2 id="_3-sub-agent-fan-out" tabindex="-1">3. Sub-agent fan-out <a class="header-anchor" href="#_3-sub-agent-fan-out" aria-label="Permalink to &quot;3. Sub-agent fan-out&quot;">​</a></h2><p>When one goal branches into many independent sub-jobs (analyze 10 articles, fix 5 files, search 8 sources), the loop spawns parallel subagents. Orchestrator synthesizes. One bloated context cannot do this. Ten small ones can.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># claude-agent-sdk-python style fan-out</span></span>
<span class="line"><span>from claude_agent_sdk import Agent, run_parallel</span></span>
<span class="line"><span></span></span>
<span class="line"><span>orchestrator = Agent.load(&quot;.claude/agents/orchestrator.md&quot;)</span></span>
<span class="line"><span>workers = [Agent.load(&quot;.claude/agents/researcher.md&quot;) for _ in range(8)]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>results = run_parallel([</span></span>
<span class="line"><span>    w.run(source=src) for w, src in zip(workers, sources)</span></span>
<span class="line"><span>])</span></span>
<span class="line"><span></span></span>
<span class="line"><span>synthesis = orchestrator.run(inputs=results)</span></span></code></pre></div><p>Anthropic engineering on multi-agent research measured +90.2% on their internal eval against a single-agent baseline. Official SDK: anthropics/claude-agent-sdk-python (7.4K stars). Heaviest public fan-out kit (60+ agent types, 314 MCP tools): ruvnet/ruflo (61K stars). Skip the fan-out and the orchestrator drowns. One context loaded with ten jobs&#39; worth of source material is the exact shape that triggers context rot.</p><h2 id="_4-scheduler-and-persistence" tabindex="-1">4. Scheduler and persistence <a class="header-anchor" href="#_4-scheduler-and-persistence" aria-label="Permalink to &quot;4. Scheduler and persistence&quot;">​</a></h2><p>What triggers the loop when you are not in the chair. cron, launchctl, systemd, a queue runner. The scheduler is deliberately dumber than the agent. If the scheduler tries to think (branch on state, decide whether to skip), it fails silently for days.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># crontab: run the loop every 30 min, log to disk</span></span>
<span class="line"><span>*/30 * * * * cd ~/my-loop &amp;&amp; ./run.sh &gt;&gt; logs/$(date +\\%Y-\\%m-\\%d).log 2&gt;&amp;1</span></span></code></pre></div><p>Or as a launchd plist on macOS:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;key&gt;StartCalendarInterval&lt;/key&gt;</span></span>
<span class="line"><span>&lt;dict&gt;</span></span>
<span class="line"><span>  &lt;key&gt;Minute&lt;/key&gt;&lt;integer&gt;0&lt;/integer&gt;</span></span>
<span class="line"><span>&lt;/dict&gt;</span></span>
<span class="line"><span>&lt;key&gt;WorkingDirectory&lt;/key&gt;&lt;string&gt;/Users/me/my-loop&lt;/string&gt;</span></span>
<span class="line"><span>&lt;key&gt;ProgramArguments&lt;/key&gt;</span></span>
<span class="line"><span>&lt;array&gt;&lt;string&gt;/bin/bash&lt;/string&gt;&lt;string&gt;run.sh&lt;/string&gt;&lt;/array&gt;</span></span></code></pre></div><p>Persistence is the other half. Every iteration must serialize what it did, what it tried, what is next. Otherwise the scheduler wakes up to an agent that forgot the goal. Pattern for promoting ad-hoc sessions into scheduled runs: Kanevry/session-orchestrator.</p><h2 id="_5-failure-modes" tabindex="-1">5. Failure modes <a class="header-anchor" href="#_5-failure-modes" aria-label="Permalink to &quot;5. Failure modes&quot;">​</a></h2><p>Three failure modes kill almost every first attempt: (a) Confident garbage. Verify step missing or weak. Wrong outputs pass and compound across iterations. (b) Context rot. Single long context where the model degrades past a threshold (Anthropic&#39;s term). Accuracy collapses around 200K tokens of accumulated history. (c) Ralph Wiggum loops. Same iteration repeats because state on disk did not capture progress. The agent re-plans the step it already finished. The Less Context, Better Agents paper (arXiv 2606.10209) measured full-history at 71% task completion versus prune-and-summarize at 91.6%, on a fraction of the tokens. [圖 4]</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>before: single-context loop, 1.48M tokens, 71% completion, three hidden hallucinations per run</span></span>
<span class="line"><span>after:  prune-and-summarize loop with verifier subagent, 553K tokens, 91.6% completion, every figure traced</span></span></code></pre></div><p>moonrunnerkc/swarm-orchestrator catalogs the 11 shortcuts agents take to fake done: relaxed tests, swallowed errors, fake renames, stub returns, comment-deletion-as-fix. Memorize the names. You will recognize them in your own logs.</p><p>A complete minimal setup wires all seven harness files into a working loop. The shape of a project directory looks like this:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>my-loop/</span></span>
<span class="line"><span>├── .claude/</span></span>
<span class="line"><span>│   ├── CLAUDE.md            # standing context for every session</span></span>
<span class="line"><span>│   ├── settings.json        # allow array + PostToolUse prettier hook</span></span>
<span class="line"><span>│   ├── agents/</span></span>
<span class="line"><span>│   │   └── verifier.md      # Haiku, reviews diffs in fresh context</span></span>
<span class="line"><span>│   └── skills/</span></span>
<span class="line"><span>│       └── db-migration-writer/</span></span>
<span class="line"><span>│           └── SKILL.md     # one skill, used three+ times</span></span>
<span class="line"><span>├── .mcp.json                # github MCP, context7 MCP</span></span>
<span class="line"><span>├── PROMPT.md                # goal spec (loop reads each iteration)</span></span>
<span class="line"><span>├── IMPLEMENTATION_PLAN.md   # state file (loop writes each iteration)</span></span>
<span class="line"><span>├── MEMORY.md                # cross-session preferences</span></span>
<span class="line"><span>├── run.sh                   # the loop runner (Plan -&gt; Act -&gt; Verify)</span></span>
<span class="line"><span>└── logs/                    # persistence, one file per cron tick</span></span></code></pre></div><p>The wiring is one-directional. The harness defines the rules, the loop runs inside them, the state file connects iteration N to iteration N+1. A single iteration walks the seven harness files and the five loop pieces in this order: cron fires run.sh, which calls claude -p. Claude Code reads CLAUDE.md and settings.json (harness 1, 2), applies the PostToolUse hook on every edit (harness 3), reads PROMPT.md and IMPLEMENTATION_PLAN.md (loop step 1), plans and acts (loop step 2), dispatches the verifier subagent in a fresh context (harness 4 + loop step 2 verify), writes the result back to IMPLEMENTATION_PLAN.md (loop step 3), updates MEMORY.md if a new preference was learned (harness 7), exits. Cron waits for the next tick (loop step 4). If any of the seven harness files is missing, a specific loop step degrades. No CLAUDE.md and the planner re-derives the project shape every iteration. No verifier subagent and the verify step happens in the main context and always passes. No MEMORY.md and the same correction gets re-applied every Tuesday. Build the seven harness files once. The loop runs forever.</p><h1 id="what-to-do-tonight" tabindex="-1">What to do tonight <a class="header-anchor" href="#what-to-do-tonight" aria-label="Permalink to &quot;What to do tonight&quot;">​</a></h1><p>Open your .claude/ folder. Run:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ls -la .claude/</span></span></code></pre></div><p>Count the files.</p><blockquote><p>If you see nothing or only settings.json, start with CLAUDE.md. Keep it under 300 lines. Copy a shape from centminmod/my-claude-code-setup. If you have CLAUDE.md and settings.json but no agents/, add a verifier subagent next. Pull review out of the main context. Shape: wshobson/agents. If you have agents/ but no skills/, promote one frequent task to a skill. The prompt you have copy-pasted three times this week. Read three SKILL.md files from anthropics/skills before you write your first one. If you have all seven harness files but no loop running, pick one repeating job, write its goal spec, and put a Plan-Act-Verify loop on top. Closest installable starting point: Chachamaru127/claude-code-harness. After choosing, do one thing: open the matching repo in a new tab and clone it. The harness is the floor. Without it, every loop runs over a hole.</p></blockquote><hr><h2 id="研究摘要" tabindex="-1">研究摘要 <a class="header-anchor" href="#研究摘要" aria-label="Permalink to &quot;研究摘要&quot;">​</a></h2><p><strong>TL;DR</strong>：.claude/ 七文件（Harness）× 五步迴圈（Loop）完整解剖，附可執行 bash loop runner、verifier subagent 範本、settings.json allow/deny 模板，並引用 arXiv 2606.10209 量化背書：prune-and-summarize 91.6% vs full-history 71% 完成率。</p><h3 id="核心論點" tabindex="-1">核心論點 <a class="header-anchor" href="#核心論點" aria-label="Permalink to &quot;核心論點&quot;">​</a></h3><ul><li><strong>兩層模型</strong>（唯一持久設計原則）：Harness（.claude/ 資料夾，靜態不變）= 廚房；Loop（動態執行）= 食譜；失敗診斷需先定位到正確層</li><li><strong>Harness 七文件映射到 Loop 失敗點</strong>： <ul><li>缺 CLAUDE.md → planner 每次重新推導 project shape</li><li>缺 verifier subagent → verify 在 main context 執行（永遠通過）</li><li>缺 MEMORY.md → 同一糾錯每週重複</li></ul></li><li><strong>三大 Loop 失敗模式</strong>： <ol><li><strong>Confident garbage</strong>：沒有獨立 verify step，錯誤輸出堆疊</li><li><strong>Context rot</strong>（Anthropic 術語）：單一長 context 在 200K token 後準確率崩塌</li><li><strong>Ralph Wiggum loops</strong>：state 未序列化到磁碟，下一次 iteration 重做已完成步驟</li></ol></li><li><strong>11 種 agent 假裝完成的捷徑</strong>（moonrunnerkc/swarm-orchestrator）：relaxed tests、swallowed errors、fake renames、stub returns、comment-deletion-as-fix 等</li><li><strong>scope 優先序</strong>：managed &gt; project &gt; local &gt; user</li></ul><h3 id="關鍵數據-工具" tabindex="-1">關鍵數據 / 工具 <a class="header-anchor" href="#關鍵數據-工具" aria-label="Permalink to &quot;關鍵數據 / 工具&quot;">​</a></h3><ul><li><strong>arXiv 2606.10209</strong> &quot;Less Context, Better Agents&quot;：91.6%（prune+verifier subagent, 553K tokens）vs 71%（full-history, 1.48M tokens）</li><li><strong>Anthropic multi-agent 內部評測</strong>：+90.2% vs single-agent baseline</li><li><strong>CLAUDE.md 硬上限</strong>：&lt; 300 行（每增一段 = 每次 turn 的稅）</li><li><strong>Context rot 閾值</strong>：~200K tokens 累積 history</li><li><strong>Memory vs Vault 分層</strong>：memory（跨 session 變動）/ vault（不變的 project canon）</li><li>Repos：wshobson/agents (37K)、anthropics/skills (155K)、affaan-m/ECC (222K)、ruvnet/ruflo (61K, 314 MCP tools)、thedotmack/claude-mem (84K)</li></ul><h3 id="批判性觀察" tabindex="-1">批判性觀察 <a class="header-anchor" href="#批判性觀察" aria-label="Permalink to &quot;批判性觀察&quot;">​</a></h3><ul><li><strong>arXiv 2606.10209 引用可信</strong>：具體實驗數字（1.48M→553K tokens, 71%→91.6% completion），需獨立驗證但引用格式正確</li><li><strong>moonrunnerkc/swarm-orchestrator 11 shortcuts</strong> 是已知但少見成文的 failure pattern，極具 workspace 價值</li><li><strong>文章本身即 harness 示範</strong>：用結構化文件描述結構化 setup，自我印證兩層模型</li><li><strong>低曝光（1437 views）與高密度</strong>：有別於病毒式行銷文，內容密度遠高於傳播量，是罕見的優質低調文章</li></ul><hr><h2 id="評分" tabindex="-1">評分 <a class="header-anchor" href="#評分" aria-label="Permalink to &quot;評分&quot;">​</a></h2><table tabindex="0"><thead><tr><th>維度</th><th>分數</th><th>說明</th></tr></thead><tbody><tr><td>A. Workspace 可行動性 (30%)</td><td>9/10</td><td>與本 workspace 架構完全吻合；settings.json allow/deny、verifier subagent 範本、MEMORY vs Vault 分層直接可用；arXiv 數據支持 context-management.md</td></tr><tr><td>B. 創新性 (20%)</td><td>8/10</td><td>Harness/Loop 兩層模型清晰化；11 shortcuts agent fake done 是具體新框架；7→5 映射關係明確</td></tr><tr><td>C. 證據品質 (20%)</td><td>8/10</td><td>arXiv 2606.10209 可引用；Anthropic 內部 +90.2% eval 有出處；11 shortcuts 有名稱列表</td></tr><tr><td>D. 技術深度 (15%)</td><td>9/10</td><td>附可執行 bash loop runner、完整 settings.json、verifier frontmatter、fan-out code、crontab 模板</td></tr><tr><td>E. 泛化性 (15%)</td><td>8/10</td><td>適用所有 Claude Code 專案；Harness/Loop 兩層概念可跨 harness 使用</td></tr><tr><td><strong>加權總分</strong></td><td><strong>8.55/10</strong></td><td>9×0.3+8×0.2+8×0.2+9×0.15+8×0.15</td></tr></tbody></table><p><strong>整合決策</strong>：Rule <strong>理由</strong>：</p><ol><li>arXiv 2606.10209（91.6% vs 71%）量化支持 context pruning 紀律，可補充 context-management.md</li><li>Harness（靜態）vs Loop（動態）兩層模型是可持久化的 debug 框架</li><li>11 shortcuts（moonrunnerkc/swarm-orchestrator）直接呼應 core.md TEST 階段的 unverified_success gate</li><li>MEMORY（跨 session 變動）/ Vault（project canon）分層補充現有 memory 設計</li></ol>`,81)])])}const g=e(o,[["render",i]]);export{u as __pageData,g as default};
