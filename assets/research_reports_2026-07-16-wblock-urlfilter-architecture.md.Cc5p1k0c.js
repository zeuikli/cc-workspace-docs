import{c as t,Q as r,j as o,m as a}from"./chunks/framework.BXc4yaFE.js";const u=JSON.parse('{"title":"wBlock × Apple URL Filters API 整合架構方案","description":"","frontmatter":{},"headers":[],"relativePath":"research/reports/2026-07-16-wblock-urlfilter-architecture.md","filePath":"research/reports/2026-07-16-wblock-urlfilter-architecture.md","lastUpdated":1785909234000}'),i={name:"research/reports/2026-07-16-wblock-urlfilter-architecture.md"};function s(n,e,d,l,c,p){return r(),o("div",null,[...e[0]||(e[0]=[a(`<h1 id="wblock-×-apple-url-filters-api-整合架構方案" tabindex="-1">wBlock × Apple URL Filters API 整合架構方案 <a class="header-anchor" href="#wblock-×-apple-url-filters-api-整合架構方案" aria-label="Permalink to &quot;wBlock × Apple URL Filters API 整合架構方案&quot;">​</a></h1><blockquote><p>目標形狀：向 upstream <code>0xCUB3/wBlock</code> 開 PR 的增量能力。 權威來源分級：<strong>SDK 實測（macOS 26 SDK）&gt; 已驗證網路研究 &gt; repo 實測 &gt; 推斷</strong>。凡標 <code>[claim:asserted]</code> 者為推斷，implement 前須驗；<code>[claim:verified]</code> 為本規劃已親跑 grep/read 驗證。 日期：2026-07-16　模式：ceiling（Reasoning Sandwich + Decision-Log + reverse peer-review 已內化）</p></blockquote><hr><h2 id="_0-前提校準-已驗證的-repo-事實" tabindex="-1">0. 前提校準（已驗證的 repo 事實） <a class="header-anchor" href="#_0-前提校準-已驗證的-repo-事實" aria-label="Permalink to &quot;0. 前提校準（已驗證的 repo 事實）&quot;">​</a></h2><table tabindex="0"><thead><tr><th>事實</th><th>驗證方式</th><th>標籤</th></tr></thead><tbody><tr><td>deployment target macOS 12.3 / iOS 15.4</td><td><code>grep DEPLOYMENT_TARGET project.pbxproj</code></td><td><code>[claim:verified]</code></td></tr><tr><td>零 NetworkExtension / NEURLFilter 引用（綠地）</td><td><code>grep -rl NetworkExtension --include=*.swift</code> → 空</td><td><code>[claim:verified]</code></td></tr><tr><td>App Group base = <code>group.skula.wBlock</code>；macOS 走 team-prefix 解析（<code>GroupIdentifier.resolvedMacOSGroupIdentifier()</code>），iOS 用 base</td><td>read <code>GroupIdentifier.swift</code></td><td><code>[claim:verified]</code></td></tr><tr><td>共用 framework <code>wBlockCoreService</code>；含 <code>FilterPreprocessor</code>（actor）、<code>RemoveParamDNRRuleGenerator</code>（保守 AdGuard 子集轉換 enum precedent）、<code>ContentBlockerTargets</code></td><td>ls + read</td><td><code>[claim:verified]</code></td></tr><tr><td>content blocker target 以 slot 對應 <code>rules_{category}_{platform}.json</code>，經 App Group 共享</td><td>read <code>ContentBlockerTargets.swift</code></td><td><code>[claim:verified]</code></td></tr><tr><td><code>FilterListCategory</code> enum：ads/privacy/security/multipurpose/annoyances/experimental/custom/foreign/scripts</td><td>read</td><td><code>[claim:verified]</code></td></tr></tbody></table><p><strong>關鍵設計槓桿</strong>：<code>RemoveParamDNRRuleGenerator</code> 是「AdGuard 規則 → 受限能力子集，保守跳過不可表達者」的<strong>現成慣例</strong>（其註解明言 &quot;conservative... skipped rather than widened&quot;）。URL Filter 的 Bloom 轉譯層應<strong>鏡像此 enum 的形狀</strong>（<code>public enum</code> + 靜態 <code>generate</code>、跳過而非放寬、產出寫 App Group），這是 upstream PR 最易被接受的形狀。<code>[claim:judgment]</code></p><hr><h2 id="_1-方案總覽" tabindex="-1">1. 方案總覽 <a class="header-anchor" href="#_1-方案總覽" aria-label="Permalink to &quot;1. 方案總覽&quot;">​</a></h2><h3 id="_1-1-架構圖" tabindex="-1">1.1 架構圖 <a class="header-anchor" href="#_1-1-架構圖" aria-label="Permalink to &quot;1.1 架構圖&quot;">​</a></h3><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart TB</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  subgraph app[&quot;wBlock 主 App (macOS 26+/iOS 26+ 條件啟用)&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    UI[&quot;SwiftUI: URLFilterSettingsView\\n(新增, @available gate)&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    URLFilterCoordinator[&quot;URLFilterManagerCoordinator\\n(NEURLFilterManager.shared 封裝)&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  subgraph core[&quot;wBlockCoreService (共用 framework)&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    FP[&quot;FilterPreprocessor (既有)&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    BLOOM[&quot;BloomEntryExtractor (新)\\nAdGuard → 可轉集合&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    PREFILTER[&quot;PrefilterBuilder (新)\\n集合 → Bloom 位圖 blob&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    GRP[&quot;GroupIdentifier (既有, 重用)&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  subgraph ext[&quot;新 AppExtension target&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    CP[&quot;URLFilterControlProvider\\n: NEURLFilterControlProvider&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  subgraph apple[&quot;Apple / 基礎設施&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    NEMGR[&quot;NEURLFilterManager.shared&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    SYS[&quot;系統網路層\\n(WebKit/URLSession 自動攔截)&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    PIR[&quot;PIR Server (必要配置)\\npir + PrivacyPass issuer + OHTTP relay&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  UI --&gt; URLFilterCoordinator</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  URLFilterCoordinator --&gt;|setConfiguration + save/loadFromPreferences| NEMGR</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  FP --&gt; BLOOM --&gt; PREFILTER</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  PREFILTER --&gt;|寫入 App Group prefilter blob| GRP</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  CP --&gt;|fetchPrefilter 讀 App Group| GRP</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  NEMGR --&gt;|controlProviderBundleIdentifier| CP</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  NEMGR --&gt; SYS</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  SYS --&gt;|Bloom 命中 → 私密查詢| PIR</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  CP --&gt;|prefilter 由 App 端預算, extension 只回傳| NEMGR</span></span></code></pre></div><h3 id="_1-2-元件清單-新增-變更" tabindex="-1">1.2 元件清單（新增/變更） <a class="header-anchor" href="#_1-2-元件清單-新增-變更" aria-label="Permalink to &quot;1.2 元件清單（新增/變更）&quot;">​</a></h3><table tabindex="0"><thead><tr><th>類型</th><th>名稱</th><th>位置</th><th>說明</th></tr></thead><tbody><tr><td>新 target</td><td><code>URLFilterControlProvider</code>（App Extension，NEURLFilterControlProvider protocol）</td><td>新目錄 <code>wBlock URLFilter/</code> + <code>(iOS)</code> 對應</td><td><code>start()/stop(reason:)/fetchPrefilter(existingPrefilterTag:)</code>；只讀 App Group 的 prefilter blob 並封成 <code>NEURLFilterPrefilter</code> 回傳。<strong>不做規則轉換</strong>（轉換在 App 端 CoreService 完成，extension memory/time 受限）<code>[claim:judgment]</code></td></tr><tr><td>新 CoreService 模組</td><td><code>BloomEntryExtractor.swift</code></td><td><code>wBlockCoreService/</code></td><td>AdGuard 規則 → 可轉 URL/domain 條目集合（見 §4）。純字串處理，可全平台編譯（不需 <code>@available</code>）</td></tr><tr><td>新 CoreService 模組</td><td><code>PrefilterBuilder.swift</code></td><td><code>wBlockCoreService/</code></td><td>條目集合 → Bloom 位圖（bitCount/hashCount/murmurSeed + murmur3）；序列化為 App Group blob + tag（內容 hash）</td></tr><tr><td>新 CoreService 模組</td><td><code>URLFilterManagerCoordinator.swift</code></td><td><code>wBlockCoreService/</code>（<code>@available(macOS 26.0, iOS 26.0, *)</code>）</td><td>封裝 <code>NEURLFilterManager.shared</code>：setConfiguration / save+loadFromPreferences / status AsyncSequence / resetPIRCache</td></tr><tr><td>UI 變更</td><td><code>URLFilterSettingsView.swift</code> + 設定入口</td><td><code>wBlock/</code> SwiftUI</td><td>條件顯示（執行期 <code>if #available</code>）：啟用開關、PIR 端點狀態、fail-open/closed 選擇、prefilter 覆蓋率顯示、隱私說明。<strong>舊 OS 完全不渲染此區塊</strong></td></tr><tr><td>entitlement</td><td><code>com.apple.developer.networking.networkextension.url-filter-provider</code></td><td>新 extension + App</td><td>dev-signed 豁免 OHTTP relay 申請；distribution 需 Apple 核准</td></tr><tr><td>pbxproj</td><td>2 新 target（mac/iOS）+ Embed App Extensions build phase</td><td><code>wBlock.xcodeproj</code></td><td>高風險手術，見 Phase 3</td></tr></tbody></table><hr><h2 id="_2-分階段實作計畫" tabindex="-1">2. 分階段實作計畫 <a class="header-anchor" href="#_2-分階段實作計畫" aria-label="Permalink to &quot;2. 分階段實作計畫&quot;">​</a></h2><blockquote><p>每階段 Done-when 為<strong>機械可驗證</strong>條件。檔位建議依「off-rails 程度 + 不可逆性 + 架構決策密度」，非依行數。</p></blockquote><h3 id="phase-0-—-可行性-spike-隔離驗證-不進主-codebase" tabindex="-1">Phase 0 — 可行性 spike（隔離驗證，不進主 codebase） <a class="header-anchor" href="#phase-0-—-可行性-spike-隔離驗證-不進主-codebase" aria-label="Permalink to &quot;Phase 0 — 可行性 spike（隔離驗證，不進主 codebase）&quot;">​</a></h3><ul><li><strong>內容</strong>：獨立最小 Xcode 專案，單一 target 實作 <code>NEURLFilterControlProvider</code> + <code>NEURLFilterManager.setConfiguration</code> + 一個 stub PIR（或 Apple 範例 PIR server）。驗證：(a) entitlement 在 dev-signed 下可載入；(b) <code>verdict(for:)</code> 對 deny 條目回 <code>.deny</code>；(c) 無 PIR 可達時 <code>shouldFailClosed</code> 兩種語義的實際行為；(d) stub-PIR 是否可配置（決定模式 C 是否成立）。</li><li><strong>Done-when</strong>：<code>NEURLFilterManager.shared.status</code> AsyncSequence 產出 active 狀態（log 展示前5/後5行）；一個已知 deny URL 的 <code>verdict(for:)</code> 回 <code>.deny</code>、一個 allow URL 回 <code>.allow</code>/<code>.unknown</code>。</li><li><strong>建議檔位</strong>：<strong>Opus</strong>（無先例、SDK 語義+PIR+entitlement 三重 unknown，off-rails，架構決策密集）。</li><li><strong>產出</strong>：spike 報告（PIR 是否真的無法繞過、fail-open 是否可行、stub-PIR 可行性）回饋 Phase 1 決策矩陣。</li></ul><h3 id="phase-1-—-coreservice-轉譯層-純資料-全平台編譯" tabindex="-1">Phase 1 — CoreService 轉譯層（純資料，全平台編譯） <a class="header-anchor" href="#phase-1-—-coreservice-轉譯層-純資料-全平台編譯" aria-label="Permalink to &quot;Phase 1 — CoreService 轉譯層（純資料，全平台編譯）&quot;">​</a></h3><ul><li><strong>內容</strong>：<code>BloomEntryExtractor</code> + <code>PrefilterBuilder</code>，鏡像 <code>RemoveParamDNRRuleGenerator</code> 慣例（<code>public enum</code>、保守跳過、單元測試覆蓋）。無 NE 依賴 → 可在 macOS 12.3 編譯與測試。</li><li><strong>Done-when</strong>：<code>swift test</code> 綠；給定固定 AdGuard 樣本（含 URL block / domain / cosmetic / exception 混合），extractor 產出的可轉條目數 == 預期黃金值；Bloom <code>contains()</code> 對 in-set URL 全命中、對 known-absent URL 誤判率 &lt; 設定閾值（統計測試）。</li><li><strong>建議檔位</strong>：<strong>Sonnet</strong>（on-rails，有 <code>RemoveParamDNRRuleGenerator</code> 明確 precedent 可鏡像；murmur3/Bloom 是標準演算法）。</li></ul><h3 id="phase-2-—-urlfiltermanagercoordinator-ui-條件啟用" tabindex="-1">Phase 2 — URLFilterManagerCoordinator + UI（條件啟用） <a class="header-anchor" href="#phase-2-—-urlfiltermanagercoordinator-ui-條件啟用" aria-label="Permalink to &quot;Phase 2 — URLFilterManagerCoordinator + UI（條件啟用）&quot;">​</a></h3><ul><li><strong>內容</strong>：<code>@available</code> 封裝 manager；SwiftUI 設定頁；執行期 <code>if #available</code> gate。</li><li><strong>Done-when</strong>：舊 OS 建置（<code>#available</code> false 分支）UI 不含 URL Filter 區塊且既有 content blocker 測試全綠（<strong>回歸不變式</strong>）；macOS 26 上開關可觸發 <code>saveToPreferences()</code> 成功（狀態 log 展示）。</li><li><strong>建議檔位</strong>：<strong>Sonnet</strong>（SwiftUI + available gate 是 repo 既有慣例；coordinator 封裝 SDK 已由 Phase 0 澄清）。</li></ul><h3 id="phase-3-—-extension-target-pbxproj-整合-高風險手術" tabindex="-1">Phase 3 — Extension target + pbxproj 整合（高風險手術） <a class="header-anchor" href="#phase-3-—-extension-target-pbxproj-整合-高風險手術" aria-label="Permalink to &quot;Phase 3 — Extension target + pbxproj 整合（高風險手術）&quot;">​</a></h3><ul><li><strong>內容</strong>：新增 2 個 App Extension target、entitlements、Embed build phase、App Group 共享設定；control provider 讀 App Group blob 回傳 prefilter。</li><li><strong>Done-when</strong>：<code>xcodebuild -scheme wBlock build</code> 對 mac+iOS 皆成功；extension bundle 出現在 <code>.app/Contents/PlugIns</code>（iOS <code>PlugIns</code>）；<code>codesign -d --entitlements</code> 顯示 url-filter-provider entitlement；既有 12 content blocker target 仍全數 build。</li><li><strong>建議檔位</strong>：<strong>Opus</strong>（pbxproj 手動編輯是最脆弱、不可逆性高、team-prefix App Group 路徑差異 edge case；破壞既有 target 風險最高）。⚠ <strong>APPLY 前 gate</strong>：pbxproj diff 摘要 + <code>git diff --stat</code> 確認只動預期 target。</li></ul><h3 id="phase-4-—-端到端-pir-決策落地-隱私文件" tabindex="-1">Phase 4 — 端到端 + PIR 決策落地 + 隱私文件 <a class="header-anchor" href="#phase-4-—-端到端-pir-決策落地-隱私文件" aria-label="Permalink to &quot;Phase 4 — 端到端 + PIR 決策落地 + 隱私文件&quot;">​</a></h3><ul><li><strong>內容</strong>：接上 Phase 3 決策矩陣選定的 PIR 模式；<code>PRIVACY_POLICY.md</code> 增補 URL Filter/PIR 隱私說明；README 增補條件能力說明。</li><li><strong>Done-when</strong>：真機/VM macOS 26 上，一條 wBlock ads 清單條目對應的追蹤 URL 被系統層攔截（實際瀏覽觀測，非 verdict 自報）；隱私文件 diff 含 PIR 保證段落。</li><li><strong>建議檔位</strong>：<strong>Opus</strong>（端到端整合 + 隱私立場論述 + upstream PR 敘事，判斷密集）。</li></ul><hr><h2 id="_3-pir-基礎設施決策矩陣" tabindex="-1">3. PIR 基礎設施決策矩陣 <a class="header-anchor" href="#_3-pir-基礎設施決策矩陣" aria-label="Permalink to &quot;3. PIR 基礎設施決策矩陣&quot;">​</a></h2><blockquote><p><strong>硬事實</strong>：<code>setConfiguration</code> 無 PIR-free 過載 → PIR server URL 是<strong>必要參數</strong>。純 prefilter-only 模式在 SDK 層<strong>無法配置</strong>（除非 Apple 允許指向一個「永遠回 unknown」的 stub PIR，見 open question 2）。<code>[claim:verified from SDK]</code></p></blockquote><table tabindex="0"><thead><tr><th>模式</th><th>隱私</th><th>成本</th><th>誤判/覆蓋</th><th>upstream 可行性</th><th>裁決</th></tr></thead><tbody><tr><td><strong>A. 自建 PIR server</strong></td><td>最佳（wBlock 全掌控，需自證不記 log）</td><td>高（伺服器營運 + OHTTP relay + PrivacyPass issuer + Apple 核准）</td><td>完整 URL 精確查詢</td><td>低（開源專案難承諾長期營運基礎設施）</td><td><strong>不建議作為預設</strong>；文件列為 self-host 選項</td></tr><tr><td><strong>B. 託管/社群共享 PIR</strong></td><td>PIR 密碼學保證 server 看不到查詢內容（PIR 賣點）；metadata/可用性依賴第三方</td><td>中（分攤）</td><td>完整</td><td>中（需信任營運方 + 資金）</td><td><strong>開放問題</strong>，需 maintainer 裁決</td></tr><tr><td><strong>C. Stub-PIR / prefilter-only 退化</strong></td><td>最佳（無查詢離開裝置）</td><td>最低</td><td><strong>僅 Bloom 快篩</strong>：命中即 deny（誤判擋錯站）、未命中放行；無 PIR 二次確認 → false positive 無補救</td><td>高（無基礎設施依賴）</td><td><strong>建議預設</strong>，前提是 Phase 0 spike 證實 stub-PIR 可配置且不 fail-closed 全擋</td></tr></tbody></table><p><strong>退化行為（Constraint b）</strong>：</p><ul><li><code>shouldFailClosed = false</code>（<strong>建議預設</strong>，隱私工具不應把使用者鎖在網外）：PIR 不可達 → 放行（fail-open）。風險：攔截失效但不破壞瀏覽。</li><li><code>shouldFailClosed = true</code>：PIR 不可達 → deny，可能大面積擋網 → <strong>不設為預設</strong>，僅進階選項並明確警告。</li><li><strong>無 PIR server 部署時</strong>：若模式 C 的 stub-PIR 不可行（Phase 0 待證），則 URL Filter 能力<strong>整體不啟用</strong>（開關 disabled + 說明），content blocker 照常運作 —— 不得因缺 PIR 而降級既有功能。</li></ul><p><strong>開發階段（Constraint c）</strong>：dev-signed build 豁免 OHTTP relay 申請 → Phase 0/1/2 全程用 dev signing + 本地/Apple 範例 PIR，不需 Apple 核准即可迭代。</p><hr><h2 id="_4-規則轉譯層-spec-bloomentryextractor-prefilterbuilder" tabindex="-1">4. 規則轉譯層 spec（BloomEntryExtractor + PrefilterBuilder） <a class="header-anchor" href="#_4-規則轉譯層-spec-bloomentryextractor-prefilterbuilder" aria-label="Permalink to &quot;4. 規則轉譯層 spec（BloomEntryExtractor + PrefilterBuilder）&quot;">​</a></h2><h3 id="_4-1-輸入-輸出" tabindex="-1">4.1 輸入 / 輸出 <a class="header-anchor" href="#_4-1-輸入-輸出" aria-label="Permalink to &quot;4.1 輸入 / 輸出&quot;">​</a></h3><ul><li><strong>輸入</strong>：AdGuard 語法規則行（經 <code>FilterPreprocessor</code> 展開 include 後的字串），與既有 content blocker pipeline 同源。</li><li><strong>輸出</strong>：<code>[BloomEntry]</code>（正規化的 host / host+path token 集合）→ <code>PrefilterBlob { tag, bitCount, hashCount, murmurSeed, bitmap: Data }</code> 寫入 App Group（重用 <code>GroupIdentifier.shared.value</code>）。</li></ul><h3 id="_4-2-可轉-不可轉分類-保守-鏡像-removeparamdnrrulegenerator" tabindex="-1">4.2 可轉 / 不可轉分類（保守，鏡像 RemoveParamDNRRuleGenerator） <a class="header-anchor" href="#_4-2-可轉-不可轉分類-保守-鏡像-removeparamdnrrulegenerator" aria-label="Permalink to &quot;4.2 可轉 / 不可轉分類（保守，鏡像 RemoveParamDNRRuleGenerator）&quot;">​</a></h3><table tabindex="0"><thead><tr><th>AdGuard 規則類型</th><th>可轉?</th><th>理由</th></tr></thead><tbody><tr><td>純網路 block <code>||example.com^</code></td><td>✅</td><td>映射為 host deny 條目</td></tr><tr><td>domain-anchored <code>||ads.example.com^</code></td><td>✅</td><td>host 條目</td></tr><tr><td>純 URL 子串 <code>/ads/banner.js</code></td><td>⚠ 部分</td><td>Bloom 是集合成員測試，只能存<strong>離散完整 URL/host</strong>，無法做子串/pattern 匹配 → 僅當可展開為有限 host+path 才轉；含 <code>*</code>/regex → <strong>跳過</strong></td></tr><tr><td>exception <code>@@||example.com^</code></td><td>❌</td><td>Bloom 無法表達 allowlist（成員測試無「排除」語義）；exception 須在 App 端於建位圖<strong>前</strong>從集合做差集移除</td></tr><tr><td>cosmetic <code>example.com##.ad</code></td><td>❌</td><td>非網路層，URL Filter 無此能力</td></tr><tr><td>scriptlet <code>#%#//scriptlet(...)</code></td><td>❌</td><td>同上</td></tr><tr><td><code>$removeparam</code> / <code>$csp</code> / modifier 類</td><td>❌</td><td>非 deny 決策，跳過</td></tr></tbody></table><p><strong>規則</strong>：不確定即跳過（不放寬），與 <code>RemoveParamDNRRuleGenerator</code> 的「conservative, skip rather than widen」一致。</p><h3 id="_4-3-覆蓋率估算方法" tabindex="-1">4.3 覆蓋率估算方法 <a class="header-anchor" href="#_4-3-覆蓋率估算方法" aria-label="Permalink to &quot;4.3 覆蓋率估算方法&quot;">​</a></h3><ol><li>對每張既有清單跑 extractor，記 <code>convertible / total</code> 比。</li><li>分類統計跳過原因（cosmetic / exception / pattern / modifier）→ 輸出報表。</li><li>Bloom 誤判率：以目標 <code>p</code>（如 0.1%）反解 <code>bitCount = -(n·ln p)/(ln2)²</code>、<code>hashCount = (bitCount/n)·ln2</code>；單元測試以隨機 known-absent URL 樣本實測誤判率，斷言 ≤ 目標。</li><li><code>tag</code> = 條目集合內容 hash → <code>fetchPrefilter(existingPrefilterTag:)</code> 可據此判斷是否需重建（未變則回 nil/沿用）。</li></ol><h3 id="_4-4-murmur-位圖" tabindex="-1">4.4 murmur / 位圖 <a class="header-anchor" href="#_4-4-murmur-位圖" aria-label="Permalink to &quot;4.4 murmur / 位圖&quot;">​</a></h3><ul><li><code>NEURLFilterPrefilter</code> 欄位 <code>murmurSeed: UInt32</code> → 用 murmur3 x86_32；<code>PrefilterData</code> 小資料走 <code>.smallFilter(Data)</code>、大位圖走 <code>.temporaryFilepath(URL)</code>（避免 XPC 傳大 Data）。<code>[claim:asserted：.temporaryFilepath 適用門檻需 Phase 0 實測]</code></li></ul><hr><h2 id="_5-edge-cases-處理" tabindex="-1">5. Edge Cases 處理 <a class="header-anchor" href="#_5-edge-cases-處理" aria-label="Permalink to &quot;5. Edge Cases 處理&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Edge case</th><th>處理</th></tr></thead><tbody><tr><td>iOS 系統 content filter 數量上限</td><td>URL Filter 走獨立 NE provider slot，<strong>不佔</strong> content blocker 的 slot 配額；但 iOS 對 NE 也有限制 → Phase 0 驗證單一 provider 可載入即足（wBlock 只需 1 個 URL filter provider）</td></tr><tr><td>Bloom false positive 擋錯站</td><td>UX：設定頁顯示「URL Filter 為機率性快篩」；提供 per-site allowlist（差集移除該 host 後重建位圖）；模式 C 下這是唯一補救（無 PIR 二次確認）</td></tr><tr><td>PIR token 過期 / server 不可達</td><td><code>URLFilterManagerCoordinator</code> 監聽 status AsyncSequence；不可達 → 依 <code>shouldFailClosed</code> 設定行為（預設 fail-open）+ UI 狀態指示；提供 <code>refreshPIRParameters()</code>/<code>resetPIRCache()</code> 手動按鈕</td></tr><tr><td>App Group team-prefix 路徑差異</td><td><strong>重用既有 <code>GroupIdentifier.resolvedMacOSGroupIdentifier()</code></strong>（已處理 team-prefix vs base + signing entitlements 來源）→ extension 與 App 用同一解析邏輯讀寫 prefilter blob，不重造</td></tr><tr><td>prefilter 建置在 extension vs App</td><td>轉換在 <strong>App 端 CoreService</strong> 完成寫 App Group；extension <code>fetchPrefilter</code> 只讀+封裝回傳（extension 記憶體/時間受限，不做百萬級轉換）</td></tr></tbody></table><hr><h2 id="_6-風險清單與開放問題-★-需-upstream-maintainer-裁決" tabindex="-1">6. 風險清單與開放問題（★ = 需 upstream maintainer 裁決） <a class="header-anchor" href="#_6-風險清單與開放問題-★-需-upstream-maintainer-裁決" aria-label="Permalink to &quot;6. 風險清單與開放問題（★ = 需 upstream maintainer 裁決）&quot;">​</a></h2><ol><li>★ <strong>PIR 基礎設施承諾</strong>：wBlock 作為開源專案是否願營運/資助 PIR server（模式 B），或只出貨 prefilter-only（模式 C）？——<strong>阻擋預設模式決策</strong>，Phase 0 後必須先裁決。</li><li>★ <strong>stub-PIR 可行性</strong>：SDK 是否允許「無真實 PIR，僅 Bloom」的配置（指向永遠回 unknown 的端點且不 fail-closed）？Phase 0 spike 的<strong>首要驗證項</strong>；若否，模式 C 不成立。</li><li>★ <strong>是否值得引入 PIR 隱私敘事複雜度</strong>：wBlock 現有隱私賣點是「零查詢離開裝置」；即便 PIR 密碼學保證 server 看不到查詢，仍是新增網路面 → maintainer 需決定是否符合專案隱私哲學。</li><li><code>.temporaryFilepath</code> 大位圖傳遞在 XPC 邊界的實際限制（Phase 0 實測）。</li><li>Apple OHTTP relay distribution 核准的前置條件與時程（影響 distribution build 上架，dev 迭代不受阻）。</li><li>iOS 上 URL Filter provider 與既有 content blocker + WebExtension 共存的資源/生命週期干擾（Phase 3 端到端驗）。</li><li>exception 規則差集移除的正確性：跨清單 allowlist 順序語義是否與 Safari content blocker 一致（避免兩條路徑行為分歧）。</li></ol><hr><h2 id="_7-reverse-peer-review-ceiling-自審一輪" tabindex="-1">7. Reverse peer-review（ceiling 自審一輪） <a class="header-anchor" href="#_7-reverse-peer-review-ceiling-自審一輪" aria-label="Permalink to &quot;7. Reverse peer-review（ceiling 自審一輪）&quot;">​</a></h2><ul><li><strong>反駁「轉換放 App 端」</strong>：若 App 未執行（背景），prefilter 過期？→ <code>fetchPrefilter</code> 由系統在 NE 生命週期觸發；若 App 端無法建，退化為沿用舊 tag。可接受，已記 open question 4/prefilterFetchInterval。</li><li><strong>反駁「模式 C 為預設」</strong>：prefilter-only 誤判無補救可能傷使用者信任 → 已加 per-site allowlist UX + 明確標示機率性；且以「不破壞既有 content blocker」為底線，URL Filter 純加值。成立。</li><li><strong>反駁「鏡像 RemoveParamDNRRuleGenerator」</strong>：Bloom 是集合而非規則清單，形狀不同 → 鏡像的是<strong>慣例</strong>（保守跳過、public enum、App Group 輸出、單元測試），非資料結構，論述成立。</li></ul>`,52)])])}const g=t(i,[["render",s]]);export{u as __pageData,g as default};
