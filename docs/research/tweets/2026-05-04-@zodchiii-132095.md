---
title: "The Claude Skills Folder That Automates 90% of Workflow (All Commands Included)"
author: "darkzodchi (@zodchiii)"
created_at: "2026-05-04T09:10:38Z"
integration_type: SKILL
source: "https://x.com/zodchiii/status/2051227992404132095"
total_score: 7.25
type: twitter-article
---

# The Claude Skills Folder That Automates 90% of Workflow

**Original URL**: https://x.com/zodchiii/status/2051227992404132095

## TL;DR

Practical guide to creating custom slash commands in Claude Code via `.claude/skills/` folder; provides 10 templates, YAML mechanics, and a 9-step automation workflow.

---

## Full Article Text


Claude Code has 60+ built-in slash commands. /compact, /clear, /model, /review. Most people know 5-6 of them.
What most people don't know is you can create your own. A markdown file in .claude/skills/ becomes a slash command you invoke with /name. Write the prompt once, use it forever.
Copy the folder, drop it in your project, and your daily workflow gets faster today.
Here's the full setup 👇
Before we dive in, I share daily notes on AI & vibe coding in my Telegram channel: https://t.me/zodchixquant🧠
[圖 1]
## How custom commands work
Create a markdown file with YAML frontmatter. Put it in one of two places:
```
.claude/skills/review/SKILL.md     → Project-specific (shared with team via git)
~/.claude/skills/review/SKILL.md   → Personal (available in every project)
```
The file name becomes the slash command. review/SKILL.md becomes /review. That's it.
The frontmatter controls behavior:
```yaml
---
name: review
description: Review current diff for bugs, security, and style
allowed-tools: Read, Grep, Glob, Bash(git diff *)
---
```
description tells Claude when to auto-invoke the skill. allowed-tools limits what the command can do. Everything below the frontmatter is the prompt Claude follows.
You can pass arguments too. $ARGUMENTS captures everything after the command name, and $1, $2 capture positional args:
```
/review src/auth/     → $ARGUMENTS = "src/auth/"
/fix-issue 423 high   → $1 = "423", $2 = "high"
```
[圖 2]
## commands/ vs skills/ — which one?
Quick answer: use skills/. As of April 2026, Anthropic merged custom commands into the skills system. Both work, but skills add two things commands don't:
```
commands/ (legacy)          skills/ (recommended)
─────────────────           ─────────────────────
Invoked with /name    ✓     Invoked with /name           ✓
Manual trigger only   ✓     Auto-invoked by Claude        ✓
No frontmatter needed ✓     Frontmatter for description   ✓
No tool restrictions  ✓     allowed-tools field           ✓
```
Skills can be triggered automatically based on context. If your /review skill description says "use when reviewing pull requests or code changes," Claude will suggest it when you're working on a PR even if you don't type /review.
Your existing .claude/commands/ files keep working. No migration needed, but new commands should go in .claude/skills/.
## The 10 commands (copy-paste ready)
## 1. /review — Code Review
```
---
name: review
description: Review code for bugs, security issues, and style violations. 
  Use when reviewing PRs, checking code quality, or when user mentions 
  "review", "PR", "code quality".
allowed-tools: Read, Grep, Glob, Bash(git diff *)
---

Review the current diff or specified files for:

1. **Bugs**: logic errors, off-by-one, null handling, race conditions
2. **Security**: OWASP Top 10, hardcoded secrets, SQL injection, XSS
3. **Performance**: N+1 queries, unnecessary re-renders, blocking calls
4. **Style**: naming conventions, dead code, TODOs left behind

Output format:
- Group findings by severity: CRITICAL / WARNING / INFO
- Each finding: file, line, issue, suggested fix
- End with a summary: "X critical, Y warnings, Z info"

If no diff exists, ask which files to review.
```
## 2. /test — Write Tests
```
---
name: test
description: Write tests for a file or function. Use when user mentions 
  "test", "tests", "coverage", or "spec".
allowed-tools: Read, Write, Edit, Bash(npm test *), Bash(npx vitest *)
---

Before writing tests:
1. Read the target file to understand function signatures and types
2. Find existing test directory, read 1-2 existing tests
3. Identify the testing framework (Jest, Vitest, Pytest, etc.)
4. Match the import style and assertion patterns

Generate tests covering:
- Happy path (expected inputs → expected outputs)
- Edge cases (empty, null, zero, max values, special characters)
- Error cases (invalid inputs, network failures, timeouts)
- Async behavior if applicable

After writing, run the tests. Fix any failures before finishing.

Match exact import style and patterns from existing tests.

```
## 3. /commit — Smart Commits
```
---
name: commit
description: Create structured git commits from current changes. Use when 
  user says "commit", "save changes", or after finishing a feature.
allowed-tools: Read, Bash(git *)
---

1. Run `git status` and `git diff` to see all changes
2. Group related changes into logical units (one feature = one commit)
3. For each unit, create a commit:

Format:
type(scope): short description under 50 chars

- What changed
- Why it changed (if not obvious)

Types: feat, fix, refactor, docs, test, chore
Scope: affected module name

4. Stage and commit each unit separately
5. Show summary: "Created N commits: [titles]"

Rules:
- Never combine unrelated changes in one commit
- Description is lowercase, present tense
- No period at end of subject line

```
## 4. /pr — Create Pull Request
```
---
name: pr
description: Generate a PR description from current branch changes. Use when 
  user mentions "PR", "pull request", or "merge".
allowed-tools: Read, Bash(git *)
---

1. Run `git log main..HEAD --oneline` to see all commits
2. Run `git diff main...HEAD --stat` for changed files summary
3. Read the key changed files to understand the full context

Generate a PR description:

## What
[One paragraph: what this PR does]

## Why
[One paragraph: why this change is needed]

## Changes
[Bullet list of key changes, grouped by area]

## Testing
[How this was tested, what to check during review]

## Notes
[Anything reviewers should know: breaking changes, migrations, follow-ups]

Output the description ready to paste into GitHub.
```
## 5. /debug — Investigate Errors
```
---
name: debug
description: Debug an error or unexpected behavior. Use when user mentions 
  "bug", "error", "broken", "not working", "debug", or pastes an error message.
allowed-tools: Read, Grep, Glob, Bash(npm test *), Bash(npx tsc *)
---

1. Identify the error: read the error message, stack trace, or user description
2. Find the source: search codebase for relevant files using grep/glob
3. Read the relevant code and understand the expected behavior
4. Form a hypothesis for the root cause
5. Verify by reading related code, tests, and recent changes (`git log -5 -- <file>`)
6. Propose a fix with explanation

Output format:
**Root cause**: [one sentence]
**Location**: [file:line]
**Fix**: [code change with explanation]
**Prevention**: [how to prevent this class of bug in future]

Do NOT make changes unless explicitly asked. Diagnosis first.
```
## 6. /refactor — Safe Refactoring
```
---
name: refactor
description: Refactor code while preserving behavior. Use when user mentions 
  "refactor", "clean up", "simplify", or "restructure".
allowed-tools: Read, Write, Edit, Bash(npm test *), Bash(npx tsc *)
---

1. Read the target file(s) completely
2. Run existing tests to establish baseline (all must pass)
3. Plan refactoring changes WITHOUT changing external behavior
4. Apply changes incrementally, one logical step at a time
5. Run tests after EACH step
6. Run type check after final step

Refactoring targets (in priority order):
- Extract duplicated code into shared functions
- Simplify complex conditionals
- Reduce function length (under 30 lines ideal)
- Improve naming clarity
- Remove dead code
- Fix type safety issues

Rules:
- NEVER change function signatures without checking all callers
- NEVER change behavior, only structure
- If tests fail after a change, revert and try a different approach

```
## 7. /docs — Generate Documentation
```
---
name: docs
description: Generate or update documentation. Use when user mentions 
  "docs", "documentation", "README", "JSDoc", or "comments".
allowed-tools: Read, Write, Edit, Glob
---

Determine what needs documenting:

**If README**: Generate project overview, setup, usage, and API reference
**If JSDoc/docstrings**: Add to all exported functions and classes
**If inline comments**: Add to complex logic only (not obvious code)

For each documented item include:
- Description of what it does (one sentence)
- Parameters with types and descriptions
- Return value with type
- Example usage (if non-obvious)
- Throws/errors (if applicable)

Style rules:
- Match existing documentation style in the project
- Be concise, not verbose
- Document WHY, not WHAT (the code shows what)
```
## 8. /migrate — Database Migration
```
---
name: migrate
description: Create a database migration. Use when user mentions "migration", 
  "schema change", "add column", "create table", or "alter table".
allowed-tools: Read, Write, Edit, Bash(npm run *), Glob
---

1. Read existing migrations to understand naming convention and ORM
2. Identify the migration tool (Prisma, Drizzle, Knex, raw SQL, etc.)
3. Create the migration file following project conventions

Rules:
- Always include both UP and DOWN migrations
- Use the project's naming convention for migration files
- Add appropriate indexes for foreign keys and frequently queried columns
- Never drop columns in production without a data migration plan
- Add NOT NULL constraints only with DEFAULT values

After creating, run the migration locally and verify it applies cleanly.
```
## 9. /deploy-check — Pre-Deployment Verification
```
---
name: deploy-check
description: Run pre-deployment checks. Use when user mentions "deploy", 
  "ship", "release", or "production".
allowed-tools: Read, Bash(npm *), Bash(npx tsc *), Bash(git *), Grep
---

Run these checks in order. Stop at first failure:

1. `npx tsc --noEmit` — type check passes
2. `npm test` — all tests pass
3. `npm run lint` — no lint errors
4. `npm run build` — build succeeds
5. Check for console.log in src/ (grep -r "console.log" src/ --include="*.ts")
6. Check for .env references in committed code
7. Check git status — no uncommitted changes
8. Verify current branch is up to date with main

Output:
✅ or ❌ for each check
Summary: "Ready to deploy" or "N issues must be fixed first"
```
## 10. /security — Security Scan
```
---
name: security
description: Scan for security vulnerabilities. Use when user mentions 
  "security", "audit", "vulnerability", "CVE", or before deployments.
allowed-tools: Read, Grep, Glob, Bash(npm audit *)
---

Scan the codebase for:

1. **Hardcoded secrets**: API keys, passwords, tokens (grep for common patterns)
2. **Dependency vulnerabilities**: run `npm audit` or equivalent
3. **SQL injection**: check for string concatenation in queries
4. **XSS**: check for unsanitized user input in HTML output
5. **CSRF**: verify token protection on state-changing endpoints
6. **Auth issues**: check for missing auth middleware on protected routes
7. **File access**: check for path traversal vulnerabilities
8. **SSRF**: check for unvalidated URLs in server-side requests

Output format:
- Severity: CRITICAL / HIGH / MEDIUM / LOW
- Location: file:line
- Issue: what's wrong
- Fix: how to fix it
- Reference: OWASP category

End with: "Found X issues: N critical, N high, N medium, N low"

```
## How to install all 10
Copy this folder structure into your project:
```
.claude/skills/
├── review/SKILL.md
├── test/SKILL.md
├── commit/SKILL.md
├── pr/SKILL.md
├── debug/SKILL.md
├── refactor/SKILL.md
├── docs/SKILL.md
├── migrate/SKILL.md
├── deploy-check/SKILL.md
└── security/SKILL.md

```
Or for personal use across all projects, put them in ~/.claude/skills/.
Commit the project-level ones to git. 
Your entire team gets the same commands instantly.
## The workflow that brings it all together
A typical feature cycle using these commands:
1. /debug         → investigate the issue
2. Write code     → implement the fix
3. /test          → generate tests for new code
4. /refactor      → clean up if needed
5. /review        → self-review before PR
6. /security      → quick security scan
7. /commit        → create structured commits
8. /pr            → generate PR description
9. /deploy-check  → verify everything before ship
Each step that used to be a 50-word prompt is now 7-15 characters. Over a month, that compounds into hours saved.
Thanks for reading!
I share daily notes on AI, finance, and vibe coding in my Telegram channel: https://t.me/zodchixquant
[圖 3]


## 5-Dimensional Analysis

### A. Workspace Actionability (9/10) — Weight: 30%

**Strengths:**
- ✓ Directly applicable to this workspace (.claude/skills/ folder structure already present)
- ✓ Copy-paste ready folder structure provided
- ✓ 10 concrete skill templates with YAML frontmatter examples
- ✓ Clear file naming convention (review/SKILL.md → /review)
- ✓ Integration path clear (project-level or ~/.claude/skills/)

**Weaknesses:**
- ✗ Templates mention generic prompts (e.g., /docs) but don't show full content

**Implication for workspace:** The skills pattern is fully adoptable. We already have .claude/skills/ structure; this article validates and codifies the patterns we're using.

---

### B. Innovation (6/10) — Weight: 20%

**Strengths:**
- ✓ Explains auto-invocation feature (context-aware trigger based on description)
- ✓ Clarifies commands/ vs skills/ migration (April 2026 context, contemporaneous with workspace)

**Weaknesses:**
- ✗ Core concept (YAML + prompt template) is well-known in Claude ecosystem
- ✗ Templates are standard task types (review, test, commit, debug)
- ✗ No novel workflow pattern or optimization technique

**Assessment:** Solid engineering synthesis, not novel methodology.

---

### C. Evidence Quality (7/10) — Weight: 20%

**Strengths:**
- ✓ 10 concrete examples provided with command names
- ✓ Example workflow chain shown (9-step feature cycle with clear sequencing)
- ✓ Time savings claim quantified ("7-15 characters vs 50-word prompt")
- ✓ YAML frontmatter structure explained with field semantics

**Weaknesses:**
- ✗ "90% automation" claim in title lacks data backing
- ✗ No performance metrics on actual time saved per developer

**Assessment:** Operational evidence sufficient; marketing claim not substantiated.

---

### D. Technical Depth (6/10) — Weight: 15%

**Strengths:**
- ✓ YAML frontmatter mechanics explained
- ✓ Argument passing ($ARGUMENTS, $1, $2) documented
- ✓ Scoping rules (allowed-tools) briefly described

**Weaknesses:**
- ✗ No discussion of skill composition or inter-skill dependencies
- ✗ No error handling or edge cases covered
- ✗ Missing: caching implications, performance considerations

**Assessment:** Introductory-level technical depth; suitable for onboarding, not reference.

---

### E. Generalization (7/10) — Weight: 15%

**Strengths:**
- ✓ Applies to any Claude Code user (not workspace-specific)
- ✓ Skills pattern generalizes across teams and projects
- ✓ Workflow cycle replicable without external dependencies
- ✓ Framework-agnostic (language, framework neutral)

**Weaknesses:**
- ✗ Some context assumes Claude Code on web (April 2026 feature set)

**Assessment:** Pattern transferable; slight anchoring to current tooling timeline.

---

## Weighted Score Calculation

| Dimension | Score | Weight | Contribution |
|-----------|-------|--------|--------------|
| A. Actionability | 9 | 0.30 | 2.70 |
| B. Innovation | 6 | 0.20 | 1.20 |
| C. Evidence | 7 | 0.20 | 1.40 |
| D. Depth | 6 | 0.15 | 0.90 |
| E. Generalization | 7 | 0.15 | 1.05 |
| **TOTAL** | — | 1.00 | **7.25** |

---

## Integration Decision

**Score: 7.25/10** ✓ (exceeds 6.0 threshold)

**Core Asset Type:** Static prompt templates + operational workflow

**Recommendation: SKILL**

**Rationale:**
- Actionability is the dominant factor (9/10). This directly improves daily workflow.
- 10 templates are immediately applicable. No architectural decision required.
- Workflow sequence is replicable across projects.
- Unlike an Agent (which requires iterative learner decisions), these are fixed prompts.
- Unlike a Rule (which is meta-guidance), these are tool implementations.

**Next Step:** Extract the 10 command templates into `.claude/skills/` directory structure; document in workspace CHANGELOG.

---

## Stats

- **Author:** darkzodchi (@zodchiii) — 16.5K followers, verified
- **Created:** 2026-05-04 09:10:38 UTC
- **Engagement:** 74 likes, 10 retweets, 4006 views
- **Content Type:** Twitter Article (Draft.js)
- **Article ID:** 2051202860675670016

---

## Archival Metadata

| Field | Value |
|-------|-------|
| Archive Path | `research/tweets/2026-05-04-@zodchiii-132095.md` |
| Checksum (SHA-256) | — |
| Last Updated | 2026-05-04 |
| Integration Status | Pending (awaiting skill extraction) |
