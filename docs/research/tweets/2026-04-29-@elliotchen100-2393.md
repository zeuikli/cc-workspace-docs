# Claude Code的32个使用技巧 - 入门，进阶，高阶 — @elliotchen100

**来源**：https://x.com/elliotchen100/status/2049299210668032393
**作者**：艾略特（@elliotchen100）
**发布日期**：2026-04-29
**收录日期**：2026-04-29
**类型**：Twitter Article
**统计**：101 likes · 19 retweets · 268 bookmarks · 25090 views

---

## 全文

昨天看到 @nateherk 的这个 YouTube 视频，觉得很不错，推荐一下。

https://www.youtube.com/watch?v=jqoFP9QapXI

他的视频风格我一直挺喜欢，简简单单清清楚楚，没有废话，开头不卖关子，结尾不拖泥带水。这条尤其密集，32 个技巧塞进 16 分钟，按入门，进阶，高阶分了三档，从 /init 一路讲到 agent team。

我先把内容快速过一遍，再挑 6 个我觉得视频里讲到了但没展开，其实最关键的点，把背后的原理拆开讲。

### 32 个技巧速览

**入门** /init 一键生成项目说明书 cloud.md plan mode 开局先规划再动手 /context 看 token 占用，超 60% 用 /compact 压缩 把 Claude 当 junior 同事，提问题不要下命令 self check 加进 todo，让它自己验自己

**进阶** sub agents 并行任务，独立 context 不打架 custom skills 复用 prompt 文件 简单活儿扔给 Haiku 省 token 跑偏了直接 exit 重prompt 截图喂回去做视觉 debug

**高阶** /loop 自动跑 recurring tasks，最长 3 天 Git worktrees 多 session 并行 VPS 托管不掉线，手机也能远程操控 ultrathink 模式给硬骨头分配最大 token agent team 多 agent 协作分工

先看这个清单，和其他 Claude Code 教程没差别，能搜到的太多了。Nate Herk 这条视频的真正价值，是他用老手的视角把这些点串起来了。下面挑 6 个我觉得别人聊得最少，但其实最关键的，把背后的原理拆开讲。

### 一、把 Claude 当 junior 同事，不是态度问题，是 prompt 工程

很多人看到这条会以为是「对它说话礼貌一点」。其实跟礼貌没关系，这是计算路径的选择。

LLM 接收命令式 prompt 时，触发的是「按指令执行」的浅层模式，直接 pattern match，输出快但思考浅。接收描述式 prompt 时，触发的是「理解后再行动」的深层模式，会展开 chain of thought，输出慢但准确度高一截。

同一个 bug，「修一下这里」和「这里看起来怪怪的，你查查为啥」出来的代码质量能差一倍。前者它就直接改你指出的那行，后者它会先去理解上下文，可能发现真正的问题在另一个地方。

更深一层，描述要具体且不带预设答案。「这里有 race condition」会让它只查 race condition，「这里偶尔会出错你查查」会让它展开完整排查。你 prompt 里预设的答案越少，激发的推理就越多。

### 二、60% 用 /compact，是对抗 lost in the middle，不是省 token

这个 60% 经常被解读成「省 token 的阈值」。其实不是。

Stanford 2023 年有篇论文揭示了 lost in the middle 现象，LLM 在长 context 里的注意力是 U 型曲线，开头记得最牢，结尾记得最牢，中间最容易被忽略。

60% 这条线不是省钱的红线，是注意力开始衰减的经验线。超过这个点之后，你前面提过的关键需求会开始被「视而不见」，模型会犯一些「明明你说过了」的错。

/compact 真正的价值是它会保留关键决策和未完成事项，把中间的散乱讨论压掉。这不是粗暴截断，是在做注意力的重新聚焦。

判断什么时候该 compact，不要先看 token 数，要看模型是不是开始忘事。一旦它问出「你想做什么」之类的问题，立刻 compact，迟一步它就会带着错误前提继续往下做。

### 三、跑偏了 exit 重开，是 token 经济学，不是耐心问题

很多人遇到 Claude 走偏，第一反应是「我再 prompt 它一下让它修正」。这个反应是错的。

被污染的 context 里，你写的修正 prompt 自己占 context，它的道歉占 context，它修正后的输出还会被错误推理影响。人类有 confirmation bias，LLM 也有，被前面的错误锚定之后很难真正跳出来。

总成本比重开高 2 到 3 倍，最终输出还可能更差。

应该建立的反射是 fail fast，看到走偏立刻 ESC，重新组织思路再 prompt，比让它自己踏回来快得多。重开不是失败，是更聪明的资源分配。

这个反射很多人建立不起来，因为觉得「重开是浪费」。其实越往后修，浪费越大。

### 四、sub agents 真正的价值是上下文隔离，不是并行

并行只是表象，隔离才是核心。

sub agents 把 context 从一维资源变成了树形资源。父 agent 是指挥者，看大局，不需要知道每个细节怎么实现。sub agent 是执行者，可以激进探索，包括各种失败和绕路。父 agent 只看 sub agent 的最终结论，那些失败的探索不会污染主线。

什么任务最适合开 sub agent，是「需要大量探索但只需要少量结论」的任务。比如「在 codebase 里找所有用了某个 deprecated API 的地方」，让 sub agent 把代码翻完，回来交一份清单就行。父 agent 不需要知道它翻过哪些文件，更不需要被它翻过的代码污染主上下文。

理解了这一点，你会发现很多任务都可以这样拆。「调研一下 X 库怎么用」「查一下这个 bug 在哪几个文件出现过」「列出所有需要改的测试」，全都是典型的 sub agent 任务。

### 五、简单活儿扔给 Haiku 不只是省钱，是避免「过度设计」

这背后是大模型的过度对齐问题。

大模型训练时见过太多复杂场景，对简单任务也会本能地加边缘情况处理，加错误处理，加注释和文档，用更复杂的设计模式。

写一个简单的 helper 函数，Sonnet 可能给你 30 行，Haiku 给你 10 行。后者在大多数场景反而更对。

我自己的判断标准是任务的「认知步骤数」：

1 到 2 步的活儿扔 Haiku 5 步以上留给 Sonnet 或 Opus 10 步以上才开 ultrathink

不要无脑用最强模型，最强模型对简单任务有「设计过度」的副作用，输出的代码后期还要花时间删冗余。

### 六、/loop 挂 3 天的真正用法是 monitoring，不是写代码

很多人看到 /loop 第一反应是「让 Claude 连续写 3 天代码」，其实不是。

/loop 真正的用法是把 Claude 从同步工具变成异步同事。它在那挂着，定期检查状态，有事叫你。

好的 /loop 任务有几个特征：

输入是状态，不是动作 输出是「有事/没事」的判断 处理时间不连续，可以等

比如每 10 分钟看一下 CI 是不是过了，没过分析失败原因，每小时扫一下数据库慢查询，每天看一遍 PR review 队列。这些以前是「我得记得去看」的事，现在变成「Claude 看到再叫我」。

这是 Claude 从写代码工具升级成值班同事的关键。

### 把这些串起来看

如果只看 32 个技巧，你最多能复述操作。但如果把上面这 6 个点连起来看，你会发现 Nate Herk 真正的心法，是一个心智模型：

把 Claude Code 当成一个「有限注意力，有限时间，有性格」的同事来管理，不要当成「无限算力的工具」来用。

所有技巧背后都是这个心智模型在起作用：

注意力有限，所以要做 context 管理，对应的是 /compact、/clear、sub agent 隔离

时间有限，所以要异步化，对应的是 /loop、agent team、多 session 并行

有性格，所以要 prompt 沟通，对应的是 plan mode，让它反问，junior 同事心态

心智模型一旦换过来，剩下的技巧都是延伸，自己也能想出来。

我自己用 Claude Code 写代码差不多大半年了，最大的认知升级不是某个具体技巧，而是从「我用一个工具」变成「我管一个同事」。这两种心态出来的代码质量，效率，token 消耗，差着一个数量级。

*注意，此文章是我同 AI 共同创作

---

## 研究摘要

**TL;DR**：不是32个技巧，而是一个心智模型——把Claude Code当有限注意力的同事来管理，不是无限工具。所有技巧背后都是这个模式：有限注意力→context管理、有限时间→异步化、有性格→prompt沟通。

### 核心论点

- Descriptive prompts (解释why) vs imperative prompts (命令what)：前者触发深层推理(chain-of-thought)，后者只是浅层pattern-match
- 60%阈值不是省token，是对抗"lost in the middle"现象：LLM注意力在长context中呈U形曲线，中间部分最易被忽视
- Exit-restart (fail-fast)是token经济学：被污染context中修正的成本是重新开始的2-3倍
- Sub-agents的核心价值是context隔离(tree structure)而非并行，让探索失败不污染主线
- 使用小模型(Haiku)避免"过度设计"：大模型会本能地加错误处理、文档、复杂模式，反而在简单任务上失效
- /loop的真正用法是异步监控状态变化(CI结果、PR队列)，不是代码生成

### 关键数据/工具

- 32 tips in 16 min (Nate Herk YouTube video)
- Stanford 2023论文："lost in the middle"——LLM在长context中的注意力衰减规律
- 60%作为context警戒线（非成本阈值）
- Exit-restart成本溢价：2-3倍
- Haiku vs Sonnet权衡：Haiku输出往往更简洁、更少冗余
- 认知步骤启发式：1-2步→Haiku, 5+→Sonnet, 10+→ultrathink

### 批判性观察

- 32个技巧看似零散，实则全是"从使用工具→管理同事"这一心智模型转变的延伸
- 大多数团队优化单个技巧而错过了系统级的思维改变
- "Fail-fast"原则(遇到走偏立刻ESC重开)与工程直觉(再试一次修正)相悖，需要组织文化支持
- 关键洞察：技巧表象背后是资源约束的深层理解(注意力、时间、AI人格)

---

## 评分

| 维度 | 分数 | 说明 |
|------|------|------|
| A. Workspace 可行动性 (30%) | 9/10 | 直接对应cc-workspace的CLAUDE.md架构：/compact阈值、sub-agent策略、prompt设计、context管理——所有规则已在用。核心框架(colleague-management model)应升格为设计方针。 |
| B. 创新性 (20%) | 8/10 | "管理同事而非使用工具"的心智模型转变对多数Claude Code用户而言是新颖的。Stanford论文(2023)虽已发表，但与60%规则的连接在实践中少见。 |
| C. 证据品质 (20%) | 8/10 | 引用Stanford论文、提供具体成本分析(2-3x restart溢价)、量化启发式(32 tips、认知步骤分级)。缺少benchmark但逻辑自洽，基于作者6个月实战经验。 |
| D. 技术深度 (15%) | 8/10 | 深入机制层面：U-curve注意力、Draft.js内容解析、confirmation bias在polluted context的影响。可更深入tokenizer行为，但足以指导实践。 |
| E. 泛化性 (15%) | 9/10 | 原则跨越所有Claude Code工作流(研究、代码、规划)。非特定领域，不依赖Nate Herk视频，转移到任何长context agent管理场景。 |
| **加权总分** | **8.4/10** | A×0.3 + B×0.2 + C×0.2 + D×0.15 + E×0.15 = 8.4 |

**整合决策**：RULE

**理由**：总分8.4>6.0。主要贡献不是技巧集合，而是可泛化的心智模型(colleague-management)，应升格为架构原则而非单篇技巧。此模型直接影响subagent-strategy、prompt工程、context管理等多条既有规则的应用方式，建议作为`.claude/rules/agent-mindset.md`补充到常驻载入层。

*注意：此文章是作者与AI共同创作。

---

**归档完成**。此文档编码了Nate Herk视频与cc-workspace实践间的关键连接。核心心智模型("有限注意力的同事")应作为所有Claude Code工作流的设计基准。建议后续在CLAUDE.md的「核心工作流」章节中加入此模型的显式陈述。
