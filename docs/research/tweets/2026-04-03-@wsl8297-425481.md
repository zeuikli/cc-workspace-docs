---
title: "Claude Code 凭什么这么强？因为 95% 的人都不知道 Harness — @wsl8297"
source: "https://x.com/wsl8297/status/2039967052887425481"
author: "Joruno（@wsl8297）"
date: 2026-04-03
type: tweet
---

# Claude Code 凭什么这么强？因为 95% 的人都不知道 Harness — @wsl8297

**來源**：https://x.com/wsl8297/status/2039967052887425481
**作者**：Joruno（@wsl8297）
**發布日期**：2026-04-03
**收錄日期**：2026-04-26
**類型**：Twitter Article
**統計**：100 likes · 16 retweets · 184 bookmarks · 55,578 views

---

## 全文

你有没有想过一个问题：

同样是 Claude 模型，为什么在 Claude Code 里能写出完整项目，直接调 API 却只会给你讲道理？

同样的大脑，一个是能干活的工程师，一个是只会说话的顾问。

这背后的秘密，藏在一个 95% 开发者都忽略的概念里——Harness。

而更残酷的是：2026 年，模型正在变成白菜价，但 Harness 才是真正的护城河。

看不懂这一点的人，注定要在 AI 时代掉队。

先问你一个扎心的问题

你花了大价钱订阅 AI 工具，兴冲冲地打开对话框，写了一段精心设计的需求，结果呢？

比如你说：“帮我把这个 React 组件的所有 class 组件改成函数组件，顺便加上 TypeScript 类型”

AI 回你：“你可以这样做：1. 把 class 改成 function；2. 用 useState 替换 this.state；3. 添加 Props 接口定义……”

然后呢？然后就没了。

它不会帮你改，不会给你代码，只会像个技术顾问一样告诉你“应该怎么做”。你得自己一个个文件去改，一行行代码去写。

但同样的任务，在 Claude Code 里：

你说“把 UserProfile.jsx 改成 TypeScript 函数组件”

它先 Read 读取文件，理解组件结构

自动 Edit 重写代码：class → function，state → hooks，加上完整类型定义

Bash 运行 npm run type-check 验证类型

3 秒后问你：“改好了，要不要我顺便把相关的测试文件也更新一下？”

同一个模型，天壤之别。

为什么？因为裸 API 只是一个会说话的大脑，而 Claude Code 有一套完整的手脚系统——这就是 Harness.

Harness 到底是什么？为什么它比模型更重要？

忘掉那些复杂的定义。我用最直白的话告诉你：

Harness = 给 AI 大脑装上手脚的系统。

 

就像马具让马从野生动物变成人类的得力助手，Harness 让 AI 模型从“只会说话”变成“真能干活”。

它包裹在模型外面，提供五个核心组件：

 

1. 工具（Tools）：AI 的“手”

没有工具，模型只能“说”。有了工具，它能“做”：

Read：读取任何文件内容

Edit：精确修改代码的任何一行

Grep：在整个代码库中搜索

Bash：执行任何 Shell 命令（这是个图灵完备的逃逸舱）

注意最后一个——Bash 工具。通过它，Claude 理论上能做操作系统能做的一切：安装依赖、运行测试、调用 API、操作数据库。

这意味着什么？Claude Code 的能力上限 = 你操作系统的能力上限。

2. 上下文管理（Context Management）: AI 的“记忆”

Claude 的上下文窗口是 200K tokens。听起来很大？

一个真实编码任务，读 20 个文件、搜索 50 次、执行 30 条命令，对话历史会瞬间膨胀到几十万 tokens。

没有管理，要么爆掉窗口，要么模型开始“失忆”。

Claude Code 的解决方案极其精巧：

当对话历史接近 92% 时，触发自动压缩

摘要早期消息，但保留最近的完整对话

关键操作：每次压缩后，CLAUDE.md、系统提示词、工具定义都会重新注入

这就是为什么你在 CLAUDE.md 里写的规则那么“持久”——不是模型记住了，而是 Harness 每次都重新塞给它。

3. 权限控制（Permissions）：AI 的“安全阀”

想象你给 AI 完全的文件系统访问权限。如果它能随意做任何事，会发生什么？

删除整个 node_modules 文件夹？

修改生产环境的配置文件？

执行 rm -rf / 命令？

这就是为什么需要权限控制。

Claude Code 有三种权限策略：

Allow（允许）：可以自动执行，无需确认例如：读取代码文件、运行测试

Deny（拒绝）：直接禁止，绝不允许例如：删除系统文件、修改 .git 目录

Ask（询问）：需要你确认后才能执行例如：安装新依赖、修改配置文件、发布代码

每次 Claude 想执行操作时，Harness 都会先检查权限。你可以在 CLAUDE.md 里提前设置规则：

 

这不是限制 AI 的能力，而是让它在安全的轨道上全速奔跑。

4. 记忆（Memory）：跨会话的持久化

上下文管理解决的是“这次对话”的记忆。但如果你明天重新打开 Claude Code，它还记得你的项目规则吗？

这就是 Memory（记忆）的作用。

它是一个跨会话的持久化存储，让 Claude 记住：

项目架构约定：“我们用 Zustand 做状态管理，不用 Redux”

代码风格偏好：“组件文件用 PascalCase，工具函数用 camelCase”

历史决策：“上次讨论决定用 React Query 处理服务端状态”

常用命令：“测试用 npm run test: watch，不用 jest"

这些信息存储在项目根目录的 .claude/memory.json 文件里。每次新对话开始，Harness 都会把这些记忆注入给模型。

Memory 让 Claude 从“临时工”变成“老员工”。

你不需要每次都重新解释一遍“我们项目用什么技术栈”、“我们的命名规范是什么”。它都记得。

 

这张架构图清晰展示了 Claude Code 的完整系统：用户通过输入 Prompt 或审批权限与系统交互，Harness 层包含工具系统（Read/Write/Edit/Bash/Grep/Gob/Agent）、权限控制（allow/deny/ask）、Hooks（事件钩子）和 Sessions（会话持久化），核心的 Agentic Loop 协调所有组件，底层则是 Claude 模型负责纯文本生成和工具调用决策。

5. 钩子（Hooks）：可编程的事件触发

Hooks 是最聪明的部分——它让 Claude 在特定事件发生时，自动执行某些操作。

想象你在 CLAUDE.md 里设置了这些规则：

如果修改了代码 → 就自动运行 lint 检查

如果创建了新组件 → 就自动生成对应的测试文件

如果发现了 TypeScript 错误 → 就立即尝试修复

如果完成了功能 → 就自动运行完整测试套件

这就像给 Claude 装上了“条件反射”：

传统方式：

你：“修改 Button 组件”

Claude：“已修改”

你：“运行 lint”

Claude：“已运行，发现 3 个问题”

你：“修复这些问题”

Claude：“已修复”

你：“运行测试”

Claude：“测试通过”

有 Hooks 的方式：

你：“修改 Button 组件”

Claude：“已修改代码，自动运行了 lint 并修复了 3 个问题，测试全部通过”

这就像给 AI 装上了“自动驾驶”。

你只需要说目标，它会自动完成所有相关的后续步骤。

Agentic Loop：让五大组件协同运转的“引擎”

有了这五大组件，还需要一个“引擎”让它们协同工作——这就是 Agentic Loop（智能循环）。

它不是第六个组件，而是让前五个组件协同运转的机制。

 

这张流程图展示了 Agentic Loop 的完整工作流程：

接收输入：用户 prompt + 系统 prompt + 工具定义 + 对话历史

模型推理：Claude 分析上下文，生成回复（可能包含工具调用请求）

执行工具：Harness 执行工具并收集结果（包含权限检查）

结果回注：工具结果追加到对话历史，回到步骤②继续推理

返回结果：如果不需要工具调用，直接返回最终结果

传统 API 调用：你问 → 模型答 → 结束。

Agentic Loop：你问 → 模型推理 → 调用工具 → 看结果 → 继续推理 → 再调用工具 → …… → 任务完成。

一个复杂任务可能跑几十轮循环。

比如你让它“重构这个函数”：

先 Read 文件，理解代码结构

Grep 搜索这个函数在哪里被调用

分析依赖关系

检查 Permissions：我可以修改这个文件吗？

查询 Memory：这个项目的代码风格规范是什么？

Edit 修改函数实现

Bash 运行测试

看测试结果，决定是否需要调整

如果失败，再次 Edit 修复

触发 Hooks：自动运行 lint 和类型检查

重新测试，直到通过

这不是一次性生成，这是真正的“思考-行动-反馈”循环。

裸 API 做不到这一点。它只会给你一个“重构建议”，然后等你手动执行。

2026 年：Harness 之战已经打响

行业已经意识到一个残酷的事实：

同一个模型在不同 Harness 中的表现差距，远大于不同模型在同一个 Harness 中的差距。

换句话说：Harness 比模型更重要。

几个数据点足以证明：

1. OpenCode 的逆袭

OpenCode（前身 SST）是最成功的第三方 Harness，它用 Client-Server 架构解决了 Claude Code 的“单表面”局限——TUI、桌面 App、IDE 插件、Slack 机器人共享同一个后端。

截至 2026 年 4 月初，OpenCode 拿到了 136K GitHub stars，超过了 Claude Code 本身的 107K stars.

一个第三方 Harness，用户量超过了官方产品。这说明什么？

用户要的不是模型本身，而是更好的 Harness。

2. Anthropic 的“封杀令”

2026 年 1 月，Anthropic 采取了一个争议性举措：封堵了第三方工具通过消费者 OAuth Token 调用 Claude API 的通道。

这直接影响了 OpenCode、Cursor、Windsurf 等工具的用户——他们之前可以用 Claude Pro 订阅的 Token 在这些第三方工具里使用 Claude，封堵后必须购买独立的 API Key。

这件事引发了开发者社区的强烈反弹。

但也揭示了 Harness 生态的核心张力：

模型提供商希望控制 Harness 层，因为 Harness 决定了 API 调用量和用户体验

第三方 Harness 希望模型层是可替换的商品，因为这样它们才能建立独立的价值

这场战争的本质，是对 AI 应用栈控制权的争夺。

3. 模型正在商品化

Claude、GPT、Gemini、DeepSeek 的能力差距正在缩小。

模型正在变成白菜价的算力商品。

但 Harness 的差异化空间巨大：

工具设计的精巧程度

上下文管理的智能程度

权限控制的安全性

执行效率的优化

每一个维度都能产生 10 倍的体验差距。

对你意味着什么？三个残酷的真相

真相 1：理解 Harness 比理解模型更重要

模型的能力由 Anthropic/OpenAI 决定，你无法改变。

但 Harness 的配置，CLAUDE.md 怎么写、工具权限怎么设、Hooks 怎么接、MCP 怎么连，这些全在你手中。

你每天在做的事情，本质上都是在调教 Harness。

真相 2：不懂 Harness，你就是在用“残废版” AI

如果你只会调 API，写 Prompt，你用的是“只有大脑没有手脚”的残废版 AI。

如果你理解 Harness，知道如何设计工具、管理上下文、优化循环，你用的是“手脚齐全”的完整版 AI。

这不是 10% 的差距，这是 10 倍的差距。

真相 3：下一代 AI 应用的胜负手在 Harness 层

看看现在的 AI 编程工具格局：

Cursor：自研 Harness，强在代码补全和多文件编辑

Windsurf：自研 Harness，强在 Cascade 多步推理

OpenCode：开源 Harness，强在多端协同

Claude Code：官方 Harness，强在工程化和稳定性

它们用的模型都差不多，但体验天差地别。差在哪？差在 Harness。

如果你想在 AI 时代建立竞争力，无论是做产品还是做开发者，你必须深入理解 Harness 的设计哲学和实现细节。

一个 30 秒的实验：亲自感受 Harness 的威力

别光看理论，动手试试。

任务：给 React 项目的所有按钮组件添加 loading 状态

方法 1：裸 API

 

结果：Claude 会告诉你：

“你可以这样做：在 Button 组件的 props 里添加 isLoading 属性，用条件渲染显示 Spinner 或按钮文字，loading 时禁用按钮点击示例代码：{isLoading ? &lt;Spinner /> : children}"

然后呢？然后就没了。你得自己去找所有 Button 组件，一个个手动改代码。

方法 2: Claude Code

在 Claude Code 里直接说：“给所有 Button 组件添加 loading 状态”

结果：

Grep 搜索所有 Button 组件文件

Read 读取 src/components/Button.tsx

Edit 自动修改：

 

Grep 找到所有使用 Button 的地方，询问：“发现 12 处使用，要不要我帮你更新调用代码？”

Bash 运行 npm run build 验证编译通过

同一个大脑，有没有 Harness，结果天壤之别。

最后，给你三个行动建议

1. 立刻停止“Prompt 工程师”的幻觉

别再花时间研究“怎么写 Prompt 让 AI 更聪明”了。

AI 不是靠 Prompt 变聪明的，是靠 Harness 变强大的。

把时间花在理解工具设计、上下文管理、循环优化上。

2. 如果你是开发者：学习 Claude Agent SDK

别只会调 API，去看 Claude Agent SDK 的文档，理解如何构建自己的 Harness。

这是下一个 10 年最值钱的技能之一。

3. 如果你是产品经理：重新审视你的 AI 产品

你的产品是不是只是“套壳 GPT”？

如果是，你没有护城河，模型会越来越便宜，你的优势会越来越小。

真正的护城河在 Harness 层：你的工具设计、上下文策略、用户体验。

写在最后

2025 年的关键词是 Agent。

2026 年的关键词是 Harness。

模型是商品，Harness 才是护城河。

看懂这一点的人，会在 AI 时代建立真正的竞争力。

看不懂的人，会继续在“哪个模型更强”的问题上纠结，然后被时代抛弃。

你选哪一边？


---

## 研究摘要

**TL;DR**：系統性解釋 Claude Code Harness 的 5 個核心組件（Tools、Context Management、Permissions、Memory、Hooks）和 Agentic Loop 機制，並以業界數據論證「Harness 比模型更重要」的核心主張：OpenCode 136K stars > Claude Code 107K stars。

### 核心論點
1. **「Harness = 給 AI 大脑裝手腳的系統」**：裸 API 只有大腦，Claude Code 有完整手腳系統 — Read/Edit/Bash/Grep 工具 + 5 個組件構成完整的 Harness
2. **CLAUDE.md 持久性的真相**：規則不是模型「記住了」，而是 Harness 在每次 context 壓縮（92% 閾值）後重新注入 CLAUDE.md
3. **Bash 工具是「圖靈完備的逃逸艙」**：通過 Bash，Claude Code 的能力上限 = 操作系統的能力上限
4. **Agentic Loop 機制**：接收→推理→工具執行（含 permission 檢查）→結果回注→繼續推理的循環，與裸 API 的「問→答→結束」根本不同
5. **Harness 比模型更重要**：「同一模型在不同 Harness 的差距 > 不同模型在同一 Harness 的差距」—— OpenCode vs Claude Code 數據佐證

### 關鍵數據 / 工具
- OpenCode: 136K GitHub stars（2026 年 4 月初）vs Claude Code: 107K stars（第三方 Harness 超越官方）
- Context 壓縮閾值：92%（Harness 自動觸發）
- Anthropic 「封堵令」（2026 年 1 月）：阻止第三方工具用 Consumer OAuth Token，影響 OpenCode/Cursor/Windsurf
- Memory 路徑：`.claude/memory.json`（跨 session 持久化）
- 55,578 views · 100 likes · 184 bookmarks

### 批判性觀察
- Harness 市場爭奪的政治分析非常到位：模型商想控制 Harness 層，第三方 Harness 想讓模型成為可替換商品
- 「封堵令」事件的描述：文章呈現了開發者社群的強烈反彈，但未分析 Anthropic 的安全考量角度
- 92% 的 context 壓縮閾值是實測數字還是官方文檔？需要驗證（cc-workspace 的 context-management.md 使用的是 70-85% 閾值建議）
- 「Bash = 圖靈完備逃逸艙」的說法有安全層面的 tradeoff，需要配合 Permissions 規則

---

## 評分

| 維度 | 分數 | 說明 |
|------|------|------|
| A. Workspace 可行動性 (30%) | 7/10 | CLAUDE.md 持久性機制、92% 壓縮閾值、Permissions 三策略可用於驗證現有 cc-workspace 設定 |
| B. 創新性 (20%) | 7/10 | 「Harness vs 模型」框架和 OpenCode 數據佐證有說服力，Bash=逃逸艙的比喻原創 |
| C. 證據品質 (20%) | 7/10 | OpenCode stars 數字、Anthropic 封堵令事件是可驗證的公開事實；92% 閾值需要來源確認 |
| D. 技術深度 (15%) | 8/10 | 5 個組件有詳細說明，Agentic Loop 流程圖式解釋，技術細節充分 |
| E. 泛化性 (15%) | 8/10 | 適用所有 AI coding tool 用戶，框架可遷移至其他 Harness 系統 |
| **加權總分** | **7.40/10** | |

**整合決策**：SKILL
**理由**：Harness 5 組件的系統性框架（特別是 CLAUDE.md 重注入機制和 Permissions 三策略）可成為 cc-workspace 的「Harness 工作原理」背景知識 Skill，幫助新進者理解 workspace 設計的底層邏輯。
