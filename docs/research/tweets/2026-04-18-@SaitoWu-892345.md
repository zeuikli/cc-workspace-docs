---
title: "什么才是真正的 Harness Engineering？— @SaitoWu"
author: "Saito（@SaitoWu）"
date: 2026-04-18
source: "https://x.com/SaitoWu/status/2045458721929892345"
type: tweet
---

# 什么才是真正的 Harness Engineering？— @SaitoWu

**來源**：https://x.com/SaitoWu/status/2045458721929892345
**作者**：Saito（@SaitoWu）
**發布日期**：2026-04-18
**收錄日期**：2026-04-26
**類型**：Twitter Article
**統計**：707 likes · 141 retweets · 1,672 bookmarks · 247,232 views

---

## 全文

刚刷完 OpenAI Ryan 在伦敦的演讲 + 访谈，干货直接爆棚。

他一句话把 Harness Engineering 的核心说穿了：代码已经免费了（code is free），实施不再是瓶颈，人类真正的价值是"steering"（指导）— 定义什么是好代码、注入 guardrails、让 agent 自己去执行。

以前我们自己敲键盘，现在 agent 写 99% 的代码，我们只管 orchestration（编排）。来，拆解他最硬核的几点观点

**代码免费，但上下文和 guardrails 永远稀缺**

Ryan 说：模型已经够强了，能像人类一样写完整代码。

真正稀缺的是人类时间、注意力和模型上下文窗口。

所以 Harness 的本质就是：把人类经验（taste、review 标准、非功能要求）全部写成文本（docs、skills、ADRs、logs），让 agent 随时能看到"什么叫好代码"。

**别再把 agent 关在小盒子里，要给它完整工具和权限**

传统做法是给 agent 预设好 scaffold（脚手架），限制它一步步走。

Ryan 的做法完全相反：让 agent 自己成为整个 box，给它 CLI、bash、observability stack、甚至自己 spin up 服务的能力。

这样 agent 就能像全栈工程师一样，从头到尾把活干完，人类只需偶尔 review。

**人类角色彻底升级：从 implementer 变成 orchestrator**

以前工程师是"写代码的人"，现在是"staff engineer 带 5000 个 agent 的团队 leader"。

你的工作不再是敲代码，而是：

- 定义优先级和 success criteria
- 观察 agent 哪里反复出错
- 把这些错误转化成 durable guardrails（lints、tests、review agents）
- 不断把人类时间解放到更高杠杆的事上

**Skills + progressive disclosure 是神器**

Ryan 超级推 skills：只把「名字 + 描述」塞进上下文（几十 tokens），需要时才加载完整内容。

这样既省 token，又让 agent 更聪明。

他团队只用 5-10 个核心 skills，但不断打磨，让 agent 像老员工一样懂团队 taste。

**Review 也要 agent 化，人类只看 post-merge**

PR 不再需要人类同步 review。

他们用 review agents 自动检查代码（看 docs、guardrails、QA plan）。

人类只在最终 merge 前抽样看，或者 post-merge 再 review。

这样 agent 能 24/7 并行工作，人类不用一直卡在 review 瓶颈。

**让代码库本身变得 agent-friendly**

- 所有东西尽量相同（one way to do X）
- 构建时间压到 1 分钟内
- 文件结构清晰、package 隔离、observability 内置
- 用 tests 和 lints 做 source code verification（验证代码结构本身）

这样 agent 无论看哪里，都能快速理解、少犯错。

**一句话总结 Ryan 的核心观点：**

Harness Engineering 不是让 agent 帮你写代码，而是让你彻底从执行层解放出来，只做 steering 和 orchestration。

代码免费了，prompt 和 guardrails 才是王道。

把团队的 taste、标准、历史经验全部写成文本喂给 agent，你就拥有了一支 24/7 永不疲倦、永远在进步的 5000 人工程团队。

---

## 研究摘要

**TL;DR**：拆解 OpenAI Ryan 在倫敦演講的 Harness Engineering 核心觀點：代碼免費、人類轉型為 orchestrator、Skills + progressive disclosure 是節省 token 的核心機制、agent-friendly codebase 的六個設計原則。

### 核心論點

- Harness Engineering 的本質：把人類的 taste、review 標準、非功能要求全部文字化（docs、skills、ADRs），讓 agent 能隨時查閱「什麼是好代碼」
- 打破「sandbox agent」的傳統做法：給 agent 完整工具和權限（CLI、bash、observability stack），讓它像全棧工程師一樣從頭到尾執行
- Skills + progressive disclosure：只把名字+描述放入 context（幾十 tokens），需要時才載入全文——現有實踐的正式理論基礎
- 人類角色從 implementer 轉型為 orchestrator，工作是「定義成功標準 + 觀察錯誤模式 + 把錯誤轉化為 durable guardrails」
- Review 也 agent 化——人類只做 post-merge 抽樣，不阻塞 24/7 並行執行

### 關鍵數據 / 工具

- Skills progressive disclosure：metadata 幾十 tokens，按需載入全文
- 團隊規模隱喻：「staff engineer 帶 5000 個 agent 的團隊 leader」
- 核心 Skills 數量建議：5-10 個，持續打磨
- Build time 目標：1 分鐘內
- 來源：OpenAI Ryan 倫敦演講 + 訪談

### 批判性觀察

- 內容源自「Ryan 的演講」但未明確指出是哪位 Ryan 或演講連結，可驗證性偏低
- 「5000 人工程團隊」的比喻是行銷語言，實際效能依任務複雜度高度差異化
- agent-friendly codebase 的 6 個原則是普遍 SRE 最佳實踐（清晰結構、快速 build），並非 AI 時代新發現

---

## 評分

| 維度 | 分數 | 說明 |
|------|------|------|
| A. Workspace 可行動性 (30%) | 8/10 | Skills progressive disclosure、guardrails 文字化、review agent 化三個核心概念均可直接映射到本 workspace 的 `.claude/rules/skill-authoring.md` 和 `/deep-review` Skill 的強化方向 |
| B. 創新性 (20%) | 7/10 | 「把人類 taste 全部文字化」和「agent-friendly codebase 設計」的框架有清晰的原創架構，不只是重複已知 |
| C. 證據品質 (20%) | 5/10 | 引用 OpenAI Ryan 演講但缺乏具體連結，無量化數據；整理品質高但可驗證性低 |
| D. 技術深度 (15%) | 7/10 | 六個 agent-friendly codebase 設計原則有具體性，Skills progressive disclosure 機制有技術深度 |
| E. 泛化性 (15%) | 8/10 | Harness Engineering 框架不限於 Claude Code，適用於所有 AI 輔助開發工作流 |
| **加權總分** | **7.10/10** | 8×0.3 + 7×0.2 + 5×0.2 + 7×0.15 + 8×0.15 |

**整合決策**：Rule
**理由**：「把人類 taste 和歷史錯誤全部文字化喂給 agent」的原則應補充到 `.claude/rules/skill-authoring.md` 的設計哲學 section，強化「為何要寫 Known Gotchas」的理論依據
